# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE
**Updated:** 2026-07-25 (Claude session, машина `rmz4val`)
**Branch:** `main` = `origin/main`.
**Last released version:** **v2026.725.1938** — раскатан на прод в этой же сессии.

## Текущая нитка

**Нет активной нитки.** Сессия закрыта релизом v2026.725.1938 — сплошная разгрузка бэклога по просьбе владельца доведена до прода.

Что вошло в релиз (9 PR, #324 и #327–#335):
- **Референс-целостность при удалении — план закрыт целиком** (Ф1 #323 ранее, Ф2/Ф3 #324 под CDP-смоуком 16/16, Ф2b #328 смоук 11/11, Ф4 #327). План → `plans/_archive/`.
- **Дефекты аудита 2026-07-22:** отчёты утиль/двигатели-контракты (#329), планшетные CSS-токены под инлайн-каналом `displayPrefs` (#330), перевес складских движений при склейке двигателей (#331).
- **Чистка и тест-долги:** снос `erp:*` IPC, синхронного AI-чата, `change_log`, мелких зомби (#333); 20 тестов — push-гейт AI-чата и шифрование клиентской БД (#332).
- **Watchdog:** все 5 хвостов — ярлыки, backoff, logTail, атрибуция heartbeat, README (#334).
- **Долги перед brain:** ADR-0007 + рефлекс #014 в `CLAUDE.md`, скелет security-audit (#057) и две находки релиза — в `mailbox/to-brain/`.

## Следующий шаг

Свободный выбор владельца. Кандидаты из `PENDING_FOLLOWUPS` (не начинать без сигнала):
- **Ф2 де-дупа `directory_parts ↔ erp_nomenclature`** — по-прежнему ждёт зелёного гейта раската клиентов (машины ниже v2026.712.1818).
- Приёмка планшетного режима и резервирования двигателя на живом планшете.
- Приёмка новых веток watchdog (ярлыки/backoff) на живой машине.

## Контекст

- Релиз v2026.725.1938 не вёз серверных миграций; на проде выполнены build серверных пакетов → артефакты updater → ledger-publish → рестарт → health/updates/blockmap.
- Ручной смоук диалога удаления на реальных данных полезен (у оператора связей больше, чем в dev-БД) — заведён в `PENDING_FOLLOWUPS` §Мелочи.

## Открытые вопросы для пользователя

- Watchdog `CleanupMatricaFiles` в `installer.nsh` целит в мёртвый путь: оставить как есть (мягче при сорванном апдейте) или перенацелить на реальную папку (чистый slate)? — висит с 2026-07-12.

## Не забыть (low-priority)

**Даты ротаций/дедлайнов — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §📅.** (ledger-токен ≤12.08, deadcode ~04.08, SSH ≤21.08.)

1. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз/комплектование неполны.
2. `RELEASE_WELCOME_HISTORY` — легаси-дубли меток `v1.18.0`–`v1.18.5`, почистить при следующем релизе.
