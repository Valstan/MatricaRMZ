# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-12, день — каталог цепочки на проде снесён (#868), релиз 3.27.0: быстрый отказ от глухого соседа при раздаче + сторож просит настоящий установщик (сессия Fable 5.1, машина `PC79`)
**Branch:** `main`.
**Last released version:** **v3.27.0**, выкат 12.09 в 12:40 MSK: оба порта и nginx 3.27.0, `/updates/status` latest 3.27.0, blockmap 200, APK `MatricaRMZ-3.27.0.apk` на проде (план планшета — `update`). Secondary минуту после старта не раздавал файлы (M126, самолечится).

## Текущая нитка

**Владелец 12.09: «на твоё усмотрение — снос каталога цепочки, раздача соседям по локалке и всё остальное надо».** Сделано в этой сессии: (1) снос каталога цепочки на проде — постоянная копия на Я.Диске `/matricarmz/ledger-final/` (sha256 совпал), каталог удалён, `MATRICA_LEDGER_DIR` снят из обоих env, бэкап-приёмка `db.dump only listed`, диск 60 % → 35 % (`COMPLETED.md` 2026-09-12). Письмо brain'у с двумя классами — `mailbox/to-brain/2026-09-12-the-chain-directory-is-gone-…`.

## Следующий шаг

1. **Раздача соседям — обход владельца после выката 3.27.0.** Замер 12.09: живых клиентов 11 (3.25.0 — 7, 3.24.0 — 2, 3.26.0 — 1, CalVer — PC34), «Раздача соседям» у всех живых выключена, правило брандмауэра нигде не ставилось. В 3.27.0 клиент при глухом соседе (порт закрыт) ждёт 8 с и одну попытку вместо 45 с × 2 (`LAN_PEER_*` в `updateService.ts`, сторож `lanPeerFailFast.guard.test.ts`). Порядок на машине: `scripts/client-ops/lan-share-firewall.ps1` от администратора → тумблер «Раздача соседям» в админке → перезапуск клиента; начать с 2–3 машин одной подсети. Приёмка — непустой `update_peers` и падение обращений к `/updates/file` (база: 895 запросов / 1,1 ГБ за 15 суток, `PENDING` §«Торрент-раздача»).
2. **Живая приёмка сторожа в 3.27.0 на первой машине:** `fetchLatestMeta` теперь передаёт `current`; при `--repair` на сломанной установке в `watchdog.log` должно быть скачивание `MatricaRMZ-Setup-3.27.0.exe`, а не заглушки `…2026.1231.2359.exe` (`PENDING` §«Watchdog-агент»). Go локально не собирается — компилирует только CI релиза.
3. **Прод-часть слияния дублей — ход владельца:** после обновления клиента «Проверить» → «Объединить» по своим парам, затем `users:parity`.
4. **Приказы о ценах на услуги — миграция + экран** (решение владельца 11.09, `PENDING` §«Приказы о ценах на услуги»). Отдельная нитка.
5. **≈19.09:** счёт `client.sync.network_transient` / `network_flapping` по `critical-events.ndjson` против замера до правки (19 обрывов на 6 машинах за 08–11.09). Через две недели — `"stop":"max_tokens"` в `llm usage` (#855).
6. Прежнее: сеть VPS рвётся (ssh 12.09 один раз упал по таймауту, со второго раза прошёл — ход хостера); E2.2 плана [`engine-inventory-lines`](plans/engine-inventory-lines-2026-09.md); хвосты слияния (`PENDING` §«Слияние дублей сотрудников: хвосты»); J3 журнала (`PENDING` §«Журнал в PostgreSQL — хвосты J3»); D-084 второй шаг — ждать письма brain про Касперского.

## Контекст

- Прод: последняя миграция — `0094_chat_rooms`; 3.26.0 миграций не везёт. Каталога цепочки и `MATRICA_LEDGER_DIR` больше нет; откат на сборку с цепочкой невозможен (и не нужен).
- Прод-БД читается скриптом через stdin (`docs/machines/PC79.md` §«Запросы к прод-БД»); `client_settings` — колонки `last_version` / `last_hostname` / `last_username`, `last_seen_at` в миллисекундах.
- Дайджест brain 12.09 (G334–G340, идеи #291–#312) прочитан: G334 у нас закрыт, G336 (проба сразу после `reload nginx` попадает в старый воркер) — взять на приёмках nginx.
- CI: `employeeSectionGate.replica.test.ts` иногда падает на раннере `fetch failed` — `gh run rerun --failed`.

## Что не сработало

- **`git stash push -- <пути>` с untracked-файлом падает целиком** (pathspec не в индексе) — либо `-u`, либо просто коммитить по путям.
- **SQL-запрос по памяти о колонках** (`app_version`, `machine_name`) — колонки называются `last_version`, `last_hostname`; сначала `\d`, потом запрос.
- Прежние (11–12.09): вопрос владельцу — с цифрами в руках; схема ≠ база (`service_price_orders`).

## Открытые вопросы для пользователя

- **Обход машин для раздачи соседям** (правило брандмауэра + тумблер + перезапуск) — после релиза с быстрым отказом от глухого пира; с каких 2–3 машин начать.
- **Telegram-логин владельца в карточке сотрудника** — правится владельцем в программе.
- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота нет.
- **Дубли листов engine_inventory** (≈206 двигателей): мягко удалить лишние — после E3, отдельным OK.
- Прежние: внешний SSH-порт прода; старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника; подписные грифы печатных форм.

## Не забыть (low-priority)

- **Keystore Android:** свёрток не собран, единственная копия ключа на PC40. Brain просит поднимать первым пунктом каждого старта.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09; следующий до R4b, начать с четырёх непокрытых линз.
- **Перемер роста диска 03.10**; там же первые цифры счётчика обращений к файлам (D-073). База после сноса цепочки: 35 % (6,6 ГБ занято) 12.09.
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка.
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Файл `MISSING 21a1b799-… Кто-то ночью спит.mp4` — строка в БД есть, файла нет.
