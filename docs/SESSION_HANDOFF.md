# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE
**Updated:** 2026-07-26 (Claude session, машина `rmz4val`)
**Branch:** `main` = `origin/main`.
**Last released version:** **v2026.726.1502** — раскатан на прод в этой же сессии.

## Текущая нитка

**Нет активной нитки.** Сессия шла тремя нитками владельца, всё доведено до прода (детали — [`COMPLETED.md`](COMPLETED.md), строка релиза v2026.726.1502):

1. **Гейт «реплика не строже источника»** (#348, идея brain #086). Компаратор клиент⊆сервер внутри CI-шага «Check Sync Contract». Первый прогон 6 красных → 3 реальные строгости починены (presence, BOM-строки, серийники), 3 ложных закрыты правилом `isNullGuardOnlyWhere`. Клиентская миграция 0020.
2. **Матрица доступов** (#349) — второй вид страницы «Доступы по разделам»: сотрудники × разделы, светофор 🔴/🟢/🔵, вертикальные заголовки.
3. **Умные тумблеры доступов + закрытие дыры** (#350). Аудит: два живых класса (роль→права, разделы), сервер требует оба. Планировщик в shared (обратные ниточки при снятии, сверка editor⇄роль, ⚠️ у несвязных наборов). **Дыра:** `section_access` не был защищён на сервере — оператор мог выдать себе редактора всех разделов; теперь только суперадмин.

## Следующий шаг

Свободный выбор владельца. Что реально ждёт (сверено с `PENDING_FOLLOWUPS`):

- **🔴 5 машин ниже v2026.712.1818** (PC71, alvina, PC49, PC18, PC14) — переустановить клиент вручную. `force_full_pull` им слать **нельзя** до обновления.
- **Пн 28.07:** проверить, что остальные клиенты забрали `force_full_pull` (`SELECT count(*) FROM client_settings WHERE sync_request_type='force_full_pull'` должен таять к нулю).
- Приёмка планшетного режима и резервирования двигателя на живом планшете; ручной смоук диалога удаления на реальных данных.
- Возможное развитие доступов (не начато, идея из аудита): карта зависимостей `SECTION_DEPENDENCIES` заполнена только 4 парами и держится в синхроне с `sectionGate.ts` вручную — можно вывести её из фактических гейтящихся lookup'ов, чтобы не расходились.
- Цифры для brain по #086 уже отправлены (`mailbox/to-brain/2026-07-26-replica-strictness-first-run-numbers.md`); по #087 просили «сколько копий разошлось» — при следующей работе с черновиками замерить.

## Контекст

- Планов в работе нет.
- Ключевые PR сессии: #348 (гейт схем), #349 (матрица), #350 (умные тумблеры + дыра), релиз #351.
- Прод: v2026.726.1502.
- Открытых PR нет, локальных веток кроме `main` нет, stash пуст.

## Что не сработало

- **M40 в четвёртый раз не наступили** — poll `latest.json.size` == размер `.exe` (шаг 8 CLAUDE.md, внесён прошлой сессией) отработал как надо. Процесс держит граблю вместо памяти.
- `pnpm db:generate` в electron-app по-прежнему упирается в snapshot-drift (спрашивает rename-vs-create по чужим таблицам) — миграция 0020 заведена вручную по рецепту GOTCHAS. Починка snapshot'а остаётся отдельной незаведённой задачей.
- `better-sqlite3` в репо стоит под Electron-ABI (148) — прогон клиентской цепочки миграций под Node падает `NODE_MODULE_VERSION`; обошли встроенным `node:sqlite` (экспериментальный, но для прогона DDL достаточно) вместо пересборки натива.

## Открытые вопросы для пользователя

_Нет._

## Не забыть (low-priority)

**Даты ротаций/дедлайнов — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §📅.** (проверка pull'ов ~28.07, deadcode ~04.08, ledger-токен ≤12.08, SSH ≤21.08.)

1. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз/комплектование неполны.
2. `RELEASE_WELCOME_HISTORY` — легаси-дубли меток `v1.18.0`–`v1.18.5`, почистить при следующем релизе.
3. Операторов предупредить: старые QR-этикетки с кодом `DET-` больше не находятся поиском (перепечатать).
