# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** ACTIVE
**Updated:** 2026-07-16 (Claude Fable 5, машина `PC40`, третья сессия дня)
**Branch:** `main` = `origin/main`. Дерево чистое, stash пуст, открытых PR нет.
**Last released version:** **v2026.716.1619** — печать табеля v2 (раскатан ранее сегодня).

## Текущая нитка

**Планшет для цеха** (обход, галочки наличия, лист дефектовки, оффлайн + автосинк). План смержен — [`docs/plans/tablet-shop-floor.md`](plans/tablet-shop-floor.md) (PR #236). Разведка показала: оффлайн-фундамент (SQLite-реплика, sync v2, LWW+stale-seq, оффлайн-вход, роль `engineer`) уже есть в Electron-клиенте. Рекомендован вариант A (Windows-планшет + существующий клиент); **владелец склоняется к Android** — решение по железу/варианту НЕ принято, вернуться к обсуждению (память `tablet-shop-floor-discussion`).

## Следующий шаг

Дождаться решения владельца по железу (Windows vs Android). Если A подтверждён — начинать **Ф1 «Планшетный режим» UI** по плану (touch-оптимизация `RepairChecklistPanel`, стартовый экран оператора, индикатор синка); Ф1 полезен при любом размере экрана, можно начать и до покупки планшета.

## Контекст

- План: [`docs/plans/tablet-shop-floor.md`](plans/tablet-shop-floor.md) — фазы Ф0–Ф4, спеки железа, стратегия конфликтов (advisory-резервирование двигателя поверх LWW).
- Коммиты сессии: `85ef72ee` (план, #236).
- Прод: v2026.716.1619, не трогали.
- Открытых PR: нет. Локальных веток с un-pushed коммитами: нет.
- **Бэкап-фикс #229:** проверить `/var/log/matricarmz/backup.log` после 03:17 17.07 — архив должен вырасти (~124М ledger вместо 58М стейла).
- Ф2 де-дуп `blank-synthetic-codes` — гейт красный, проверить versions ~2026-07-24.

## Открытые вопросы для пользователя

- **Планшет: Windows (вариант A, рекомендация) или Android (вариант B, переписывание оффлайн-движка)?** + бюджет, кто носит в пилоте, TTL резерва.
- **AI/VPS** — отложено владельцем на неопределённый срок; не поднимать.

## Не забыть (low-priority)

1. Ledger release-token — exp **2026-08-15**; ротация ≤ ~2026-08-12.
2. Ротация SSH-ключей прода — до 2026-08-21.
3. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст.
4. deadcode-прогон — ~2026-08-04.
5. Наряд-черновик №90 — глянуть через пару недель (само).
