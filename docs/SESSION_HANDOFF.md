# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** ACTIVE
**Updated:** 2026-07-17 (Claude session, машина `PC40`)
**Branch:** `main` = `origin/main`. Дерево чистое, stash пуст, открытых PR нет.
**Last released version:** **v2026.717.1856** — комплектование двигателя + приход из заявки + фикс «Готовности к сборке» (раскатан на прод сегодня, smoke зелёный).

## Текущая нитка

Рефакторинг **модуля движения запчастей** по плану [`plans/parts-movement-refactor-2026-07.md`](plans/parts-movement-refactor-2026-07.md) (14 GAP, фазы Ф1–Ф6). Отгружено v2026.717.1856: **Ф1** «Комплектование двигателя» (#251), фикс **G13** «Готовности к сборке» (#252), **Ф2-ядро** «приход из заявки» + отчёт «Заявки без прихода» (#253). Все три — CDP-смоук на живом клиенте. Остаток фаз — открыт, по сигналу владельца/отзыву поля.

## Следующий шаг

Ждём **отзыв операторов** по комплектованию/готовности/приходу из заявки (владелец хотел проверить релиз вживую перед следующим шагом). Дальше по плану, в порядке ROI:
1. **Вопрос владельцу перед Ф2-хвостом:** добавить в заявку снабжения поле **«ожидаемая дата поставки»**? Без него одобренные заявки не попадают в прогноз сборки как будущий приход (остаток Ф2 заблокирован). Рекомендация — да, опц. поле на позиции/шапке.
2. **Ф3** — адресная выдача со склада: поля «двигатель»/«наряд» в `stock_issue`/`stock_writeoff` (регистр `movements.engineId` уже готов, в UI выбора нет — G3) + печать требования-накладной из карточки документа (G11). Точки: `StockDocumentDetailsPage.tsx`, `shared/src/domain/warehouse.ts` (`WarehouseDocumentLineInput`).
3. Иначе — хвосты PENDING (§движение запчастей Ф4–Ф6).

## Контекст

- План: [`plans/parts-movement-refactor-2026-07.md`](plans/parts-movement-refactor-2026-07.md) — Ф1/Ф2 отмечены ✅ в теле плана; G13/G14 (BOM/документы вне клиентского sync → REST-first) разобраны там же.
- Связанные коммиты: #250 (ревизия-план), #251 (kitting), #252 (фикс готовности G13), #253 (приход из заявки Ф2), #254 (релиз v2026.717.1856).
- **Прод: v2026.717.1856** — `/health` и `/updates/status` = новая версия, blockmap 200, дельта работает. Без миграций (code-only), серверный build на проде выполнен.
- Открытых PR: нет. Локальных веток с un-pushed коммитами: нет.
- Открытые пункты по нитке — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §«Движение запчастей — остаток фаз Ф3–Ф6» и §«Обкатка Комплектования двигателя».
- Прошлый релиз v2026.717.1605 (утиль/нормы) — обкатка причин утиля тоже в PENDING (не забыть спросить отзыв).

## Открытые вопросы для пользователя

- **Поле «ожидаемая дата поставки» в заявке снабжения** — добавляем? (нужно для заявок в прогнозе, остаток Ф2).
- Отзыв операторов по «Комплектованию двигателя» / приходу из заявки — когда пощупают, сказать в сессии.
- Планшет для цеха: Windows vs Android (доклад распечатан для руководства ранее в сессии; решение не принято).
- AI/VPS — отложено владельцем, не поднимать.

## Не забыть (low-priority)

1. Ledger release-token — exp **2026-08-15**; ротация ≤ ~2026-08-12 (PENDING §Ротация).
2. Ротация SSH-ключей прода — до 2026-08-21.
3. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз/комплектование по «выдано в сборку» неполны.
4. deadcode-прогон — ~2026-08-04.
5. Ф2 де-дуп `blank-synthetic-codes` — гейт красный (5 машин с застрявшим автообновлением), ждёт ручной переустановки клиентов владельцем; после — перепроверить версии и `--apply` с бэкапом (прод-мутация, отдельный ок).
6. Бэкап-таблицы на проде (`*_bak_20260717`, `*_bak_norms20260717`) — снести ~август после обкатки.
7. Прод-деплой: stale_manifest на первом рестарте (артефакты были готовы заранее) — вылечен вторым рестартом; это ровно вариант GOTCHAS **M20** (latest.torrent крупного installer дописывался после старта сервиса).
