# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** ACTIVE (полевая проверка: релиз v1708 раскатан, ждём подтверждения на живом клиенте владельца)
**Updated:** 2026-07-01 (Claude Opus 4.8, машина `PC40`)
**Branch:** `main` (= origin/main). Дерево чистое, stash пуст, открытых PR нет, локальная только `main`.
**Last released version:** **v2026.701.1708 на проде** (оба сервиса active, `/health` = 2026.701.1708, `/updates/status` latest = 2026.701.1708, blockmap 200).

## Текущая нитка

**Баг `2Ж03АТ0479` (список кажет старую дату отгрузки) — вылечен по коду и раскатан релизом v2026.701.1708.** Осталась **полевая проверка** после того, как клиент владельца автообновится до 1708.

**Корень (dual-source, подтверждён):** карточка правит статус-дату `status_customer_sent_date` (флаг «Отправлен заказчику», отображается как «Дата отгрузки») и **никогда** не пишет прямой атрибут `shipping_date`. А список/отчёт **предпочитали** прямой `shipping_date` — замороженный февральский импорт (все 1460 строк записаны 2026-02-10..26, ноль после). Фикс: развернули приоритет чтения → статус-дата первична, legacy-`shipping_date` только историч. фолбэк. Тот же класс закрыт для `is_scrap` (убран из OR утиля). Данные не мутировались.

## Следующий шаг

1. **Подтвердить у владельца на живом клиенте (после автообновления до 1708):**
   - **172 двигателя** с уже расходящейся статус-датой → список должен показать дату из карточки **автоматически**, без ввода.
   - **`2Ж03АТ0479` и ~1173 двигателя без статус-даты** → список пока покажет старую импортную дату; лечится **однократным** вводом «Даты отгрузки» в карточке (создаётся `status_customer_sent_date`, список сразу подхватывает). Попросить владельца проверить именно на `2Ж03АТ0479`: открыть карточку → отметить/ввести дату отгрузки → сохранить → убедиться, что список показал её.
2. **Операторам — перезапустить программу**, чтобы подтянулось автообновление до 1708.
3. Если после ввода даты список НЕ подхватил — тогда дебажить дальше (но код-путь верифицирован юнит-тестом + прод-аудитом; вероятнее всего сработает). Файлы: `listEngines` ([engineService.ts](../electron-app/src/main/services/engineService.ts)) + `resolveEngineShippingState` ([reportEngineShippingState.ts](../electron-app/src/main/services/reportEngineShippingState.ts)).

## Контекст

- **Целостный аудит проведён (запрос владельца «нет ли ещё где застрявшего»):**
  - **Dual-source (список≠карточка)** — прогнан по всем модулям (Explore-агент): найден **только** в двигателях (`shipping_date`+`is_scrap`), оба вылечены. Прочие (сотрудники, инструмент, наряды, заявки, generic-справочники) читают в списке те же атрибуты, что пишет карточка, либо единое хранилище (metaJson) — расхождений нет.
  - **Дубли активных строк** одного `(entity,attr)` — **чист по ВСЕМ сущностям, 0 групп** (write-collapse v1325/v1437 + force_full_pull закрыли класс).
  - **Замороженные legacy-атрибуты** — из 7 проблемным был только `shipping_date`; `is_scrap`-близнец безвреден (2 записи, обе согласованы с картой).
- **Релиз v2026.701.1708:** миграций нет, код-only (main-процесс Electron). Прод: серверные пакеты пересобраны, 3 артефакта в `/opt/matricarmz/updates/`, ledger опубликован, оба сервиса перезапущены. Грабля класса → GOTCHAS **M23**.
- **PR:** #18 (shipping_date), #19 (is_scrap), #20 (релиз) — все смержены. **Открытых PR:** нет. **Stash:** пуст. **Ветки:** только `main`.

## Открытые вопросы для пользователя

- **На живом клиенте (1708) для `2Ж03АТ0479`: после однократного ввода даты отгрузки в карточке список её показал?** От этого зависит, полностью ли закрыта нитка или нужен доп. дебаг.

## Не забыть (low-priority)

1. **Идентичность двигателей по номеру** (brain 2026-06-30 `engine-same-number-re-arrival-identity`): номер не уникален во времени, дубли вроде `2Ж03АТ0479` — симптом. Возможная нитка «repair-case с суррогатным ID». Route: /zavod (дозревание каркаса). См. PENDING §«Наряды … батч 2026-06-30».
2. **Ledger release-token протухает ~2026-07-23** (PENDING ⏳) — первый релиз после этой даты упадёт 401. Этот релиз (1708) прошёл штатно.
