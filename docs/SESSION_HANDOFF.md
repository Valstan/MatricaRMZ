# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-04 (сессия Fable 5.1 → Opus 5, машина `PC79`)
**Branch:** `main` = `origin/main` на `a1aa820` (+ PR этой правки); открытых PR нет, веток кроме `main` нет.
**Не выпущено, ждёт релиза клиента 3.20.0:** шаблоны «Моих отчётов» ([#764](https://github.com/Valstan/MatricaRMZ/pull/764)), unique в режиме отчёта ([#765](https://github.com/Valstan/MatricaRMZ/pull/765)), самолечение пропущенных зависимостей ([#767](https://github.com/Valstan/MatricaRMZ/pull/767)). **⚠️ Серверная часть #765/#766 выкачена ТОЛЬКО НА `secondary`** — см. `PENDING` §«Прод: primary не перезапущен…». Перед релизом 3.20.0 обязателен рестарт `primary`, иначе снимок схемы будет разным на двух инстансах.
**Last released version:** **v3.19.0** — на проде с 2026-08-31. Теги `v3.19.0` и `android-v3.19.0` отправлены.

## Текущая нитка

**Две нитки, и первая перебивает вторую.**

1. **🔴 Диск прод-бокса и мёртвый бэкап** (найдено 03.09 по мандату brain «замер роста диска»): фото с камеры двух загрузчиков за 27–28.08 и 01.09 заняли 27 % раздела, ledger — 22 %, свободно 19,5 %; шифрованный бэкап (`matricarmz-backup-encrypted`) **падал с ENOSPC каждую ночь с 14.08 без алерта**, ledger без резервной копии с 13.08. **Прод-окно ночи на 04.09 отработано: бэкап чинён и проверен дважды, 1120 файлов уехали с бокса, порог поставлен — свободно 9,0 ГБ вместо 3,8, ledger снова с копией впервые с 13.08.** **Утро 04.09: п. 6 пройден** — ночной автозапуск отработал (`done:` 04.09 03:25, `matricarmz-backup-20260904-001701.tar.zst.gpg`, 1703 МБ), гейт для пп. 7–8 открыт. **Telegram (D-076) закрыт 04.09:** не блокировка, а потеря ~половины SYN на сети хостера (M101); повторы в коде и ops-скриптах, оба флага на проде `true`, алерт бэкапа дошёл в чат 08:35, backend перезапущен с новым кодом. Таблица-остаток снесена (п. 8). Осталось: п. 7 (после 06.09), п. 9 (учебное восстановление), окно ретенции `diagnostics_snapshots` — вопрос владельцу. Полный разбор — `PENDING` §🔴 «Диск прод-бокса…» и [письмо brain'у](../mailbox/to-brain/2026-09-03-disk-measured-photos-not-logs-and-a-backup-that-died-in-silence.md).
2. **Матрица 4, трек B, этап B3 — R4b (cutover)** по-прежнему **ждёт парк ≥ v3.19.0**: на 03.09 из 17 активных win32-клиентов на 3.19.0 — 7, ещё 8 на 3.7–3.14, два на CalVer (идут через Заглушку). Состав R4b и инварианты — план `docs/plans/matrica-v4-kickoff-2026-08.md` §R4.
   **Предусловия на стороне данных проверены 03.09 и все зелёные:** `users:parity` на проде — расхождений нет (458 карточек / 36 аккаунтов / 35 кредов / 191 доступ / 0 отказов зеркала), `users_sync_outbox` и `users_mirror_failures` пусты. Остался только парк.
3. **Пункт 14 (перекосы источников) — прогон 03.09 сделан целиком, обеими половинами.** Первая половина: шесть однозначных находок закрыто, шесть оставлено владельцу (`PENDING` §«Перекосы источников»). Вторая половина нашла **утечку доступа**: политика закрытых нарядов применялась только во вкладке «Наряды», мимо неё смотрели четыре отчёта, «Где используется» и открытие наряда по id — на проде это 71 наряд из 145 при 22 аккаунтах с доступом к отчётам. Закрыто в [#752](https://github.com/Valstan/MatricaRMZ/pull/752) со сторожем; там же серверный `permissions-view`, отдававший карту прав любого аккаунта любому вошедшему. Вторая половина доведена целиком: 26 находок → 12 подтверждено, 6 спорных, 8 опровергнуто (все «мёртвые таблицы» — у них нет читателя). Шесть однозначных закрыто со сторожами, семь оставлено владельцу — `PENDING` §«Перекосы источников».

## Следующий шаг

**Прод-часть нитки 1. Порядок переписан предполётным разбором 03.09** (6 линз × 3 опровергателя, 36 находок → 16 подтверждено); обоснование каждого сдвига — `PENDING` §🔴 «Диск прод-бокса…».

- [x] ~~**0. Смержить PR**~~ — [#754](https://github.com/Valstan/MatricaRMZ/pull/754) смержен, все 8 проверок зелёные, `prod-ops-backup` — 11 кейсов.
- [x] ~~**1. Заморозить 14 старых архивов**~~ — сделано 03.09: `matricarmz-backup-*.tar.gpg` (31.07–13.08, 15,5 ГБ) в `/matricarmz/files/backups-frozen-2026-08/`. В базовой папке архивов 0, вложения не тронуты. **Не размораживать до учебного восстановления** (п. 9).
- [x] ~~**2. Выкат**~~ — сделано 03.09: прод на `58388d2c`, установщик напечатал «passphrase already exists … keeping» (ветка регенерации не сработала), `diff` cron-файла пуст, новые ручки в `/usr/local/sbin/matricarmz-backup-encrypted` на месте. Backend не пересобирался: сервисы active, `/health` = 3.19.0.
- [x] ~~**3. Первый полный прогон**~~ — **успех 03.09 23:14–23:23 MSK, первая копия ledger с 13.08.** `db.dump: 67 MB`, `encrypted: 1703 MB` (ratio 49 %), `verified: db.dump + 6 root + 395813 block(s)`, `uploaded and verified … sha256 ok` (1 786 712 944 Б), `kept 1 of 1 backups`, `done:`. Предупреждения «блоки впереди индекса» не было. Предполётная строка подтвердила, что правка ручки была нужна: `need ~3062 MB` при 3853 свободных — со старой константой 512 МиБ было бы 3830, зазор 23 МБ.

- [x] ~~**4. Перенос фото**~~ — **сделано 04.09 00:10–01:19.** Две партии, **отказов ноль в обеих**: 100 файлов (660,8 МБ) и 1020 (4611,6 МБ). Итого 1120 файлов / 5272 МБ. Бокс: занято 15 → **10 ГБ**, свободно 3,8 → **9,0 ГБ** (81 % → 53 %), `uploads/` 5,4 ГБ → 201 МБ. Раскладка: `local` 201 файл / 84 МБ, `yandex` 1203 / 6208 МБ. Манифест — `~/offload-manifest-20260903.log` на боксе (строки `OK <id> … sha256=`).
  - Единственная осечка — **предсуществующая**: `MISSING 21a1b799-… Кто-то ночью спит.mp4` (4 МБ), строка в БД есть, файла на диске нет. Инструмент строку не тронул. Это последний `local`-файл крупнее 1 МиБ; разобраться отдельно.
  - Сироты не тронуты (10 без строки в БД, удалено 0) — как и задумано: сироту инструмент удаляет, только если её строка уже на Яндексе и копия подтверждена.
  - Проверка выборкой (2 PDF + 2 JPG): файл скачан с Я.Диска, sha256 совпал со строкой в БД, локальной копии нет. **Маршрут приложения (`GET /files/:id`) не дёргали** — он под авторизацией; риск невелик (на Яндексе и раньше жили 83 файла, код в проде не первый день), но открыть вложение из клиента стоит.
- [x] ~~**5. Порог + рестарт**~~ — **сделано 04.09 00:16.** Ключа не было **ни в одном** из двух env-файлов (не «пустой», а отсутствовал). `MATRICA_MAX_LOCAL_BYTES=1048576` записан в оба (`/etc/matricarmz/matricarmz.env` для сервисов + `backend-api/.env` для dotenv), резервные копии рядом с суффиксом `.bak-20260904`. Проверено не по файлу, а по `/proc/<pid>/environ` обоих процессов. Оба сервиса слушают и отвечают. **Рост остановлен**, а не только разобран задним числом.
- [x] ~~**5а. Внеочередной бэкап после переноса**~~ — **сделано 04.09 01:21–01:31**, обязательный шаг: архив 03.09 нёс больше тысячи строк со `storage_kind='local'`, и восстановление из него после переноса указывало бы на удалённые файлы. `db.dump: 67 MB`, `encrypted: 1703 MB`, `verified … 395813 block(s)`, `uploaded and verified … sha256 ok`, **`kept 2 of 2 backups`**, `done:`. Предполётная строка — `need ~3062 MB … free 9127 MB` (накануне было 3853). Итого на Я.Диске два архива нового формата; замороженный набор из 14 старых не тронут.

- [x] ~~**6. Утром 04.09 — проверить ночной автозапуск**~~ — **прошёл**: `done:` 04.09 03:25, файл `…20260904-001701.tar.zst.gpg` (1703 МБ), имя отличается от прогонов 23:14 и 01:20; свободно 9,0 ГБ. Гейт для пп. 7–8 открыт.
- [x] ~~**6а. Telegram, ops-часть**~~ — **сделано 04.09 08:35 MSK** после merge [#758](https://github.com/Valstan/MatricaRMZ/pull/758): три скрипта установлены в `/usr/local/sbin/matricarmz-*` (прежние — `.bak-20260904-tg`), `MATRICA_OPS_TELEGRAM_ENABLED=true` дописан в оба env (бэкапы `.bak-20260904-tg`). Искусственный отказ (`MATRICA_BACKUP_DUMP_EST_BYTES=99999999999` → «need ~193542 MB, free 9123 MB» → `fail`): первая попытка curl упала по таймауту, повтор доставил — строки `WARN: telegram delivery failed` нет, то есть Telegram ответил 2xx на `sendMessage` в чат алертов. **Владелец подтверждает глазами:** сообщение «❌ Backup failed: not enough free space…» в 08:35. Это строка «алерт бэкапа дошёл в чат 04.09» для отчёта D-069/D-076.
- [x] ~~**6б. Telegram, backend-часть**~~ — **сделано 04.09 08:45–08:47 по OK владельца** (в рабочий день, поочерёдно: secondary поднялся за 4 с, primary — за 52 с, M100 подтверждён). Сборка `shared` + `backend-api` из `main` (`a5ca42f`, lockfile не менялся — install пропущен), `MATRICA_TELEGRAM_ENABLED=true` в обоих env (`.bak-20260904-tgflag`), флаг проверен по `/proc/<pid>/environ` обоих процессов, `/health` на :3001 и :3002 — 3.19.0. В журнале строки `disabled` больше нет; строка `notifier started` в journal не попадает (не `critical`), ошибок `telegram unreachable` — ноль. **Первое реальное critical-event покажет доставку** — глянуть чат при следующем событии.
- 7. **Снос внерепозиторной ретенции** — ждёт штатного прогона репозиторного `cleanup-updates` 06.09 (воскресенье 03:00). Юнит и скрипт скопировать себе до удаления.
- [x] ~~**8. `DROP TABLE ledger_tx_index_backup_20260212_102104`**~~ — **сделано 04.09 08:50** по OK владельца: 48 МБ / 142 654 строки / зависимых 0; база 878 МБ после. `apt clean` не нужен (156 КБ). **Ретенция `diagnostics_snapshots` не делалась — окно не задано**; замер и предложение (90 дней для `server`/`client`/`ai_agent_event`) — `PENDING` §🔴 «Диск прод-бокса…» п. 6. Нужен ответ владельца на окно.
- [x] ~~**10. D-073 — выкат**~~ — **сделано 04.09 10:40–11:05 по OK владельца** ([#761](https://github.com/Valstan/MatricaRMZ/pull/761)): миграция 0089 применена (три колонки + индекс проверены), `MATRICA_MAX_LOCAL_BYTES` снят из обоих env (`.bak-20260904-d073`), поочерёдный рестарт (secondary 4 с, primary 52 с). `files:offload-to-yandex --apply`: **199 OK, отказов 0**, `local` 201 → 2 — оба `MISSING` (строка есть, файла нет): `…Кто-то ночью спит.mp4` (4 МБ, известен) и **`img-251226134548-001.jpg` (251 КБ, новый факт)**. Сироты без строки — 10, не тронуты. `uploads/local` 84 МБ → 1,4 МБ; всего на Яндексе 1402 файла. Внеочередной бэкап после переноса — на Я.Диске `matricarmz-backup-20260904-075731.tar.zst.gpg` (1708 МБ, 11:05 MSK). Джоб ретенции `diagnostics_snapshots` ([#762](https://github.com/Valstan/MatricaRMZ/pull/762)) выкачен вторым рестартом 11:10. **Осталось наблюдать:** после первого открытия вложения из клиента `access_count` > 0; в журнале через час — `file cache sweep`; ack D-069+D-073 — письмо в этом же PR. Грабля: `ssh … | tee` в фоне — обрыв ssh не убивает удалённый пайплайн (цепочка `&&` доработала и запустила бэкап сама, второй ручной запуск упёрся в `flock`), вывод в лог-файл бэкапа при этом не пишется (лог ведёт cron-редирект).
- 9. **После окна:** учебное восстановление архива на машине без passphrase (`pg_restore --list` → `Compression: zstd`, накат в scratch-БД, сверка старшего блока с `index.json`). Только после этого размораживать набор из п. 1.

**Долг перед brain:** D-069 просит `ack: report` **одним письмом, когда выкачены все пять пунктов**. Три закрыты в ночь на 04.09, два ждут предусловий — формальный ack ещё не отправлен, и это осознанно. Промежуточный статус brain уже знает из [письма 04.09](../mailbox/to-brain/2026-09-04-a-number-from-a-retired-script-and-a-health-check-that-cannot-see-a-dead-instance.md). Не забыть отправить ack с `ref: 2026-09-03-d069-owner-ok-offload-photos-threshold-1mb-compress-kill-the-twin-defectovka-is-every-time` — при неверном `ref:` счётчик brain'а считает директиву просроченной и эскалирует сделанную работу как тишину (AGENTS §Mailbox, brain G233).

**Не трогать `matricarmz-nightly-backup.timer`** — он наш (`dist/scripts/nightlyBackup.js`), держит суточную копию БД в `base_reserv` и питает экран «есть ли бэкап».

**Потом — нитка 2**, когда парк доедет: предусловия R4b — зелёный `users:parity`, пустые `users_sync_outbox` и `users_mirror_failures`, `client_settings.lastVersion ≥ 3.19.0` у активных.

## Что сделано в этой сессии

**Сессия 04.09 (день, Fable 5.1) — техдолг после выката D-073.** 🔴 «Шаблоны „Моих отчётов“ стирают соседей» закрыт: запись в бакет вынесена в `electron-app/src/main/services/customReportTemplatesBucket.ts` — трогает одну строку, остальные (включая непонятые санитайзером) переписывает как лежали, лимит — отказ, не усечение; `applyCustomReportTransform` при колонках/фильтрах, которых нет у источника, бросает `CustomReportSpecMismatchError` (отчёт отвечает ошибкой с перечнем вместо чужого разреза под именем шаблона; неизвестный ключ сортировки по-прежнему игнорируется). 10 тестов. Ждёт релиза клиента. 🔴 «Unique не доезжают до клиента» — шаг 1: сервер кастует `::text` + парсер текстовой формы (сторож), клиент дедупит по серверным unique **только в режиме отчёта** (`UNIQUE_DEDUP_APPLY=false`, строка `repair_unique_dedup_report` в серверный лог клиента); шаг 2 (включение) — после недели отчётов парка, см. `PENDING`. Серверную часть выкатывать можно сразу (снимок станет полнее, клиент на неё не реагирует удалением). **Первое critical-event в Telegram (PC19, 11:46, таймаут — транзиент) вывело на M102:** голые uuid в `sync_state` — это аккаунты (push пишет `actor.id`), а не машины-призраки; PC19 неделю пушил 8 нарядов на 4 несуществующих двигателя — сервер теперь печатает `missing_ids`/`row_ids` ([#766](https://github.com/Valstan/MatricaRMZ/pull/766), на проде), клиент с 3.20.0 лечит сам (`sync/dependencyRequeue.ts`). Что проверить после обновления PC19 — `PENDING` §«`sync_state.client_id`…». **Ledger замерен и записан начисто** (`PENDING` §«Ledger `state.json` — 194 МБ…»): раздувают проекцию не мёртвые строки (5 %), а живые списки деталей двигателя — 1179 штук по 121 КБ в `operations.meta_json`; прежняя причина в доках («не сработала ротация») опровергнута — размерной ротации в коде нет вовсе. **Ничего не чинил:** первым шагом нужен инструмент пересборки `state.json` из блоков, потому что сейчас проекция ниоткуда не восстанавливается, и любая компакция необратима.

**Сессия 04.09 (утро, Fable 5.1).** Гейт п. 6 пройден. Почта brain: D-073 (ничего не удаляется, всё на Я.Диск + кэш ~3 дня + счётчик обращений — **код написан в PR «feat(files): D-073»**: кэш-копия + немедленная загрузка на Яндекс с проверкой, прогрев на промахе, часовой сборщик по TTL с подтверждением копии, счётчик обращений, миграция `0089`, 13 тестов чистой машины состояний; **прод не трогал** — DDL под OK, п. 10), D-076 (чинить Telegram) — выполнена диагностика и код. **Telegram:** серия голых TCP-connect с двух боксов myjino и с машины владельца показала потерю ~половины SYN ко всем диапазонам Telegram на сети хостера (не SNI, не DNS, не CA); Сарафан «работает» теми же 60 % без учёта неудач. Обход через relay отвергнут (все боксы на той же сети). В PR: `telegramFetch` с таймаутом попытки и повторами (`MATRICA_TELEGRAM_ATTEMPTS`, `MATRICA_TELEGRAM_ATTEMPT_TIMEOUT_MS`), три теста; curl-повторы в трёх ops-скриптах; M101; SECURITY/PENDING/env.example. Замер молчания critical-events: флаг `false` с ≤ 26.05 (≥ 101 день), журнал с 17.08 — только `disabled`.

**Сессия 03.09 (вторая, предполётная).** Разбор перед выкатом бэкапа: 6 линз × 3 опровергателя, 36 находок → 16 подтверждено, 20 опровергнуто. Замеры на боксе: реальный `db.dump` — **70,8 МБ** (не 1,5 ГБ: та цифра из лога старого скрипта с `--compress=0`, новый дампит `--compress=zstd:3`), места хватает с запасом ~912 МиБ, переноса фото ждать не надо. Найдено: предполётный гейт проходил с зазором 24 МиБ на устаревшей константе; Telegram с бокса недоступен вовсе; два прод-env разошлись, и порог `MATRICA_MAX_LOCAL_BYTES` пошёл бы не в тот файл; ротация снесла бы 14 старых архивов с `permanently=true` при непроверенном новом формате; комментарий скрипта оправдывал приём `tar` exit 1 несуществующей пересборкой проекции ledger'а. В PR: `MATRICA_BACKUP_DUMP_EST_BYTES`, `MATRICA_OPS_TELEGRAM_ENABLED`, исправленный путь в runbook восстановления, обязательная проверка высоты блоков, предупреждение о блоках впереди индекса, два новых кейса смоука.

- **Почта brain (7 писем) разобрана.** D-066: `/obriv` удалён со всеми ссылками, SessionStart-хук `.claude/scripts/session_start.ps1` печатает handoff и git status; аудит инструкций под Fable 5.1 применён целиком (порядок `/start` по #032, пин модели снят, дневник из AGENTS убран); тег отката `pre-fable-freeze-2026-09-02`. Ответ — `mailbox/to-brain/2026-09-03-instruction-audit-applied-matricarmz.md`.
- **Ретенция обновлений:** механизмов на боксе три, не два; политики сравнены, `cleanup-updates.sh` дополнен удалением `.blockmap`; снос внерепозиторного — под OK.
- **Замер диска** (см. нитку 1) и **инцидент бэкапа**: скрипт переписан в один поток с предполётной проверкой места, проверкой архива и алертом на любой исход; README восстановления под новую раскладку.
- **Тул `files:offload-to-yandex`** (dry-run по умолчанию; sha256 до и после; строка переводится на `yandex` только после подтверждения Яндексом; превью не трогаются) + `MATRICA_MAX_LOCAL_BYTES`.
- **Адверсариальное ревью своего же диффа** (6 линз × 2 опровергателя) — подтверждённое исправлено: алерт не доходил (проверки шли до сорсинга `.env`), `PIPESTATUS` был недостижим под `set -e`, загрузка на Я.Диск считалась успешной по коду PUT (теперь sha256+размер сверяются с тем, что посчитал Яндекс, и только потом ротация — с пагинацией: базовая папка общая с вложениями), `RETENTION=0` стёр бы все копии, архив без keyring считался годным.
- **Смоук-тест бэкапа + CI-джоб `prod-ops-backup`** (фикстура ledger, шимы `pg_dump`/`curl`, `--no-upload`): happy-path + семь путей отказа, каждый — с проверкой «алерт ушёл ровно один раз». У единственного бэкапа ledger не было ни одной автопроверки.
- **Перепроверка recon-обхода D-038** с непустым контролем (G286/G288): августовский результат был ложно-чистым в шести местах живых файлов — вычищено; седьмое (действующий внешний SSH-порт в закрытом письме в публичной истории) текстом не лечится. Письмо — `mailbox/to-brain/2026-09-03-recheck-found-six-and-a-port-that-text-cannot-unpublish.md`.
- Дамп журнала при падении (письмо 01.09 «secrets не маскируют…») — у нас такого дампа в CI нет; молчание = «нет».
- **Пункт 14 владельца — первый прогон проверки перекосов.** Шесть линз по коду + опровергатели + сверка с прод-данными. Закрыто в PR: разрез зарплатного свода (было: 264 из 408 сотрудников в «(не указано)», потому что читался только `department_id`, а у цеховых заполнен `workshop_id`); фильтр «Марка двигателя» в «Плане закупок» (читал колонку, снесённую миграцией 0047 → пусто, теперь 24 марки); гейт ссылок при удалении марки (не видел `directory_parts.brand_links_json` → отпускал марку с 1036 деталями); «Справочники» больше не заводят цех/склад в мёртвых EAV-типах; поле «Цех» в браузерной админке (было всегда пусто); UUID вместо названия детали в «Аудите двойного учёта». Открытое и опровергнутое — в `PENDING`.
- **Предусловия R4b по данным проверены** (см. нитку 2) — остался только парк.
- **Вторая половина пункта 14 и утечка закрытых нарядов** ([#752](https://github.com/Valstan/MatricaRMZ/pull/752)): пресеты отчётов, «Где используется» и `workOrders:get` теперь спрашивают ту же политику, что список; отказ fail-closed, «не найден» неотличим от отказа. Попутно — серверный гейт на `permissions-view`. Хвост: проба «реплика налита» смотрит только на `users`, поэтому в окне «аккаунты доехали, доступы нет» политика пуста — чинится вместе с R4b.
- **Грабля M99** (`n_live_tup` врёт на два порядка) и письмо brain'у с тремя переносимыми классами — главный из них про список исключений, который сам требует себя вычеркнуть.

## Что не сработало (и почему это стоит помнить)

- **Код с обратными кавычками через `node -e "…"` в Bash ломается молча:** bash исполнил `` `OAuth ${token}` `` как подстановку команды, в файл легло `Authorization:  }`, а скрипт отчитался «updated». Правило: код вставлять только через Write/Edit или через `node script.js` с heredoc в одинарных кавычках; после любой текстовой вставки — `typecheck`.
- **`ssh … || break` в цикле ожидания — не «дождался», а «сеть моргнула».** Дважды подряд фоновое ожидание конца переноса завершилось само (exit 255) на транзиентном обрыве ssh, хотя процесс на боксе работал. Правильно — явный маркер: `r=$(ssh … 'pgrep -f "[o]ffload" >/dev/null && echo RUNNING || echo GONE')`, и выходить только по `GONE`; пустой `$r` (ssh не дошёл) означает «повторить».
- **Прежде чем объявлять зависание, посмотри на часы бокса.** Я объявил primary зависшим на проде, увидев «журнал молчит после `sync schema guard ok`». На деле с рестарта прошла минута, а не девять: `ps -o etime` показывал `01:21`. Primary биндится ~53 с (грабля **M100**), а `systemctl is-active` печатает `active` до бинда. Разница между «висит» и «ещё поднимается» — один запрос `date` на боксе.
- **Неэкранированная обратная кавычка внутри template-literal рвёт скрипт workflow молча:** в блоке фактов было `` `[[ ! -s ]]` ``, литерал оборвался на первой кавычке, `-s` уехал в код, и запуск умер с `ReferenceError: s is not defined` до старта агентов (токены целы, но проход потерян). В строках workflow обратные кавычки не использовать вовсе — кавычить кодовые фрагменты обычными.
- **Цифра из лога — не измерение текущего кода.** Весь первый вывод («места не хватит, сначала переносить фото») стоял на `db.dump: 1.5G` из `backup.log`, но это лог **старого** скрипта с `--compress=0`; новый дампит с `--compress=zstd:3` и даёт 70,8 МБ — в 22 раза меньше. Прежде чем строить план на числе из лога, проверять, тем ли кодом оно получено (`git show <прод-HEAD>:<файл>`).
- **Workflow наследует cwd Bash-инструмента:** после `cd backend-api && …` ревью стартовало из подкаталога, и пути `scripts/prod-ops/…` у агентов не резолвились. Перед запуском workflow — `cd` в корень репо (или абсолютные пути в командах).
- **`grep -c "Backup failed"` в логе — не признак отсутствия алертов:** текст алерта уходит в Telegram, в лог пишется `ERROR:`. Проверять по обеим строкам.
- **Переменная окружения не перебивает env-файл, который скрипт сорсит сам:** два негативных кейса смоук-теста передавали `MATRICA_LEDGER_DIR` через окружение, `set -a; . "$ENV_FILE"` его затирал, и оба кейса молча гоняли happy-path (зелёные по неверной причине — тот же класс, что сторожа 31.08). Поймал сам тест на первом же прогоне в CI. Для таких кейсов — отдельный env-файл.
- **Смоук-тест бэкапа на PC79 не идёт** (нет `zstd` и `flock`) — печатает `SKIP`. Судить о правках `backup-encrypted.sh` только по CI-джобу `prod-ops-backup`.
- **Рабочая копия на PC79 в CRLF, и это ломает многострочные якоря** в скриптах правки (`node -e … replace('…\n…')` не совпадает, а `cat -A` `^M` не показывает — по глазам не отличить). Потерял на этом проход по тест-файлу. Многострочные правки — инструментом Edit; деталь в профиле машины.
- **`n_live_tup` из `pg_stat_user_tables` на проде сильно устарел** (показывал 489 строк там, где реально 45 116, и ноль у живых `directory_parts`/`change_log`). Судить о «мёртвых» таблицах только по настоящему `count(*)` — иначе вывод «таблица пуста» о живой таблице. Годный однострочник — `query_to_xml` по всем таблицам разом (есть в истории сессии).
- **Опровергатели правы не всегда, но и находки — не всегда.** Из 16 находок аудита три опровергнуты, а у двух пришлось смягчить формулировку по прод-данным («отчёт всегда пуст» → «печатает 1,4 % данных»; «флаг всегда ложен» → «потерял одно слагаемое из трёх»). Сверка находки с данными обязательна до записи в PENDING.

## Открытые вопросы для пользователя

- ~~🔴 Чем заменить Telegram для алертов?~~ — **решено владельцем 04.09 (D-076): чинить, не менять.** Диагноз и лечение — `PENDING` §🔴 «Telegram с прод-бокса…», GOTCHAS M101. Остаётся согласовать **окно рестарта backend** для п. 6б (рабочий день — нет).
- **Заморозка 14 старых архивов на Я.Диске** (п. 1 «Следующего шага») — подтвердить перенос: это единственные копии цепочки ledger'а до 13.08, а новый формат ещё не восстанавливали.
- ~~OK на пять прод-шагов нитки 1~~ — **дан** письмом D-069 (03.09).
- ~~Партии фото — разовое или норма?~~ — **ответ: «так теперь каждая дефектовка»** (D-069). Следствия: горизонт диска считать по августовскому темпу; компакция ledger — в ближайший релизный цикл; перемерить рост через месяц (03.10).
- ~~Обязательные сроки хранения~~ — **ответ D-073: не удаляется ничего**, хранилище Я.Диск, бокс — кэш. Код готов (см. «Следующий шаг», п. 10).
- **Окно ретенции `diagnostics_snapshots`** — предложено 90 дней для `server`/`client`/`ai_agent_event` (п. 8).
- **Менять ли внешний SSH-порт прода?** Действующий порт лежит в закрытом письме от 03.06 в публичной истории репо (`PENDING` §🔴 «Внешний SSH-порт…»). Правка записи ничего не отзывает — лечение только операционное: новый порт в панели хостера + `~/.ssh/config` на PC79/PC40/rmz4val.
- Прежние, не снятые: стереть ли старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника на парк.

## Не забыть (low-priority)

- **Keystore Android:** свёрток так и не собран (единственная копия ключа на PC40). Brain просит поднимать первым пунктом каждого старта, пока владелец не скажет «собрал».
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09 сделан; следующий до R4b, начать с четырёх непокрытых линз (`PENDING` §«Перекосы источников»).
- **Кэш DeepSeek на проде:** с 31.08 строки `llm usage` пишет только `log-analysis` (два прогона в сутки, `hitPct` 41–76 %, `cacheRead` 768 постоянно); чат-модули не логируют, потому что не зовутся. Brain'у дослать нечего — расхождения с замером на пробе нет.
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка (та же метрика, что R4b).
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Хвосты R4a с моментом смерти R4b — `PENDING` §«Хвосты R4a».
- Ledger: 395 тыс. файлов-блоков и `readdirSync` всего каталога в `ledger/src/store.ts:372` — при следующем массовом скрипте посмотреть время старта.
