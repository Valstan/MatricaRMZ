# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (три фичи смержены в `main` и ждут релиза; активной нитки нет)
**Updated:** 2026-07-05 (Claude Fable 5, машина `rmz4val`)
**Branch:** `main` (= origin/main). Дерево чистое, stash пуст, открытых PR нет, локальная только `main`.
**Last released version:** **v2026.704.951 на проде** (2026-07-04). Сегодняшние #76–#78 — в `main`, к операторам НЕ уехали.

## Текущая нитка

_n/a_ — сессия 2026-07-05 закрыла три задания владельца (все CDP-верифицированы, гейты зелёные):
1. **#76 Поиск объединён в верхнее поле** (директива: одно поле на список): нижний `ListSearchBar` удалён, tier-2 (поиск внутри карточек/EAV) влит в верхние фильтры + добавлен в Контрагенты/Контракты/Марки/Услуги/справочники. CDP 16/16.
2. **#77 Карточка марки**: фильтр деталей + режимы (Все/Комплектовка/Дефектовка/По узлам) + печать текущего среза. CDP 11/11.
3. **#78 Ремфонд-осведомлённый прогноз** (по итогам всеобщего аудита цепочки «наряды→склад→ремфонд→дефектовка→прогноз», 3 Explore-агента — цепочка корректна): дефицит = «ремонтом закрыть M» + «закупить N»; кнопка «Создать заявку в снабжение» из дефицитов (предзаполненная); бейдж «⚠ Не занесено в ремфонд» на карточке двигателя. CDP 7/7. План: `plans/_archive/forecast-remfond-aware-2026-07.md`.

## Следующий шаг

**Главный кандидат — релиз** (`/reliz`): в `main` накоплены 3 операторские фичи (#76–#78), миграций нет, чисто renderer+backend. Владелец сказал «скажу „создай релиз"» — ждёт его команды.

Прочие кандидаты (сверено с PENDING на 2026-07-05):
- **Security-остаток (мелкий):** bootstrap-суперадмин + `/client/*` heartbeat-спуфинг — backend-правки, обсуждены как «следующий заход».
- **Решения владельца:** M2b (сброс кэша операторам), forward-proxy VPS для AI (🔴 блокер).

## Контекст

- Прод: **v2026.704.951**, оба сервиса active (probe в начале сессии). Сегодняшние изменения прод не трогали.
- PR сессии: #76, #77, #78 — все смержены squash, ветки удалены.
- Аудит цепочки движений деталей (3 отчёта Explore-агентов) — выводы вшиты в план `plans/_archive/forecast-remfond-aware-2026-07.md` §Контекст; известные не-взятые хвосты: классификация Ф3-экземпляров не питает прогноз, dayOffset=1 у ремонта, silent-skip нерезолвнутых partId.
- Машинные грабли rmz4val пополнены: backend для verify-стека запускать долгоживущей задачей напрямую (НЕ start-backend.ps1 — дерево убивается харнесом после завершения задачи-обёртки); форма-логин в CDP-смоке надёжнее bridge, но требует переинжекта хелперов.

## Открытые вопросы для пользователя

- Делать релиз с #76–#78? (скажи «создай релиз»)

## Не забыть (low-priority)

1. **Спот-чек владельцем после следующего релиза:** верхний поиск ищет вглубь карточек (нижней панели нет); карточка марки — фильтр/«По узлам»/печать среза; прогноз — подсказки «ремонтом/закупить», кнопка заявки; бейдж ремфонда на двигателе с незанесённой дефектовкой.
2. **Ledger release-token — ротация до ~2026-08-01** (PENDING ⏳).
3. **Ротация SSH-ключей прода — до 2026-08-21** (PROJECT_STATE).
4. AV-ложнопозитивы watchdog'а — поглядывать в «Критические события».
5. `uiSearchRegistry.ts` пополнять кнопками страниц по жалобам «не нашёл».
7. Плейнтекст-остатки клиента — план sqlcipher §остатки, по желанию.
