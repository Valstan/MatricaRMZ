# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (релиз отгружен, бэклог фич закрыт — активной нитки «в полёте» нет)
**Updated:** 2026-07-14 (Claude Opus 4.8, машина `PC40`)
**Branch:** `main` (= origin/main). Дерево чистое, stash пуст, локальная только `main`, открытых PR нет.
**Last released version:** **v2026.714.1646 на проде** (раскатано в этой сессии). Оба сервиса active.

## Текущая нитка

Нет активной нитки. Сессия 2026-07-14: (1) доступы owner-batch (Щербик all-viewer, Рамзия оператор нарядов); (2) закрыт бэклог фич brain (кроме UI-конструктора); (3) релиз **v2026.714.1646** собран и раскатан на прод. Всё смержено, задач-хвостов в полёте нет — следующая сессия выбирает из открытых кандидатов.

## Следующий шаг

Выбор владельца в начале сессии (все — открытые, ни одна не горит):

1. **Калькулятор себестоимости** 🔴 — заблокирован методикой (решение владельца 2026-07-14: отложить до методики от бухгалтерии). Разблокируется, когда владелец даст: чем оценивать детали (закупка/учётная/средняя), % и базу накладных, нужна ли наценка/цена. Детали — [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §Бэклог фич brain п.1.
2. **H7 шаг «в»** — fail-closed `normalizeRole` (безопасен, живых `user`=0; не 1 строка — задевает `roleReport` + тесты, отдельный PR). PENDING §Security п.1.
3. **Ф2 де-дуп `blank-synthetic-codes`** — ждёт гейта «все недавние клиенты ≥ 2026.712.1818». PENDING §Техдолг. С раскатом v2026.714.1646 гейт мог сдвинуться — проверить долю клиентов перед apply.
4. **QR-сканер двигает остатки** 🟢 — надстройка над складом, но упирается в offline-контур (L–XL) + железо. PENDING §Бэклог фич brain п.2.
5. **Пилот UI-конструктора** — паспорт ремонта / QR-этикетки / дефект→BOM закрыты; остаётся конструктор (3 вопроса владельцу в [`plans/ui-builder-modules.md`](plans/ui-builder-modules.md)).

## Контекст

- **Отгружено в этой сессии (2026-07-14):**
  - Доступы (PR [#205](https://github.com/Valstan/MatricaRMZ/pull/205), прод-данные): zamkomdir → viewer во всех 8 разделах; ramzia → `work_orders:editor` + `restricted_work_orders:editor` (сама видит только свои). Переатрибуция несборочных нарядов до 2026-06-20 → ramzia (по факту все 16 уже были за ней; репропагировано синком).
  - Фичи (PR [#206](https://github.com/Valstan/MatricaRMZ/pull/206)): паспорт ремонта (вкладка «История ремонта», shared `engineTimeline`), печать QR-этикеток (`qrcode` dep, `LabelPrintDialog`, `qrLabels`). Проверены CDP e2e (verifier-electron).
  - Доки (PR [#207](https://github.com/Valstan/MatricaRMZ/pull/207)): COMPLETED + PENDING обновлены.
  - Релиз (PR [#208](https://github.com/Valstan/MatricaRMZ/pull/208), тег `v2026.714.1646`): деплой code-only (миграций нет), 3 артефакта + ledger-publish + рестарт; `/health` = 2026.714.1646, `/updates/status` latest = 2026.714.1646 (infoHash живой), endpoints .exe/blockmap 200.
- Прод: HEAD = `96e4efaf` (v2026.714.1646), оба сервиса active.
- Открытых PR: нет. Un-pushed веток: нет. Stash пуст.

## Открытые вопросы для пользователя

- **Калькулятор себестоимости** — ждём методику (см. «Следующий шаг» п.1). Пока в бэклоге как заблокированное.
- **UI-конструктор** — 3 вопроса из [`plans/ui-builder-modules.md`](plans/ui-builder-modules.md) §Открытые вопросы.
- **Watchdog `CleanupMatricaFiles`** — no-op или перенацелить (PENDING §Watchdog).

## Не забыть (low-priority)

1. **Ledger release-token** — ротация до ~2026-08-04 (первый релиз после ~2026-08-01 упадёт 401; лечение — PENDING ⏳). В этом релизе токен ещё был жив (ledger-publish прошёл).
2. **Ротация SSH-ключей прода** — до 2026-08-21.
3. **Ф2 гейт** — не гнать apply, пока не все недавние клиенты ≥ 2026.712.1818; env сорсить (M30); после apply — M31-верификация PG.
4. **Мастера жмут «Выдать в работу»** на ремнарядах, иначе прогноз по ремонту пуст.
5. AV-ложнопозитивы watchdog'а — поглядывать в «Критические события».
6. deadcode-прогон (месячная дельта) — ~2026-08-04.
7. Паразитный `~/MatricaRMZ/backend-api/ledger` на проде — удалить при случае.
8. **SSH-флап PC40↔прод** — banner-exchange timeout бывает разово (случилось в этой сессии на install); лечится повтором через ~20с (профиль `docs/machines/PC40.md`).
