# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-08-19 (Claude session, машина `PC40`)
**Branch:** `main` = `origin/main`, дерево чистое.
**Last released version:** v3.3.0 (эта сессия; раскатан на прод — см. отчёт релиза).

## Текущая нитка

**Трек B (финиш EAV→erp_*) идёт полным ходом** — план [`docs/plans/matrica-v4-kickoff-2026-08.md`](plans/matrica-v4-kickoff-2026-08.md). За сессию выполнено и **раскатано релизом v3.3.0**:

- **B0 гигиена** ✅ (#617): мёртвый `/erp`-прототип снесён целиком (роутер, сервис, 3 опасных/мёртвых скрипта, 5 PG-таблиц миграцией 0082, 7 клиентских SQLite-реплик миграцией 0021); пути «читаем пустоту» разобраны; дрейф карт синка починен (`erp_engine_instances` в snapshot-карте `/ledger/state/snapshot`).
- **B1 марки** ✅ (#618, миграция 0083): `directory_engine_brands` — полное триггерное зеркало EAV (не lazy); 3 брендовых FK перевешены с `entities` на строгую таблицу (brand_links, nomenclature.default_brand_id, repair_norm_set_brand_links) — **FK-блокер вывода entities из синка снят**.
- **B2 шаги 1–2** ✅ (#619, #620, миграция 0084): строгие `erp_contracts`/`erp_counterparties` в каноничной форме с **сохранением EAV-id**, питаются rebuild-триггерами (приём 0052); id-пространство договоров унифицировано (`erp_engine_instances.contract_id` → реальные договоры); чтения (lookups склада, sections, ИИваныч, серверный гейт номеров #612) — на строгих таблицах. Секции/execution_parts/платежи пока JSON-колонками.

## Следующий шаг

**B2 остаток** (крупный, требует клиентских правок):
1. Реляционная модель платежей + **cutover CRUD** договоров/контрагентов (записи через серверное API вместо EAV-синка) — только теперь, когда зеркала живут на проде.
2. Затем **B3 — auth-срез сотрудников (L)**: строгая таблица `users` (id = entities.id), переписать внутренности `employeeAuthService.ts`, включить в синк. **Схему `users` перед реализацией — письмом брейну на согласование как v4-канон** (brain ждёт: письмо 2026-08-18, смотреть будет инварианты-в-схеме / что осознанно не переезжает / границу «канон vs на усмотрение v4»).

## Контекст

- Паттерн этапов B1/B2 — «id-тождественное триггерное зеркало → перевес FK → переворот чтений» (образец 0052/0083/0084); повторять для последующих доменов.
- Клиентские реплики продолжают читать EAV до этапа B6 (вывод EAV из синка) — строгие таблицы серверные, в синк не входят.
- Unique на `erp_contracts.internal_number` не ставить, пока владелец не разберёт исторические дубли «20/ГОЗ-25» (вопрос в PENDING).
- Дропнутые таблицы остались в wire-enum'ах `LedgerTableName`/`ErpSyncTableName` осознанно (толерантность к историческому леджеру) — ревизия на B6.

## Что не сработало / грабли сессии

- Релизный коммит B1 чуть не ушёл прямо в `main` (забыл ветку) — branch protection отбил, перенесено штатно.
- `gh pr checks` возвращает exit 8 при pending — вотчер писать через `until s=$(...); [ -n "$s" ] && ! grep -q pending`.

## Открытые вопросы для пользователя

- Прежние (PENDING): три договора «20/ГОЗ-25» (теперь ещё и блокер unique-констрейнта); ключ PC40 от 22.05 снять после **24.08**; окно на 12 обнулённых конфигов прода; чистка ~200 мусорных типов `t_bulk_*` (кандидат B0, ждёт ок).

## Не забыть (low-priority)

- Ledger-токен ≤04.09 🔴 (PENDING).
- Механизм исполнения ReportContribution — спроектировать до Ф1 (Matrica4).
- brain проверит артефакт сканера секретов 24.08 — у нас зелёный, делать ничего не надо.
