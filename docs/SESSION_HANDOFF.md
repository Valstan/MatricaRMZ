# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** ACTIVE
**Updated:** 2026-07-17 (Claude Fable 5, машина `PC40`)
**Branch:** `main` = `origin/main`. Дерево чистое, stash пуст, открытых PR нет.
**Last released version:** **v2026.717.1605** — утиль с причинами + отчёт «Утиль: реестр» + нормы расхода УТД-20/В-84 (раскатан на прод сегодня).

## Текущая нитка

Сессия «хвосты PENDING + запросы владельца» закрыта релизом. Отгружено: причины утиля + отчёт-реестр (#248, план `plans/scrap-transparency-2026-07.md`), импорт норм расхода УТД-20/В-84 (#247, applied на прод), фильтр «В ремонте сейчас» в отчёте по двигателям + чистка 25 зависших статусов (#246), фикс watchdog стейл-installer (#245), приборка клиентов (77→16 строк, бэкап `client_settings_bak_20260717`).

Активной кодовой нитки нет. Ожидающие сигнала владельца направления:
1. **5 машин с застрявшим автообновлением** (PC71, PC20/alvina, PC49, PC18, PC14) — владелец сказал, что **переустановит клиент вручную**; после этого гейт `blank-synthetic-codes` должен позеленеть → `--apply` (по отдельному ок, прод-мутация).
2. **Обкатка причин утиля** и отчёта «Утиль: реестр» операторами.
3. **Планшет для цеха** — по-прежнему припаркован на решении владельца по железу.

## Следующий шаг

По сигналу владельца: (а) после ручной переустановки клиентов — перепроверить версии и гнать де-дуп Ф2 `blank-synthetic-codes --apply` (бэкап перед этим); (б) отзыв операторов по утилю/эскизнику/«Моим отчётам» → докрутка; (в) отчёт «план закупок по нормам» для экономистов (PENDING §норм). Иначе — хвосты PENDING.

## Контекст

- Планы: `plans/scrap-transparency-2026-07.md` (✅ Ф1–Ф3 отгружены), `plans/ui-mockup-constructor.md` (✅), `plans/tablet-shop-floor.md` (припаркован).
- PR сессии: #244 (docs-sweep), #245 (watchdog), #246 (отчёт по двигателям), #247 (нормы), #248 (утиль), релизный.
- **Прод: v2026.717.1605** (см. релизный PR). Бэкапы на проде: `client_settings_bak_20260717`, `directory_parts_bak_norms20260717`, `erp_nomenclature_bak_norms20260717`, `bom_lines_bak_norms20260717` — снести при случае после обкатки (~август).
- Бэкап-фикс #229 сверен: ночной архив теперь ~800М (реальный ledger); Я.Диск ~11 ГБ ретенция — приглядывать за квотой.

## Открытые вопросы для пользователя

- Ручная переустановка клиента на PC71/PC20(alvina)/PC49/PC18/PC14 — когда сделана, сказать в сессии (перепроверю гейт де-дупа).
- Планшет: Windows vs Android (переносится; решение не принято).
- AI/VPS — отложено владельцем, не поднимать.

## Не забыть (low-priority)

1. Ledger release-token — exp **2026-08-15**; ротация ≤ ~2026-08-12 (см. PENDING §Ротация).
2. Ротация SSH-ключей прода — до 2026-08-21.
3. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст.
4. deadcode-прогон — ~2026-08-04.
5. Ф2 де-дуп `blank-synthetic-codes` — гейт красный (5 машин), ждёт ручной переустановки клиентов владельцем.
6. Бэкап-таблицы на проде (`*_bak_20260717`, `*_bak_norms20260717`) — снести ~август после обкатки.
