# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-22 (Claude Opus 5, машина `PC79`) — релиз v3.47.0 выкачен, внешний SSH-порт прода сменён (D-101)
**Branch:** `main`
**Last released version:** **v3.47.0**, выкачено 22.09 (см. тело PR релиза)

## Текущая нитка

**Пакет владельца 22.09 выпущен релизом v3.45.0**, приёмка на проде дала правку индикатора срока — она вышла релизом **v3.46.0**. План с деталями по каждому пункту: [`docs/plans/owner-package-2026-09-22.md`](plans/owner-package-2026-09-22.md).

| # | PR | Что |
|---|---|---|
| 1 | [#997](https://github.com/Valstan/MatricaRMZ/pull/997) | точный поиск по умолчанию: Ctrl+K, 8 списков, остатки склада; пикеры оставлены терпимыми |
| 2 | [#998](https://github.com/Valstan/MatricaRMZ/pull/998) | без «/» короткий номер контракта не строится |
| 3 | [#999](https://github.com/Valstan/MatricaRMZ/pull/999) | дубль детали создать нельзя, окно ведёт в существующую; у двигателей переход вынесен первым |
| 4 | [#1000](https://github.com/Valstan/MatricaRMZ/pull/1000) | свежий и архивный заезд видны в пикерах, списке, карточке и фасете |
| 5 | [#1001](https://github.com/Valstan/MatricaRMZ/pull/1001) | срок ремонта из контракта, отсчёт от даты поступления |
| 6 | [#1002](https://github.com/Valstan/MatricaRMZ/pull/1002) | бланк дефектовки в два столбца, флаг «Свой номер» (**миграции 0099 и 0026**) |
| 7 | [#1003](https://github.com/Valstan/MatricaRMZ/pull/1003) | бирки 6/4/2 на А4 по выделенным строкам |
| 8 | [#1004](https://github.com/Valstan/MatricaRMZ/pull/1004) | замер скорости интерфейса, два ускорения, залипание фокуса |

## Вторая порция 22.09 — наряды и видимый вотчдог (выпущена v3.47.0)

Владелец после приёмки v3.46.0 дал список по нарядам (оператор — Рамзия) и наблюдение по клиенту. Шаги (каждый — свой PR):

| # | Шаг | PR |
|---|---|---|
| 1 | стрелки у чисел меняют целую часть (`step="any"`), кнопка «Сброс» снята | [#1011](https://github.com/Valstan/MatricaRMZ/pull/1011) |
| 2 | кнопка «Обновить цены» — подставить текущие цены услуг только в открытый наряд | [#1012](https://github.com/Valstan/MatricaRMZ/pull/1012) |
| 3 | ширина наряда: длинные наименования переносятся, таблица работ прокручивается внутри панели, блоки работ и комплектовки во всю ширину | [#1014](https://github.com/Valstan/MatricaRMZ/pull/1014) |
| 4 | печать: галочки на блоки подписей и роспись бригады; вкладка «Подписи» без сворачивания, пустые legacy-блоки скрыты; снятый гриф больше не возвращается | [#1013](https://github.com/Valstan/MatricaRMZ/pull/1013) |
| 5 | клиент: окно статуса запуска, повторный клик по ярлыку его поднимает | [#1015](https://github.com/Valstan/MatricaRMZ/pull/1015) |
| 6 | сторож (Go): нативное окно этапов при восстановлении и на `--repair` | [#1016](https://github.com/Valstan/MatricaRMZ/pull/1016) |
| 7 | смоук стенда по всей порции; настоящая причина пустой вкладки «Подписи» — панель была вложена внутрь «Содержимого» | [#1018](https://github.com/Valstan/MatricaRMZ/pull/1018) |
| 8 | грабля M144: строка списка и вкладки карточки открываются только настоящим кликом мыши; панели закрытых карточек остаются в DOM | [#1019](https://github.com/Valstan/MatricaRMZ/pull/1019) |

## Что ждёт слова владельца

- **Дубли деталей по одному лишь названию НЕ блокируются**, если артикулы разные: это законная семья («Вал коленчатый 3305-01-18 / …-17»), довод записан в шапке `duplicateBlockReason` в `shared/domain/partsDedup.ts`. Захочет строже — расширять там же, одним местом.
- ~~Горящие без даты поступления~~ — **проверено на проде 22.09, предупреждение было неверным по направлению.** По прежнему правилу отсчёт не шёл НИ У ОДНОГО двигателя в работе (датированный аванс есть у 12, и все отремонтированы). Новое правило дало отсчёт 457 двигателям, из них 335 красных, максимум 854 дня; у 198 не было работ два месяца, 142 стоят больше года. По решению владельца индикатор ограничен двигателями с работами за 60 дней (`COUNTDOWN_STALE_DAYS`) — забытые карточки уходят в значение «без движения», а не гаснут.
- **Дата окончательной технической приёмки** считается как срок ремонта плюс запас `TECHNICAL_ACCEPTANCE_LAG_DAYS` (сейчас 10 дней) в `utils/engineTagPrint.ts`. Владелец называл «пусть будет семьдесят» при сроке шестьдесят — если запас другой, меняется одной константой.
- **При сроке 60 дней «прошло 40» — ещё жёлтый**, красный с 47-го: доли порогов сохранены от прежних 90 дней.

## Следующий шаг

1. **Приёмка v3.47.0 у Рамзии** — открыть наряд и проверить глазами: стрелки у цены и количества меняют рубли и штуки; «Обновить цены» подтягивает новые цены только в открытый наряд; строка не уезжает за правый край; вкладка «Подписи» показывает редактор и состав бригады; в окне печати галочки подписей и росписи бригады.
2. **Приёмка видимого восстановления** — на медленной машине парка: при долгом старте появляется окно с шагом, повторный клик по ярлыку его поднимает. У сторожа окно проверяется живьём (снести каталог установки либо нажать ярлык «Восстановить Матрицу РМЗ») — на машине разработчика Go не собирается, компиляцию держит CI.
3. **Замер скорости на слабой машине** — `localStorage['matrica:perfTrace']='1'`, перезапуск, сценарий из тела PR [#1004](https://github.com/Valstan/MatricaRMZ/pull/1004). Пока цифр нет, «стало легче» остаётся догадкой.
4. **Нитка G возвращается.** Построчная запись принята на проде 22.09. Дальше G3 (E3-флип) и G4 (чистка дублей листов, apply под явный OK владельца).
5. К 16.10 по D-096: таблица дверей записи REST/скрипты/фоновые задачи (`docs/WRITE_DOORS.md`), `typescript` в сторе бокса, `immutable` на ассетах, обход `!operatorScoped` + open для неизвестного типа — вместе с B6.
6. ✅ **D-101 исполнен 22.09: внешний SSH-порт сменён**, старый закрыт, проверено с `PC79`, fail2ban переведён, отчёт Мозгу отправлен. Номер — в канале владельца, в репо его нет нигде.
   **Хвост 1 — КАРМАН:** порт и новый ключ бэкапа не положены в комнату `matricarmz`. Разобрано 22.09: у живой комнаты **нет ни одного машинного пути к праву записи** (`provision` → 409, паспорт без заведённой личности, выдачи требуют записи с обеих сторон), плюс адрес хранилища у нас нигде не записан. Письмо Мозгу отправлено — просим паспортную личность с правом записи и путь для значений, живущих вне CI. Ждём ответа.
   **Хвост 2 — `rmz4val`:** там в `~/.ssh/config` прежний порт, вход не пройдёт. Сети до машины с `PC79` нет, сессий на ней нет — правится на самой машине одной командой `scripts/set-prod-ssh-port.ps1 -Port <номер>` (скрипт сверяет отпечаток ключа сервера и сам проверяет вход; прогнан целиком на `PC79`). Напоминание дописано в профиль машины, его прочитает `/start`.
   **`PC40` упразднён владельцем 22.09** — профиль удалён, устаревшие ссылки вычищены. На проде остался её осиротевший ключ доступа: снятие ждёт слова владельца (пункт в `PENDING_FOLLOWUPS`).
7. Дальше — блок H (авторство и история объектов, журнал действий).

7. **Ключи: свёрток сделан 22.09 и проверен побайтово** (`matricarmz-keys.sealed`, внутри Android-keystore, его пароль и новый `backup-private.pem`). Осталось физическое: переписать пароль с рабочего стола `PC79` на бумагу и удалить файл, копию свёртка на флешку в сейф и вторую вне здания. До этого свёрток и пароль лежат на одной машине.

**Даты мандата D-096 — в календаре `PENDING_FOLLOWUPS`** (01.10 ✅, 02.10 ✅, 16.10 частично, 30.10, 30.11).

## Контекст

- **План:** [`docs/plans/autumn-2026-program.md`](plans/autumn-2026-program.md) (нитка G, блок H).
- **Связанные коммиты:** `941e1755` G2 · `b5ff4a79` G1 · `b23af95b` владение строкой #015 · `1e4438bc` rollback командой · `f8ecf8f5` open redirect · `26b362f0` админка в артефакте · `9ff02bbf` релиз v3.43.0.
- **Прод:** v3.44.0 — статус приёмки в теле релизного PR. Откат — `deploy-backend.sh rollback` (копия снимается скриптом перед раскладкой).
- **Открытых PR:** нет. **Локальных веток с неотправленными коммитами:** нет.
- **Почта brain:** мандат **D-096** принят (`ack: report`), пять строк отданы досрочно; отправлены три письма 18.09 — по мандату, по `grep -q`/буферу трубы и по трём классам из гейта дублей.

## Что не сработало

- **Жёсткий запрет дублей (уникальный индекс) — отвергнут дважды, по разным причинам.** Технически: ключ дубля лежит внутри `meta_json`, значит нужен expression-индекс в PG и его точная копия в SQLite-реплике — это ровно **M142**, из-за которого утром встал весь парк; плюс на существующих дублях индекс не создался бы. По существу: двигатель реально возвращается на тот же этап, и запрет обошли бы сдвигом даты — потеряли бы достоверность дат, а не дубль.
- **Префикс возврата в названии (предложение владельца) — отвергнут.** Данные в имени не считаются и не фильтруются, ломаются опечаткой. Отдельный вид работ — хуже: разъехался бы с основным во всех группировках отчётов.
- **Установка значения в пикер через сеттер свойства больше не работает** — подсказка рисуется порталом и только при `dropdown.open`. Смоук из-за этого показал «гейт не поднялся», хотя дело не доходило до гейта вовсе. Класс записан как **M143**; committed-драйверы, написанные до v3.41.0, считать устаревшими.
- **Доказательство, собранное после таймаута драйвера, принимать нельзя.** Первый прогон дал противоречие: драйвер «упал», а строка возврата в базе появилась. Причина — цикл ожидания внутри одного `Runtime.evaluate` висит до таймаута, пока страница живёт дальше. Опрос состояния — только короткими повторными вызовами со стороны драйвера.

## Открытые вопросы для пользователя

- **Обход машин для раздачи соседям** — подобраны 17.09 (`PC76`, `PC19`, `PC20`, запасная `PC36`; логины — в чате); ждёт утверждения владельца и обхода с `lan-share-firewall.ps1`.
- **Telegram-логин владельца в карточке сотрудника** — правится владельцем в программе.
- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота нет.
- **Дубли листов engine_inventory**: владелец 16.09 взял E2.2→E2.3→E3→чистку в программу (блок G плана); apply — отдельным OK.
- **Название этапа ОТК перед отгрузкой** — предложено «Выходной контроль ОТК» (запасное — «Комплектация к отгрузке»); узел заводит владелец в справочнике видов работ.
- **B6** (сужение этапа «Комплектовка сделана», замер 95 → 3) и **B4** (снятие `engine_stages` по приёмке отчёта) — ждут слова владельца.
- Прежние: внешний SSH-порт прода; старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника; подписные грифы печатных форм.

## Не забыть (low-priority)

- **Печатный бланк и отчёт номер прохода пока не показывают** — гейт дублей отдан без этого осознанно; если владелец захочет видеть возвраты в печати, это отдельный маленький шаг.
- **Свежей суточной цифры 409 нет** — последний замер 07.09 (23/33/12/0). Перемерить и заодно проверить, не вернулась ли строка `slower than its tick`.
- **Опрос Telegram не отменяется на `SIGTERM`** — оставшийся источник «рестартовых» 409 при каждом выкате; обещано brain 07.09.
- **Сторож схемы синка краснеет на двух серверных таблицах** (`service_price_history`, `service_price_orders`) при каждом старте бэкенда, с 15.09 — заведено в `PENDING_FOLLOWUPS` 22.09 с тремя путями решения. Сегодня это шум, но он обесценивает сторож.
- **Забытый worktree** `.claude/worktrees/practical-hopper-0f5ca3` (от сессии 14.09, detached HEAD, дерево чистое) — gitignored, репо не портит; снести можно в любой момент.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09; следующий до R4b.
- **Перемер роста диска 03.10**; база отсчёта — 5.6 ГБ занято (60 %) на 14.09.
- ⏰ **Ротация ledger-токена — срок 2026-09-24, то есть послезавтра** (перенесено сюда из общего списка: дата подошла). Ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка.
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
