# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE
**Updated:** 2026-07-27 (Claude session, машина `PC40`)
**Branch:** `main` = `origin/main`.
**Last released version:** **v2026.727.923** — раскатан на прод в этой же сессии.

## Текущая нитка

**Нет активной нитки.** Сессия шла одним обращением владельца (три жалобы → два корня, детали — [`COMPLETED.md`](COMPLETED.md), строка релиза v2026.727.923):

1. **Сборочный наряд не сохранялся** ([#359](https://github.com/Valstan/MatricaRMZ/pull/359)) — клиентский близнец регресса #319/#325; «наряд не найден» и «BOM не применён» были производными от отклонённого сохранения (deferred-create).
2. **Призрачные клиенты** ([#360](https://github.com/Valstan/MatricaRMZ/pull/360), [#361](https://github.com/Valstan/MatricaRMZ/pull/361)) — `clientId` в стираемой SQLite + watchdog, засчитываемый как heartbeat приложения.

## Следующий шаг

Свободный выбор владельца. Что реально ждёт (сверено с `PENDING_FOLLOWUPS`):

- **~30.07: перепроверить парк клиентов.** После раската v2026.727.923 станет видно, кто из «5 машин ниже v2026.712.1818» (PC71, alvina, PC49, PC18, PC14) **действительно** жив: живой = строка с непустым `last_hostname`. Скорее всего часть — призраки, и тогда блокер раската Ф2-дедупа снимается сам. Призрачные строки `client_settings` (сейчас ~9 явных дублей: PC40×4, PC41×2, PC69, rmz4val×2) — вычистить по возрасту; **прод-мутация, спросить владельца в том же ходе**.
- **Проверить, что клиенты забрали `force_full_pull`** (`SELECT count(*) FROM client_settings WHERE sync_request_type='force_full_pull'` → должен таять к нулю). Пункт висел на 28.07.
- **Ручная приёмка сборочного наряда на живых данных** — мастер выписывает наряд на сборку с деталью из справочника, выдаёт в работу. Фикс закрыт тестами и гейтами, но живьём после раската не проверялся.
- Приёмка планшетного режима и резервирования двигателя на живом планшете; ручной смоук диалога удаления на реальных данных.
- `SECTION_DEPENDENCIES` заполнена вручную 4 парами и держится в синхроне с `sectionGate.ts` руками — можно вывести из фактических гейтящихся lookup'ов.

## Контекст

- Планов в работе нет.
- Ключевые PR сессии: #359 (каталожные ссылки наряда), #360 (sidecar clientId), #361 (watchdog ≠ heartbeat), релиз #362.
- Прод: v2026.727.923. Серверных миграций релиз не вёз.
- Открытых PR нет, локальных веток кроме `main` нет, stash пуст.

## Что не сработало

- **`git -C ../brain_matrica pull --ff-only` падает** — в рабочем дереве brain'а лежат untracked `.codex/agents/project-auditor.toml` и `AGENTS.md`, которые перекрывает входящий коммит. Из проектной сессии в чужой репо не пишем (ADR-0001) → **не трогал**; входящие письма читались с устаревшего клона (`c4ef5eb7`, доступен `96afcb4d`). Развязать может только сессия в самом brain_matrica.

## Открытые вопросы для пользователя

_Нет._

## Не забыть (low-priority)

**Даты ротаций/дедлайнов — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §📅.** (парк клиентов ~30.07, deadcode ~04.08, ledger-токен ≤12.08, SSH ≤21.08.)

1. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз/комплектование неполны.
2. `RELEASE_WELCOME_HISTORY` — легаси-дубли меток `v1.18.0`–`v1.18.5`, почистить при следующем релизе.
3. Операторов предупредить: старые QR-этикетки с кодом `DET-` больше не находятся поиском (перепечатать).
4. `CURRENT_RELEASE_WELCOME` / `PREVIOUS_RELEASE_WELCOME` остались мёртвыми экспортами после #355 — снести при следующей чистке deadcode (не в релизном PR: инцидент v2026.725.2033).
