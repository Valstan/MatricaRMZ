# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-18 (Claude Opus 5, машина `PC79`) — **почта brain разобрана: мандат D-096 принят, пять строк отданы досрочно, вскрыты четыре дефекта**. Три правила пула внесены в канон (`AGENTS.md`): **#125** гейт гоняет CI (CDP e2e-smoke — единственное именованное исключение, его прогон называется строкой в теле PR), **#166** фича на 3+ PR с новой границей получает сквозной проход по всей ветке, **#152** одна дверь для дат. Пять дат (01.10, 02.10, 16.10, 30.10, 30.11) заведены в календарь дедлайнов — было 0 из 5. Закрыто 4-е место класса G355 (`deploy/systemd/install.sh:60`) с **поправкой к самому классу**: `| grep -q` под `pipefail` стреляет только когда вывод производителя не влезает в буфер трубы (64 КБ) — проверено замером, на этом месте гейт не молчал. Снята протухшая 🔴 «прод не собирает бэкенд» (закрыта ещё 14.09 PR #912, противоречила `PROJECT_STATE.md`). Recon-чистка живой прозы: имя хостера и id бота убраны из `GOTCHAS`/`PENDING` (закрытые письма не трогали). **Новые долги:** необратимый выкат бэкенда (🔴 откат снесён переездом на артефакты, артефакт живёт 14 дней), open redirect с отражением `Host` в nginx, `web-admin/dist` вне артефакта, write-authz #015 (🔴 6 из 25 таблиц синка держатся только на имени таблицы), sourcemap'ы админки публичны, опрос Telegram не отменяется на `SIGTERM`, веерное ревью не печатает покрытие.

**Предыдущее (18.09):** **релиз v3.41.0: три просьбы владельца 18.09 одной пачкой** ([#973](https://github.com/Valstan/MatricaRMZ/pull/973)–[#975](https://github.com/Valstan/MatricaRMZ/pull/975) + релизный PR). Синхронизация стала мгновенной (сервер будит клиента ждущим запросом, свои правки уезжают по сторожу локальной БД, интервал 5 мин → 60 с как страховка, миграция `0098` — индексы `last_server_seq` 19 таблицам); всплывающие меню выведены поверх окна порталом (грабля **M140**); складские справочники собираются из реплики без кэша «60 с в памяти». Релиз **с бэкендом и миграцией**. Прод-шаг прежней нитки закрыт в тот же день: `sync:restamp --tables erp_nomenclature --all` — 1665/1665. Прод-статус выката — в комментарии к релизному PR.

**Предыдущее (18.09):** складские справочники собираются на клиенте из реплики ([#975](https://github.com/Valstan/MatricaRMZ/pull/975), смержен): кэш «60 секунд в памяти» над REST снят, ответ 6 мс, правка справочника видна сразу. Живой прогон 8/8 — все 11 списков совпали с серверными.

**Предыдущее (18.09):** всплывающие меню выведены поверх окна порталом ([#974](https://github.com/Valstan/MatricaRMZ/pull/974), смержен; грабля M140, живой прогон 8/8) — корень был в контексте наложения предка, а не в `z-index`.

**Предыдущее (18.09):** **синхронизация стала мгновенной** ([#973](https://github.com/Valstan/MatricaRMZ/pull/973)): сервер будит клиента ждущим запросом `GET /ledger/state/wait`, клиент отправляет свои правки по сторожу локальной БД (1 с), интервал `startAuto` 5 мин → 60 с как страховка; миграция `0098` — индексы `last_server_seq` 19 синкаемым таблицам. Значок синка: крутится только стрелка. Живой прогон 10/10: чужая правка у клиента за 457 мс, своя на сервере за 615 мс. **Не выпущено — ждёт релиза с бэкендом и миграцией.**

**Предыдущее (17.09):** релиз v3.40.0 — пачка 5 программы осень-2026 ([план](plans/autumn-2026-program.md) §E1, §E2, §F1; [#968](https://github.com/Valstan/MatricaRMZ/pull/968)–[#971](https://github.com/Valstan/MatricaRMZ/pull/971) + релизный PR). Релиз с бэкендом (#968: журнал штампует `last_server_seq`); прод-шаг `sync:restamp --apply` выполнен при выкате.

**Предыдущее (17.09, E1):** **E1 программы осень-2026 сделан; попутно закрыта дыра доставки журнала** ([план](plans/autumn-2026-program.md) §E1; PR [#969](https://github.com/Valstan/MatricaRMZ/pull/969) + [#968](https://github.com/Valstan/MatricaRMZ/pull/968)). Список номенклатуры читает реплику первой, рисуется одним `VirtualTable` (заголовки групп — строки той же таблицы), «unknown» стал честным текстом, запись дожидается догоняющего синка, индекс родителя — в парити обоих клиентов. **Живой прогон на стенде PC79 с 720 деталями (`.verifier-electron/cdp-e1-nomenclature.mjs before|after`): до — 708 `<tr>`, ~520–570 мс до первой строки; после — 38 `<tr>`, 57 мс; 12/12.** Прогон поймал настоящий дефект контура: реплика не получала REST-правок (`pulled=0` при сдвиге курсора) — журнал в PG (#788) не штамповал `last_server_seq` на строку, а 16 прямых писателей (номенклатура, BOM, дедуп, скрипты) шли мимо `applyPushBatch`; на проде без номера 1607/1625 позиций и все строки BOM. Закрыто в журнале (`stampRowSeq`, #968) + `sync:restamp` (стенд: 1048 → 0); pull теперь переносит полную строку номенклатуры (раньше реплика была «тонкой» — без источника/родителя/артикула). **Прод-шаги релиза (по OK владельца): после выката бэкенда `sync:restamp` dry-run → `--apply` (≈1607 + 1425 + 1 строк); после обновления парка — `sync:restamp:apply -- --tables erp_nomenclature --all`.** Миграций нет; backend меняется (#968).

**Предыдущее (17.09, релиз v3.39.0):** **релиз v3.39.0: пачка 3 программы осень-2026 целиком** ([план](plans/autumn-2026-program.md) §B3, §B5, §D5, §C6, §D6; [#962](https://github.com/Valstan/MatricaRMZ/pull/962)–[#966](https://github.com/Valstan/MatricaRMZ/pull/966) + релизный PR). Отчёты «Двигатели» и «Этапы работ» стали списками (страницы `pages/reports/EnginesReportPage.tsx`, `WorkSheetsReportPage.tsx`; builder «Двигатели» снесён, builder «Этапы работ» остался источником конструктора); две новые ступени по датам ремонта в списке двигателей; «Базовые детали» раскрыты; своё ФИО в подпись — один раз на лист, память в самих `answers` (`_signature_prefill_offered`); печать одного этапа работ снята. Попутно: подбор отчётов ИИ ранжирует по словам задачи раньше принятых фильтров (списки фильтров маркером не принимают). **Живые прогоны на стенде PC79: `cdp-pack3.mjs` — все шаги, `cdp-pack3-d6.mjs` — все шаги** (драйверы в gitignored `.verifier-electron/`). Миграций нет, backend не тронут — code-only; на проде меняется только клиент. Прод-статус выката — в комментарии к релизному PR. **Ответы владельца 17.09 записаны в план** (§Context, B6, C6, D5, D6, блок H); **B6 замерен по проду (95 → 3) и ждёт его слова**.

**Предыдущее (17.09, релиз v3.38.0):** **релиз v3.38.0: пачка 4 программы осень-2026 целиком — карточка двигателя** ([план](plans/autumn-2026-program.md) §D1, §D2, §D4; [#958](https://github.com/Valstan/MatricaRMZ/pull/958)–[#960](https://github.com/Valstan/MatricaRMZ/pull/960) + релизный PR). «Детали и акты» разрезаны на две акт-вкладки при ОДНОЙ панели под обеими; обе проводки переехали в шапку, новая «Провести комплектность» датировала этап «Комплектовка сделана» в отчёте; пикеры сотрудников ранжируются по частоте выбора. Новые грабли: **M137** (динамический `storageKey` у `useListUiState`), **M138** (два разных признака комплектности + расходящиеся копии шаблона), **M139** (автоподстановка на изменяемом состоянии). Миграций нет, backend не тронут — code-only; на проде меняется только клиент. **Главный урок пачки:** три из четырёх дефектов нашли ЖИВЫЕ прогоны, а не ревью и не типы — смоук обязан ходить тем путём, которым ходит оператор (мышью), и сам готовить предусловие, а не предполагать его.

**Предыдущее (17.09, D4):** **D4 программы осень-2026: пикеры сотрудников ранжируются по частоте выбора** ([план](plans/autumn-2026-program.md) §D4; PR #TBD). Чистое ядро `ui/utils/pickerRank.ts` (счётчики `{n,last}` на точку выбора, защитный разбор, кэп 24) + привязка `ui/hooks/usePickerRank.ts` — МОДУЛЬНЫЙ синглтон поверх `localStorage['matrica:picker-rank:v1']` через `useSyncExternalStore` (полей на экране несколько, и каждое писало бы весь объект из своей копии состояния, затирая соседей). Необязательный проп `rankKey` у `EntityReferenceField` и `SearchSelect`; ключ называет РОЛЬ, а не экран, и проставлен на всех пикерах сотрудников. Частота, а не recency: один случайный выбор не должен вытеснять того, кого берут каждый день; хвост списка сохраняет исходный порядок вызывающего. Попутно удалён третий механизм — локальная «recency» подписантов в `WorkOrderDetailsPage` (`wo:recentSignatureEmployees`). Подстановка своего ФИО в подписи акта стала одноразовой на загруженный лист (новая грабля **M139**). **Живой прогон поймал три настоящих дефекта этой же правки, которых не увидело ревью:** рейтинг ЗАМЕРЗАЛ навсегда (кэп выбрасывал новичка сразу после записи — новый сотрудник не мог попасть в подсказку никогда); клик мышью не считался (у строки списка был свой `onChange` мимо воронки — счётчик рос только с клавиатуры); подстановка ФИО перестала срабатывать вовсе (эффект успевал пометить подписи пройденными до того, как лист приедет с сервера). Все три исправлены, на каждый — тест. Гейты зелёные целиком (`typecheck`, `lint`, `test` — 3189 тестов), CDP-смоук `cdp-employee-picker-rank.mjs` **прогнан на живом стенде: все шаги пройдены**. Миграций нет, backend не тронут — code-only.

**Ранее (17.09, D2):** **D2 программы осень-2026: кнопки «Провести …» в шапке акт-вкладок** ([план](plans/autumn-2026-program.md) §D2; PR #TBD). «Провести дефектовку» переехала из синей рамки под таблицей в плоский ряд шапки панели (гейт и `conductDefect()` те же, ряду добавлен перенос; в рамке остались «Печать требования», строка состояния версии и подпись про поэкземплярный учёт). Новая «Провести комплектность» под `props.canEdit` ставит дату осмотра в акт, ЕСЛИ поле пусто, — новых атрибутов нет, серверной транзакции нет (у комплектности не двигаются ни остатки, ни экземпляры), идемпотентность по построению: при заполненной дате функция выходит до записи и говорит «уже проведена». По этой дате этап «Комплектовка сделана» в отчёте наконец получил «Дату этапа»; **членство в группе НЕ сужено** — ветка осталась «дата ИЛИ прежний признак», сужение вынесено владельцу (см. открытые вопросы). `save()` теперь возвращает, легла ли запись, а таймер «Сохранено» гасит только собственное сообщение. Гейты зелёные целиком (`typecheck`, `lint`, `test` — 3162 теста в четырёх пакетах), CDP-смоук `cdp-engine-conduct-buttons.mjs` **прогнан на живом стенде: 39/39**. Первый прогон дал 8 FAIL и поймал настоящее поведение, а не регрессию: у стендового двигателя список деталей пуст, и кнопка честно отказала («в акте комплектности нет строк»); смоук теперь заводит строку-фикстуру мостом ДО открытия карточки (панель читает лист при монтировании) и снимает её в уборке. Миграций нет, backend не тронут — code-only.

**Ранее (17.09, D1):** **D1 программы осень-2026: вкладка «Детали и акты» разрезана на «Акт комплектности» и «Акт дефектовки»** ([план](plans/autumn-2026-program.md) §D1; PR [#958](https://github.com/Valstan/MatricaRMZ/pull/958)). Панель под обеими вкладками **одна**, и обёртка одна: лист `engine_inventory` общий — одно сохранение, одна очередь автосейва, два экземпляра затирали бы правки друг друга, а две обёртки размонтировали бы панель при переключении. Вид акта приходит в панель пропом, собственного переключателя у неё больше нет; внутри каждой вкладки три сворачиваемых блока («Оформление», «Сведения», «Детали» — развёрнут), свёрнутость помнится раздельно по видам акта (новая грабля **M137** — почему два вызова хука, а не один с динамическим ключом). Дефектовочный хвост (заявка в снабжение, наряд, «Провести дефектовку», истории и версии) виден только на дефектовке. Хранение, `meta_json` и ключи `answers` не тронуты, миграций нет — code-only. Гейты `typecheck` и `lint` зелёные; сторожа-свойства и CDP-смоук вкладок едут тем же PR, рецепт стенда (`docs/machines/PC79.md`) переведён на новый якорь вкладки.

**Ранее (16.09):** релиз v3.37.0 — пачка 2 программы осень-2026 целиком ([план](plans/autumn-2026-program.md) §C1–C5; [#950](https://github.com/Valstan/MatricaRMZ/pull/950)–[#955](https://github.com/Valstan/MatricaRMZ/pull/955) + релизный PR). Этапы работ: переименование, правка раздвижной строкой в списке, вторая панель, Ctrl+K, печатный бланк. Плюс по просьбе владельца: «Колонки списка» вернулись в тулбар, «Сбросить фильтр» — первым в панели, значок ярлыка вкладки стал закладкой. Гейты зелёные, живые смоуки на стенде PC79 прошли по каждому шагу. Миграций нет, lockfile не менялся — code-only. Прод: выкачено и проверено 17.09 08:08–08:13 МСК — оба порта отдают 3.37.0, `/updates/status` без ошибки, blockmap 200 (дельта жива), APK 3.37.0 у диспетчера; подробности — [комментарий к релизному PR](https://github.com/Valstan/MatricaRMZ/pull/956#issuecomment-5709081013).

**Branch:** `main` (релиз v3.39.0 смержен; следующая ветка — по пачке 5).
**Last released version:** **v3.39.0**, выкат 17.09 (пачка 3).

**Текущая нитка:** просьбы владельца 18.09 закрыты и выпущены в v3.41.0. Дальше — программа осень-2026: выпущены пачки 1–5 (v3.36.0–v3.40.0), впереди **нитка G** (строгие строки листа, дубли: G1 → G2 → релиз → G3 → релиз → G4), затем **блок H** (авторство и история объектов, журнал действий, время онлайн).
**E2 ✅ (17.09, [#970](https://github.com/Valstan/MatricaRMZ/pull/970)):** памятка в `docs/WAREHOUSE.md` §«Обобщённая позиция и варианты», три PR-кандидата в PENDING (импорт съедает родителя; `part_id` без правила — нужно решение владельца; остатки агрегируются только сводом).
**F1 ✅ (17.09):** машины для обхода подобраны по проду — `PC76`, `PC19`, `PC20` (запасная `PC36`), список с логинами выдан владельцу в чате; запись в PENDING §«Торрент-раздача…». Пачка 5 закрыта.
**Сделано после разбора почты (18.09):** **выкат бэкенда стал обратимым** — копия прежнего `dist` в `.deploy-backup/<стамп>/` до замены, откат при отказе health primary, отказ до `rm -rf` при неустановленной рантаймовой зависимости, окно ожидания приведено к канону (было 60 с при требовании ≥90 — здоровый медленный primary объявлялся упавшим и после появления отката стрелял бы им по исправному выкату). Гейт `deploy-backend.test.sh` — 16 проверок, подключён в `lint.yml`, **проверен контролем** (3 мутации → 2/2/4 красных). Новая грабля **M141**: переезд уносит гарантию, которая держалась на прежнем устройстве, а не на требовании.

**Следующий шаг:** (0) **из разбора почты, по убыванию остроты:** open redirect в nginx (одна строка, но катится через `deploy/nginx/install.sh` — прод-шаг); `web-admin/dist` в артефакт `backend-dist.yml`; дальше #015 механизмом (поле «кто владеет строкой» + сторож-тест), отмена опроса Telegram на `SIGTERM`, `immutable` + sourcemap админки. Даты мандата — в календаре `PENDING_FOLLOWUPS`; (1) **через день-два повторить** `corepack pnpm -F @matricarmz/backend-api sync:restamp:apply -- --tables erp_nomenclature --all` на проде — второй прогон закрывает машины, которые синхронизировались, не успев обновиться на 3.41 (перештамп повторяем, данных не меняет; в парке есть отстающая на 3.24.0); (2) ждут владельца: машины F1 (`PC76`, `PC19`, `PC20`, запасная `PC36`) и обход с `lan-share-firewall.ps1`, B6 (сужение этапа «Комплектовка сделана», замер 95 → 3), B4 (снятие `engine_stages` по приёмке отчёта), название этапа ОТК, три PR-кандидата E2; (3) далее нитка G, затем блок H. Помнить: (1) `_journal.json` в `backend-api/drizzle/meta/` — единственный список миграций, которые мигратор исполняет: файл `.sql` без записи в нём молча не применяется, приёмка миграции — по состоянию БД; (2) всплывающее меню обязано ехать через `PopupLayer` (сторож `popupLayer.guard.test.ts`, грабля M140); (3) подписи складских справочников живут в двух местах (сервер `listWarehouseLookups` и клиент `warehouseLookupsReplica.ts`) — меняешь одно, меняй второе; (4) отчёты «Двигатели» и «Этапы ремонта» считают комплектовку «дата ИЛИ галочки», пока B6 не решён (M138); (5) `engine_stages` не трогать до приёмки B4; (6) на стенде PC79 после смоука Node-ABI `better-sqlite3` возвращён из скретчпада.

**Решения владельца 16.09** (продублированы в плане): «Провести комплектность» фиксирует акт; нитка E2.2→E3→дубли берётся последней; рейтинг по частоте во всех пикерах сотрудников; «Заготовки отчётов» → «Готовые отчёты»; машины для LAN-обхода подбираю по прод-данным, утверждает владелец.

**Приёмка живьём (за владельцем):** отчёт «Двигатели на заводе: этапы ремонта» на настоящих данных — условие снятия `engine_stages` (B4); раздвижная строка этапа работ у мастера — завести и **поправить** строку прямо в списке (после правки в колонке «Кто» встаёт правщик). **После C1 добавилось:** в «Колонках списка» того же отчёта теперь рядом «Дата этапа» (этап на заводе) и «Дата этапа работ» (строка) — подписи разные, дублей нет, но взглянуть стоит; в ленте истории ремонта старые записи остались с префиксом «Ведомость: », новые пишутся «Этап работ: » (GOTCHAS **M136**, миграция note — только по отдельному OK).

**Прод (17.09):** свободно **4.0 ГБ из 9.9** (занято 5.5 ГБ, 58 %) — 14.09 было 5.6 ГБ, рост за три дня практически нулевой. Инструмент повторного съёма — `scripts/prod-ops/prune-virtual-store.mjs`; корневой `pnpm install` на проде вернёт снятое.

## Открытые вопросы для пользователя

- **Обход машин для раздачи соседям** — подобраны 17.09 (`PC76`, `PC19`, `PC20`, запасная `PC36`; логины — в чате); ждёт утверждения владельца и обхода с `lan-share-firewall.ps1`.
- **Telegram-логин владельца в карточке сотрудника** — правится владельцем в программе.
- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота нет.
- **Дубли листов engine_inventory**: владелец 16.09 взял E2.2→E2.3→E3→чистку в программу (блок G плана); apply — отдельным OK.
- **Название этапа ОТК перед отгрузкой** — предложено «Выходной контроль ОТК» (запасное — «Комплектация к отгрузке»); узел заводит владелец в справочнике видов работ.
- Прежние: внешний SSH-порт прода; старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника; подписные грифы печатных форм.

## Не забыть (low-priority)

- **Keystore Android:** ключ перевыпущен 13.09, отпечаток `C1:EB:…:D0:39`, хранилище на PC79, четыре секрета репо перезаписаны, гейт зелёный. **Офлайн-копия сделана владельцем 14.09** — пункт закрыт и с каждого старта снят.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09; следующий до R4b, начать с четырёх непокрытых линз.
- **Перемер роста диска 03.10**; там же первые цифры счётчика обращений к файлам (D-073). База отсчёта новая: **5.6 ГБ занято (60 %) на 14.09** после чистки; прежние отметки — 6.6 ГБ 12.09 и 7.9 ГБ утром 14.09.
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка.
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Файл `MISSING 21a1b799-… Кто-то ночью спит.mp4` — строка в БД есть, файла нет.
