# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (релиз **v2026.711.2043** выпущен и раскатан на прод; активной нитки нет)
**Updated:** 2026-07-12 (Claude Opus 4.8, машина `rmz4val`)
**Branch:** `main` (= origin/main). Дерево чистое, stash пуст, открытых PR нет, локальная только `main`.
**Last released version:** **v2026.711.2043 на проде** (сверено 2026-07-12): `/health` = 2026.711.2043, `/updates/status.latest` = 2026.711.2043 (`lastError: null`, infoHash заполнен), оба сервиса `active`, прод-checkout HEAD = `3d0e159f` (#175).

## Текущая нитка

_n/a_ — активной нитки нет.

- Сессия **2026-07-11** отгрузила **owner-батч 2026-07-10 (темы A→H, 11 заданий)** релизом **v2026.711.2043** (#161–#175): A/H — integrity ролей/синка + подсказки зависимостей разделов; B/D/E — список нарядов + наряд сборки из двигателей (ПКМ) + авто-fill BOM марки; C — local-first карточка номенклатуры (тормоза 30-60с); F — одна панель «Вложения» + фикс несеянных defs марки; G — bulk-галочки актов + act-scoped «Распространить». Прод-данные — скриптом `fix:owner-batch-20260710`.
- Сессия **2026-07-12** (эта) закрыла **хвост документации** прошлой сессии (PR #176): закоммичены строки COMPLETED.md (§A+§H, §B+§D+§E от прошлой сессии + добита §C+§F+§G), завершённый план `owner-batch-2026-07-10.md` заархивирован в `plans/_archive/`. Затем — **сверка и чистка бэклога**: PENDING_FOLLOWUPS переписан в «только открытое» (удалены ✅-стены owner-батча A→H, черновики 3d, товары/услуги deferred-create, прогноз Фаза 3b, security H1/H8 — всё уже в COMPLETED/git; ложно-открытые пункты handoff-бэклога тоже сняты). Причина исходной путаницы: свежий handoff собран копированием старого backlog без сверки с PENDING.

## Следующий шаг

**Активной нитки нет.** Бэклог сверен 2026-07-12 — [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) теперь «только открытое». Реально открытое верхнего уровня:
- 🔴 **forward-proxy VPS для AI** (Anthropic режет РФ-IP) — решение владельца, PENDING §Блокер.
- 🟢 **Пилот конструктора UI** — план готов ([`plans/ui-builder-modules.md`](plans/ui-builder-modules.md)); перед стартом 3 вопроса владельцу (кто автор экранов / первый кейс / per-user vs общезаводские).
- 🟢 **4 фичи-идеи** (паспорт ремонта / дефект+фото→BOM / калькулятор себестоимости / QR) — каждая отдельной ниткой с разведки+плана.
- 🟡 **Глубокая де-дупликация `directory_parts↔erp_nomenclature`** — техдолг DET-зеркал (трогает прод-данные → план + решение владельца по семантике мерджа).
- 🟡 **Watchdog-хвосты** (install-dir discrepancy / field-test / AV-мониторинг), **H7-остаток** security, **унификация вложений детали** (хвост F).
- Операционное (не код): суперадмин-логин прекратить, мастера жмут «Выдать в работу», обкатка «Резинового».
- ⏳ Ждёт окна: ledger-токен ~2026-08-04, deadcode-дельта ~2026-08-04.

## Контекст

- Прод: **v2026.711.2043**, оба сервиса active (сверено read-only probe 2026-07-12). Миграций в этом релизе, помимо 0074 (акт-шаблоны, релиз 709), owner-батч не вводил новых DDL — фиксы аддитивные/EAV.
- Ключевое в батче A→H — план (архив): [`plans/_archive/owner-batch-2026-07-10.md`](plans/_archive/owner-batch-2026-07-10.md) (полная механика тем + статус-таблица A→H все ✅). Системная находка A3: **LWW-guard по `updated_at` в `applyPushBatch`** — закрыта дыра всего EAV-синка (отставший клиент со старой pending-очередью тихо затирал свежее значение).
- Открытых PR: нет. Локальных веток с un-pushed коммитами: нет (после мержа chore-хвоста).

## Открытые вопросы для пользователя

- Перед пилотом конструктора UI: (1) кто автор экранов — только владелец/админ или любой оператор? (2) какой первый реальный кейс собрать? (3) спеки per-user или общезаводские с правами? (план §Открытые вопросы)

## Не забыть (low-priority)

1. **Housekeeping:** `docs/plans/owner-batch-2026-06-19.md` висит неархивированным («Активный план», ~3 недели) — 5/6 отгружено, единственный открытый хвост (F — унификация оргмоделей) теперь захвачен в PENDING § Техдолг, так что план можно заархивировать в `plans/_archive/`. Заголовок «Активный» устарел.
2. **AV-ложнопозитивы watchdog'а** — поглядывать в «Критические события» (принятый риск unsigned-бинаря).
3. Ledger release-token — ротация до ~2026-08-04 (PENDING ⏳); первый релиз после ~2026-08-01 упрётся, минтить новым.
4. Ротация SSH-ключей прода — до 2026-08-21 (PROJECT_STATE).
5. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст (операционный, передать мастерам).
6. deadcode-прогон (месячная дельта) — ~2026-08-04.
