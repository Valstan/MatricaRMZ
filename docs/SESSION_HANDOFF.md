# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-09-15 (Fable 5.1, машина `PC79`) — **релиз v3.35.0: где какой двигатель на заводе; ведомость одной строкой; права на ведомости по именам** ([#938](https://github.com/Valstan/MatricaRMZ/pull/938), [#939](https://github.com/Valstan/MatricaRMZ/pull/939), [#940](https://github.com/Valstan/MatricaRMZ/pull/940) + релизный PR). Задание владельца 15.09 (поздний вечер) закрыто по коду целиком; оба UI-шага проверены на стенде. Миграций нет. Прод-статус — тело релизного PR.
**Branch:** `main`, синхронна с origin. Открытых PR нет.
**Last released version:** **v3.35.0**, выкат 15.09.

**Прод-шаг по правам (после деплоя v3.35.0):** `corepack pnpm -F @matricarmz/backend-api perm:revoke-work-sheets` (dry-run) → `…:apply` — явный `allowed=false` обоих кодов всем, кроме суперадмина; **запись в прод-БД — по явному OK владельца**. После этого владелец выдаёт в web-admin поимённо `work_sheets.edit` (строки) и `work_sheet_types.edit` (виды работ) — Савиных, Сапегин, Фатыхова по прежней договорённости. Без apply старые admin/`user` право по роли уже не получают (цикл исправлен), но остаются прежние персональные overrides, если были.

**Открытое после релиза — ждёт слова владельца:** перевод остальных отчётов в уклад списка (план и кандидаты — `docs/plans/reports-as-lists-2026-09.md`: `engines`, `work_sheets`, `engine_stages`); даты стадий «отремонтирован / утиль / ремонт начат» в строке списка двигателей (в отчёте пока прочерк); печатный бланк ОДНОЙ ведомости; ведомость во второй панели «2 рядом»; поиск ведомостей по Ctrl+K. **Приёмка живьём:** отчёт «Двигатели на заводе: этапы ремонта» на настоящих данных (порядок групп, что считать «дефектовкой»); черновая строка ведомости у мастера; `grep -q` под `pipefail` в prod-ops — 3 места, отдельный PR (PENDING).

**Прод (15.09):** свободно ~4 ГБ из 9.9 после съёма клиентской обвязки ([#923](https://github.com/Valstan/MatricaRMZ/pull/923)). Инструмент повторного съёма — `scripts/prod-ops/prune-virtual-store.mjs`; корневой `pnpm install` на проде вернёт снятое.

## Открытые вопросы для пользователя

- **Название вкладки «Заготовки отчётов»** — взято вместо «Витрина шаблонов» из плана (в каталоге уже есть секция «Шаблоны отчётов» про другое). Переименовать — одна строка.
- **Обход машин для раздачи соседям** (правило брандмауэра + тумблер + перезапуск) — с каких 2–3 машин начать.
- **Telegram-логин владельца в карточке сотрудника** — правится владельцем в программе.
- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота нет.
- **Дубли листов engine_inventory** (≈206 двигателей): мягко удалить лишние — после E3, отдельным OK.
- Прежние: внешний SSH-порт прода; старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника; подписные грифы печатных форм.

## Не забыть (low-priority)

- **Keystore Android:** ключ перевыпущен 13.09, отпечаток `C1:EB:…:D0:39`, хранилище на PC79, четыре секрета репо перезаписаны, гейт зелёный. **Офлайн-копия сделана владельцем 14.09** — пункт закрыт и с каждого старта снят.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09; следующий до R4b, начать с четырёх непокрытых линз.
- **Перемер роста диска 03.10**; там же первые цифры счётчика обращений к файлам (D-073). База отсчёта новая: **5.6 ГБ занято (60 %) на 14.09** после чистки; прежние отметки — 6.6 ГБ 12.09 и 7.9 ГБ утром 14.09.
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка.
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Файл `MISSING 21a1b799-… Кто-то ночью спит.mp4` — строка в БД есть, файла нет.
