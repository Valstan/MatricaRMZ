# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (Electron 41→43 апгрейд завершён и отгружен релизом v2026.705.2235 на прод; активной нитки нет)
**Updated:** 2026-07-06 (Claude Opus 4.8, машина `rmz4val`)
**Branch:** `main` (= origin/main). Дерево чистое, stash пуст, открытых PR нет, локальная только `main`.
**Last released version:** **v2026.705.2235 на проде** (2026-07-06). `/health` подтверждён, оба сервиса active, `/updates/status.latest` = 2026.705.2235, blockmap 200 (дельта работает). Накопленные #76–#89 доехали операторам этим релизом.

## Текущая нитка

_n/a_ — сессия 2026-07-05/06 закрыла самодостаточную нитку **Electron 41→43** (PR #89) и **раскатала релиз v2026.705.2235** (PR #90) на прод. Всё зелёное, прод-верифицировано. Грабля прод-деплоя задокументирована (PR #91).

## Следующий шаг

**Активной нитки нет — выбрать из бэклога.** Кандидаты (сверено с PENDING на 2026-07-06):
- **3d для EAV-редакторов** (договоры/контрагенты/марки — `SimpleMasterdataDetailsPage`, другой снапшот-механизм, чем у заявок); deferred-create товаров/услуг через номенклатурную машинерию. Продолжение Phase 3d (заявки уже сделаны, #84).
- **Live-аудит раскладки клиента** (PENDING) — **теперь частично разблокирован:** на rmz4val **есть** установленный релиз-клиент v2026.704.951 (профиль раньше ошибочно говорил «клиент не установлен»); можно осмотреть `%LOCALAPPDATA%\Programs\@matricarmzelectron-app\` / `%APPDATA%\MatricaRMZ\` прямо тут.
- **Решения владельца:** M2b (сброс кэша операторам), forward-proxy VPS для AI (🔴 блокер).

Прочее открытое — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md).

## Контекст

- Прод: **v2026.705.2235**, оба сервиса active. Сессия трогала прод деплоем (git pull + install + build серверных пакетов + артефакты updater + ledger-publish + рестарт) — всё обратимо/штатно. Убран orphaned-зомби `pnpm install` с 3 июля.
- PR сессии: #89 (E43), #90 (релиз v2026.705.2235), #91 (GOTCHAS M16) — все смержены squash, ветки удалены.
- План завершён и заархивирован: [`plans/_archive/electron-43-upgrade.md`](plans/_archive/electron-43-upgrade.md).
- COMPLETED: добавлена строка по E43 (секция «Обновления / установка / версии»).
- Грабля прод-деплоя: [`GOTCHAS.md`](GOTCHAS.md) **M16** дополнен (флаки-сеть fetch + env-флаги + pkill-self-match).
- Находка отправлена в brain: [`mailbox/to-brain/2026-07-06-pnpm-install-network-stall-deploy.md`](../mailbox/to-brain/2026-07-06-pnpm-install-network-stall-deploy.md).

## Открытые вопросы для пользователя

- Нет.

## Не забыть (low-priority)

1. **Спот-чек владельцем после того, как клиент обновится до v2026.705.2235:** черновики заявок (автосейв/восстановление/«Сохранить как черновик» + раздел «Черновики»); прогноз→дефициты→«Создать заявку в снабжение»; перечень деталей по марке (фильтр/режимы/печать среза).
2. **Локальный натив на rmz4val теперь под Electron-ABI 148** (после этой сессии). Для backend-тестов на этой машине нужен Node-ABI rebuild (`pnpm rebuild better-sqlite3`) — GOTCHAS M2 / профиль rmz4val.
3. Ledger release-token — следующая ротация до ~2026-08-04 (PENDING ⏳).
4. Ротация SSH-ключей прода — до 2026-08-21 (PROJECT_STATE).
5. AV-ложнопозитивы watchdog'а — поглядывать в «Критические события».
6. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст (операционный, передать мастерам).
