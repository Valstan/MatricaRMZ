# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (релиз раскатан, активной длинной нитки нет)
**Updated:** 2026-07-16 (Claude Fable 5, машина `PC40`)
**Branch:** `main` = `origin/main`. Дерево чистое, stash пуст, открытых PR нет.
**Last released version:** **v2026.716.1408** — табель: сортировка сотрудников + компактная печать со шрифтами по блокам.

## Текущая нитка

Активной длинной нитки нет. Сессия 2026-07-16: (1) закрыт H7 шаг «в» — fail-closed `normalizeRole` (#228); (2) найден и починен баг ночного бэкапа — таарил стейл-паразита вместо канонического ledger (#229, раскатано на прод); (3) ротирован ledger release-token (exp 2026-08-15); (4) фича табеля по запросу владельца (#230) + релиз v2026.716.1408.

## Следующий шаг

Выбор владельца:

1. **Обкатка табеля вживую** — А→Я / стрелки / перетаскивание, печать на один лист с настройкой шрифтов.
2. **Ф2 де-дупа `blank-synthetic-codes`** — гейт **красный** 2026-07-16: активны клиенты < 2026.712.1818 (2026.707.1412 ×2, 2026.701.843, 2026.628.2326). Проверить versions снова через неделю-полторы; после apply — M31-верификация PG.
3. **Обкатка пилота UI-конструктора «Мои экраны»**.

## Контекст

- **Релиз v2026.716.1408** — клиент (табель UI) + backend (rows-order endpoint, fail-closed normalizeRole). Миграций не вёз.
- **Бэкап-фикс #229:** проверить лог `/var/log/matricarmz/backup.log` после 03:17 17.07 — архив должен вырасти (~124М ledger вместо 58М стейла).
- Наряды-черновики: №93 самопочинился, №90 ещё на контейнере (само; глянуть через пару недель).

## Открытые вопросы для пользователя

- **Удалить стейл-паразита `~/MatricaRMZ/backend-api/ledger`** (58 МБ, внутри копия server-key.json)? Отрастание остановлено (`MATRICA_LEDGER_DIR` в `.env`), бэкапы перенацелены — сносить безопасно, но это rm на проде → жду явного «да».
- **AI/VPS** — отложено владельцем на неопределённый срок; не поднимать.

## Не забыть (low-priority)

1. Ledger release-token — ротирован 2026-07-16, exp **2026-08-15**; следующая ротация ≤ ~2026-08-12. Протухшая копия в `/etc/matricarmz/matricarmz.env` — мёртвый конфиг, вычистить при случае.
2. Ротация SSH-ключей прода — до 2026-08-21.
3. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст.
4. deadcode-прогон (месячная дельта) — ~2026-08-04.
5. Старые двигатели без внутреннего номера — прочерк, задачу не заводим.
