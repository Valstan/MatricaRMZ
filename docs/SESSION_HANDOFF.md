# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-09-10, день — релиз v3.25.0: лист деталей нового двигателя ждёт двигатель, телеметрия пишется у всех; нитка закрыта релизом (сессия Opus 5, машина `PC79`)
**Branch:** `main`.
**Last released version:** **v3.25.0**, выкат 10.09 днём (этот релиз; прод-приёмка — в теле релизного PR).

## Текущая нитка

**Свободных ниток нет.** Тревогу 10.09 11:05 (`client.sync.blocked_rows`) разобрали замером и закрыли кодом — [#853](https://github.com/Valstan/MatricaRMZ/pull/853), релиз v3.25.0. Итог одной строкой — в [`COMPLETED.md`](COMPLETED.md), классы — **M122** и **M123** в [`GOTCHAS.md`](GOTCHAS.md).

Коротко, чтобы не поднимать заново: потерь не было — 47 из 52 пропущенных строк доехали через 5–40 с. Лист деталей новой карточки уходил раньше двигателя (deferred create + автосохранение), брошенная карточка оставляла сироту, два автосохранения давали два листа. Пункт «PC19 — единственная копия работы» снят: 8 строк оказались пустыми листами брошенных карточек (прочитаны в журнале сервера).

## Следующий шаг

Своего продолжения нитка не требует. Что лежит наготове (по убыванию срочности):

1. **Приёмка 3.25 после обновления парка** (не раньше, чем PC19 и соседи встанут на 3.25): новых `client.sync.blocked_rows` на новые двигатели нет; двигателей с двумя живыми листами, созданными после выката, — ноль; `audit_log ui.*` появился у операторов вне «Администрирования». Скрипты замера — как в разборе 10.09 (`ssh matricarmz "bash -s" < скрипт`, профиль `docs/machines/PC79.md` §«Запросы к прод-БД»).
2. **Сеть VPS рвётся круглосуточно** (Telegram, клиенты, ssh — один путь). Открытый пункт 🟡 в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md); следующий ход — хостер, не код.
3. **Прод-часть слияния дублей сотрудника** — dry-run → отчёт → OK владельца **в том же ходе** → apply → `users:parity` (гейт #025).
4. **Снос каталога цепочки `~/matricarmz-ledger` (4,8 ГБ)** — ждёт отдельного OK владельца (план §J2 шаг 7; после этого откат невозможен, M30).
5. **E2.2 плана [`engine-inventory-lines`](plans/engine-inventory-lines-2026-09.md)** — чтение списка деталей из реплики с откатом на `meta_json`.
6. Хвосты: реплика справочника складов у обновившегося клиента; PostgreSQL на зоне `Host`; дубли двигателей разбирают операторы руками.

## Контекст

- Остальное из обзора логов за 48 часов 10.09 — не код: 141 сбой опроса Telegram и 16 обрывов у клиентов (сеть VPS, пункт 🟡); 55 предупреждений о логине на двух машинах; 20 отказов доступа у учётки без роли (6 минут 08.09); 3 полных скачивания установщика вместо дельты.
- PC19 сама обновилась до 3.24.0 утром 10.09 — 8 старых пустых строк остались в её карантине, вреда от них нет.
- CDP-смоук нового двигателя — `.claude/skills/verifier-electron/scripts/cdp-new-engine-list.mjs`; грабли стенда (две оболочки вкладок, старые помощники в живой странице) — в профиле `docs/machines/PC79.md`.
- Планы: [`plans/engine-inventory-lines-2026-09.md`](plans/engine-inventory-lines-2026-09.md) (E2.2), [`plans/ledger-journal-in-pg-2026-09.md`](plans/ledger-journal-in-pg-2026-09.md) (§J2), [`plans/matrica-v4-kickoff-2026-08.md`](plans/matrica-v4-kickoff-2026-08.md) §R4.
- Прод: последняя применённая миграция — `0094_chat_rooms`. Каталог цепочки 4,8 ГБ ещё лежит.
- **Чат оповещений с «Сарафаном» — общий, доказано хэшем**; бот НЕ общий. Разведение чатов — ход владельца.
- Письма brain'у: `2026-09-10-a-child-that-saves-before-its-parent-exists.md` (в релизном PR), ранее — `2026-09-09-the-chat-hash-matches-and-we-do-not-go-through-your-worker.md`.

## Что не сработало

- **Посылку «единственная копия работы» приняли без взгляда в содержимое** (09.09). Журнал пишется до применения — отвергнутые строки со всем содержимым лежали на сервере, и одна выборка показала бы пустые листы. Правило: прежде чем объявлять данные потерянными или уникальными, прочитать их там, где они точно есть.
- **Смоук трижды падал на пробе, а не на программе:** в DOM две оболочки вкладок (щелчок уходил в невидимую кнопку МЕНЮ), панель листа скрыта на «Основном», помощники прошлого прогона оставались в странице. Всё записано в профиль машины.
- **Числа из AI-отчёта трижды приняли за замер** (06.09, 08.09, 09.09). Величину, которую цитируют человеку, проверять вопросом «отвечает ли она ровно на то, чем названа».
- **Автомерж съедает коммит, досланный после вооружения.** Сверять по содержимому `main`, вооружать автомерж последним действием.
- **`git show origin/main:.claude/…` в Git Bash** переписывает путь после двоеточия в Windows-путь — нужен `MSYS_NO_PATHCONV=1`.

## Открытые вопросы для пользователя

- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота у проекта нет.
- **Общий чат оповещений** — разводить ли (создать чат, добавить бота).
- **Снос каталога цепочки на проде** — нужен отдельный OK (план §J2 шаг 7).
- **Раздача соседям** — включается по машинам (правило брандмауэра → тумблер в админке → перезапуск клиента).
- **Дубли листов engine_inventory** (≈206 двигателей): мягко удалить лишние — после E3, отдельным OK (данные прода).
- Прежние, не снятые: менять ли внешний SSH-порт прода; стереть ли старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника на парк; подписные грифы печатных форм (ФИО в коде).

## Не забыть (low-priority)

- **Keystore Android:** свёрток так и не собран, единственная копия ключа на PC40. Brain просит поднимать первым пунктом каждого старта.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09 сделан; следующий до R4b, начать с четырёх непокрытых линз.
- **Перемер роста диска 03.10** — по августовскому темпу; там же первые цифры счётчика обращений к файлам (D-073).
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка (та же метрика, что R4b).
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Файл `MISSING 21a1b799-… Кто-то ночью спит.mp4` — строка в БД есть, файла нет; разобрать отдельно.
