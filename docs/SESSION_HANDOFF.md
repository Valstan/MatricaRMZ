# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (релиз раскатан, активной длинной нитки нет)
**Updated:** 2026-07-16 (Claude Fable 5, машина `PC40`)
**Branch:** `main` = `origin/main`. Дерево чистое, stash пуст, открытых PR нет.
**Last released version:** **v2026.716.1619** — печать табеля v2: часы без «Я» во всю ячейку, галочки блоков, прямая печать, тёмные линии.

## Текущая нитка

Активной длинной нитки нет. Сессия 2026-07-16 (вторая): печать табеля v2 по замечаниям владельца (#234) + релиз v2026.716.1619. CDP-смоук прошёл (экран/превью/окно прямой печати).

## Следующий шаг

Выбор владельца:

1. **Обкатка печати табеля вживую на принтере** — часы во всю ячейку, тёмные линии, комментарии отдельным листом.
2. **Ф2 де-дупа `blank-synthetic-codes`** — гейт **красный** 2026-07-16: активны клиенты < 2026.712.1818. Проверить versions ~2026-07-24; после apply — M31-верификация PG.
3. **Обкатка пилота UI-конструктора «Мои экраны»**.

## Контекст

- **Релиз v2026.716.1619** — клиент-only (печать табеля); миграций не вёз, lockfile не менялся.
- **Бэкап-фикс #229:** проверить лог `/var/log/matricarmz/backup.log` после 03:17 17.07 — архив должен вырасти (~124М ledger вместо 58М стейла).
- Наряды-черновики: №93 самопочинился, №90 ещё на контейнере (само; глянуть через пару недель).

## Открытые вопросы для пользователя

- **AI/VPS** — отложено владельцем на неопределённый срок; не поднимать.

## Не забыть (low-priority)

1. Ledger release-token — ротирован 2026-07-16, exp **2026-08-15**; следующая ротация ≤ ~2026-08-12.
2. Ротация SSH-ключей прода — до 2026-08-21.
3. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст.
4. deadcode-прогон (месячная дельта) — ~2026-08-04.
5. Старые двигатели без внутреннего номера — прочерк, задачу не заводим.
