# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE
**Updated:** 2026-07-21 (машина `PC40`)
**Branch:** `main` = `origin/main`.
**Last released version:** **v2026.721.1616** (отчёт «Двигатели и контракты» + кнопка входа + списочное кол-во деталей по маркам). Прод-статус — в финальном отчёте релиза / теле релизного PR.

## Текущая нитка

Активной нитки нет — обе нитки прошлой сессии закрыты:

1. **Отчёт «Двигатели и контракты»** (`engines_contracts_overview`) — отгружен в v2026.721.1616 (PR #291 отчёт + #292 кнопка). Разрезы контракты/марки/двигатели, сворачиваемые секции, KPI, unit-тест билдера. COMPLETED §Интерфейс, план `plans/engines-contracts-report-2026-07.md`.
2. **Облачная AI-рутина (REST)** — первый реальный цикл **проверен** 2026-07-21 (read-only прод-probe): REST-поверхность жива, `run-select` под `ai_readonly` отвечает, `last_run_at` свежий (подпись `mark-run` рутины), заявки 4 answered / 0 pending. Рутина завершает цикл poll→работа→mark-run. COMPLETED §Интерфейс.

## Следующий шаг

Ждём сигнала владельца / отзыва операторов. Кандидаты (не начинать без сигнала):
- Отзыв по отчёту «Двигатели и контракты» — сходятся ли цифры на реальных прод-данных, нужен ли ручной скаляр «план N» (PENDING §Обкатка отчёта).
- Пассивно: первый понедельничный AI-дайджест (`post-digest`) — данные телеметрии копятся после раскатки клиентов.

## Открытые вопросы для пользователя

- Общее облачное окружение рутин Матрицы и Сарафана — оставить или разделить (рекомендация: разделить; env-vars видны всем рутинам окружения). Не срочно.
- Планшет для цеха: Windows vs Android — решение не принято ([`plans/tablet-shop-floor.md`](plans/tablet-shop-floor.md)).

## Не забыть (low-priority)

1. Ledger release-token — exp **2026-08-15**; ротация ≤ ~2026-08-12 (PENDING §Ledger release-token). Первый релиз после ~2026-08-01 упадёт без ротации.
2. Ротация SSH-ключей прода — до 2026-08-21.
3. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз/комплектование неполны.
4. deadcode-прогон — ~2026-08-04.
5. Ф2 де-дуп `blank-synthetic-codes` — перепроверка гейта раската ~2026-07-24.
6. Бэкап-таблицы на проде (`*_bak_20260717`, `*_bak_norms20260717`) — снести ~август.
7. Локальные untracked-файлы окружения (`.agents/`, `.codex/`, `AGENTS.md`) не относятся к проекту — намеренно не коммитятся.
