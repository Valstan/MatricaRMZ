# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** ACTIVE
**Updated:** 2026-07-17 (Claude Opus 4.8, машина `PC40`)
**Branch:** `main` = `origin/main`. Дерево чистое, stash пуст, открытых PR нет.
**Last released version:** **v2026.717.729** — эскизник модулей + «Мои отчёты» (раскатан на прод сегодня).

## Текущая нитка

**Отгружено и на проде: эскизник модулей + «Мои отчёты»** (релиз v2026.717.729). Активной *кодовой* нитки в работе нет — вся работа сессии смержена и раскатана. Открытые направления, ожидающие сигнала владельца:
1. **Обкатка в поле** свежих фич (эскизник «Мои экраны» + «Мои отчёты») — ждём отзыв операторов, что удобно/что докрутить.
2. **Планшет для цеха** — по-прежнему припаркован на решении владельца по железу (Windows vs Android); это самый крупный открытый owner-вопрос.

## Следующий шаг

Развилка по указанию владельца (спросить, если не задаст сам):
- **Планшет** — если владелец назвал железо: начинать **Ф1 «Планшетный режим» UI** по [`docs/plans/tablet-shop-floor.md`](plans/tablet-shop-floor.md) (touch-оптимизация `RepairChecklistPanel`, стартовый экран оператора, индикатор синка). Ф1 полезен при любом размере экрана.
- **Докрутка конструктора/отчётов** по отзыву операторов: опц. Ф4-хвост эскизника (копирование группой, множественное выделение) и развитие «Моих отчётов» (группировки с агрегатами, шаринг шаблонов между операторами, отчёт-виджет как блок эскиза). План — [`docs/plans/ui-mockup-constructor.md`](plans/ui-mockup-constructor.md).
- Иначе — разобрать хвосты из `PENDING_FOLLOWUPS.md`.

## Контекст

- Планы: [`docs/plans/ui-mockup-constructor.md`](plans/ui-mockup-constructor.md) (эскизник Ф1–Ф3 ✅, Ф4 ✅, ветка Б «Мои отчёты» ✅); [`docs/plans/tablet-shop-floor.md`](plans/tablet-shop-floor.md) (планшет, припаркован).
- Коммиты сессии: `2b6edeeb` (#238 эскизник-пивот), `4ea3ea67` (#239 Ф4), `083b7bbb` (#240 «Мои отчёты»), `3fc325fb` (#241 рефакторинг reportPresetService), `cea5f595` (#242 релиз).
- **Прод: v2026.717.729** раскатан и проверен (`/health` + `/updates/status` = новая версия, blockmap 200 → клиенты получат дельту). Диск прибран **79% → 51%** (снёс осиротевший `/usr/local/lib/ollama` 4.3 ГБ + apt/pnpm/electron кэши).
- Открытых PR: нет. Локальных веток с un-pushed коммитами: нет.
- Рефакторинг #241: `reportPresetService.ts` (5003 стр.) распилен на `electron-app/src/main/services/reports/` (format / context / options / presets-по-доменам / dispatch / render); старый путь — тонкий шим. Эквивалентность доказана снапшот-гейтом (выдача всех 25 пресетов бит-в-бит до/после).

## Открытые вопросы для пользователя

- **Планшет: Windows (вариант A, рекомендация) или Android (вариант B, переписывание оффлайн-движка)?** + бюджет, кто носит в пилоте, TTL резерва. (переносится из прошлой сессии — решение так и не принято)
- **Отзыв операторов** по эскизнику и «Моим отчётам» — что докрутить в первую очередь.
- **AI/VPS** — отложено владельцем на неопределённый срок; не поднимать.

## Не забыть (low-priority)

1. Ledger release-token — exp **2026-08-15**; ротация ≤ ~2026-08-12 (см. PENDING §Ротация).
2. Ротация SSH-ключей прода — до 2026-08-21.
3. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст.
4. deadcode-прогон — ~2026-08-04.
5. Ф2 де-дуп `blank-synthetic-codes` — гейт красный (сверено 2026-07-17: 5 машин ниже v2026.712.1818), перепроверить ~2026-07-24; см. PENDING §застрявшее автообновление.
6. Я.Диск-квота: после бэкап-фикса #229 архив вырос до ~800М/сутки × 14 шт ≈ 11 ГБ — приглядывать.

_(Бэкап-фикс #229 ✅ сверен 2026-07-17: ledger.tar.zst 81М → 671М (реальный blocks/ 2.4Г + state.json 123М), upload ок.)_
