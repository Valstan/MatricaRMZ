# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-10, день — тревога `blocked_rows` разобрана замером, фикс листа деталей и телеметрии в PR, дальше релиз 3.25 (сессия Opus 5, машина `PC79`)
**Branch:** `fix/engine-inventory-autosave-race` → PR в `main`.
**Last released version:** **v3.24.0** на проде с 2026-09-09 00:37 MSK. 10.09 оба сервиса active, `/health` = 3.24.0 на обоих портах порознь.

## Текущая нитка

**Лист деталей нового двигателя + телеметрия интерфейса → релиз 3.25.** Разбор тревоги 10.09 11:05 (`client.sync.blocked_rows`) и клиентских логов за двое суток:

- **Тревога ложная, данных не теряли.** 47 из 52 пропущенных за 48 часов строк — листы деталей новых двигателей, ушедшие на сервер на 5–40 с раньше самого двигателя; все дошли. Класс — **M122** в [`GOTCHAS.md`](GOTCHAS.md).
- **Пункт «PC19 держит единственную копию работы» снят:** его 8 строк — четыре брошенные карточки по два пустых листа (0 деталей, в шапке одна буква номера); последние версии лежат в журнале сервера. Во всём журнале 29 операций-сирот — все пустые листы, кроме одного полного, у которого на сервере есть полный двойник.
- **Дубли листов:** два листа на двигатель с разницей в 12 мс — гонка автосохранения; в сентябре так созданы все 69 новых пар.
- **Телеметрия (`audit:add`) отбивалась у всех вне «Администрирования»** с 3.1.0 — **M123**.

Фикс: main откладывает автозаполненный лист до строки двигателя, ручная правка двигатель создаёт, без номера листа берётся существующий; гейт `audit:` сужен до `audit:list`; `.catch` в `uiUsageLog`.

## Следующий шаг

1. **Смёрджить PR фикса** (гейты: typecheck, `typecheck:test`, lint, полный `-r test`, CDP-смоук новой карточки двигателя).
2. **Релиз 3.25** по `/reliz` — только клиент (backend и миграции не менялись), плюс тег `android-v3.25.0`.
3. **Проверка после выката** (не раньше, чем PC19 и соседи встанут на 3.25): новых `client.sync.blocked_rows` на новые двигатели нет; двигателей с двумя живыми листами, созданными после выката, — ноль; `audit_log ui.*` появился у операторов вне «Администрирования».
4. Дальше — прежняя очередь: сеть VPS (ход хостера); прод-часть слияния дублей сотрудника (гейт #025); снос каталога цепочки (отдельный OK); E2.2 плана [`engine-inventory-lines`](plans/engine-inventory-lines-2026-09.md).

## Контекст

- Прод-разбор 10.09 шёл читающими скриптами через `ssh matricarmz "bash -s" < скрипт` (см. профиль `docs/machines/PC79.md` §«Запросы к прод-БД»): критические события — `backend-api/logs/critical-events.ndjson` в каталоге сервиса (хранятся 3 дня), клиентские логи — `client-YYYY-MM-DD.log` там же, пропуски — `journalctl` обоих юнитов.
- Остальное за 48 часов — не код: 141 сбой опроса Telegram и 16 обрывов у клиентов (сеть VPS, пункт 🟡); 55 предупреждений о логине на двух машинах; 20 отказов доступа у учётки без роли (6 минут 08.09); 3 полных скачивания установщика вместо дельты.
- PC19 сама обновилась до 3.24.0 утром 10.09 — 8 старых пустых строк остались в её карантине, вреда от них нет.
- Прод: **v3.24.0**, последняя применённая миграция — `0094_chat_rooms`. Каталог цепочки 4,8 ГБ ещё лежит.
- **Чат оповещений с «Сарафаном» — общий, доказано хэшем**; бот НЕ общий. Разведение чатов — ход владельца.
- Письма brain'у: `2026-09-09-the-chat-hash-matches-and-we-do-not-go-through-your-worker.md`, ранее — `2026-09-08-a-cure-cancelled-by-its-neighbour.md`.

## Что не сработало

- **Посылку «единственная копия работы» приняли без взгляда в содержимое** (09.09). Журнал пишется до применения — отвергнутые строки со всем содержимым лежали на сервере, и одна выборка показала бы пустые листы. Правило: прежде чем объявлять данные потерянными или уникальными, прочитать их там, где они точно есть.
- **Числа из AI-отчёта трижды приняли за замер** (06.09, 08.09, 09.09). Величину, которую цитируют человеку, проверять вопросом «отвечает ли она ровно на то, чем названа».
- **Лечение счётчика нельзя принимать по первому окну** (M121).
- **Автомерж съедает коммит, досланный после вооружения.** Сверять по содержимому `main`, вооружать автомерж последним действием.
- **Длинный текст с кавычками через `git commit -m` / `gh pr create --body`** рвётся в Git Bash. Только через файл.

## Открытые вопросы для пользователя

- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота у проекта нет.
- **Общий чат оповещений** — разводить ли (создать чат, добавить бота).
- **Снос каталога цепочки на проде** — нужен отдельный OK (план §J2 шаг 7).
- **Раздача соседям** — включается по машинам (правило брандмауэра → тумблер в админке → перезапуск клиента).
- **Дубли листов engine_inventory** (≈206 двигателей): мягко удалить лишние — после E3, отдельным OK (данные прода).
- Прежние, не снятые: менять ли внешний SSH-порт прода; стереть ли старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника на парк; подписные грифы печатных форм (ФИО в коде).

## Не забыть (low-priority)

- **Keystore Android:** свёрток так и не собран, единственная копия ключа на PC40. Brain просит поднимать первым пунктом каждого старта.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09 сделан; следующий до R4b, начать с четырёх непокрытых линз.
- **Перемер роста диска 03.10** — по августовскому темпу; там же первые цифры счётчика обращений к файлам (D-073).
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка (та же метрика, что R4b).
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Файл `MISSING 21a1b799-… Кто-то ночью спит.mp4` — строка в БД есть, файла нет; разобрать отдельно.
