# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE
**Updated:** 2026-08-10 (Claude session, машина `PC40`)
**Branch:** `main` = `origin/main`, дерево чистое.
**Last released version:** **v2026.809.1623** на проде (код после релиза в `main` есть, но НЕ выпущен).

## Текущая нитка

_n/a_ — нитка демо-стенда закрыта целиком. Директива brain'а **D-021** выполнена: сид `matricarmz_demo` ([#521](https://github.com/Valstan/MatricaRMZ/pull/521)) и все **10 кадров** ([#522](https://github.com/Valstan/MatricaRMZ/pull/522)) отгружены, план переехал в [`plans/_archive/portfolio-demo-stand-2026-08.md`](plans/_archive/portfolio-demo-stand-2026-08.md), отчёт отправлен brain'у ([`to-brain/2026-08-10-demo-stand-done-and-the-seed-that-was-not-idempotent.md`](../mailbox/to-brain/2026-08-10-demo-stand-done-and-the-seed-that-was-not-idempotent.md)).

## Следующий шаг

Нитки нет — следующая сессия выбирает из открытого в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md). Наиболее готовые к работе кандидаты (сверено с PENDING на 10.08):

1. **Приёмка планшетного режима на DIGMA** — код в `main` с 09.08, блокер снят, APK-артефакт CI **уже установлен на планшет в этой сессии**. Осталась только тач-эргономика вживую: пять сценариев из `PENDING_FOLLOWUPS` §«Большой рефакторинг», п.1 (листание ≥48 px убирает хром, возврат к началу возвращает, 20 с бездействия, язычок ▾ пальцем в перчатке, 🔍 на новой координате).
2. **Релиз** — в `main` после v2026.809.1623 накопилось: планшетный режим ([#516](https://github.com/Valstan/MatricaRMZ/pull/516)), debug-APK на не-тег сборках ([#518](https://github.com/Valstan/MatricaRMZ/pull/518)), демо-стенд. Плюс обещанное «на один релиз» снятие dual-write в `v2.session`.
3. **Остаток программы большого рефакторинга** — R6 (web-admin), R7 («Цех-дуэль»), [`plans/grand-refactor-2026-08.md`](plans/grand-refactor-2026-08.md).

## Контекст

- **Коммиты сессии:** `c8332151` (#521, сид демо-стенда), `966b73c9` (#522, 10 кадров + пять фиксов сида).
- **Прод:** v2026.809.1623, не трогали в этой сессии.
- **Открытых PR:** нет. Локальных веток кроме `main`: нет. Un-pushed коммитов: нет.
- **Демо-стенд поднимается заново за один прогон:** рецепт (пересоздание БД из прод-дампов схемы + `demo:seed`) — в [`machines/PC40.md`](machines/PC40.md) §«Демо-стенд портфолио». Логин `demo` / `demo2026`. Прогон занимает ~15 мин: каждая запись идёт штатным sync-путём с подписью в ledger.
- **Фоновые процессы стенда этой сессии остановлены** (backend :3001, Electron CDP :9333, vite-спайк :5199, headless Chrome :9444). БД `matricarmz_demo` на локальном PG осталась — она нужна только для пересъёмки, места не жалко.

## Что не сработало

- **Браузерный спайк `android-app` для кадра планшета** — план рассчитывал снять им кадр 9, но спайк намеренно без входа и синка («Спайк Ф0: вход появится в Ф1»), данных в нём нет и быть не может. Кадр снят эмуляцией планшетных метрик (`Emulation.setDeviceMetricsOverride`) в том же клиенте. Не идти этим путём снова, пока не появится Ф1.
- **Повторный прогон сида «чтобы досеять недостающее»** — вместо пропуска удваивал парк двигателей (причина — общий PRNG, см. **M69**). Починено, но урок общий: идемпотентность скрипта проверять `count(*)` до/после, а не «зелёным прогоном».
- **Пересев при живом клиенте и общем ledger** — выглядит как зависание: прогресса нет, в `pg_stat_activity` пусто (**M70**). Клиента на время сида останавливать, ledger стенду давать отдельный.

## Открытые вопросы для пользователя

- ⚠️ **Сверить выдуманных контрагентов с ЕГРЮЛ** перед публикацией кадров: АО «Камаволгатехфлот», ООО «Севдизельремкомплект», АО «Обьтрансдизель», ООО «Волгасудмеханика». Совпало с настоящим юрлицом — поменять имя в `CUSTOMERS` (`backend-api/src/scripts/seedPortfolioDemo.ts`), пересеять, переснять кадры 1, 2, 3, 10. Это единственный оставшийся пункт демо-стенда, и закрыть его может только человек.
- **Живая онлайн-демка вместо статичных кадров** — обсуждали в этой сессии: главные экраны живут в Electron/планшете, в браузере крутится только служебный web-admin, поэтому «выложить стенд на сайт» покажет админку, а не флагманы. Компромисс, если захочется «живости», — скринкаст работы настоящего клиента на демо-данных. Решение за владельцем; оформлю предложением brain'у, если скажешь.
- Прежние ответы (эскалации ИИваныча, Касперский, `mubvera` с застрявшими операциями, PC20/`alvina` — ручная переустановка) всё ещё ждут.

## Не забыть (low-priority)

**Даты:** ledger release-token — ротация ≤ 2026-09-04; SSH-ключи прода ≤ 2026-08-21; deadcode-дельта (knip) просрочена с ~04.08 (`PENDING_FOLLOWUPS` §📅).

1. **Снять dual-write в `v2.session`** — обещан «на один релиз», выпущен в v2026.809.1623.
2. **Осиротевшие `ChromeApi.route()` и слой `sections`** — не зовутся ниоткуда после сноса выдвижной панели (`PENDING_FOLLOWUPS`, «Хвосты R3»).
3. **`typecheck` НЕ покрывает тест-файлы** — у них отдельный `tsconfig.vitest.json`; перед push'ем правок в тестах гнать `typecheck:test`.
4. **Нативный модуль сейчас под Electron-ABI** (`electron-builder install-app-deps` прогонялся в этой сессии). Перед backend-тестами/vitest — обратная пересборка `corepack pnpm -r rebuild better-sqlite3`.
5. Драйверы съёмки кадров лежат в `.verifier-electron/_demo-*.mjs` (gitignored) — при пересъёмке переиспользовать, а не писать заново.
