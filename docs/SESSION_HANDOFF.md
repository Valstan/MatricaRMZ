# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-08-28 (Claude session, машина `rmz4val`)
**Branch:** `main` = `origin/main`, дерево чистое.
**Last released version:** **v3.14.0** на проде (эта сессия релизов не выпускала — docs/design).

## Текущая нитка

**Трек B плана Матрицы 4 — финиш EAV→erp_*** ([план](plans/matrica-v4-kickoff-2026-08.md)), возобновлён после месячной паузы (стоял с v3.3.0, 19.08). Эта сессия: (1) сверила состояние трека с кодом и разметила план (PR #704 — B0 ✅, B1 ✅ серверная половина, B2 ~40%, найден «узел офлайн-записи»); (2) спроектировала схему `users` для этапа B3 (панель 3 проектов + 2 судьи) и отправила её brain'у как v4-канон (PR #705); (3) выполнила R0 — прод-аудит зелёный (дублей логинов ноль, 0083/0084 применены, `merged` не встречается).

## Следующий шаг

**Ждём ack brain'а по схеме `users`** (письмо `mailbox/to-brain/2026-08-28-users-schema-canon-b3.md`). После ack — **R1**: миграция `0086_users_strict.sql` (5 таблиц + rebuild-функции + триггеры + бэкфилл) + parity-скрипт в CI — точная спецификация в плане, секция «B3: схема users → Последовательность внедрения». Если владелец скажет «не жди brain» — R1 можно начинать сразу: R0-предусловия выполнены. Проверить на `/start` входящие от brain — мог ответить и на схему, и на вопрос про роль `'user'` (`2026-08-28-legacy-user-role-question.md`).

## Контекст

- План: [`plans/matrica-v4-kickoff-2026-08.md`](plans/matrica-v4-kickoff-2026-08.md) — секции «Сверка состояния 2026-08-27» и «B3: схема users» — актуальная карта и спецификация.
- Коммиты сессии: `56792f05` (#704 сверка), `0001eaf8` (#705 схема + письмо + R0).
- Прод: v3.14.0, оба сервиса штатно; релизный workflow v3.14.0 (висел на Яндекс-загрузке) сам дошёл до конца ✅.
- Открытых PR: нет.
- Локальные ветки со «своими» коммитами (остатки прошлых сессий, squash-разошедшиеся хэши): `fix/android-ci-debug-apk-non-tag`, `release/v3.8.0`, `release/v3.9.0` — не трогал; кандидаты на чистку `git branch -D` после сверки, что содержимое на проде.
- Полные материалы разведки/дизайна — workflow-транскрипты сессии (runId `wf_67fba712-fd3` сверка, `wf_fd73384f-e13` дизайн схемы); суть перенесена в план.

## Что не сработало

- Утверждение агентов разведки «для строгих таблиц нет push-прецедента» оказалось преувеличением — push-хендлеры есть у 8 строгих app-таблиц (`operations`, `chat_*`, `notes`…), нет только у `erp_*`. Урок тот же, что в memory: факты агентов сверять построчно перед записью в план.
- Фоновое ожидание CI сразу после `gh pr create` ловит «no checks» и выходит мгновенно — чеки регистрируются с лагом; в цикл ожидания нужен стартовый `sleep ~45s` (наступил на PR #705).

## Открытые вопросы для пользователя

Прежние из сессий 26–27.08 (не сняты): 1) старый ledger-токен валиден до 07.09, отзыва нет; 2) история релизов и телефон в `releaseWelcome.ts`; 3) стереть ли старые значения рекламации в БД (3 двигателя); 4) `attribute_values` в конструкторе отчётов (`PENDING` §Security п.6); 5) раскатка Касперский-помощника на парк не прогонялась.

Новое: 6) **легаси-роль `'user'`** — вопрос ушёл brain'у (владелец: «не понимаю, что с ним делать»). Факты: дыры через регистрацию нет (новые = `pending` без прав), но **4 живых аккаунта на проде ходят с `'user'` = полный доступ**. Мой план по умолчанию в письме: владелец пересаживает четверых на содержательные роли → `'user'` уходит из каталога в R1/R2. Пересадка — операционное действие владельца, можно делать не дожидаясь brain.

## Не забыть (low-priority)

- Ротация SSH-ключа прода для RMZ4VAL ≤15.11.
- Живая приёмка ярлыка автозагрузки watchdog (хвост v3.13.0).
- Проверить свой gitleaks фикстурой на наших классах секретов — обещано brain'у.
- Пункт 14 владельца (двухнедельная проверка перекосов, с 2026-09-01).
- Живой разрыв инкрементального pull для server-write erp-строк (`erp_engine_instances`, регистры) — проверить на стенде до опоры на него (записан в плане, сверка 2026-08-27).
- EAV-долг растёт ~+47 значений/день (44 146 живых на 28.08) — аргумент не затягивать B4.
