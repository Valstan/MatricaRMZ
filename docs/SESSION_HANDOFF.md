# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-09-09, ночь — **3.24.0 на проде**, вторая пачка владельца закрыта целиком; свободных ниток нет (сессия Opus 5, машина `PC79`)
**Branch:** `main`. Планы нитки — [`plans/owner-batch-2026-09-09.md`](plans/owner-batch-2026-09-09.md) и [`plans/owner-batch-2026-09.md`](plans/owner-batch-2026-09.md), оба закрыты.
**Last released version:** **v3.24.0** — выкачена на прод 2026-09-09 в 00:30–00:40 MSK (теги `v3.24.0` + `android-v3.24.0`). Оба порта отвечают 3.24.0 порознь, `/updates/status` = 3.24.0 без ошибок, blockmap 200, планшетам план обновления выдаётся. Миграция `0094_chat_rooms` применена, схема проверена.

## Текущая нитка

**Свободных ниток нет.** За сессию выкачены два релиза:

- **3.23.0** (08.09, 22:13–22:20 MSK) — первая пачка владельца целиком: фильтры под одной кнопкой, точный поиск и «≈ Похожие», история ремонта, вид договора, шелл (порог вкладок, незакрываемые вкладки, колокольчик, версия клиента в меню), закрытие программы с несохранёнными и прогресс обновления, слияние дублей сотрудников.
- **3.24.0** (09.09, 00:30–00:40 MSK) — вторая пачка: тулбар списка в одну строку с меню переполнения, «Колонки списка» в панели фильтров, однострочные шапки с шириной по данным, порядок бесед по свежести, **комнаты чата**, жирные три цифры в номере договора, зажим выпадающих панелей в границы окна. Миграции `0094_chat_rooms` (сервер) и `0025_chat_rooms_replica` (клиент).

Проверка обеих пачек — на живом стенде PC79 через CDP; смоук второй пачки (`cdp-list-chrome-and-rooms.mjs`) зелёный три прогона подряд. Стенд нашёл четыре бага, которых не увидели юнит-тесты: **M118** (подпись строки списка без новых полей), **M119** (гейт прав возвращает успех, а читается как отказ), **M120** (замер ширины в скрытой вкладке + признак, потерянный горячей перезагрузкой) — все в `GOTCHAS.md`.

## Следующий шаг

1. **Прод-часть слияния дублей Цветковой** — ждёт хода владельца: dry-run → отчёт → его OK **в том же ходе** → apply → `users:parity` (гейт #025).
2. **Приёмка 3.23.0 и 3.24.0 утром** — обновился ли парк (`client_settings.last_version`), и живы ли на реальных данных «Фильтры», «≈ Похожие», история ремонта, комнаты чата.
4. **Приёмка 3.22.0, не снятая:** упал ли до нуля счётчик залипшего push'а PC19 после её обновления:
   ```bash
   ssh matricarmz "journalctl -u matricarmz-backend-primary --since '2 hours ago' --no-pager | grep -c 'sync dependency rows skipped'"
   ```
   Не упал → карантин не сработал, разбираться по клиентскому логу.
5. **Как только PC19 обновится — прочитать её критические события** (`client.sync.blocked_rows` / `client.sync.invalid_row`): лежат ли её 4 двигателя локально (работа за 20.08–31.08 восстановима) или их нет вовсе.
6. **Хвост приёмки 3.21.0:** реплика справочника складов у обновившегося клиента и работоспособность клиента СТАРОЙ версии без миграции 0024.
7. **Снос каталога цепочки `~/matricarmz-ledger` (4,8 ГБ) — ждёт отдельного OK владельца** (план §J2 шаг 7; после этого откат невозможен, M30).
8. **E2.2 плана [`engine-inventory-lines`](plans/engine-inventory-lines-2026-09.md)** — чтение списка деталей из реплики с откатом на `meta_json`.
9. Хвосты: PostgreSQL на зоне `Host`; дубли двигателей разбирают операторы руками; нитка диска п. 7 и п. 9.

## Контекст

- Планы: [`plans/owner-batch-2026-09-09.md`](plans/owner-batch-2026-09-09.md) (**вторая пачка, закрыта по коду**), [`plans/owner-batch-2026-09.md`](plans/owner-batch-2026-09.md) (закрыт: 8 из 8, прод-часть шага 8 ждёт OK владельца), [`plans/engine-inventory-lines-2026-09.md`](plans/engine-inventory-lines-2026-09.md) (E2.2), [`plans/ledger-journal-in-pg-2026-09.md`](plans/ledger-journal-in-pg-2026-09.md) (§J2 — прод-ранбук с разделом отката), [`plans/matrica-v4-kickoff-2026-08.md`](plans/matrica-v4-kickoff-2026-08.md) §R4.
- Прод: **v3.24.0**, оба сервиса active, последняя применённая миграция — `0094_chat_rooms`. Свободно на диске 7,5 ГБ; каталог цепочки 4,8 ГБ ещё лежит. Журнал 861 МБ, 1,16 млн записей — из них **65 069 (5,6%) от строк, ни разу не попавших в проекцию** (записано в `PENDING`).
- Парк на утро 08.09: 7 клиентов на 3.21.0, по одному на 3.20.0 / 3.19.0 / 3.5.0.
- Открытых PR нет (кроме закрывающего сессию). Локальных веток кроме `main` нет.
- Письма brain'у: `2026-09-08-a-cure-cancelled-by-its-neighbour.md` (классы этой сессии), ранее — `2026-09-07-a-nul-byte-hides-a-file-…`, `2026-09-07-a-total-larger-than-the-sum-of-its-columns-…`.
- Входящее от brain 08.09 (`…token-is-not-at-setka…`, прочитано): токена нашего бота у «Сарафана» нет — доказано `getMe` по каждому токену, не сравнением имён переменных; непроверенной честно названа одна ветка — секреты Cloudflare-воркера, это ход владельца. Общий чат алертов подтверждён хэшем; развести стоит одной переменной, но требует хода владельца (создать чат, добавить бота) — **заранее ничего не перенастраивать**, brain скажет обоим. Шесть наших классов ушли в пул (#296–#301), два в грабли (G329/G330).

## Что не сработало

- **Числа из AI-отчёта дважды приняли за замер.** Первый раз 06.09 («~200 конфликтов за 12 часов»), второй — 08.09 («200 сетевых сбоев за 12 часов» при 28 фактических). Оба раза это была **длина выборки**, поданная в промпт рядом со словами «за окно». Починено в #831, но урок шире инструмента: величину, которую собираются цитировать человеку, надо проверять на вопрос «отвечает ли она ровно на то, чем названа».
- **Автомерж съедает коммит, досланный после вооружения** (ловилось дважды). PR показывает «слит», а второй `git push` молча воссоздаёт ветку с коммитом, которого в `main` нет. Ловится только сверкой по **содержимому** `main` (`git show origin/main:<файл> | grep …`), не по статусу PR. Вооружать автомерж последним действием.
- **Длинный текст с кавычками через `git commit -m` / `gh pr create --body`.** Двойные кавычки рвут строку в Git Bash, и `git add` получает куски текста как пути. Только через файл (`-F`, `--body-file`).
- **Ожидание «процесса нет» вместо «работа закончена».** Условие «процесса нет» истинно и до старта работы — ждать надо появления записи о завершении, а не отсутствия процесса.
- **Вывод «значит, снаружи» из тишины в собственном логе.** Отрицательный результат — не улика, пока не проверен заведомо присутствующий сосед (M103).
- Грабли этой сессии вынесены в [`GOTCHAS.md`](GOTCHAS.md): **M116** (лечение, отменённое соседней функцией), **M117** (гейт считает попытки, а называет строки).

## Открытые вопросы для пользователя

- **Судьба четырёх листов деталей PC19** — решать после п. 2 «Следующего шага», когда станет известно, восстановимы они или нет.
- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота у проекта нет; ни код, ни боксы этот вопрос не закрывают. Одна минута в телефоне владельца.
- **Снос каталога цепочки на проде** — нужен отдельный OK (план §J2 шаг 7).
- **Раздача соседям** — включается по машинам (правило брандмауэра → тумблер в админке → перезапуск клиента).
- **Дубли листов engine_inventory** (483 листа без строк): мягко удалить старые — после E3.
- Прежние, не снятые: менять ли внешний SSH-порт прода; стереть ли старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника на парк; подписные грифы печатных форм (ФИО в коде).

## Не забыть (low-priority)

- **Keystore Android:** свёрток так и не собран, единственная копия ключа на PC40. Brain просит поднимать первым пунктом каждого старта.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09 сделан; следующий до R4b, начать с четырёх непокрытых линз.
- **Перемер роста диска 03.10** — по августовскому темпу; там же первые цифры счётчика обращений к файлам (D-073).
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка (та же метрика, что R4b).
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Файл `MISSING 21a1b799-… Кто-то ночью спит.mp4` — строка в БД есть, файла нет; разобрать отдельно.
- В `docs/machines/PC79.md` последний раздел «Порты / сервисы» всё ещё говорит «не заводились (нет PG)», хотя PG и стенд описаны выше — поправить при следующей правке профиля.
