# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE
**Updated:** 2026-07-26 (Claude session, машина `rmz4val`)
**Branch:** `main` = `origin/main`.
**Last released version:** **v2026.726.1951** — раскатан на прод в этой же сессии.

## Текущая нитка

**Нет активной нитки.** Сессия шла одной темой владельца — помощник в программе (детали — [`COMPLETED.md`](COMPLETED.md), строка релиза v2026.726.1951):

1. **Дренаж очереди + пульс** (#354). Платформа claude.ai **не разрешает cron чаще раза в час** — такт держим длиной прогона (~50 мин опроса), `mark-run` стал пульсом «на связи».
2. **Окно «Что нового»** (#355) — проза убрана, новинки отбираются по датам из CalVer за 2 дня.
3. **Имя и талисман** (#356, #357) — **ИИваныч** + человечек с ключом, крутящий гайку при открытии панели.

## Следующий шаг

Свободный выбор владельца. Что реально ждёт (сверено с `PENDING_FOLLOWUPS`):

- **🔴 5 машин ниже v2026.712.1818** (PC71, alvina, PC49, PC18, PC14) — переустановить клиент вручную. `force_full_pull` им слать **нельзя** до обновления.
- **Пн 28.07 (послезавтра):** проверить, что остальные клиенты забрали `force_full_pull` (`SELECT count(*) FROM client_settings WHERE sync_request_type='force_full_pull'` должен таять к нулю).
- **Пн 27.07 утром — посмотреть на первый прогон ИИваныча с дренажом.** В панели ИИваныча поле «Последний запуск» должно подрастать каждые ~2.5 мин почти до конца часа. Если штамп замирает через несколько минут — платформа режет длинные прогоны, и тогда возвращаемся к плану A: три рутины со сдвигом `:00/:20/:40` (каждая формально ежечасная, вместе дают такт 20 мин). Рутина сама пишет в свой лог, сколько минут прожила.
- **Окно «Что нового» на busy-днях длинное.** 25–26.07 вышло 5 релизов → окно в 2 дня давало 22 строки. Если владельцу покажется много — `RELEASE_WELCOME_DAYS = 1` в `shared/src/domain/releaseWelcome.ts` (одна константа).
- Приёмка планшетного режима и резервирования двигателя на живом планшете; ручной смоук диалога удаления на реальных данных.
- `SECTION_DEPENDENCIES` заполнена вручную только 4 парами и держится в синхроне с `sectionGate.ts` руками — можно вывести из фактических гейтящихся lookup'ов.

## Контекст

- Планов в работе нет.
- Ключевые PR сессии: #354 (дренаж + пульс), #355 (welcome-дайджест), #356 (ИИваныч + талисман), #357 (переименование), релиз #358.
- Прод: v2026.726.1951.
- Облачная рутина: `trig_01EWroLC8Ls3ikWW82TFNGNT` «ИИваныч — Матрица РМЗ», cron `0 5-14 * * 1-5` (менять на `*/20` **нельзя** — платформа отвергает).
- Открытых PR нет, локальных веток кроме `main` нет, stash пуст.

## Что не сработало

- **`*/20 5-14 * * 1-5` отвергнут API рутин:** `cron interval too short; minimum interval is 1 hour`. Первая попытка ускорить помощника частотой запусков провалилась целиком — обошли длиной прогона. Зафиксировано в `docs/ai-chat/ROUTINE.md`, чтобы следующая сессия не пробовала снова.
- **Скриншот через Browser-панель не снимается** (`the Browser pane is not displayed, so the page is not compositing frames`), а `file://` вне проекта открывается статическим снимком. Рабочий обход для проверки рисунков — **headless Chrome**: `chrome.exe --headless=new --screenshot=<abs.png> <url>` + `Read` полученного PNG. Путь вывода задавать абсолютным (относительный молча уезжает не туда). Занесено в профиль машины.
- Значок помощника на просимых 26px нечитаем — рисованный персонаж на таком размере превращается в пятно. Поставлен 40px.

## Открытые вопросы для пользователя

_Нет._

## Не забыть (low-priority)

**Даты ротаций/дедлайнов — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §📅.** (проверка pull'ов ~28.07, deadcode ~04.08, ledger-токен ≤12.08, SSH ≤21.08.)

1. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз/комплектование неполны.
2. `RELEASE_WELCOME_HISTORY` — легаси-дубли меток `v1.18.0`–`v1.18.5`, почистить при следующем релизе.
3. Операторов предупредить: старые QR-этикетки с кодом `DET-` больше не находятся поиском (перепечатать).
4. `CURRENT_RELEASE_WELCOME` / `PREVIOUS_RELEASE_WELCOME` остались мёртвыми экспортами после #355 — снести при следующей чистке deadcode (не в релизном PR: инцидент v2026.725.2033).
