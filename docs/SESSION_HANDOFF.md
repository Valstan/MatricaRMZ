# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-22 (Claude Opus 5, машина `PC79`)
**Branch:** `perf/renderer-scanners-and-focus`
**Last released version:** **v3.44.0**, выкачено 21.09 (см. тело PR релиза)

## Текущая нитка

**Пакет владельца 22.09** — девять задач одним сообщением, нитка G сдвинута за него. План: [`docs/plans/owner-package-2026-09-22.md`](plans/owner-package-2026-09-22.md). Порядок PR и решения по развилкам — там же.

- **Шаг 1 ✅ точный поиск по умолчанию** — слит ([#997](https://github.com/Valstan/MatricaRMZ/pull/997)). Умолчание общих матчеров `matchesQueryInRecord`/`filterPreparedRecords` перевёрнуто на `exact`; кнопка «≈ Похожие» добавлена в Ctrl+K, на 8 второстепенных списков и на остатки склада (там намерение едет параметром `similar` до сервера и до оффлайн-реплики). Пикеры оставлены терпимыми намеренно. Сторож `SearchMode.guard.test.ts` переписан на обход дерева — новый список без кнопки краснеет сам.
- **Шаг 2 ✅ короткий номер контракта** — слит ([#998](https://github.com/Valstan/MatricaRMZ/pull/998)): без «/» короткий номер не строится, печатается номер как есть.
- **Шаг 3 ✅ дубли деталей** — слит ([#999](https://github.com/Valstan/MatricaRMZ/pull/999)). Правило в `shared/domain/partsDedup.ts` (`duplicateBlockReason`): жёстко блокируются пара (имя, артикул) и занятый непустой артикул при любом имени. Одно лишь совпадение имени при РАЗНЫХ артикулах не блокируется — это законная семья («Вал коленчатый 3305-01-18 / …-17»), довод записан в шапке правила. Владельцу сказано; если захочет запрет и по голому имени — расширять там же.
- **Шаг 4 ✅ пометки заездов** — слит ([#1000](https://github.com/Valstan/MatricaRMZ/pull/1000)): роль заезда (`arrivalPlacements` в shared) считается один раз в `listEngines` и едет полем `arrival` в строку; видна в выпадашках выбора двигателя, в списке, в карточке и отдельным фасетом «Заезд» в отчётах.
- **Шаг 5 ✅ срок ремонта** — слит ([#1001](https://github.com/Valstan/MatricaRMZ/pull/1001)). Поле «Срок ремонта, дней» в карточке контракта (внутри `contract_sections`, без миграций); отсчёт переведён с даты стартового аванса на **дату поступления двигателя на завод**; пороги подсветки масштабируются под срок (`countdownThresholds`). **Без даты поступления отсчёта нет** — осознанно: срок, посчитанный не от того события, хуже отсутствия. Такие двигатели видны значением «без даты поступления» в новом фасете «Срок ремонта».
- **Шаг 6 ✅ бланк дефектовки** — слит ([#1002](https://github.com/Valstan/MatricaRMZ/pull/1002)). Флаг «Свой номер» ставится на детали в комплекте марки, наследуется в строки листа с переопределением. **Есть миграции:** PG `0099_engine_inventory_lines_has_own_number`, реплика клиента `0026_inventory_lines_has_own_number` — обе добавляют две nullable-колонки, бэкфилла нет. Бланк печатается в два столбца, клетка под номер рисуется только у помеченных деталей.
- **Шаг 7 ✅ бирки** — слит ([#1003](https://github.com/Valstan/MatricaRMZ/pull/1003)): раскладки 6/4/2 на А4 с выбором в печатной форме, печать по выделенным строкам списка и из карточки. Дата окончательной технической приёмки в базе не хранится — считается как срок ремонта плюс запас (`TECHNICAL_ACCEPTANCE_LAG_DAYS`, сейчас 10 дней), менять одной константой.
- **Шаги 8–9 производительность и фокус** — в работе одним PR. Появился `utils/perfTrace.ts`: включается на обычной сборке через `localStorage['matrica:perfTrace']='1'` и перезапуск, сводка в консоль раз в 10 с. Без него любые выводы о скорости были бы догадками — средств замера в репо не было вовсе.

## Следующий шаг

1. Дальше по плану: жёсткий запрет дублей деталей → пометки заездов в пикерах → «дней на ремонт» в контракте → бланк дефектовки в два столбца → бирки 6/4/2 → производительность UI → залипание фокуса.
2. Релиз — после каждых 2–3 PR, не одним куском. Бирки и срок ремонта лучше в одном релизе: бирка печатает дату окончания по договору.
3. **Нитка G после пакета:** контроль на живом потоке (один лист → одна строка в `erp_engine_inventory_lines`, одна транзакция в `ledger_tx_index`, читать по `operation_id`); затем G3 (E3-флип) и G4 (чистка дублей листов, apply под явный OK владельца).
4. К 16.10 по D-096: таблица дверей записи REST/скрипты/фоновые задачи (`docs/WRITE_DOORS.md`), `typescript` в сторе бокса, `immutable` на ассетах, обход `!operatorScoped` + open для неизвестного типа — вместе с B6.
5. Дальше — блок H (авторство и история объектов, журнал действий).

**Даты мандата D-096 — в календаре `PENDING_FOLLOWUPS`** (01.10 ✅, 02.10 ✅, 16.10 частично, 30.10, 30.11).

## Контекст

- **План:** [`docs/plans/autumn-2026-program.md`](plans/autumn-2026-program.md) (нитка G, блок H).
- **Связанные коммиты:** `941e1755` G2 · `b5ff4a79` G1 · `b23af95b` владение строкой #015 · `1e4438bc` rollback командой · `f8ecf8f5` open redirect · `26b362f0` админка в артефакте · `9ff02bbf` релиз v3.43.0.
- **Прод:** v3.44.0 — статус приёмки в теле релизного PR. Откат — `deploy-backend.sh rollback` (копия снимается скриптом перед раскладкой).
- **Открытых PR:** нет. **Локальных веток с неотправленными коммитами:** нет.
- **Почта brain:** мандат **D-096** принят (`ack: report`), пять строк отданы досрочно; отправлены три письма 18.09 — по мандату, по `grep -q`/буферу трубы и по трём классам из гейта дублей.

## Что не сработало

- **Жёсткий запрет дублей (уникальный индекс) — отвергнут дважды, по разным причинам.** Технически: ключ дубля лежит внутри `meta_json`, значит нужен expression-индекс в PG и его точная копия в SQLite-реплике — это ровно **M142**, из-за которого утром встал весь парк; плюс на существующих дублях индекс не создался бы. По существу: двигатель реально возвращается на тот же этап, и запрет обошли бы сдвигом даты — потеряли бы достоверность дат, а не дубль.
- **Префикс возврата в названии (предложение владельца) — отвергнут.** Данные в имени не считаются и не фильтруются, ломаются опечаткой. Отдельный вид работ — хуже: разъехался бы с основным во всех группировках отчётов.
- **Установка значения в пикер через сеттер свойства больше не работает** — подсказка рисуется порталом и только при `dropdown.open`. Смоук из-за этого показал «гейт не поднялся», хотя дело не доходило до гейта вовсе. Класс записан как **M143**; committed-драйверы, написанные до v3.41.0, считать устаревшими.
- **Доказательство, собранное после таймаута драйвера, принимать нельзя.** Первый прогон дал противоречие: драйвер «упал», а строка возврата в базе появилась. Причина — цикл ожидания внутри одного `Runtime.evaluate` висит до таймаута, пока страница живёт дальше. Опрос состояния — только короткими повторными вызовами со стороны драйвера.

## Открытые вопросы для пользователя

- **Обход машин для раздачи соседям** — подобраны 17.09 (`PC76`, `PC19`, `PC20`, запасная `PC36`; логины — в чате); ждёт утверждения владельца и обхода с `lan-share-firewall.ps1`.
- **Telegram-логин владельца в карточке сотрудника** — правится владельцем в программе.
- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота нет.
- **Дубли листов engine_inventory**: владелец 16.09 взял E2.2→E2.3→E3→чистку в программу (блок G плана); apply — отдельным OK.
- **Название этапа ОТК перед отгрузкой** — предложено «Выходной контроль ОТК» (запасное — «Комплектация к отгрузке»); узел заводит владелец в справочнике видов работ.
- **B6** (сужение этапа «Комплектовка сделана», замер 95 → 3) и **B4** (снятие `engine_stages` по приёмке отчёта) — ждут слова владельца.
- Прежние: внешний SSH-порт прода; старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника; подписные грифы печатных форм.

## Не забыть (low-priority)

- **Печатный бланк и отчёт номер прохода пока не показывают** — гейт дублей отдан без этого осознанно; если владелец захочет видеть возвраты в печати, это отдельный маленький шаг.
- **Свежей суточной цифры 409 нет** — последний замер 07.09 (23/33/12/0). Перемерить и заодно проверить, не вернулась ли строка `slower than its tick`.
- **Опрос Telegram не отменяется на `SIGTERM`** — оставшийся источник «рестартовых» 409 при каждом выкате; обещано brain 07.09.
- **Забытый worktree** `.claude/worktrees/practical-hopper-0f5ca3` (от сессии 14.09, detached HEAD, дерево чистое) — gitignored, репо не портит; снести можно в любой момент.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09; следующий до R4b.
- **Перемер роста диска 03.10**; база отсчёта — 5.6 ГБ занято (60 %) на 14.09.
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка.
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
