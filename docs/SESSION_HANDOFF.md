# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** ACTIVE (полевая проверка: релиз v2026.702.1024 раскатан; за владельцем — проверка даты отгрузки `2Ж03АТ0479` на живом клиенте, теперь уже на 1024)
**Updated:** 2026-07-02 (Claude Fable 5, машина `PC40`)
**Branch:** `main` (= origin/main). Дерево чистое, stash пуст, открытых PR нет, локальная только `main`.
**Last released version:** **v2026.702.1024 на проде** (оба сервиса active, `/health` = 2026.702.1024, `/updates/status` latest = 2026.702.1024, blockmap 200, миграции 0071+0072 применены).

## Текущая нитка

**Backlog-sweep сессия завершена релизом v2026.702.1024** (10 пунктов: токен, со-локация даты в наряде, FK 0071, дроп `users` 0072, warnings прогноза, semgrep-гейт, прополка PROJECT_STATE, уборка должностей — детали в [`COMPLETED.md`](COMPLETED.md) §Прочее). Остаётся **полевой хвост прошлой нитки**: подтверждение владельцем на живом клиенте, что дата отгрузки `2Ж03АТ0479` подхватывается после однократного ввода в карточке (код-фикс v1708, клиент теперь автообновится до 1024).

## Следующий шаг

1. **Полевая проверка у владельца (не код):** на живом клиенте (после автообновления до 1024) открыть карточку `2Ж03АТ0479` → ввести «Дату отгрузки» → сохранить → убедиться, что список показал её. 172 двигателя с расходящейся статус-датой должны были исправиться автоматически ещё на 1708.
2. Заодно владелец увидит новую со-локацию «Дата выполнения» + кнопка закрытия в карточке наряда (v1024) — если вёрстка на его экране кривая, это быстрый фикс.
3. Если полевая проверка зелёная — нитка закрывается совсем; дальше брать из [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md): всё автономно-безопасное выметено, остались пункты под решение владельца (H1-B2 матрица ролей, сброс кэша M2b), /zavod-дозревание (рекламация, идентичность двигателей) или живой клиент оператора (visual-хвосты печати).

## Контекст

- **Релиз v2026.702.1024:** PR #22–#29. Миграции **0071** (FK `user_permissions→entities`) + **0072** (`DROP TABLE users` — бэкап 5 строк: прод `~/users-table-backup-20260702.sql`) применены на проде. Деплой-грабля: `db:migrate` падал `must be owner of table ai_chat_history` (таблица была под `postgres`) — вылечено `ALTER ... OWNER TO valstan`, постоянный фикс → GOTCHAS **M24**.
- **Ledger release-token** ротирован 2026-07-02 → протухает **~2026-08-01**; рецепт самопродления (старый токен минтит новый) — PENDING ⏳ + письмо в brain (`mailbox/to-brain/2026-07-02-admin-jwt-self-renewal-pattern.md`).
- **Semgrep на PR теперь блокирующий** (diff-aware, `--baseline-commit`); если чей-то PR внезапно красный по semgrep — это находка в его диффе, не дрейф правил.
- Открытых PR нет, stash пуст, ветки — только `main`.

## Открытые вопросы для пользователя

- **На живом клиенте (1024) для `2Ж03АТ0479`: после однократного ввода даты отгрузки в карточке список её показал?** От этого зависит, полностью ли закрыта нитка dual-source.

## Не забыть (low-priority)

1. **Ledger release-token — следующая ротация до ~2026-08-01** (PENDING ⏳, теперь 2-минутный рецепт по ssh).
2. **Ротация SSH-ключей прода — до 2026-08-21** (90-дневный цикл, PROJECT_STATE).
3. AV-ложнопозитивы watchdog'а — продолжать поглядывать в «Критические события».
