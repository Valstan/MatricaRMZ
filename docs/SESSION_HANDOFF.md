# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (нитка «Рекламация MVP + вкладки карточки двигателя + повторный заезд» закрыта полностью и на проде)
**Updated:** 2026-07-03 (Claude Opus 4.8, машина `rmz4val`)
**Branch:** `main` (= origin/main). Дерево чистое, stash пуст, открытых PR нет, локальная только `main`.
**Last released version:** **v2026.703.2147 на проде** (вкладки карточки двигателя + рекламации + повторный заезд + фикс имён деталей #56).

## Текущая нитка

_n/a_ — сессия 2026-07-03 (вечерняя) закрыла задание владельца из /zavod целиком:
1. **#58 (Ф0):** карточка двигателя → 4 вкладки, save-on-close/черновики/печать не тронуты.
2. **#59 (Ф1):** рекламационный учёт (EAV, 8 атрибутов, вкладка + синяя точка + фильтр «Рекламационные»).
3. **#60 (Ф2):** повторный заезд — 3 пути при дубле номера, обход гейтов flag+intent, исключение из склейки (3 места), «архивный заезд». Попутно починена pre-existing гонка stale `engineDetails` при key-ремоунте.
4. **Релиз v2026.703.2147** выпущен и задеплоен: оба сервиса active, `/health` и `/updates/status` отдают 2147, blockmap 200. Увёз и висевший #56.

Все фазы CDP e2e-verified (12/12, 10/10, 10/10). Детали — [`COMPLETED.md`](COMPLETED.md) §Акты/дефектовка/наряды; план — [`plans/_archive/reclamation-mvp-2026-07.md`](plans/_archive/reclamation-mvp-2026-07.md).

## Следующий шаг

Задачу выбирает владелец. Кандидаты из [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md):
- **Решения владельца:** M2b (сброс кэша операторам), forward-proxy VPS для AI (🔴 блокер).
- Полный RMA-каркас (warranty-наряды, бакет затрат, статистика причин) — следующие фазы рекламаций, по дозреванию через /zavod.

## Контекст

- Прод: **v2026.703.2147**, оба сервиса active, /health ok, `/updates/status` latest=2147, blockmap 200 (дельта живая). Миграций в релизе не было.
- Коммиты сессии: #58 Ф0, #59 Ф1, #60 Ф2, #61 релиз (HEAD `c0c72e33` + handoff-PR).
- Письмо в brain: `mailbox/to-brain/2026-07-03-keyed-remount-stale-props-and-flag-bypass.md` (две находки).
- Открытых PR: нет. Un-pushed веток: нет.

## Открытые вопросы для пользователя

- Нет.

## Не забыть (low-priority)

1. **Спот-чек владельцем на живом клиенте** (после обновления до 2147): карточка двигателя — вкладки; «Принять по рекламации» → синяя точка в списке; ввод существующего номера при создании → три пути.
2. **Ledger release-token — ротация до ~2026-08-01** (PENDING ⏳, 2-минутный рецепт по ssh).
3. **Ротация SSH-ключей прода — до 2026-08-21** (PROJECT_STATE).
4. AV-ложнопозитивы watchdog'а — поглядывать в «Критические события».
5. Единицы измерения ~180 старых позиций номенклатуры — маленький backfill, если владелец захочет.
