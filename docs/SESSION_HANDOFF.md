# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE
**Updated:** 2026-07-22 (Claude session, машина `PC40`)
**Branch:** `main` = `origin/main` (`567dc0ac`).
**Last released version:** **v2026.721.1616** (отчёт «Двигатели и контракты»). Планшетный режим **смержен в main (#294), но ещё НЕ выпущен** — уедет ближайшим релизом.

## Текущая нитка

Активной форсированной нитки нет. Сессия отгрузила **планшетный режим клиента Ф1** (PR #294, смержен) — все оставшиеся шаги ждут сигнала владельца / пилота:

- **Планшетный режим (Windows)** — Ф1a (кнопка «Комп/Планшет» + настройка «Это планшет» + CSS-масштаб), Ф1b (крупные степперы) и Ф-later #1 (экранный numpad количества) смержены. Код-гейты зелёные; CDP-смоук не прогнан (на PC40 нужен UAC для PG — проверить на реальном пилотном планшете). PENDING §Планшетный режим. Планы: [`plans/tablet-windows-ui-mode-2026-07.md`](plans/tablet-windows-ui-mode-2026-07.md), [`plans/tablet-shop-floor.md`](plans/tablet-shop-floor.md).

## Следующий шаг

Ждём сигнала владельца. Кандидаты (не начинать без сигнала):
- **Ф-later #2 — упрощённое операторское меню в планшете** (прятать бухгалтерские разделы): нужен продуктовый выбор владельца, какие разделы оставить оператору, + пилот.
- **Ф2 родительского плана — advisory-резервирование двигателя** («Взять в работу», read-only у других, мягкий серверный гейт в `applyPushBatch`): ~2-3 сессии, отдельная нитка.
- **Пилот:** купить x64-планшет (Intel N100/N150/N200) → текущий инсталлятор ставится нативно; пометить «Это планшет» в Настройках → проверить touch-режим вживую.

## Контекст
- Планы: [`plans/tablet-windows-ui-mode-2026-07.md`](plans/tablet-windows-ui-mode-2026-07.md) (UI-режим + ответ по архитектуре CPU/установке) + родитель [`plans/tablet-shop-floor.md`](plans/tablet-shop-floor.md).
- Связанный PR: [#294](https://github.com/Valstan/MatricaRMZ/pull/294) (смержен, ветка удалена).
- Прод: v2026.721.1616 (планшетный режим НЕ на проде — только в main).
- Открытых PR: нет.
- Локальные ветки с un-pushed коммитами: нет (только `main`).

## Открытые вопросы для пользователя
- Планшетное меню оператора (Ф-later #2): какие разделы оставить в цеху (двигатели/наряды/дефектовка/…), остальное скрыть? — определяет объём фильтрации.
- Общее облачное окружение рутин Матрицы и Сарафана — оставить или разделить (рекомендация: разделить). Не срочно (тянется с прошлой сессии).

## Не забыть (low-priority)
1. Ledger release-token — exp **2026-08-15**; ротация ≤ ~2026-08-12 (PENDING §Ledger). Первый релиз после ~2026-08-01 упадёт без ротации.
2. Ротация SSH-ключей прода — до 2026-08-21.
3. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз/комплектование неполны.
4. deadcode-прогон — ~2026-08-04.
5. Ф2 де-дуп `blank-synthetic-codes` — перепроверка гейта раската ~2026-07-24.
6. Бэкап-таблицы на проде (`*_bak_20260717`, `*_bak_norms20260717`) — снести ~август.
7. Планшетный режим: CDP-смоук прогнать на реальном пилотном планшете (на dev-PC40 блокирован UAC для PG).
