# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** ACTIVE
**Updated:** 2026-07-25 (Claude session, машина `rmz4val`)
**Branch:** `main` = `origin/main`.
**Last released version:** **v2026.724.1603** (раскатан на прод). Работа этой сессии **в main, но НЕ выпущена релизом** — см. «Следующий шаг».

## Текущая нитка

**Сплошная разгрузка бэклога** по просьбе владельца («доделывай все планы, висяки, бэклоги»). За сессию закрыто 8 ниток, 8 PR (#324, #327–#334), все смержены в `main` под зелёными гейтами.

Что сделано:
- **Референс-целостность при удалении — план закрыт целиком.** Ф2/Ф3 (#324) прогнан живым CDP-смоуком 16/16 и смержен; Ф2b (#328) — диалог намерения стал стандартным путём удаления в генерик AdminPage-флоу (админ), смоук 11/11; Ф4 (#327) — серверный гейт видит JSON/junction-хранилища, 5 тестов. План → `plans/_archive/`.
- **Дефекты аудита 2026-07-22 в отгруженном:** отчёты утиль/двигатели-контракты (#329), планшетные CSS-токены, которые перебивал инлайн `displayPrefs` (#330), перевес `erp_reg_stock_movements` при склейке двигателей (#331).
- **Чистка + тест-долги:** снос мёртвых IPC-поверхностей `erp:*` и синхронного AI-чата, `change_log`, мелких зомби (#333); тесты push-гейта AI-чата и шифрования клиентской БД, 20 штук (#332).
- **Watchdog:** все 5 хвостов — ярлыки, backoff, logTail, атрибуция heartbeat, README (#334, зелёный Go-билд в CI).
- **Долги перед brain:** ADR-0007 и рефлекс #014 вшиты в `CLAUDE.md`, обезличенный скелет security-audit (#057) отправлен в `mailbox/to-brain/`.

## Следующий шаг

**Решение владельца: выпускать ли релиз** с накопленным в `main` (8 PR, включая пользовательски заметное — диалог удаления связей во всех справочниках, планшетные размеры, фиксы отчётов). Новых серверных миграций в этой пачке **нет**; изменения затрагивают и backend (`adminMasterdataService`, `applyPushBatch`, `engineDedupeService`), и клиент → релиз обычный, по `CLAUDE.md` §Release process.

Если релиз — то `/reliz`; после раската имеет смысл ручной смоук диалога удаления на реальных данных (у оператора связей больше, чем в dev-БД).

## Контекст

- **Ф2 де-дупа `directory_parts ↔ erp_nomenclature`** — не трогалась, по-прежнему ждёт зелёного гейта раската клиентов (5 машин ниже v2026.712.1818). Детали — `PENDING_FOLLOWUPS` §🔴.
- Приёмка новых веток watchdog (восстановление по пропавшим ярлыкам, backoff) на живой машине не гонялась — шло под CI-гейтом.
- Планшетные фиксы (#330) проверяются только на пилотном планшете — общий блокер планшетной нитки.

## Открытые вопросы для пользователя

- Выпускать релиз с накопленным в `main` сейчас или копить дальше?
- Watchdog `CleanupMatricaFiles` в `installer.nsh` целит в мёртвый путь: оставить как есть (мягче при сорванном апдейте) или перенацелить на реальную папку (чистый slate)? — висит с 2026-07-12.

## Не забыть (low-priority)

**Даты ротаций/дедлайнов — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §📅.** (ledger-токен ≤12.08, deadcode ~04.08, SSH ≤21.08.)

1. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз/комплектование неполны.
2. `RELEASE_WELCOME_HISTORY` — легаси-дубли меток `v1.18.0`–`v1.18.5`, почистить при следующем релизе.
