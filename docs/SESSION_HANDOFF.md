# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (релиз раскатан, активной длинной нитки нет)
**Updated:** 2026-07-15 (Claude Opus 4.8, машина `PC40`)
**Branch:** `main` = `origin/main`. Дерево чистое, stash пуст, открытых PR нет.
**Last released version:** **v2026.715.1549 на проде** — внутренний номер двигателя + список двигателей (акт комплектности/печать/отчёт) + утиль двигателя (две метки + снятие блока сборки). Невыпущенных фич в `main` нет.

## Текущая нитка

Активной длинной нитки нет. Сессия: применение мандата brain'а по автономному `/reliz` (оказался уже выполненным в [#214](https://github.com/Valstan/MatricaRMZ/pull/214) — сверено по 5 пунктам, ack на месте), затем утиль двигателя ([#220](https://github.com/Valstan/MatricaRMZ/pull/220) + [#221](https://github.com/Valstan/MatricaRMZ/pull/221)) и релиз v2026.715.1549 первым прогоном автономного `/reliz`.

## Следующий шаг

Выбор владельца в начале следующей сессии:

1. **Обкатка утиля вживую** — оператор проводит первый утильный двигатель по новому пути: «Признан утильным» по дефектовке → наряд на сборку (детали со склада «Утиль») → «Утиль — отправлен заказчику». Обратная связь: хватает ли двух меток, не мешает ли что-то в наряде.
2. **H7 шаг «в» — fail-closed `normalizeRole`** (безопасен: живых `user`=0). Флип [`employeeAuthService.ts:265`](../backend-api/src/services/employeeAuthService.ts) `return 'user'` → fail-closed. ⚠️ Не 1 строка: задевает `roleReport` + тесты — отдельный PR. Детали — [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §Security.
3. **Ф2 де-дупа `blank-synthetic-codes`** (владелец дал добро «делать как раскатано»). Гейт: недавние клиенты ≥ 2026.712.1818. Детали — [`plans/parts-nomenclature-deep-dedup-2026-07.md`](plans/parts-nomenclature-deep-dedup-2026-07.md) §Ф2.
4. **Обкатка пилота UI-конструктора «Мои экраны»** — собрать вживую первый дашборд-навигатор. Уровень 2 (нейронка) отложен (гео-блок Anthropic).

## Контекст

- **Релиз v2026.715.1549** — четыре фичи, миграций/backfill не вёз. Итоги ниток — [`COMPLETED.md`](COMPLETED.md) §Акты/дефектовка/наряды; эффекты — [`zavod/PROGRAM_EFFECTS.md`](zavod/PROGRAM_EFFECTS.md).
- **Утиль (#220/#221) — ключевое решение:** метка живёт на **двигателе**, не на наряде (первоначальная идея владельца — «метка Утильный на наряде, снимающая ограничения на детали»). Причина: ограничения на детали в наряде **не существует** (`warehouseSourceOptions` отдаёт все активные локации, включая `scrap`), а реальным блоком была выдача наряда по утилю в дефектовке. Метка на наряде + метка на двигателе = два расходящихся источника истины. Подробности — `COMPLETED.md`.
- Профиль `PC40` дополнен: deferred-create нарядов + `quantity` в строках дефектовки (см. ниже).
- Открытых PR нет. Un-pushed веток нет. Stash пуст.

## Открытые вопросы для пользователя

- **AI/VPS** — отложено владельцем на неопределённый срок; PENDING 🔴 остаётся, не поднимать.

## Не забыть (low-priority)

1. **Старые двигатели без внутреннего номера** — владелец решил присваивать **только новым**; у старых колонка «Внутр. №» с прочерком. Отдельной задачи не заводим.
2. **H7 шаг «в»** — безопасен (живых `user` нет), но не 1 строка (тесты `roleReport`); отдельный PR.
3. **Ф2 гейт** — не гнать apply, пока не все недавние клиенты ≥ 2026.712.1818; env сорсить (M30); после apply — M31-верификация PG.
4. Ledger release-token — ротация до ~2026-08-04 (первый релиз после ~2026-08-01 упадёт 401, лечение — PENDING ⏳).
5. Ротация SSH-ключей прода — до 2026-08-21.
6. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст.
7. deadcode-прогон (месячная дельта) — ~2026-08-04.
8. Паразитный `~/MatricaRMZ/backend-api/ledger` на проде — удалить при случае.
