# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Перезаписывается целиком командой `/close_session`. История — через `git log -- docs/SESSION_HANDOFF.md`.
>
> Если работы в потоке нет — `Status: IDLE` и пустые секции. Команда `/start` это увидит и не будет ничего навязывать.

**Status:** IDLE (обе фичи смержены в `main`; активной длинной нитки нет)
**Updated:** 2026-07-15 (Claude Opus 4.8, машина `PC40`)
**Branch:** `main` = `origin/main` (`6166f48f`). Дерево чистое, stash пуст, открытых PR нет, локальных веток кроме `main` нет.
**Last released version:** **v2026.715.45 на проде** (пилот UI-конструктора). В `main` лежат **две невыпущенные фичи** — уедут ближайшим релизом.

## Текущая нитка

Активной длинной нитки нет. Сессия отработала список двигателей ([#218](https://github.com/Valstan/MatricaRMZ/pull/218), смержен): статус акта комплектности (колонка + сортировка + фильтр + фильтр по контрагенту), печать списка с выбором полей, точка входа в отчёт по двигателям. Гейты зелёные (shared 420, backend 400, CDP-смоук вживую, CI 5/5).

## Следующий шаг

Выбор владельца в начале следующей сессии:

1. **Релиз** — в `main` две невыпущенные фичи: внутренний номер двигателя ([#216](https://github.com/Valstan/MatricaRMZ/pull/216)) и список двигателей ([#218](https://github.com/Valstan/MatricaRMZ/pull/218)). `/reliz` (он сам включает закрытие сессии — `/close_session` после него не звать). Миграций ни одна не везёт (EAV + renderer), backfill не нужен.
2. **H7 шаг «в» — fail-closed `normalizeRole`** (безопасен: живых `user`=0). Флип [`employeeAuthService.ts:265`](../backend-api/src/services/employeeAuthService.ts) `return 'user'` → fail-closed. ⚠️ Не 1 строка: задевает `roleReport` + тесты — отдельный PR. Детали — [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §Security.
3. **Ф2 де-дупа `blank-synthetic-codes`** (владелец дал добро «делать как раскатано»). Гейт: недавние клиенты ≥ 2026.712.1818. Детали — [`plans/parts-nomenclature-deep-dedup-2026-07.md`](plans/parts-nomenclature-deep-dedup-2026-07.md) §Ф2.
4. **Обкатка пилота UI-конструктора** — собрать вживую первый дашборд-навигатор, обратная связь операторов. Уровень 2 (нейронка) отложен (упирается в гео-блок Anthropic).

## Контекст

- **Отгружено в `main` (2026-07-15), обе — до релиза:**
  - Список двигателей — [#218](https://github.com/Valstan/MatricaRMZ/pull/218). Критерий «акт заполнен» = хотя бы одна деталь `present` в `engine_inventory` (решение владельца: важен факт начала, не полнота). Флаг `hasCompletenessAct` считается тем же проходом по `operations`, что и авто-брак по картеру. Отчёт — **обогащён существующий пресет `engines_list`**, нового не заводили. Итог — [`COMPLETED.md`](COMPLETED.md) §Акты/дефектовка/наряды; эффект — [`zavod/PROGRAM_EFFECTS.md`](zavod/PROGRAM_EFFECTS.md).
  - Внутренний номер двигателя — [#216](https://github.com/Valstan/MatricaRMZ/pull/216). Ключевые решения — [`CLAUDE.md`](../CLAUDE.md) §Key architecture decisions.
- **Прод:** v2026.715.45 (в этой сессии не трогали).
- Открытых PR нет. Un-pushed веток нет. Stash пуст.
- Профиль `PC40` дополнен: CDP-навигация по меню только через `span.v2-menu-btn-label` (общий перебор по тексту — тихий промах).

## Открытые вопросы для пользователя

- **Релиз двух фич** — выпускать ближайшим `/reliz` или ещё копить? (внутренний номер + список двигателей; обе в `main`, на проде их нет)
- **AI/VPS** — отложено владельцем на неопределённый срок; PENDING 🔴 остаётся, не поднимать.

## Не забыть (low-priority)

1. **Старые двигатели без внутреннего номера** — владелец решил присваивать **только новым**; у старых колонка «Внутр. №» будет с прочерком. Разметка задним числом — руками в карточке (поле года для того и редактируемо). Отдельной задачи не заводим.
2. **H7 шаг «в»** — безопасен (живых `user` нет), но не 1 строка (тесты `roleReport`); отдельный PR.
3. **Ф2 гейт** — не гнать apply, пока не все недавние клиенты ≥ 2026.712.1818; env сорсить (M30); после apply — M31-верификация PG.
4. Ledger release-token — ротация до ~2026-08-04 (первый релиз после ~2026-08-01 упадёт 401, лечение — PENDING ⏳).
5. Ротация SSH-ключей прода — до 2026-08-21.
6. Мастера жмут «Выдать в работу» на ремнарядах, иначе прогноз по ремонту пуст.
7. deadcode-прогон (месячная дельта) — ~2026-08-04.
8. Паразитный `~/MatricaRMZ/backend-api/ledger` на проде — удалить при случае.
