# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-04 (сессия Fable 5.1 → Opus 5, машина `PC79`)
**Branch:** `main` = `origin/main` на `b1a0275`. Открытых PR нет; на origin **ровно одна ветка — `main`** (04.09 вычищено 16 слитых остатков; не верить этой строке вслепую — проверять `git ls-remote --heads origin`).
**Last released version:** **v3.19.0** — на проде с 2026-08-31.

**Не выпущено, ждёт релиза клиента 3.20.0:** шаблоны «Моих отчётов» ([#764](https://github.com/Valstan/MatricaRMZ/pull/764)), unique в режиме отчёта ([#765](https://github.com/Valstan/MatricaRMZ/pull/765)), самолечение пропущенных зависимостей ([#767](https://github.com/Valstan/MatricaRMZ/pull/767)). Серверные части #765/#766 на **обоих** инстансах прода.

## Текущая нитка

**Ledger: от «проекция невосстановима» к «состояние выводится из цепочки».** Инструмент пересборки сделан и прогнан на проде — и первым же прогоном вскрыл следы гонки дописывания 2026 года. Три подменённых блока усечены, расхождения записаны как известные. **Осталось два хвоста:** семь потерянных транзакций (сверить с PG) и собственно компакция, ради которой всё затевалось.

Смежная нитка **Матрица 4, трек B, R4b (cutover)** по-прежнему ждёт парк ≥ v3.19.0 (на 03.09 — 7 из 17 активных win32-клиентов). Предусловия по данным зелёные, остался только парк.

## Следующий шаг

1. **Сверить семь потерянных транзакций с PostgreSQL.** `seq` 421759…421765, 06.03.2026 между 08:23:45 и 08:23:53. Вопрос: дошли ли те изменения до рабочего хранилища или потерян не только след. Искать в `change_log` / `audit_log` за это окно; по ledger содержимое не восстановить (см. M103 — молчание `ledger_tx_index` там ничего не доказывает). Разбор — `PENDING` §«Ревизия цепочки ledger».
2. **Повторить `ledger:rebuild-state` на проде** — теперь, когда три файла усечены, проход должен дойти до конца и дать вердикт. Команда:
   ```
   cd ~/MatricaRMZ/backend-api && corepack pnpm exec tsx src/scripts/rebuildLedgerState.ts --out /tmp/rebuilt.json
   ```
   Первый прогон 04.09 дошёл до 210 тыс. блоков за минуты, пик RSS 225 МБ — памяти хватает с запасом, поднимать кучу не нужно. Гонять вне окна ночного бэкапа (00:17–03:25).
3. **После зелёного вердикта — компакция** (`PENDING` §«Компакция/ретенция ledger-истории»): теперь она возможна, потому что состояние снова выводимо из блоков.
4. Хвосты нитки диска: п. 7 (снос внерепозиторной ретенции — после штатного прогона `cleanup-updates` 06.09) и п. 9 (учебное восстановление архива, до него не размораживать 14 старых архивов).

## Контекст

- План v4: `docs/plans/matrica-v4-kickoff-2026-08.md` §R4 (для нитки R4b).
- Ключевые коммиты сессии: `8fa5cc4` (инструмент пересборки), `bc467be` (битый блок называет себя), `b1a0275` (ревизия цепочки), `beabacd` (усечение + известные расхождения).
- Прод: v3.19.0, оба сервиса active, свободно 9,0 ГБ. Telegram работает, бэкап под наблюдением, вложения на Я.Диске с кэшем.
- Копии до усечения: `~/ledger-fix-backup-20260904/*.before-truncate` на боксе.
- Открытых PR нет. Локальных веток кроме `main` нет.

## Что не сработало

- **Судить о целостности по одному `stateHash` — нельзя.** Я сам записал это в приёмку накануне, и первый же прогон опроверг: хеш меняется от появления одной новой **пустой** таблицы, когда данные совпадают до байта. Инструмент проверки, который врёт на здоровом ledger'е, вреднее отсутствия инструмента. Вердикт даётся по **потабличным** хешам.
- **Отрицательный результат без непустого контроля ничего не стоит** (теперь GOTCHAS **M103**). Дважды за одну ревизию: (1) моя формула хеша блока была неверной, и «не сходились» все блоки подряд — спасли здоровые контрольные; (2) семи пропавших `seq` нет в `ledger_tx_index`, но их **соседей там тоже нет**, значит молчание таблицы не доказывает потерю.
- **Проверка «пусто = чисто» неотличима от несостоявшейся проверки.** `[ -z "$(git ls-remote … | grep -v main)" ]` отрапортовал чистоту, когда `ls-remote` просто не дошёл до GitHub. Отчёт владельцу ушёл неверным. Форма с разбором кода возврата — в `PENDING` §«Ветки-остатки на origin».
- **Автомерж съедает коммит, досланный после вооружения.** Слил первый коммит, удалил ветку, а `git push` второго молча воссоздал ветку — при этом PR всё время показывал «слит». Вооружать последним действием; проверять по содержимому `main`, а не по статусу PR.
- **«Выкат прошёл» нельзя утверждать по тому, что команда не упала.** Фоновый выкат оборвался по ssh, перезапустив один сервис из двух; primary пять часов работал на старой сборке. Ловится сверкой mtime сборки с `ps -o lstart=` каждого сервиса.
- **`node -e "…"` в Bash-инструменте ломает обратные кавычки и обратные слэши.** Наступил дважды за сессию, хотя это записано в профиле машины. Код — только через Write/Edit или `node script.js`.

## Открытые вопросы для пользователя

- **Список деталей двигателя вынести из `operations.meta_json` в строгую таблицу?** Это главный источник роста ledger: 1179 списков по 121 КБ = 139 МБ из 187. Трек B плана v4 туда и ведёт. Снимет и рост блоков, и стоимость записи.
- **Включить «Always suggest updating pull request branches»?** Я оставил выключенной: пользы нет (требование свежести ветки выключено), а нажатие стоит полного прогона CI. Автомерж и автоудаление веток включены 04.09.
- Прежние, не снятые: менять ли внешний SSH-порт прода; стереть ли старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника на парк; подписные грифы печатных форм (ФИО в коде).

## Не забыть (low-priority)

- **Keystore Android:** свёрток так и не собран, единственная копия ключа на PC40. Brain просит поднимать первым пунктом каждого старта.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09 сделан; следующий до R4b, начать с четырёх непокрытых линз.
- **Перемер роста диска 03.10** — по августовскому темпу; там же первые цифры счётчика обращений к файлам (D-073).
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка (та же метрика, что R4b).
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Файл `MISSING 21a1b799-… Кто-то ночью спит.mp4` — строка в БД есть, файла нет; разобрать отдельно.
