# Session Handoff

> Sticky-note для непрерывности разработки между сессиями и компьютерами. Обновляется в PR каждого шага нитки (D-066); `/close_session` — страховка. История — через `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-14 (Opus 5, машина `PC79`) — **диск прода разгружен**: снимки сжимаются при загрузке ([#902](https://github.com/Valstan/MatricaRMZ/pull/902), в 5.4 раза), старые пересжимаются проходом ([#904](https://github.com/Valstan/MatricaRMZ/pull/904)), **кэш вложений снят — клиенты качают прямо с Я.Диска** ([#909](https://github.com/Valstan/MatricaRMZ/pull/909)). Занято 7.9 → 6.2 ГБ. Раньше за день: шапка акта + два прохода по проду ([#898](https://github.com/Valstan/MatricaRMZ/pull/898), [#900](https://github.com/Valstan/MatricaRMZ/pull/900)); android-app в CI ([#896](https://github.com/Valstan/MatricaRMZ/pull/896)); окно обслуживания машины ([#895](https://github.com/Valstan/MatricaRMZ/pull/895), PR открыт).
**Branch:** `main`; открыт `feat/client-ops-window-tabs` → [#895](https://github.com/Valstan/MatricaRMZ/pull/895).
**Last released version:** **v3.32.0**, выкат 13.09 (детали прод-статуса — тело релизного PR). Везёт **миграцию 0096** (`parent_nomenclature_id`, аддитивная) и первый APK на новом ключе подписи (`android-v3.32.0`).

## Текущая нитка

**Пакет владельца 12.09 закрыт по коду целиком** (3.29.0–3.32.0). Планы: отчёты — `plans/_archive/reports-fast-start-2026-09.md`; BOM — [`plans/bom-simplify-2026-09.md`](plans/bom-simplify-2026-09.md) (§9 — ходы владельца). Следующей нитки владелец не назначал.

## Открытое по этой нитке

- **Три листа, где марка была дописана по префиксу** — проверить глазами, не была ли она вписана оператором намеренно: `5М04АТ1224` «В-46-2С1» → «В-46-2С1М», `К08АТ8367` «В-46» → «В-46-1», `П12АТ1531` «В-55» → «В-55В». У номера префикс = однозначный обрезок, а у марки «В-46» это ещё и настоящее имя другой марки. Остальные 32 листа с отличающейся маркой и 26 с отличающимся внутренним № проход не трогал. Правится вручную в карточке; прежнее значение видно в истории версий акта.

## Диск прода — что выяснилось 14.09

Свободно было 1.5 ГБ из 9.9 (85 %). Причина не в базе: за сутки залили **350 снимков на 1.5 ГБ**. Фото уходят на Я.Диск штатно (все 355 строк сегодня — `yandex`, локальных всего 2), место занимал **трёхдневный кэш** на боксе (1511 МБ) плюс превью (160 МБ, лежат всегда).

Вылечено в корне: снимки теперь сжимаются при приёме. Оставшееся — в `PENDING`: прод не собирает бэкенд (OOM) и пересжатие 6.4 ГБ старых снимков.

Быстро освободить, если понадобится (≈480 МБ, ничего рабочего не трогая): кэш apt 182 МБ, системный журнал 112 МБ, `~/ledger-fix-backup-20260905` 186 МБ.

## Следующий шаг

1. **Ходы владельца после 3.32.0** — `PENDING` §«Пакет владельца 12.09» (архив пустого «BOM В-84», флаг «основная для…», первые обобщённые позиции по `warehouse:suggest-parent-positions`, живая приёмка 3.29–3.32).
2. **Планшет:** первый APK на новом ключе — на DIGMA `uninstall` + `install`, вход, полный pull (экран включён, вайтлист DuraSpeed, грабля M74). Самообновление этот переход не сделает.
3. **Приёмка 3.28.0 живьём:** «Снабжение → Приказы о ценах» (первый приказ и строка → карточка услуги показывает цену); два решения владельца по приказам.
4. **Раздача соседям — обход владельца** (правило брандмауэра → тумблер → перезапуск, 2–3 машины одной подсети; приёмка — непустой `update_peers`).
5. **Живая приёмка сторожа 3.27.0** (`--repair` качает настоящий установщик).
6. **Прод-часть слияния дублей — ход владельца** («Проверить» → «Объединить», затем `users:parity`).
7. **≈19.09:** счёт `client.sync.network_transient` / `network_flapping`; через две недели — `"stop":"max_tokens"` в `llm usage` (#855).
8. Прежнее: сеть VPS рвётся (ход хостера); E2.2 плана [`engine-inventory-lines`](plans/engine-inventory-lines-2026-09.md); хвосты слияния; J3 журнала; D-084 второй шаг — ждать письма brain про Касперского. Старые клиенты <3.5.0 (6 машин за 30 дней) — при следующей колонке в легаси-таблице снова прятать её через `LEGACY_HIDDEN_COLUMNS`.
## Контекст

- **Планы:** [`bom-simplify-2026-09.md`](plans/bom-simplify-2026-09.md) (код закрыт, §9 — ходы владельца); [`engine-inventory-lines-2026-09.md`](plans/engine-inventory-lines-2026-09.md) (E2.2 открыт). Отчёты — в `_archive/`.
- **Открытых PR нет.** Смержено за день: [#883](https://github.com/Valstan/MatricaRMZ/pull/883)–[#885](https://github.com/Valstan/MatricaRMZ/pull/885) (ключ подписи + гейт), [#886](https://github.com/Valstan/MatricaRMZ/pull/886) (этап 2B), [#888](https://github.com/Valstan/MatricaRMZ/pull/888)–[#893](https://github.com/Valstan/MatricaRMZ/pull/893) (BOM: план, решения, E1–E4), релизный PR 3.32.0.
- **Локальные ветки с неотправленными коммитами** (не трогать, но и не бояться): `feat/employee-merge-repoints-references` и `fix/employee-merge-refuses-on-references` от 11.09. Их работа **уже в `main`** squash-мержами [#866](https://github.com/Valstan/MatricaRMZ/pull/866) и [#863](https://github.com/Valstan/MatricaRMZ/pull/863) — это остатки, ничего не потеряно; удалять придётся через `-d` с форсом, поэтому оставлены как есть.
- Прод: последняя миграция — `0096_nomenclature_parent_position` (96 из 96). Каталога цепочки и `MATRICA_LEDGER_DIR` больше нет; откат на сборку с цепочкой невозможен (и не нужен).
- Прод-БД читается скриптом через stdin (`docs/machines/PC79.md` §«Запросы к прод-БД»); `client_settings` — колонки `last_version` / `last_hostname` / `last_username`, `last_seen_at` в миллисекундах.
- Дайджест brain 12.09 (G334–G340, идеи #291–#312) прочитан: G334 у нас закрыт, G336 (проба сразу после `reload nginx` попадает в старый воркер) — взять на приёмках nginx. Новых писем на 13.09 нет; наше письмо 13.09 — `mailbox/to-brain/2026-09-13-a-secret-in-ci-signs-but-is-not-a-backup.md`.
- CI: `employeeSectionGate.replica.test.ts` иногда падает на раннере `fetch failed` — `gh run rerun --failed`.
- **CI гоняет android-app с 14.09** (`test.yml` + `typecheck.yml`). До этого пакета не было ни в одном гейте: версия схемы клиента разъехалась (android 12 против electron 13 с #892) и доехала до зелёного `main` — сторож честно краснел только локально. Лечение и сторожа — PR 14.09. Остаток: **`lint` android-app по-прежнему не гоняется** (в `package.json` пакета нет скрипта `lint`, `pnpm -r lint` его молча пропускает).

## Что не сработало

- **«Таймингозависимый флейк» три недели скрывал настоящий баг** (14.09, GOTCHAS **M128**). `boot.test.ts` краснел только в пачке — списали на перегруз четырёхъядерной машины и записали в PENDING «это не баг». На деле тесты android делили один файл сессии: `sessionSidecarStore` шимился только для браузерного таргета, в vitest работал настоящий, а `app.getPath` шима отдаёт фиктивный путь — все воркеры писали в `<диск>:\android\appData\...`. Разбор 2026-08-31 искал файл в `%APPDATA%`, не нашёл и закрыл версию. **Правило:** «зелёный в одиночку, красный в пачке» — сначала искать общий ресурс ВНЕ процесса, и только потом тайминг.

- **Колонку реплики нельзя везти drizzle-файлом** (13.09, E3): `ALTER TABLE … ADD COLUMN` в `electron-app/drizzle/*.sql` уронил клиент стенда в цикл self-heal (журнал гоняется на всех базах, `ADD COLUMN` неидемпотентен, без breakpoint падает и на свежей). Только PRAGMA-обёртки `clientSchemaMigrations` + `ensureClientSchemaParity`. GOTCHAS **M127**.
- **`erp_nomenclature` — в замороженном снимке схемы для клиентов <3.5.0** (6 машин за 30 дней): новая колонка двигала бы им хеш и перестраивала базы. Спрятана через `LEGACY_HIDDEN_COLUMNS` + сторож. Прецедент 0094 (`room_id`) хеш уже двигал — учитывать при любой колонке легаси-таблицы.
- **Сущность, созданная мостом на стенде, живёт в реплике до `sync.run()`** — сервер отверг связку BOM на новую марку (FK), карточка показала «сохранено» ложно-зелёным по DOM. В смоуках после засева — `sync.run()` и пауза; синтетический `focus` React не слышит — `el.focus()`.
- **Секрет в CI — не бэкап, и проверить его можно только работой** (13.09). «Ключ потерян» было неверным диагнозом: он жил в секрете и продолжал бы подписывать. Потеряна была возможность его прочитать. Отсюда же гейт `android-signing-check.yml` — до него «валиден ли ключ» выяснялось только релизным тегом.
- **`openssl rand` в Git Bash пишет CRLF, `tr -d '\n'` снимает только LF, а `gh secret set` хвостовой `\r` срезает.** Хранилище создано паролем из 33 символов, в CI уехали 32: локально открывается, в CI «keystore password was incorrect». Значение для секрета не должно иметь пробельных символов по краям. Записано в `machines/PC79.md`.
- **`keytool | grep` в гейте съел сообщение об ошибке** — шаг краснел с пустым логом. Фильтровать вывод только после того, как он сохранён целиком.
- **Мостовой `auth.login` в CDP-смоуке не переводит рендерер с формы входа**: сессия есть, `auth.status()` отвечает `superadmin`, а на экране логин — драйвер рапортует «пункт меню не найден». Логиниться формой. Там же: панель меню бывает свёрнута, а смоук обязан быть идемпотентным — со второго прогона вкладка уже открыта со своим поиском и устаревшим списком. Записано в `verifier-electron/SKILL.md` §«грабли 2026-09-13».
- **`-r test` останавливается на первом красном пакете**, и знакомый флейк `android-app/src/core/boot.test.ts` прячет то, что backend-api и electron-app не запускались вовсе. Добивать их вручную.
- **Многострочный python-патч по якорю не совпадает** — рабочая копия в CRLF. Для таких правок инструмент Edit.
- Прежние (11–12.09): вопрос владельцу — с цифрами в руках; схема ≠ база (`service_price_orders`).

## Открытые вопросы для пользователя

- **Бэкап ключа подписи Android вне PC79 — единственное действие, которое ждёт владельца.** Скопировать папку `C:\Users\valstan\.matricarmz-keys` (два файла: `matricarmz-release.keystore` + `.pw`) на флешку в сейф и вторую копию вне здания. Скрипт свёртка `seal-key-bundle.mjs` для этого **не годится** — он требует `backup-private.pem`, которого на PC79 нет; решение владельца: положить pem рядом, сделать участников необязательными или оставить обычное копирование.
- **Название вкладки «Заготовки отчётов»** — взято вместо «Витрина шаблонов» из плана (в каталоге уже есть секция «Шаблоны отчётов» про другое). Переименовать — одна строка.
- **Обход машин для раздачи соседям** (правило брандмауэра + тумблер + перезапуск) — с каких 2–3 машин начать.
- **Telegram-логин владельца в карточке сотрудника** — правится владельцем в программе.
- **`/mybots` у BotFather** — единственный реестр, способный доказать, что второго бота нет.
- **Дубли листов engine_inventory** (≈206 двигателей): мягко удалить лишние — после E3, отдельным OK.
- Прежние: внешний SSH-порт прода; старые значения рекламации у 3 двигателей; `attribute_values` в конструкторе отчётов; раскатка Касперский-помощника; подписные грифы печатных форм.

## Не забыть (low-priority)

- **Keystore Android:** ключ перевыпущен 13.09, отпечаток `C1:EB:…:D0:39`, хранилище на PC79, четыре секрета репо перезаписаны, гейт зелёный. **Копия по-прежнему одна** — офлайн-носителя нет (см. «Открытые вопросы»). Brain просит поднимать первым пунктом каждого старта.
- **Пункт 14 владельца** (двухнедельная проверка перекосов) — прогон 03.09; следующий до R4b, начать с четырёх непокрытых линз.
- **Перемер роста диска 03.10**; там же первые цифры счётчика обращений к файлам (D-073). База после сноса цепочки: 35 % (6,6 ГБ занято) 12.09.
- Ротация ledger-токена ≤ 2026-09-24; ротация SSH-ключа прода для RMZ4VAL ≤ 15.11.
- Пересадка `asklad`, `uklad`, `asia2` → `storekeeper` ждёт обновления парка.
- EAV-долг: 44 223 живых значения на 30.08, ~+77/сутки.
- Файл `MISSING 21a1b799-… Кто-то ночью спит.mp4` — строка в БД есть, файла нет.
