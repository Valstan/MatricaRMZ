# Pending Followups — только открытое

Открытые задачи, техдолги, отложенные решения. Точка возврата для продолжения из любой сессии / с любого компа.

> **Раскол «открытое vs сделанное» ([memory-reorg](plans/_archive/memory-reorg-2026-06.md)):** здесь **только открытое**. Завершённое — в [`COMPLETED.md`](COMPLETED.md) (done-индекс) + `git log` + тела PR. Грабли — в [`GOTCHAS.md`](GOTCHAS.md). Закрытые планы — в [`plans/_archive/`](plans/_archive/).

## Правила ведения

- Добавлять сюда, когда что-то **отложено / техдолг / вернуться позже**. Приоритет: 🔴 блокер → ⏳ ждёт внешнего окна → 🟡 техдолг → 🟢 план/идея.
- **При закрытии — НЕ оставлять ✅-запись здесь:** удалить пункт и перенести 1 строкой в [`COMPLETED.md`](COMPLETED.md) (делает `/close_session` §7).
- **Метки старения** ([ADR-0004](../mailbox/to-brain/2026-06-09-deferred-backlog-aging-applied.md)): к открытому пункту — `🗓 since:YYYY-MM-DD`. `/start` всплывает пункты старше ~30 дней. Externally-blocked метку не несут (ждут события, не «забыты»).

---

## 🔴 Блокер

### Anthropic API блокирует РФ-IP → все AI-фичи на проде выключены

Anthropic режет РФ-IP на edge (прод `195.161.41.30`, myjino, RU → `403 "Request not allowed"`). `AI_ENABLED=false` в прод `.env` → `chatService`/`analyticsService`/`logAnalysisAgentService`/`aiAgentChatLearningService` молчат.

- **Решение (рекомендуемое):** forward HTTPS-proxy на зарубежном VPS (Hetzner DE/FI ~€4/мес) → `HTTPS_PROXY` в `.env` + `httpAgent` в `claudeProvider.ts`. Минимальные правки. **Ждёт решения владельца по VPS.**
- **Сцеплено (разблокируется вместе):** GitHub secret `ANTHROPIC_API_KEY` + раскомментить `pull_request` в `claude-code-review.yml` (авто-ревью PR); отложенные AI-tools (`get_report`, спец `get_work_orders`/`get_supply_requests`).
- Память: [[anthropic_geo_block]].

---

## 🟡 Security hardening — остаток аудита 2026-06-25 🗓 since:2026-06-25

Исчерпывающий мультиагентный аудит безопасности (51→46 подтверждённых находок). План/трекер всех фаз с file:line — [`plans/security-hardening-2026-06.md`](plans/security-hardening-2026-06.md). **Отгружено и на проде (см. [`COMPLETED.md`](COMPLETED.md)):**

- **v2026.625.1722:** C1 (саморегистрация→выгрузка всей БД), C2 (эскалация суперадмина через `system_role`), H2–H5 (увод чужих чатов/заметок/EAV-PII через конструктор отчётов и `/ledger/state/query`), отзыв refresh-токенов.
- **v2026.626.651:** bounded Фаза 3 (#598) — `/ledger/blocks` admin-only, серверная атрибуция `audit_log`, ReDoS-гард `/ledger/state/query`, IPC-гейт `e2e:keys:export/rotate`; + 4 ранее отложенных (#600/#601/#602) — auth на `/updates/peers` (с сохранением P2P/LAN/авто-апдейта), IDOR файлов (linkage-authz) + allowlist путей `chat:sendFile`/`files:upload`, login typeahead+recent (убран enum-oracle `/auth/login-options`).

_H8 (закоммиченные ledger-секреты в публичном репо) — ✅ закрыт 2026-06-26, см. [`COMPLETED.md`](COMPLETED.md) (#614/#615/#616, v2026.626.2207)._

**Открыто:**

1. **H1 — read-authz на pull.** «Полный рефактор» (2026-06-27), разбит на A+B — план [`plans/h1-table-read-authz-2026-06.md`](plans/h1-table-read-authz-2026-06.md). **A + B1a + зачистка хэшей ✅ на проде (v2026.627.1151).**
   - **A ✅** (#619): privacy-gap — `/state/snapshot` и `/state/query` режут чужие чаты/заметки per-user (единый `syncPrivacy.ts`).
   - **B1a ✅** (#620): pull read-authz чокпойнт — `password_hash` режется всем (была утечка хэшей на все клиенты), `audit_log` admin-only, HR-поля (`birth_date`/`hire_date`) операторам только своё.
   - **B\* зачистка ✅** (#621): клиент при старте чистит уже утёкшие `password_hash` из локального SQLite.
   - **B2 → SUPERSEDED → ✅ ЗАКРЫТО доступом по разделам (2026-07-03):** вся нитка section-access Ф0–Ф3 **на проде и активирована** (v2026.703.1049 + `access:backfill-sections --apply`, 21/21 логин) — см. [`COMPLETED.md`](COMPLETED.md), план в [`plans/_archive/section-access-2026-07.md`](plans/_archive/section-access-2026-07.md). Осознанные щели гейта (search:global, tools:, сквозные связки разделов) задокументированы в плане §Ф2 — сужать согласованно, если владелец будет резать матрицу вручную.
2. **H7-остаток** — legacy-роль `user` обходит operator-scoping записи (auth-атрибуты уже закрыты C2-backstop'ом для всех ролей; остаётся scope прочих таблиц). **Существенно смягчён 2026-07-03:** section-гейт Ф3 в `ledgerAuthzGuard` режет запись ниже editor-уровня для ВСЕХ ролей кроме superadmin (membership засеян 21/21) — legacy `user` теперь ограничен своими разделами.
3. **Mediums/lows не-bounded (Фаза 2 / инфраструктура):** локальный SQLite + E2E-ключ без шифрования (stolen-laptop → SQLCipher/`safeStorage`); updater без Authenticode; nginx security-headers (CSP) + CORS allow-all + plaintext `:18080`; bootstrap-суперадмин ставит пароль с первого неаутентифицированного логина; неаутентифицированный `/client/*` heartbeat (спуфинг `lastUsername`).
5. **✅ H8-хвост «вычистка истории» — решён иначе (2026-06-30):** репо понадобилось сделать **публичным** (биллинг Actions) → вместо `filter-repo`+force-push сделан **fresh-snapshot**: новый `Valstan/MatricaRMZ` из 1 чистого коммита (gitleaks 0), старая история с ключами → приватный `MatricaRMZ-archive`. Публичный репо ключей не содержит. См. [`COMPLETED.md`](COMPLETED.md) + память [[repo-public-split-archive]].
6. **✅ Доаудит ротации ledger-ключей — ЗАКРЫТО 2026-06-30** (см. [`COMPLETED.md`](COMPLETED.md)). Сверка живых прод-ключей с историей архива: подписной ротирован (отпечаток `d2b91b5e…` ≠ историч. `1563d94f…`), активный data-key `k-mq3wacgz` создан **после** untrack #252 → в трекинг не попадал. **Активные ключи чисты, ротация не нужна, H8 полон.** Граница (необратимо): `v1-legacy` остаётся в keyring для replay `enc:v1`, историч.-публичные данные экспонированы навсегда — защищается только будущее.

_(Реакция на утечку ПДн по 152-ФЗ — закрыто 2026-06-27: владелец решил, что формальное уведомление не требуется. Живой риск был снят ранее — ключ ротирован, репо приватный.)_

**Хорошо защищено (проверено `ss -tlnp`/UFW на проде 2026-06-25):** PostgreSQL (5432) + backend (3001/3002) слушают только `127.0.0.1`; UFW активен, default-DROP; настоящий Let's Encrypt-серт; клиент не отключает TLS-валидацию (`rejectUnauthorized:false` нигде нет). Поэтому все дыры — на уровне аутентифицированного API/RBAC, чинятся backend-кодом без переезда инфраструктуры.

---

## 🎙️ Owner backlog — батч 2026-06-16 (диктофон, 7 тем из 12 заданий) 🗓 since:2026-06-16

Наговорено владельцем, причёсано и сгруппировано. **Выполнять по очереди.** Каждая тема при подъёме: разведка кода → точечный план → PR-flow под гейтами. Файлы — best-effort по [`CODEBASE_MAP.md`](CODEBASE_MAP.md), проверять при старте темы.

**Осталось:** **G выпущен v2026.623.1642** (2026-06-23) — остались только мелкие хвосты (см. ниже). ✅ Сделано: F/A + B-#3/#10 (v2026.616.2017), B-#4 + C(#8/#5/#6) + D-#9 (v2026.617.30, #443–#448), **E фотогалерея #11 (#450, на проде v2026.617.1614)** — см. [`COMPLETED.md`](COMPLETED.md).

### G. Watchdog-агент самовосстановления клиента (#7) — ✅ ВЫПУЩЕН v2026.623.1642 (2026-06-23); остались мелкие хвосты 🗓 since:2026-06-22

Внешний Go-агент: при сорванном NSIS-обновлении (папка приложения снесена → exe/ярлыки пропали, встроенный апдейтер бессилен) сам переустанавливает из локального/скачанного инсталлятора; провал → «Критические события»; канал удалённого `reinstall` из админки.

**✅ Сделано (в `main`):** решения владельца (Scheduled Task + Go + текущий сертификат + ~15 мин); **Фаза 2** (#548 — команда `reinstall` + кнопка «Переустановить» в админке); **Фазы 3-4** (#549 — Go-бинарь `watchdog/` чистый stdlib 5.1 МБ, handshake `%APPDATA%\MatricaRMZ\watchdog.json`, bridge-эндпойнт `/client/watchdog/report`, CI `watchdog-build.yml`). План: [`docs/plans/client-watchdog-agent.md`](plans/client-watchdog-agent.md).

**✅ Фаза 5a код — в `main` (#551):** `installer.nsh` `customInstall` копирует watchdog в `%APPDATA%\MatricaRMZ\` + создаёт 2 per-user Scheduled Task (logon + every-15-min); `customUnInstall` снимает их. Бинарь вшит как `win.extraResources`, Go-сборка в релизном workflow; `workflow_dispatch` = тест-сборка без публикации. **On-machine функц-тест на `rmz4val` зелёный** (2026-06-22): install→задачи с корректным quoted-action→снос папки→watchdog переустановил (exit 0, source=updates-dir)→uninstall снял задачи.

**Code-signing — решено НЕ делать (2026-06-22):** в репо нет `CSC_LINK`/`CSC_KEY_PASSWORD`; прод-клиент и так `NotSigned`. Сертификат владельцу недоступен (РФ-юрлицо — западные CA / Azure Trusted Signing не обслуживают, как и geo-block Anthropic). Решение владельца — **раздавать watchdog без подписи**; шаг подписи 5b удалён (PR `chore/watchdog-drop-signing`). **Принятый риск:** AV/Defender может пометить unsigned-бинарь, который запускает инсталляторы → watchdog могут карантинить на машине, которую он чинит. Снижено выбором Go/stdlib/минимализм (нет упаковки, низкий эвристический матч), не исключено — мониторить в поле после раската.

**Открыто:**
1. 🐛 **Дискрепанс install-папки (найдено в тесте 5a):** реальная per-user install-папка — `%LOCALAPPDATA%\Programs\@matricarmzelectron-app\` (от `name`), а **не** `…\Programs\MatricaRMZ\` (от `productName`). Следствия: (а) `watchdog/main.go:81` `standardInstallExe = Programs\MatricaRMZ\MatricaRMZ.exe` — мёртвый fallback (primary через handshake.appExePath работает, тест это подтвердил); (б) `installer.nsh` `CleanupMatricaFiles` чистит несуществующий `MatricaRMZ` (до-существующее, electron-builder свой uninstall делает сам). Корень — почему install-dir от `name`, не `productName`. Чинить осознанно (смена install-dir у ВСЕХ клиентов = миграция орфанов) — отдельная задача.
2. **Функц-тест report→критсобытие** с реального клиента не прогнан (с dev-стенда нет outbound 443 на прод — ходит только через SSH; на машинах операторов канал штатный). Серверный `/client/watchdog/report` покрыт Фазой 3-4.
3. ✅ **Релиз выполнен (v2026.623.1642, 2026-06-23):** кнопка «Переустановить» + watchdog выпущены одним релизом; версия-гейт `clientHasWatchdog(lastVersion)` (граница `2026.622.1241`) зажигает кнопку per-client по мере обновления.
4. ⚠️ **Мониторить AV-ложнопозитивы первые дни** (принятый риск unsigned-watchdog): «Критические события» / жалобы операторов. Если карантинят — задокументировать паттерн (какой AV, какой вердикт) для будущего решения о подписи.

---

## 🟢 Owner directives — батч 2026-06-30 (через brain) 🗓 since:2026-06-30

Свежий батч писем brain (`from-brain/2026-06-30-*`, разобран на старте сессии 2026-06-30). Dev-директивы — заводить ниткой (разведка→план→PR под гейтами); zavod-идеи — пища для `/zavod` через владельца.

### Наряды: доработка карточки + изоляция Рамзии — ✅ ВСЁ НА ПРОДЕ (2026-07-01), остались хвосты
План: [`plans/work-order-rework-2026-06.md`](plans/work-order-rework-2026-06.md). **Phases 0-3 + переработка изоляции — на проде** (релизы 843→1437, см. [`COMPLETED.md`](COMPLETED.md)). Быстрые фазы (даты/фильтр типа/кнопка «выполнен»), ролевая изоляция Рамзии, **переход на display-filter модель** (полная база на клиентах + фильтр отображения), write-block, редактируемый гриф, пароль без подсказок — всё отгружено. Открытые хвосты:
- 🟡 **Атомарность проводки сборки — ОТЛОЖЕНО** (`2026-06-30-naryad-card...`, pool #043): идемпотентность уже есть (guard'ы `op.status='closed'`/`doc.status='posted'` в `postAssemblyWorkOrder`) → повторное нажатие **не** списывает вдвое. Открыто: single-tx обёртка release→post→close (узкая дыра post-после-release при сбое → ретрай может снять резерв повторно). Полная обёртка = рефактор shared warehouse-posting (постит из многих мест), marginal-value низкий.
- 🟢 **Идентичность двигателей по номеру** (brain 2026-06-30 `engine-same-number-re-arrival-identity`) — номер не уникален во времени, авто-слияние опасно; дубли вроде `2Ж03АТ0479` — симптом. Возможная нитка «repair-case с суррогатным ID». Пока route: /zavod (дозревание каркаса).

### Жизненный цикл двигателя — 3 письма (route: /zavod, НЕ дев сейчас)
Дизайн заводского процесса/идентичности — разбирает `/zavod`; программировать по дозревании каркаса через владельца:
- **Рекламационные двигатели** (`2026-06-30-reclamation-engines-lifecycle`): ✅ **разобрано в /zavod 2026-07-02, MVP определён владельцем → **отгружен v2026.703.2147** (см. COMPLETED 2026-07-03)**. Полный каркас (наряды с тегом `warranty`, бакет затрат, статистика причин) — следующие фазы, MVP не должен им противоречить. Идея: [`zavod/ideas/011`](zavod/ideas/011-reklamacii-garantii.md).
- **Идентичность при повторном приезде** (`2026-06-30-engine-same-number-re-arrival-identity`): ✅ **разобрано в /zavod 2026-07-02, схема утверждена владельцем → **отгружено v2026.703.2147** (см. COMPLETED 2026-07-03)**. Рефактор PK не нужен (ключ уже суррогатный); полный repair-case — только если повторные заезды станут массовыми. Идея: [`zavod/ideas/012`](zavod/ideas/012-identichnost-dvigatelya-povtorny-zaezd.md).
- **Приёмка без договора** (`2026-06-30-zavod-engine-intake-no-contract-workflow`): весь путь двигателя приёмка-без-договора → первичный ввод (только номер) → акт комплектности → наряд-разбор=дефектовка → прогрессивная дефектовка → ремфонд → наряд-сборка → проводка+списание; «красные без договора» чистить, поздняя привязка договора.

### ADR-0006 mirror-secrets (heads-up, low, ⏳ ждёт KARMAN client-рецепт)
Ратифицирован cross-project ADR-0006: каждый проект обязан зеркалить секреты/токены/ключи в KARMAN-vault. Mirror-API строится в KARMAN, client-рецепт придёт отдельно. **Сейчас действий нет** — быть в курсе + фиксировать новые секрето-несущие узлы (прод `.env`, `/etc/matricarmz/matricarmz.env`, ledger-ключи, `MATRICA_LEDGER_RELEASE_TOKEN`). Письмо `from-brain/2026-06-30-adr0006-mirror-secrets-heads-up`.

---

## 🟢 Owner directives — батч 2026-06-18 (через brain) 🗓 since:2026-06-18

### RBAC операторов (#474) — ✅ ОТГРУЖЕНО + АКТИВИРОВАНО (v2026.621.1815), остались хвосты раската 🗓 since:2026-06-21
Вся нитка реализована (M1–M4, PR #524–#529) и роли активированы 12 операторам на проде. Done-строка — [`COMPLETED.md`](COMPLETED.md). **Открытые хвосты (пассивные):**
1. **Сброс кэша операторам (M2b)** — write-гейт работает сразу; read-фильтр действует на свежих pull'ах, но исторически-скачанные чужие ЛД/зарплаты остаются в локальном кэше до re-pull. Механизм — `client_settings` sync-request (fullPull). **Решение владельца** — делать ли.
2. **deny-лог watch ~2 недели** — `server.authz.denied` в критсобытиях (`…/backend-api/logs/critical-events.ndjson`) → кому не хватило прав → доращивать роль (UI суперадмин / `user_permissions`).
3. **nastya_tech supply-override** — назначена technolog, но делала заявки (аудит); грант `supply_requests.*` если понадобится. Не проставлен.
4. **Операторам перелогиниться** (или авто-обновление до 621.1815) — синхрон UI-капов с серверными гейтами.

### «Конституция» проекта — ✅ РАТИФИЦИРОВАНА владельцем 2026-06-22 (#475), остался хвост прополки доков 🟡
[`docs/CONSTITUTION.md`](CONSTITUTION.md) — 12 статей ратифицированы и в `main` (#475). **Прополка доков:** ✅ `PROJECT_STATE.md` прополот 2026-07-02 (105→~50 строк: «Последние важные изменения» 35→6 пунктов по собственному правилу файла, git-производные UI-мелочи выпилены, SSH-факты слиты в «Текущее состояние», снято противоречие про «текстовый протокол README»). **Открыто (🟡, низкий):** аналогичный проход по `CLAUDE.md` — оценён 2026-07-02: файл уже почти весь процедура, дублей-принципов на 2–3 косметические ссылки → по ст. 12 (анти-спам) правка несущего файла ради косметики отложена; поднимать только вместе с содержательной ревизией CLAUDE.md. Письмо-источник: `from-brain/2026-06-18-project-constitution-and-advisor-stance.md`.

### ЗАВОД-контур (route: /zavod, не dev) — отложено в завод-режим
`from-brain/2026-06-17-zavod-productivity-and-scrap-accounting.md` (производительность + учёт утиля без бюрократии) — пища для `/zavod`, не программировать сейчас.

### Мета-идеи: модульность ЭРП + визуальный конструктор интерфейсов (idea, low)
`from-brain/2026-06-17-modular-plugins-and-visual-ui-builder.md` — расколоть ЭРП на ядро + модули; визуальный конструктор UI → нейрокодоген. Архитектура за нами; не ломать рабочую структуру. Долгосрочная пища, без срока.

---

## 🟡 Прогноз сборки — хвосты аудита точности 2026-06-22 🗓 since:2026-06-22

Аудит модуля прогноза сборки ([`warehouseForecastService.ts`](../backend-api/src/services/warehouseForecastService.ts) + [`assemblyForecast.ts`](../shared/src/domain/assemblyForecast.ts)). 3 безопасных фикса отгружены (двойной счёт техн.локаций под фильтром, `carter`→роль, диагностика «остаток без склада»). Открыто:

1. **(A) Канал ремнарядов «выдан в работу» — ✅ НА ПРОДЕ (v2026.622.1241, #541), остался операционный раскат.** Реализация смержена и раскатана (флаг `repairIssued` + кнопка «Выдать в работу / Отозвать»; прогноз учитывает приходом только выданные ремнаряды). **Открытый хвост (операционный, не код):** прогноз по ремонту **пуст**, пока мастера не пройдутся по реально-ремонтируемым нарядам и не нажмут «Выдать в работу» — передать мастерам. (CDP UI-smoke кнопки на живом клиенте не делался — опц.)
2. **(опц.) Плановый приход «ожидается сегодня» (dayOffset=0)** считается доступным в день 0 — оптимистичнее консервативного `+1` у канала ремонта. Стандартное MRP-допущение; сделать консервативным только по запросу.

---

## ⏳ Ждёт внешнего окна / события

### Ledger release-token протухает ~2026-08-01 (ротирован 2026-07-02) 🗓 since:2026-06-24

Прод `.env` `MATRICA_LEDGER_RELEASE_TOKEN` — admin-JWT с TTL (max 720ч = 30 дней, повторяющаяся ротация). **Обновлён 2026-07-02** (exp ≈ 2026-08-01; бэкап `.env.bak-20260702` рядом). `release:ledger-publish` (шаг 8 релиза) **упадёт 401 «invalid token»** после протухания. **Триггер:** первый релиз после ~2026-08-01. **Лечение (2 мин по ssh, пока старый токен ещё жив):** сам токен из `.env` — валидный admin-JWT → им же минтится новый: `curl -fsk -X POST https://127.0.0.1/auth/release-token -H "Authorization: Bearer $OLD" -d '{"ttlHours":720}'` → заменить строку в `backend-api/.env` (с бэкапом; рестарт сервисов не нужен — скрипт читает `.env` при запуске). Если старый уже протух — web-admin → Admin → Release token. На доставку обновления клиентам ledger-publish НЕ влияет (latest.json/infoHash генерит `updateTorrentService` при пересканировании) — только подписанная audit-запись релиза в реестре. Память/инцидент 2026-06-24 (релиз 1021).

### Live-аудит раскладки клиента (Install/Update Блок 1) 🗓 since:2026-05-30

На машине **оператора** задокументировать `%LOCALAPPDATA%\Programs\MatricaRMZ` / `%APPDATA%\MatricaRMZ` / `pending-update`, мусор (старые .exe, app-X.Y.Z дубли), ротацию `matricarmz.log`, очистку на uninstall. **Нельзя на dev-машине** (клиент не установлен) — нужен доступ к машине оператора. ADR-0001 install-update.

### Электрон 42 — вернуться, когда better-sqlite3 адаптирует `External::New`

Сейчас на Electron 41 (42 не собирает better-sqlite3 — V8 `External::New` сменил сигнатуру). Триггер: новый релиз better-sqlite3 под Electron 42.

### Финальная визуальная проверка иконки клиента

exe/ярлык/окно/таскбар на реально обновлённом Windows-клиенте — хвост с прошлых релизов.

---

## 🟡 Техдолг

### Эпик «не сохранять пустые карточки + черновики/восстановление» — открытые фазы 🗓 since:2026-06-29

План [`plans/drafts-no-empty-cards-recovery-2026-06.md`](plans/drafts-no-empty-cards-recovery-2026-06.md). **Отгружено:** v2026.629.1711 — Phase 1 (чистка пустых), 2a/2b (двигатели/сотрудники), 3a (`card_drafts`); **v2026.630.1141** — 3b (автосейв+восстановление+close-guard), 3c (раздел «Черновики» + «Сохранить как черновик»), Phase 2 deferred-create для **нарядов/заявок/контрагентов/марок/договоров** (+ ядро `admin:entities` fallbackTypeId). **Открыто (🟡):**
- **Товары/услуги** — последний пользовательский тип, но через **warehouse-номенклатуру** (`NomenclatureDirectoryPage`→`createNomenclatureLineFromPreset`: source-сущность + `nomenclatureUpsert` + template/group/unit), НЕ через `admin:entities`. Бэкенд авто-зеркало entity→номенклатура есть только для parts. Чтобы отложить корректно, первый save карточки должен дотянуть весь пресет → связать `SimpleMasterdataDetailsPage` с номенклатурной машинерией (трогает parts/nomenclature-зеркало). Отдельный спланированный заход.
- **`AdminPage:1077` raw-сетка** — master-detail грид (новая запись не видна в списке до 1-го сохранения → awkward); админ-внутреннее, низкий риск, есть фильтр «Без названия» + Phase 1 cleanup. Владелец согласился **пропустить** deferral (предотвращение — для пользовательских details-карточек, для грида достаточно чистки).
- **Phase 3d** — расширить автосейв/черновики/3-кнопочный close-guard на прочие details-страницы.
- **🟡 Глубокая де-дупликация `directory_parts ↔ erp_nomenclature`** — устранить DET-зеркала в данных (одна истина по id/коду). Симптом findability снят #656, дубли в данных остаются. Связано с #648/#4 («техдолг UUID-only»).

### Owner batch 2026-06-19 — хвосты 🗓 since:2026-06-19

Батч **5/6 отгружен** в v2026.619.1703; **E (глобальный поиск Ctrl+K) и C-хвост («Сохранить и выйти» на 3 карточках) отгружены в v2026.620.1715** (см. [`COMPLETED.md`](COMPLETED.md); планы → `plans/_archive/`). Открытые хвосты:

- **F полная унификация (🟢):** подразделения/цеха/склады. ✅ Цех-сторона расчищена (2026-06-22, SSOT): `employee.workshop_id`→`directory_workshops`, 4 дубль-департамента «Цех № N» удалены, дубли цех/подразделение устранены (см. COMPLETED). Остаётся (XL, только при явной потребности): полное слияние оставшихся оргмоделей (офисные подразделения + склады) в одну таблицу оргединиц — миграция + sync + ломка FK.

### Owner batch-2 (2026-06-17) — хвосты 🗓 since:2026-06-17

Батч **полностью отгружен**: #1/#2/#5 (v2026.617.1614), #3 дельта-foreground + #6 Phase 3.8 scope A (v2026.618.117). Открытые хвосты:

- **Детали-двойники (коллизия `code`) — ✅ ЗАКРЫТО (подтверждено прод-проверкой 2026-06-20).** Все целевые пары уже разрешены на проде (слиты 2026-06-19, по одной активной строке на код, проигравший soft-deleted): `327-00-62` (survivor «Насос топливный»), `3309-25-2` («Генератор с муфтой привода»), `303-07-22` («Гильза стальная»), картеры `3301-15-30` («Картер нижний»). По `406-12-44` «Блок правый» был слит в «Крышка люка» вопреки заметке «исправить код, не сливать» — **владелец решил оставить как есть** (2026-06-20). Активных дублей среди этих кодов не осталось. (Прод-мутаций не делалось — только read-only проверка.)

### deadcode-сканер #036 (brain) — ✅ заведён; файловые кандидаты закрыты, exports — месячная дельта

`pnpm deadcode` (knip) настроен (`knip.json`, 2026-06-16): entry'и electron renderer (pages грузятся `import.meta.glob` → нужен явный entry), backend-скрипты/тесты/спавн-фикстуры, `.claude/` игнор. **depcheck отдельно не ставил** — knip покрывает unused/unlisted deps (одна тулза, меньше шума). Report-only, без авто-удаления (по директиве).

**Первый прогон — триаж по #028 ✅ ЗАКРЫТ (файлы/deps все разобраны, knip unused-files 0):**
- ✅ **Удалены:** `releaseInfo.ts` (дубликат inline-чтения в `updateService`) + dep `@matricarmz/ledger`; **sync-трио** `sync/{index,pullApplicator,pushCollector}.ts` (заброшенная модуляризация, живой путь = `syncService`/`syncManager`, без каскада); **`workOrderCardSignaturesHtml.ts`** — git-история показала, что это **не спящая фича, а вытесненный orphan**: подписи осознанно перенесены с карточки наряда в payroll-отчёт в v1.18.3 (`23f1a18a`), на карточке не нужны (решение владельца 2026-06-16 — payroll-подписей достаточно). Все typecheck зелёные.
- **116 unused exports + 42 types (прогон 2026-07-04; был 101+34 на 2026-06-16) — фоновая месячная дельта:** unused files по-прежнему 0. Дельта +15/+8 триажирована — реального мёртвого кода нет: (а) лишний `export` при module-internal использовании (`createSectionIpcGate`, электроновские `findEngineDuplicateByNumber`/`engineHasDuplicateBypassFlag`); (б) экспорт ради юнит-тестов (`DEDUPE_EXEMPT_FLAG_CODES`); (в) типы-части публичных сигнатур (`DirectoryPartDedupeUsage`, `WatchdogHandshake`, blockmap/sync-schema типы). Категория AI-сервисов (`callClaude`/`AI_PROFILE`/`collectLogAnalysisContext`…) — «живое, но не подключённое» из-за geo-block, **НЕ трогать**. Следующий прогон ~2026-08-04.

**Фон (по директиве):** ежемесячный прогон (только дельта новых) + квартальный стратегический self-review (первое окно Q3 2026, авг-сен → письмо в `to-brain`). Письмо: `from-brain/2026-06-10-deadcode-gate-and-self-review.md`.

### Компакция/ретенция ledger-истории (changelog) 🗓 since:2026-06-12

changelog-walk отдаёт мёртвые версии строк (доля растёт с историей). **Триггер:** cold-snapshot отгружен (✅) И wall первой страницы changelog-walk у живых клиентов начнёт расти (видно по `pull attempt=ok durMs`). Предусловие (атомарная запись + advisory-lock LedgerStore) — снято ✅. Гнать in-process job'ом на primary под `withLock`. Файл: `backend-api/src/services/ledger.ts`.

### Серверный pg_trgm typeahead (поиск #035 Ф3) 🗓 since:2026-06-13

Отложено как преждевременное (замер 2026-06-13: `erp_nomenclature` 637 строк, Seq Scan 6.6мс — pg_trgm не выгадает). **Триггер:** каталог вырастет до ~10–20k+ строк ИЛИ серверный поиск по большой таблице (движения/документы). Тогда GIN + restructure предиката (drop `COALESCE`, функц-индекс, EXPLAIN BitmapOr). До тех пор клиентский tiered-matcher (Ф0–Ф2 ✅) покрывает. Главная поверхность: `nomenclatureSearchCondition` (`warehouseService.ts:1291`).
- **(опц., малое)** `matchesQueryInRecord` пер-строчно зовёт `prepareLookupOptions` — для очень больших списков переключить на memoized `prepareRecordSearch`+`filterPreparedRecords` (готово в `ui/utils/search.ts`). Сейчас приемлемо.

### UUID-only склад — задепрайоритизирован (low ROI) 🗓 since:2026-05-27

Phase 2.x (FK `warehouse_location_id`) ✅ на проде (v1.31.0). Остаток — полный UUID-only:
- **shared-константы** (`WAREHOUSE_LOCATION_*`, `workshopWarehouseId`) — **не мёртвый код, а живая протокольная конвенция** (enum-значения `payloadJson` `repair_fund`/`scrap`, code→uuid резолв, UI-классификация). Снос = миграция протокола `payloadJson` на UUID (breaking-change + миграция исторических данных). Низкий ROI, система работает. Безопасная зачистка провабельно-мёртвых экспортов уже сделана.
- **shared types optional** (`StockBalance/StockMovement/WarehouseDocumentListItem.warehouseId?`) — снимается только вместе с полной UUID-миграцией. Coupled.
- *Поднимать только если завезём осознанную протокол-миграцию.* Файлы: `shared/src/domain/warehouseLocations.ts`, `warehouse.ts`.

### Мелочи (small, по случаю)

- **Live-verify печати наряда — `***NNN`+контрагент остался непроверенным вживую** 🗓 since:2026-06-24 — основная часть печати **проверена вживую** (v2026.625.726, CDP e2e PASS: панель печати, степперы, гриф «Утверждаю», подразделение/цех в подписях, колонка «Артикул», два блока подписей, нативный A4). Грабля «печать по CDP флакует» снята: **живое A4-превью — это `<iframe srcdoc>`, его srcDoc читается через CDP напрямую** (в обход `window.print()`; см. brain-письмо 2026-06-25). **Открыто (низкий):** `***NNN`+контрагент в шапке вживую НЕ отрисовывались — ни один наряд dev-БД не линкует двигатель в строках работ (suffix берётся по первому двигателю наряда); покрыто юнит-тестами `contractNumberSuffix`. Глянуть на наряде с привязанным двигателем.
- **Печать наряда — визуальная сверка одного листа + рамок таблиц после раската 1722** 🗓 since:2026-06-25 — в v2026.625.1722 (#595) подписи отвязаны от низа листа (тянутся кверху + отступ 24px + `@page A4`) → должны помещаться на 1 лист; таблицы «виды работ / наименование изделия» и «бригада» получили чёткие рамки как у реквизитов. Покрыто typecheck/lint; **вживую renderer не гонялся** (CDP-verify не делал — релиз был приоритетнее). Глянуть на клиенте после обновления до 1722; при нужде — патч отступа/толщины линий. 🟢
- **Шаблоны/умолчания печати наряда — пока per-machine (localStorage)** 🗓 since:2026-06-25 — `woPrintTemplates.ts` хранит шаблоны и умолчания для вида наряда в localStorage клиента → не следуют за оператором между машинами, у каждого компа свои. Для пресетов вёрстки разумный v1. **Если владелец захочет единые на весь завод** — апгрейд на бэкенд-стор (таблица/EAV + sync + IPC). Решение владельца, отдельная нитка. 🟢
- **`forecastVariantKey.dayOffset` относительный** 🗓 since:2026-05-27 — на следующий день старые Assembly-наряды с вчерашним dayOffset не матчат прогноз (кнопка не блокируется). По логике OK; UX-улучшение — блок «Открытые наряды из прежних прогнозов». `warehouseForecastService.ts`.
- **`reserveAssemblyDraftReservation` только `assembly_consumption`** 🗓 since:2026-05-27 — если понадобится резерв для `stock_issue`/`stock_writeoff`/repair-fund — обобщить параметром `docType`. `warehouseService.ts:~2656`.
- **Integration-test закрывающей ветки Workshop-наряда** 🗓 since:2026-05-26 — нужен mock-стек `warehouseService`+`db`+`enginePhaseService`. Pure-prune покрыта 6 shared-тестами; реальный flow — через verifier-electron. Low-prio (логика стабильна ≥ v1.25.0).
- **Орфан-гигиена (standing, частично снято):** с v2026.617.1614 `createDirectoryPart` **сам создаёт зеркало `erp_nomenclature`** (#458) → новые детали больше не «без складской карточки». Backfill `warehouse:backfill-orphan-part-nomenclature` — теперь только для легаси-сирот и при коллизии `code` (см. 2 детали-двойника в §хвосты батча-2).

---

## 🟢 Планы / идеи

### #6 Phase 3.8 — вариант B (offline-specs) — отложено 🗓 since:2026-06-18

Scope A ✅ отгружен (v2026.618.117, см. COMPLETED). **Вариант B (НЕ делаем без явной потребности):** завести `directory_parts` в синк-реестр + клиент читает спеки из синканного SQLite (offline-specs) + снести code/name-зеркало целиком. Это разворот рабочего live-HTTP (Phase 3.7 WS1) + расширение sync-протокола (риск массовой порчи). Поднимать только если понадобится offline-доступ к спекам. План-фундамент: [`plans/_archive/parts-nomenclature-phase3-8.md`](plans/_archive/parts-nomenclature-phase3-8.md).

### UI-эргономика проход-2 (brain) 🗓 since:2026-06-13 — ✅ объективные фиксы НА ПРОДЕ, остался только V2/V3

> **Статус-апдейт 2026-06-22:** объективные фиксы #1–#6 и V1/V4/V5 **в проде** — их коммиты в теге `v2026.616.2017` и далее (внутри текущего `v2026.621.1815`); заметка «не релизились» была устаревшей. **Открыто только V2/V3** (мелкая обрезка раскладки, низкий приоритет — см. «Остаётся» внизу).

Проход-1 ✅ (на проде). **Проход-2 аудит выполнен 2026-06-16** (код/DOM-аудит поверхностей: акты, BOM, наряд, договоры, снабжение, поиск, журналы — клиент не дёргали, владелец смотрел матч). **Объективные #1–#6 пофикшены** (на проде): #1 журнал изменений — убрана always-on `root=/id=` UUID-строка; #2 печать актов дефектовки/претензии — договор стал условным + пустые даты → прочерк-бланк (`dateOrFillIn`); #3 BOM схема-редактор — `typeId`→«ID типа», `active/draft/archived`→рус., dev-плейсхолдеры рус.; #4 поиск hover-title `id`→`label`; #5 AuditPage — снят dev-title `action=…|entityId=…`; #6 UUID-fallback'ы (Contracts/WorkOrder) → `(без названия)`/`(без номера)`/«строка N». typecheck+lint зелёные.
**Визуальная сверка на живом клиенте ✅ выполнена 2026-06-16** (windows-mcp по запущенному прод-клиенту v2312; CDP-screenshot по-прежнему флакует — путь computer-use подтверждён). Поймала то, что код-аудит пропустил:
- **V1 (🔴, пофикшено):** заголовки вариантов BOM показывали сырой ключ «Вариант: `__kit_52d1d7cd3b8b`» оператору (3 сайта: BOM table/tree-view + селектор «Вариант сборки» наряда). Фикс — renderer-util `formatAssemblyVariantLabel` (`__kit_`-ключ → «Вариант N» по порядку; человеческое имя варианта оставляется). typecheck+lint зелёные.
- **V4 (🟡, пофикшено):** в подсказке кнопки «Заявка в снабжение» (карточка двигателя → панель деталей) оператору утекал сырой код статуса «…после одобрения директором **(director_approved)**» → убран. Sweep по остальным операторским текстам — других raw-code-in-parens нет.
- **V2/V3 (🟡 мелочь, записаны):** обрезанный заголовок «Дата поступлен…» на Заявках; крайняя «Удалить»→«Уд» в таблице работ наряда (переполнение). pass-1 #5-класс — точечный min-width/раскладка, низкий приоритет.
- **Подтверждено вживую:** pass-1 #1 (контрагент UUID в карточке двигателя) — уже **исправлен** (показывает имя); pass-1 #4 (пустое поле у булевых) — **намеренное** (поле даты статуса); поля комиссии актов «ФИО/должность/подпись» — намеренные (3-я колонка = подпись на бумаге).

**✅ Визуальная сверка 3 поверхностей завершена 2026-06-16** (windows-mcp по живому прод-клиенту v2312):
1. **Карточка договора** (`ContractDetailsPage`) — чисто: статусы человеческие («Принято заказчиком»/«Начат ремонт»), контрагент/марки — имена, заголовки русские. Минор (backlog, не pass-2): обрезка длинного номера контракта в поле ввода (= #3-аналог раскладки); пустая секция «Детали» → «Нет строк» (= owner-backlog #6 «скрывать пустые разделы»). UUID контрагента/марок сидит только в accessibility-имени combobox'а, оператору не отрисован.
2. **Журналы:**
   - **Изменения** (`ChangesPage`) — 🔴 **V5 (пофикшено):** фильтр «Применены» показывал оператору сырьё — `c.note` = технический action-key `part.update_attribute:status_rejected` (дубль человеческого `fieldLabel`) + колонки «Что было»/«Как стало» = **сырой JSON-дамп** EAV-строки (UUID `entity_id`/`attribute_def_id`, epoch `created_at`, `sync_status`). #429(#1) убрал только monospace `root=/id=`-строку, эти два остались. Фикс: `isHumanNote` прячет технические note-коды; `humanizeLines` рендерит «Что было»/«Как стало» по-человечески (keyRu + пропуск технических/FK-UUID ключей, парс `_json`), убран `<pre>`-дамп. typecheck+lint зелёные.
   - **Журнал/Аудит** (`SuperadminAuditPage`) — чисто: Аккаунт(login)+ФИО, действия/разделы человеческие, без сырого JSON (в файле нет `beforeJson/afterJson`-detail).
3. **Печатные акты** (`engineInventoryPrintHtml` — комплектности/дефектовки/претензии) — чисто (код-аудит билдера): заголовки/колонки/подписи человеческий русский, данные = артикул/набитый номер/кол-ва (без UUID), `branchLabel` рус., #2-фикс (условный «Номер договора» + `dateOrFillIn` прочерк-бланк) на месте. Акт заказчику корректен.

**V2/V3 — ✅ код закрыт (2026-06-22):** V2 «Дата поступления» уже была в проде (v2026.616.2017, `global.css:503` — заголовок переносится, не клипается); V3a «Удалить»→«Уд» (nowrap на ячейке) + V3b обёртка табов «Изменени я» (`overflowWrap:'break-word'`→`'normal'`) — PR #535. **Хвост (тонкий):** пиксельная сверка V3 на живом клиенте при узкой ширине + доезд операторам со следующим релизом renderer'а (CDP-скриншот флакует, проблему ловили windows-mcp). Весь объективный бэклог проход-2 (#1–6, V1/V4/V5) — в проде. (#7 ✅ #452.) Метод/проход-1: `mailbox/to-brain/2026-06-13-ui-ergonomics-audit-pass1.md`.

### 4 фичи-идеи бэклога (brain, suggest) 🗓 since:2026-06-04

Одобренные владельцем идеи (`from-brain/2026-06-04-feature-backlog-traceability-costing-qr.md`): **паспорт ремонта двигателя**, **дефект+фото → авто-BOM**, **калькулятор себестоимости**, **QR-коды на детали/склад**. Каждая — отдельная нитка, начинать с разведки+плана.

### Дефектовка → заявка: MVP-3 offline-очередь фото 🗓 since:2026-06-08 (ревизия ~через полгода)

MVP-1/2 ✅ (онлайн-кейс). MVP-3 = персистентная очередь в client-SQLite для offline-фото. **Триггер:** дефектовку с фото начнут делать **вне сети** (планшет в цеху/поле). Перед стройкой уточнить устройство/сценарий у владельца (probe-before-build, brain #023). План: [`plans/_archive/defect-to-supply-request-2026-06.md`](plans/_archive/defect-to-supply-request-2026-06.md) §MVP-3.
