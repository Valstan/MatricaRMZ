# Журнал эффектов программы

> Мост dev-потока в завод-контур: **не задания и не код**, а смысл — для чего сделано, что улучшило, в каком модуле, как это может сказаться на результативности завода. По 1-3 строки на отгруженное изменение, новое сверху. Заполняется в `/close_session` (§7.5), когда в сессии отгружена функциональность; рутинные фиксы/рефакторинги сюда не попадают.
>
> Консультант `/zavod` читает этот файл, чтобы знать, что программа уже умеет и что улучшалось в последнее время.

| Дата | Версия | Модуль | Зачем сделано → что улучшило |
|---|---|---|---|
| 2026-08-12 | v2026.812.1551 | Отчёты · Двигатели | **Бумага по заказчикам стала годовым отчётом, а не срезом «на сегодня».** Отчёт «Движение двигателей по заказчикам» разбит на блоки по годам прихода: внутри года — заказчики, договоры и марки, а итоги считаются на каждом уровне (договор, заказчик, марка за год, год целиком, все годы) плюс свод по маркам за год и за все годы. Начальнику производства и бухгалтерии больше не нужно складывать годы вручную, чтобы ответить «сколько за год пришло и сколько вернули». Номер договора печатается коротко — как в наряде, три последние цифры со звёздочкой, поэтому цех узнаёт договор в обеих бумагах одинаково. |
| 2026-08-12 | v2026.812.1551 | Отчёты · Печать | **Печать подстраивается под бумагу конторы, а не наоборот.** Кегль таблицы, шапки и каждой отдельной колонки задаётся в фильтрах отчёта, ненужные колонки убираются галочкой, а подобранная раскладка сохраняется в шаблоне фильтров — один раз настроили и печатаете так всегда. Раньше отчёт печатался мелким фиксированным шрифтом и растягивался из-за длинных номеров договоров. |
| 2026-08-12 | v2026.812.1551 | Цеховой планшет | **Обновление планшета перестало стирать данные.** У планшетной программы появился постоянный ключ подписи: следующие версии встают поверх прежней, без удаления программы, повторного входа и многочасовой первой загрузки. Эта установка — последняя «через снос»: данные на планшете стёрты, нужен вход и первая полная загрузка. |
| 2026-08-11 | v2026.811.1728 | Вложения и фотографии · Карточки · Цеховой планшет | **Фотографии и документы перестали быть «прикрепил и забыл» — с ними теперь можно работать.** Список файлов в карточке разворачивался и не сворачивался обратно (кнопки просто не было) — теперь одна кнопка на том же месте. У файлов появились галочки: отметил нужные — и распечатал, скопировал в папку или на флешку, подготовил к отправке сразу пачкой. При печати программа спрашивает, что печатать: список названий или **сами файлы** (фотографии — по одной на лист); раньше можно было получить только список названий, что для «покажите, что пришло в брак» бесполезно. Кнопка «Папка скачивания» ничего не открывала, а лишь меняла настройку — теперь «Папка файлов» открывает папку с файлами именно этой карточки, без чужих. Файлы можно перетащить мышкой из проводника прямо в карточку или вставить из буфера обмена — программа сама разберёт, что скопировано: файл, картинку или текст. В фотогалерее выбор больше не сбрасывается при листании: отмечаешь нужные снимки по ходу просмотра и в конце делаешь с ними одно действие. **Длинные карточки разложены по вкладкам** — наряд, контракт, сотрудник, заявка в снабжение: реквизиты, содержимое, подписи и файлы больше не идут одним свитком, до нужного места не нужно прокручивать всю карточку. **На планшете появились «Фото и документы»** — раньше вкладки не было вовсе: можно приложить файл и **сфотографировать деталь прямо на месте**, снимок сразу прикрепится к карточке. Кнопки меню на планшете теперь закрепляются касанием скрепки (раньше она была невидима — показывалась только при наведении мышью, которого на планшете нет). **Для завода:** дефектовка и рекламация перестают жить в телефоне мастера и мессенджере — фото делается в цеху и сразу ложится к своему двигателю, а бухгалтерия печатает и передаёт подтверждающие снимки, не выуживая их по папкам. |
| 2026-08-11 | v2026.811.1322 | Двигатели · Наряды · Контракты | **Двигатель больше не «уезжает» заказчику с незакрытым нарядом, а внутри контракта он переставляется одним выбором.** Бывало: наряд на сборку не закрыт, а в карточке двигателя уже стоит «Отправлен заказчику» — наряд висит просроченным, отчёты врут, разбор «кто виноват» через неделю. Теперь программа при этой галочке сама сверяется с нарядами: наряд открыт — предложит закрыть его сегодняшним числом, отказ — галочка не встанет (и если нет права закрывать наряды, честно скажет, к кому идти). Второе: перенести двигатель из первичного контракта в ДС (или между ДС) теперь можно прямо в карточке контракта — выбором в строке двигателя; карточка двигателя обновляется сама, а платежи остаются на прежнем месте, как и положено деньгам. **Для завода:** наряды и отгрузка перестали расходиться, а типовая операция бухгалтера «двигатель переехал в ДС-2» перестала быть многошаговой возней с двумя карточками. |
| 2026-08-11 | v2026.811.1322 | Интерфейс · ИИваныч | **Меню перестало занимать весь экран, а ИИваныч стал обычной вкладкой.** Меню теперь выпадает узкой панелью поверх работы и сворачивается после выбора; развёрнут всегда один раздел кнопок, закреплённые — всегда сверху, заголовки разделов подкрашены и не путаются с кнопками. Чат ИИваныча открывается вкладкой, как любой раздел (кнопка в меню открывает, крестик на вкладке закрывает) — вместе с релизом до пользователей доехал и «мгновенный» ИИваныч: ответ за десятки секунд с видимым «думает», без расписания. Вернулся и еженедельный отчёт по использованию программы — теперь по-человечески: фамилии вместо логинов, разделы как в меню. **Для завода:** меньше кликов на каждое переключение, помощник по данным — под рукой в один клик, а руководитель по понедельникам видит, кто и как реально работает в программе. |
| 2026-08-11 | v2026.811.1322 | Цеховой планшет · Надёжность установки | **Планшет доезжает до конца первой загрузки, а программа на компьютерах научилась самовосстанавливаться.** На планшете первая загрузка данных обрывалась на середине из-за нехватки памяти при приёме больших пачек — двигатели появлялись, наряды никогда; пачки теперь режутся по размеру, загрузка доходит до конца. На рабочих местах программа и её сторож теперь чинят друг друга: антивирус или сбойное обновление снесли одного — второй восстановит (сторож переустановит программу с сервера, программа вернёт сторожа и его расписание). На рабочем столе появился ярлык «Восстановить Матрицу РМЗ» — принудительный ремонт в один клик, без звонка и ожидания. **Для завода:** «у бухгалтера пропала программа» перестаёт быть простоем до прихода админа — машина чинит себя сама, а владелец видит инцидент в «Критических событиях». |
| 2026-08-10 | на проде (backend), клиентская часть — следующим релизом | ИИваныч (помощник по данным) | **ИИваныч начал отвечать сразу, а не по расписанию.** Раньше вопрос попадал в очередь и ждал: помощник включался раз в час и только по будням с 8:00 до 17:00, так что заданный в 17:30 вопрос лежал до утра. Пользоваться этим можно было разве что заранее — «спрошу и посмотрю завтра», — поэтому почти не пользовались. Теперь ответ приходит за десятки секунд, в любое время и в выходные, и всё это время видно, что помощник **работает над вопросом**: подпись «ИИваныч думает и собирает данные» и счётчик времени вместо тишины, из-за которой казалось, будто вопрос ушёл в никуда. Длинные ответы он по-прежнему прикладывает файлом Excel. Данные берёт из базы **правами того, кто спросил** — чужого не покажет; если данных или прав не хватает, вопрос уходит администратору, а не превращается в выдуманный ответ. **Для завода:** вопрос к базе на обычном языке («сколько таких деталей на складе», «что с этим двигателем») перестал требовать ожидания и планирования — им можно пользоваться прямо в разговоре, у станка и в конце смены, когда ответ нужен сейчас. |
| 2026-08-09 | (следующий релиз) | Цеховой планшет | **На планшете вернулся режим «данные на весь экран» — он не работал с 07.08.** Задумано так: мастер листает список нарядов или деталей, и вспомогательные полосы (лента вкладок, панель поиска, заголовок карточки) уезжают с экрана, отдавая место данным, а возвращаются жестом от края, нажатием на язычок или сами — как только мастер возвращается к началу списка. Два дня ничего этого не происходило: режим выключал сам себя через две секунды после запуска, молча, потому что искал на экране элемент, снесённый при переделке верхней полосы. Вторая поломка того же рода: прокрутка **любого списка** не засчитывалась как «работа с данными», то есть панели не уезжали бы и с исправленным первым. Заодно убран мёртвый язычок ☰ слева — нажатие на него затемнило бы весь экран поверх данных, и снять затемнение можно было бы только вторым нажатием; разделы давно открываются вкладкой МЕНЮ. **Для завода:** на планшете в цеху экран маленький и руки заняты — вернулась примерно четверть высоты экрана под сами данные, а не под служебные полосы. Проверено на стенде; на живом планшете остаётся проверить пальцем в перчатке и краевые жесты. |
| 2026-08-09 | v2026.809.1623 | Интерфейс: рабочий стол оператора | **Программа стала помнить рабочее место.** Раньше уход на другую вкладку сбрасывал работу в разделе: набранный поиск, выставленные фильтры и место в длинном списке пропадали, и всё приходилось выставлять заново — при работе «наряд ↔ склад ↔ двигатель» это повторялось десятки раз за смену. Теперь разделы остаются такими, какими вы их оставили. Программа запоминает **всю** ленту вкладок, а не только карточки: после закрытия и запуска вернутся и разделы, и открытые карточки, и та вкладка, на которой шла работа. Отдельно исправлены два случая, когда лента пропадала совсем: выход и повторный вход под своим логином (вкладки исчезали безвозвратно) и смена сотрудника за общим компьютером (новому показывались чужие вкладки). Вернулась возможность открыть **две карточки рядом** для сравнения, а длинные карточки снова прокручиваются до конца — раньше низ мог обрезаться. **Для завода:** меньше потерянного времени на восстановление контекста после каждого переключения и посменной пересадки за общий компьютер; попутно программа стала реже дёргать сервер фоном, что заметно на слабой заводской связи. |
| 2026-08-08 | v2026.809.1623 | Интерфейс: вкладки | **Программа перестала «белеть» и глохнуть на кнопках.** Открытие «Настроек» из меню аккаунта гасило весь экран в белый лист — приходилось перезапускать программу; теперь открывается штатно. Кнопки «Настройки» и «Профиль» иногда переставали работать совсем (нажатие без всякой реакции) — исправлено. Когда открытых карточек становится слишком много, программа прямо говорит «закройте ненужную вкладку» вместо того, чтобы молча выбросить самую старую: раньше карточка, с которой работали утром, могла исчезнуть из ленты сама. Возврат из МЕНЮ и закрытие вкладок больше не приводят к пустому экрану. **Для завода:** меньше поводов перезапускать программу посреди работы и меньше случаев «нажал — ничего не произошло», из-за которых операторы решают, что программа зависла. |
| 2026-08-08 | v2026.808.1830 | Контракты и авансы (бухгалтерия) | **Учёт по контракту перестал врать в самом чувствительном месте — деньгах.** Если в договоре несколько строк с марками (например, «10 двигателей по одной цене и 5 по другой»), программа заводила места под двигатели только по последней строке, а цену брала из первой — итог по контракту расходился с бумагой, а часть двигателей оставалась «без места». Теперь количества складываются по всем строкам, цена берётся из своей строки, и авансовый платёж разносится на **отмеченные галочками** двигатели в любом разделе договора, включая дополнительные соглашения, — раньше деньги ложились на первые по списку, независимо от того, за что они пришли. Готов и разовый прогон по всем ранее заведённым договорам: доведёт места под двигатели до плана и рассадит уже заведённые двигатели по своим маркам, а расхождения (лишний двигатель, чужая марка) покажет списком бухгалтеру — решение по ним остаётся за человеком. |
| 2026-08-08 | v2026.808.1723 → .1830 | Интерфейс и цеховой планшет | **Работа с программой перестала «терять место».** Списки снова прокручиваются, переход в МЕНЮ больше не закрывает открытую карточку (наряд остаётся вкладкой рядом — можно вернуться одним нажатием), и все разделы без исключения открываются вкладками, а не молча остаются на месте. Сверху справа теперь видно, под каким сотрудником открыта программа, — это снимает частую путаницу «кто именно завёл документ», когда за одним компьютером работают посменно. **На цеховом планшете** списки нарядов и двигателей стали загружаться сразу после запуска (раньше данные могли не появляться до пяти минут), в меню добавлены «Документы склада», а свёрнутые разделы меню запоминаются между запусками — мастеру не нужно каждый раз разворачивать нужное заново. |
| 2026-08-07 | v2026.807.1551 → .1711 | Интерфейс: всё во вкладках | **Программа переведена на вкладки, как браузер: каждый раздел, карточка, чат и ИИваныч открываются в своей вкладке, дубли не создаются, а МЕНЮ — всегда на месте слева и не закрывается.** Раньше списки и карточки «сменяли» друг друга в одной области, и чтобы вернуться к предыдущему списку, приходилось идти заново через меню; теперь открытые разделы висят вкладками и переключаются одним кликом. Верхняя тёмная полоса с названием раздела убрана — вместо неё логотип у вкладки МЕНЮ и кнопка синхронизации справа: во время обмена с сервером она крутится, показывает проценты и по завершении пишет «Обновилось N документов» — оператор видит, что данные доехали. Секции меню по умолчанию свёрнуты, закреплённые кнопки всегда сверху — меню не занимает полэкрана. **Для завода:** меньше кликов между списком и карточкой, видимый прогресс синхронизации вместо «зависло?», и ни один раздел не теряется за другими окнами. |
| 2026-08-07 | v2026.807.1243 → .1433 | Меню программы / синхронизация | **Все кнопки собраны в одно меню слева, рассортированы по темам, разделы сворачиваются, любимые кнопки закрепляются наверху — их копия, оригинал остаётся на месте.** Темы, палитра, поиск, синхронизация, чат и ИИваныч больше не разбросаны по верхней панели и углам экрана — оператор не ищет кнопку по всей программе, а открывает меню. Планшет сам держит связь с сервером (наряды подгружаются без ожидания). **Если синхронизация не приняла часть изменений** (например, операция ссылается на удалённый двигатель) — программа показывает красное предупреждение вместо молчаливого «зависло». Раньше одна-единственная дубль-строка номенклатуры могла остановить синхронизацию целиком — теперь такие строки отбрасываются по одной, и данные остальных доезжают. **Для завода:** расхождение версий и «у меня не видно» перестают быть тихими — видно сразу и адресно. |
| 2026-08-04 | v2026.804.1441 | Обновление программы на рабочих местах | **Обновления снова доходят до компьютеров сами.** Программа теряла уже скачанное: если работник закрывал её, пока обновление докачивается, при следующем запуске загрузка начиналась с нуля. На небыстром интернете обновление не доходило никогда — компьютер месяцами оставался на старой версии, а выглядело это как «обновление не находится», и его ставили руками. Теперь загрузка продолжается с того места, где остановилась. **Для завода:** парк перестаёт расползаться по версиям сам по себе — а расхождение версий и есть причина, по которой данные, набранные на одном рабочем месте, иногда не понимает другое. Плюс программа ставится в папку с понятным именем, а прежняя удаляется сама и освобождает место на диске. |
| 2026-08-03 | (следующий релиз) | Справочник инструментов | **Кнопка «копировать в новый» в карточке инструмента заработала — раньше она молча не делала ничего.** Нажатие проходило без ошибки и без сообщения, но копия не создавалась: работник думал, что создал новую карточку по образцу, а её не было. Теперь копия создаётся и открывается. Нашлось не по жалобе, а при сверке описания программного интерфейса с тем, что он делает на самом деле. |
| 2026-08-03 | v2026.803.926 → .1121 | Табель учёта рабочего времени | **Распечатанный табель стал рабочим бланком, а не справкой.** Начальник цеха заполняет табель ручкой на бумаге, а печаталась плотная сводка: мелкие клетки, полное ФИО и должность в полстроки, выходные светло-серой заливкой, которую принтер цеха не проявлял вовсе. Теперь клетки занимают всю свободную высоту листа и всю ширину (ФИО — фамилия с инициалами, должность на бумагу не идёт), чернота выходных и толщина линий настраиваются под конкретный принтер, а в клетке печатается одно значение: часы жирными, код отсутствия — обычной буквой во весь размер. Прежняя вёрстка ставила код мелко над крупными часами, и командировку принимали за явку. |
| 2026-08-03 | v2026.803.1013 → .1121 | Табель учёта рабочего времени | **Полсмены теперь можно провести в программе, а не дописывать на полях.** В ячейку ставятся дробные часы (9,5 / 11,5) — раньше только целые, и «девять с половиной» приходилось округлять или помечать вручную. Плюс шапка таблицы с числами и днями недели закреплена при прокрутке: у последних сотрудников длинного табеля больше не надо угадывать, в какой день недели проставляются часы. |
| 2026-08-02 | (следующий релиз) | Контракты (платежи и привязка двигателей) | **Номер договора стало можно уточнять, не теряя учёт.** Раньше связь двигателей и оплат с договором держалась на самом его номере: стоило поправить номер в карточке (уточнение, опечатка, лишний пробел) — и двигатели уходили в «без секции», а платежи переставали учитываться в контракте, причём молча. Теперь связь держится сама по себе, а перед сменой номера программа показывает, сколько двигателей и на какую сумму оплат к нему привязано. |
| 2026-08-02 | v2026.802.1359 | Карточки двигателя и инструмента · Дефектовка · Журнал изменений | **Правки перестали пропадать при закрытии карточки.** Раньше работник мог изменить в карточке инструмента только свойство или фотографию, а в карточке двигателя — только внутренний номер (клеймо) или причину списания, закрыть карточку — и правка молча возвращалась к прежнему значению, без всякого сообщения. То же самое происходило в дефектовке с правками, сделанными за те секунды, пока подгружается список деталей марки. Теперь сохраняется всё, а если карточку в этот момент занял другой работник, программа прямо говорит, что правки НЕ сохранены, и не закрывает карточку. Плюс в журнале изменений двигателя наконец видно, кто и какие поля правил (раньше такие записи не появлялись вовсе) — есть на что опереться при разборе спорных случаев. |
| 2026-08-01 | v2026.802.1359 | Дефектовка и акты двигателя | **Состав дефектовки теперь правится на каждом двигателе — и есть кнопка вернуть всё как в марке.** Раньше поля «Марка двигателя», «№ двигателя», «Внутренний №», «Номер договора» и «Дата приёмки» в актах были **заблокированы для ввода**, а если значение всё же оказывалось другим, программа молча возвращала его из карточки двигателя и сохраняла. Оператор не мог поправить ни опечатку, ни частный случай. Теперь эти поля открыты, программа только **подставляет пустое** и никогда не перетирает введённое. Чтобы не бояться экспериментов, рядом со списком деталей появилась кнопка **«Сбросить по двигателю и марке»**: она возвращает строки к спецификации марки, а шапку акта — к данным карточки двигателя. Кнопка спрашивает подтверждение и честно предупреждает, что ручные правки (добавленные строки, изменённые количества, брак, отметки наличия) будут потеряны. **Для завода:** дефектовка перестала быть жёсткой формой — мастер подгоняет её под конкретный двигатель, а типовой список марки всегда в одном нажатии. Заодно клиент и веб-админка стали вести себя одинаково: раньше в веб-админке правка сохранялась, а в программе откатывалась. |
| 2026-07-31 | v2026.731.1440 | Контракты (платежи) / карточка двигателя / наряды (печать) | **Аванс теперь разносится на те двигатели, за которые он пришёл, а не «на первые сколько-то».** В карточке контракта двигатели и зарезервированные места перестали быть двумя разными таблицами: теперь один список, сгруппированный по маркам, где у каждой строки видно и двигатель, и все его платежи с датами. Бухгалтер отмечает галочкой всю марку (или отдельные двигатели), вводит сумму и дату — и программа сама делит поровну, показывая до нажатия, сколько придётся на каждый. Раньше разнести аванс только по одной марке в контракте с двумя марками было невозможно. **Двигатель встаёт на место своей марки.** Если аванс уже выписан под 10 двигателей марки А, приехавший двигатель марки Б больше не займёт их место — программа посадит его к своей марке или честно скажет, что мест под эту марку в контракте не осталось (значит, ошиблись либо маркой в карточке, либо планом контракта). Это работает из всех трёх мест, где двигатель привязывают, включая его собственную карточку — раньше она вообще не резервировала место. **Наряды печатаются по-разному.** Наряд на сборку остался как был, а обычный и ремонтный получили свою форму: без «Утверждаю» и без «Работу сдал / Работу принял», зато с подписью каждого члена бригады прямо в таблице, с КТУ, стоимостью услуг и подписями нормировщика, отдела кадров, ОТК и мастера цеха. В строках появился номер детали — тот самый, что набит заводом-изготовителем и вносится при дефектовке; он подставляется в наряд сам. **Для завода:** деньги заказчика привязаны к конкретным двигателям, а не к порядку строк, поэтому сроки ремонта считаются по тем двигателям, за которые действительно заплачено; нормировщица печатает свой наряд, а не сборочный с чужими подписями. |
| 2026-07-30 | серверная правка (без нового выпуска клиента) | Пустые карточки · Спецификации сборки (BOM) | **Пустые спецификации сборки больше не прячутся — их видно и можно убрать самому.** Раздел «Пустые карточки» до сих пор умел находить брошенные пустые карточки двигателей, договоров, сотрудников, нарядов и заявок, но про спецификации сборки не знал ничего. Из-за этого у марки В-84 почти месяц (с 1 по 30 июля) лежала вторая спецификация — с названием и привязанной маркой, но **без единой строки деталей**. Вреда от такой пустышки больше, чем от отсутствия: пока у марки две спецификации, наряд на сборку перестаёт подставлять её сам и начинает спрашивать у мастера, какую взять, — а если выбрать пустую, программа отвечает «в основном варианте BOM нет материалов». Теперь такие спецификации собираются в отдельную группу «Спецификации сборки (BOM)» под своим названием — пустой дубль видно рядом с настоящей, и удаляется он галочкой, без обращения к разработчику. Пустой считается спецификация без строк — заполненное название и привязка к марке от проверки не спасают, именно поэтому дубль столько и прожил. **Для завода:** брошенная при заведении спецификация перестаёт молча ломать выбор в нарядах сборки; на сегодня на сервере такая ровно одна — у марки В-84, её и стоит удалить первой. |
| 2026-07-30 | backend на проде 30.07 (клиент — следующим релизом) | Наряды сборки / спецификации сборки (BOM) | **Двигатель в наряде на сборку теперь меняется — раньше не менялся ни у кого и никогда.** Оказалось, программа требовала, чтобы у марки двигателя одна из спецификаций была отмечена «основной», а отметить её было негде — поэтому на любой двигатель любой марки выбор упирался в вопрос «выберите спецификацию вручную», причём поле показывало выбранный двигатель, а сохранялся прежний. Теперь: если у марки спецификация одна, программа берёт её сама и ничего не спрашивает; если их действительно несколько — двигатель меняется сразу, а спецификацию можно подтянуть отдельным шагом. Плюс в карточке спецификации появилась галочка «основная для марки» — назначил один раз, и наряды больше не спрашивают. **Для завода:** перебивка двигателя в наряде (обычное дело, когда на ремонт встаёт другой агрегат) перестала быть работой для разработчика; отчёты и провенанс деталей привязываются к тому двигателю, который реально собирают. Отдельно: у марки В-84 в справочнике лежит пустой дубль спецификации от 01.07 — его стоит удалить, иначе именно на этой марке вопрос про спецификацию останется. |
| 2026-07-29 | v2026.729.1551 | Меню разделов / двигатели (статусы) / синхронизация | **Меньше ручных галочек и меньше потерянных записей.** Статус двигателя «Начат ремонт» теперь встаёт сам, когда начинают дефектовку (появились решения по деталям), а «Отремонтирован» — когда закрывают наряд на сборку: мастеру не нужно помнить про переключатель, а отчёты по загрузке и прогноз перестают врать из-за забытой галочки. Перебивка номера двигателя больше не упирается в отсутствие спецификации сборки — наряд можно переназначить на другой двигатель и выдать в работу. Меню разделов стало настраиваемым списком (перетаскивание, закрепление нужных наверху) — у каждого работника свой порядок под свои задачи. Записи, заведённые на компьютере оператора и по какой-то причине не доехавшие до остальных, программа теперь досылает сама. **Для завода:** статус двигателя в программе соответствует тому, что реально происходит в цеху, без дисциплины «не забудь нажать»; данные, набранные на рабочем месте, перестают теряться по дороге и не требуют вмешательства разработчика. |
| 2026-07-29 | v2026.729.1041 | Контракты / двигатели / отчёты (платежи) | **Учёт оплат по каждому двигателю — бухгалтер видит деньги и сроки, не выходя из программы.** В карточке двигателя вкладка «Платежи» (стоимость, авансы, окончательный расчёт, итог «сколько осталось доплатить»); с первого аванса программа считает 90 дней на ремонт и подкрашивает двигатель в контракте жёлтым/красным по мере приближения срока, а в списке контрактов колонка «Горящие» показывает проблемные контракты без открытия. Деньги, пришедшие раньше двигателей, разносятся по «слотам» контракта одной кнопкой и сами привязываются к двигателю, когда тот приедет. Служебная записка директору «запустить в ремонт» собирается галочками в один документ на несколько двигателей. Два отчёта: матрица платежей контракта (по ДС) и журнал оплат по датам/заказчикам. **Для завода:** просрочка авансовых сроков видна заранее, а не при разборе с заказчиком; решение «что срочно брать в работу» подкреплено деньгами и сроками, а не памятью бухгалтера. |
| 2026-07-28 | v2026.728.2350 | Интерфейс (вид «Вкладки» — теперь единственный) | **Программа у всех открывается в виде «Вкладки» — владелец принял его и убрал старые виды совсем.** Слева разделы, рядом список, каждая открытая карточка — своя вкладка сверху, как в браузере. По просьбе владельца доделано главное из «Резинового»: границу между разделами и списком можно двигать мышкой (ширина запоминается за работником), **две карточки можно поставить рядом на пол-экрана каждая** — кнопка ⑃ на вкладке, удобно сверять два двигателя или два наряда; крестик закрытия появился на всех вкладках, включая страницы вроде Истории, которые раньше было не закрыть. Кнопки переключения видов из шапки убраны — путаться больше не в чем, у всех одинаково. **Для завода:** работник держит открытыми список и две-три карточки одновременно и не теряет введённое при переключении; сравнение двух двигателей бок о бок — раньше только на бумаге или по памяти. |
| 2026-07-28 | v2026.728.1659 | Склад → Импорт из 1С | **Поправка к записи ниже: до этой сборки описанным ниже пользоваться было нельзя.** В выпуске v2026.728.1630 окно «Импорт из 1С» не открывалось вовсе — программа замирала при нажатии кнопки, и ни одна из перечисленных ниже возможностей до кладовщика не доходила. Исправлено; окно открывается с первого нажатия. **Для завода:** ежедневная сверка остатков с 1С реально доступна начиная с этой сборки, а не с предыдущей — приёмку имеет смысл проводить только на ней. |
| 2026-07-28 | v2026.728.1630 | Склад → Импорт из 1С | **Импорт остатков из 1С стал делом одной кнопки на все склады.** Выгрузка из 1С содержит сразу все склады завода (основной, стройматериалов, цех №3, цех №4), а программа заставляла выбирать их по одному: сопоставил склад → провёл → вернулся → выбрал следующий. Теперь склады из файла показываются таблицей, напротив каждого выбирается склад программы (совпадающие по названию подставляются сами), и импорт проходит по всем по очереди. Если в выгрузке появился склад, которого в программе ещё нет, его заводят тут же, не выходя из окна. Заодно починено заведение новых позиций: раньше импорт обрывался на первой же незнакомой позиции и не проводился вовсе — из-за этого дневная выгрузка (546 новых наименований) не заходила совсем. **Что это даёт заводу:** ежедневная сверка остатков с 1С перестала быть многошаговым ритуалом — один файл, один проход, все склады; расхождения между полкой и программой видно в тот же день, а не когда дойдут руки повторить процедуру для каждого склада. |
| 2026-07-28 | серверная правка (без нового выпуска клиента) | Обмен данными между компьютерами | **Данные, «застрявшие» на одном компьютере, снова доходят до всех.** Работники жаловались: двигатель, заведённый на одной машине, не виден остальным, а смена двигателя в наряде после перезахода показывает старое значение. Выглядело как проблема с правами — на деле программа на той машине **вообще ничего не отправляла на сервер уже неделями**. Причина: при отправке данные уходят пачкой, и одна-единственная строчка с испорченной ссылкой (сослалась на цех, который переехал в другой справочник) заставляла сервер отклонять **всю пачку целиком** — вместе с двигателем, дефектовкой и правками нарядов. Теперь сервер отбраковывает только виновную строчку, остальное принимает, а про отбракованное пишет владельцу в «Критические события» — раньше это происходило молча, поэтому и тянулось неделями. **Что это даёт заводу:** работа, сделанная за компьютером, гарантированно доходит до общей базы; если что-то всё же не проходит — это видно сразу и адресно (какая машина, какая строка), а не выясняется через месяц по жалобе «у меня не видно». ⚠️ Правку наряда 101, потерянную за это время, нужно внести заново. |
| 2026-07-28 | v2026.728.1355 | Склад (импорт остатков из 1С) | **Сверка склада с 1С заработала и перестала врать в минус.** При первом боевом прогоне программа не узнала ни одной позиции из шестисот: в 1С артикул пишут прямо в названии детали, а у нас он лежит отдельным полем — сравнивать было нечего. Теперь сверка идёт по артикулу, по названию и по их сочетанию, поэтому одна и та же деталь узнаётся, как бы её ни записали. Второе, более важное: изменился сам смысл сверки. Раньше выгрузка сравнивалась с предыдущей выгрузкой, и деталь, уже списанная нарядом в программе, списывалась второй раз, когда её списание доходило до 1С, — ошибка накапливалась с каждым импортом. Теперь остаток просто приводится к числу из выгрузки: в 1С стоит реальное количество с полки, и оно становится верным, а расход по нарядам не удваивается. Задержка выгрузки на день-два больше не страшна — следующий импорт сам всё выправляет. Позиции, о которых 1С не знает (снятые при разборке двигателей), сверка не трогает — они живут по документам программы. Наконец, то, чего в справочнике нет, заводится прямо при импорте одной галочкой: название без артикула, артикул в своём поле (по нему и проверяются повторы), единица измерения подобрана, а сама позиция сразу попадает в свой тематический раздел — подшипники к подшипникам, кольца в РТИ, свёрла в инструмент. На живой выгрузке девять из десяти новых позиций находят раздел сами. **Что это даёт заводу:** остатки в программе становятся сверяемыми с 1С ежедневно и без ручного ввода, а прогноз комплектования опирается на числа, которые не разъезжаются с полкой. |
| 2026-07-28 | v2026.728.954 | Наряды (список) · Управление доступами · Верхняя строка | **Наряды перестали пропадать у тех, кому положено их видеть.** Работники жаловались, что видят не весь список: у двоих из сотни нарядов показывалось меньше трети, причём подряд идущим куском — «с 81-го по 115-й нет». Причина оказалась не в потере данных: в программе есть раздел закрытых нарядов, где «полный доступ» означает не «видеть больше», а «мои наряды скрыты от остальных, и сам я вижу только свои». Такой уровень был по недоразумению выставлен учётной записи администратора — и все наряды, выписанные под ней (а под ней работали на чужих компьютерах, отсюда 54 наряда из 100), тихо стали закрытыми. Со стороны это было неотличимо от сбоя, а сам администратор видел полный список — его учётная запись обходит все ограничения, поэтому поломку не замечал именно тот, кто мог бы её заметить. Теперь учётная запись администратора убрана из таблицы доступов совсем: у неё полный доступ по должности, настраивать там нечего, а случайно выдать себе «закрытые наряды» больше нельзя. Закрытые наряды Рамзии остались закрытыми, как и задумано. **Верхняя строка перестала расти вниз.** Кнопки в шапке не помещались и переползали на второй ряд, отъедая место у рабочей области. Теперь значок, название и кнопки стоят одной строкой по центру: при нехватке места у кнопок по очереди прячутся подписи и остаются значки (подсказка при наведении сохраняется), а полоса «скачиваем обновление» убрана — она занимала место постоянно ради нескольких секунд в месяц; обновление по-прежнему ставится само при перезапуске. Для завода: список нарядов снова полон у всех, кому открыт раздел, — а значит планирование и отчётность по нарядам перестают зависеть от того, под чьей учётной записью наряд был выписан. |
| 2026-07-27 | v2026.727.1516 | Интерфейс (вид, оформление, карточки) · Склад (остатки из 1С) | **Три изменения одним выпуском — по прямому заказу владельца.** (1) **Новый вид программы «Вкладки»** (пробный, включается кнопкой): слева всегда закреплены «РАЗДЕЛЫ» и «Список …», а каждая открытая карточка живёт в своей вкладке во весь экран. Работник переключается между двумя-тремя карточками и списком, не теряя ни введённого, ни места в списке; лимит — десять вкладок, после пяти программа напоминает закрыть отработанные, чтобы не забыть их сохранить. Раньше открытая карточка занимала весь экран одна, и возврат к списку означал её закрытие. (2) **Остатки склада можно занести из 1С**: кнопка «Импорт из 1С» в складских документах принимает выгруженный из 1С отчёт по остаткам и до проведения показывает, что нашлось в справочнике, что не нашлось и что изменится. Устроено слоями: импорт правит только то, что сам принёс — остатки, заведённые нарядами и складскими документами программы, он не трогает, а позиции, пропавшие из новой выгрузки, обнуляет сам (раньше «нулевые» пришлось бы искать глазами). Ежедневная сверка со складом 1С перестаёт быть ручной работой. (3) **Оформление на свой вкус**: переключатель «Строго / Красиво» и пять цветовых гамм — каждый работник настраивает себе своё. Заодно строгий вид стал читаемее (строки в списках подсвечены через одну, рамки заметнее, серый текст темнее), карточки растянулись на всю ширину экрана и уплотнились, а перегруженные карточки разгрузились: в двигателе отметки о ходе ремонта убраны в сворачиваемый блок, в наряде свёрнуты подписи, в спецификации сборки — длинные предупреждения. Пока что-то грузится, крутится кружок ожидания и правка заблокирована — работник не вводит данные в ещё не загруженную карточку. Для завода: меньше потерянного ввода при переключении между делами, ручной перенос остатков из 1С заменён импортом с предпросмотром, а экраны цеховых карточек перестали быть стеной из галочек. |
| 2026-07-27 | v2026.727.1153 | Справочники · Ввод данных (все карточки) | **Выбор из справочника мышкой заработал, и точное написание больше не требуется.** Оператор набирал в поле марки «в84», программа честно показывала подсказку «В-84», но клик по ней не срабатывал — вместо выбора выскакивало «элемент не выбран», и оставалось либо угадывать написание с дефисом, либо ловить строку стрелками. Теперь клик по подсказке подставляет то, что выбрано, а дефисы и пробелы при поиске не важны: «в84» само превращается в «В-84», как записано в справочнике. Если программа всё же переспросит — первой кнопкой предложит ближайшее найденное. Заодно закрыт путь к двойникам: создать «в84» рядом с существующей «В-84» кнопка создания не даст. Для завода: заполнение карточек (контракты, наряды, дефектовка) перестало требовать от людей помнить каталожные написания — меньше брошенных на полпути карточек и меньше дублей в справочниках, из-за которых потом расходятся отчёты по маркам. |
| 2026-07-27 | v2026.727.923 | Наряды на сборку · Учёт рабочих мест | **Сборочный наряд снова можно выписать.** Мастер создавал наряд на сборку от двигателя, все поля заполнялись, номер присваивался — а при выдаче в работу программа отвечала «наряд не найден», после переоткрытия — «BOM не применён». Причина одна на три жалобы: программа не узнавала деталь, выбранную из справочника (детали переехали в другой каталог, а проверка искала их по-старому), поэтому наряд молча не записывался в базу — дальше всё сыпалось следом. Сборочный контур на этих машинах стоял; теперь наряд сохраняется и выдаётся. **Компьютер перестал двоиться в учёте.** Программа предупреждала «логин активен на 2 машинах», хотя человек работал за одним столом: после пересборки локальной базы клиент представлялся серверу новой машиной, и умерший «двойник» ещё десять минут числился живым. Такие же двойники накапливались от служебного сторожа, который опрашивал сервер и засчитывался как работающая программа — из-за этого в списке рабочих мест годами «жили» давно удалённые установки со старыми версиями. Для завода: предупреждение о двойном логине снова означает реальную проблему (два человека под одним логином — искажённая атрибуция нарядов), а список рабочих мест показывает те машины, что действительно в строю — по нему видно, кого надо обновлять. |
| 2026-07-26 | v2026.726.1951 | Помощник в программе (ИИваныч) · Окно «Что нового» | **У заводского помощника появилось имя и лицо, и он стал отвечать быстрее.** (1) Помощника зовут **ИИваныч** — работник обращается к нему по имени, а не к «ИИ-агенту»; в углу его окна стоит человечек с гаечным ключом, который при открытии затягивает гайку. Мелочь, но она превращает непонятную технологию в понятного собеседника, к которому не страшно обратиться. (2) **Ответ приходит за минуты, а не в конце часа.** Раньше помощник просыпался раз в час, разбирал накопившиеся вопросы и засыпал: спросил в 10:05 — ждёшь до 11:00. Теперь он не уходит после первой пачки, а почти весь час держит очередь разобранной, и пока он на связи — в окне так и написано. Работник видит, ждать ему пару минут или до ровного часа. (3) **Окно «Что нового» после обновления стало вдвое короче** — только список новинок, без длинного пересказа того же самого; и показывает новинки всех выпусков за последние два дня, а не только последнего. Для завода: помощник по остаткам и двигателям становится инструментом, которым пользуются в потоке работы, а не «спрошу и завтра посмотрю». |
| 2026-07-26 | v2026.726.1502 | Управление доступами (суперадмин) | **Права работников стало видно одной картиной.** На странице «Доступы по разделам» появился второй вид — таблица: слева работники, сверху разделы, на пересечении светофор (красный — доступа нет, зелёный — только смотрит, синий — смотрит и правит). Раньше владелец обходил десять разделов, чтобы понять права одного человека. **Программа стала подсказывать последствия переключения.** Разделы связаны: в карточке двигателя ищутся договоры, в наряде выбирается двигатель, в заявке — номенклатура склада. Раньше подсказка была только при выдаче доступа; теперь при снятии программа предупреждает, каким уже выданным разделам он нужен, и спрашивает, снимать ли. А если выдать «полный доступ» человеку, чья должностная роль не позволяет там сохранять, программа сразу скажет — раньше такой доступ выглядел выданным, но записи молча отклонялись. У работников с несогласованным набором прав появился значок ⚠️ с расшифровкой. **Закрыта брешь:** раньше работник теоретически мог выдать себе полный доступ ко всем разделам в обход интерфейса — сервер этого не проверял; теперь менять доступы может только суперадминистратор. Для завода: разграничение прав перестало быть на честном слове, а владелец видит всю раскладку доступов на одном экране. |
| 2026-07-26 | v2026.726.1033 | Рабочее место оператора · Черновики карточек | **Убраны две вещи, которые ежедневно дёргали работника.** (1) Программа-сторож перестала мигать чёрным окном: раз в 15 минут он проверяет, на месте ли программа и ярлыки, и каждая проверка на секунду выбрасывала на экран служебное окно — теперь всё выполняется скрыто, сторожа не видно и не слышно. (2) **Сохранённый черновик больше не воскресает.** Работник дорабатывал карточку, нажимал «Сохранить», а при следующем запуске она снова висела как незаконченный черновик — автосохранение успевало записаться уже после сохранения и возвращало только что закрытый черновик. В нарядах это чинили раньше, теперь защищены все карточки: двигатель, заявка, контракт, контрагент, сотрудник, марка, справочники. (3) **Черновики стали общими для всех машин работника.** При закрытии программы клиент досылает на сервер несохранённое, включая отметки о закрытых черновиках; раньше они ждали следующего запуска именно того компьютера, и на втором рабочем месте всплывал уже закрытый черновик. Это важно там, где один человек работает на нескольких машинах. Для производства: работник перестаёт разбираться, какой из черновиков настоящий, и не боится, что «недоделка» вернётся сама. |
| 2026-07-25 | v2026.725.2248 | Справочник номенклатуры | Из 145 позиций убраны служебные артикулы-заглушки (`DET-…`, `NM-…`), которые программа генерировала сама: у детали без артикула поле теперь пустое, а не заполнено выдуманным кодом. Кладовщик и снабженец видят правду — «артикул неизвестен» вместо мнимого номера, по которому ничего не найти у поставщика. **Побочный эффект для цеха:** старые QR-этикетки с кодом `DET-` перестали находиться поиском (перепечатать), а в экране «Дубли деталей» прибавилось групп одноимённых позиций без артикула — сливать их можно только уверенно, объединение необратимо. |
| 2026-07-25 | v2026.725.2248 | Надёжность рабочего места | Если у оператора пропал значок программы (снесли ярлык, сорвался апдейт), сторож восстанавливает его сам за секунды и без скачивания установщика. Раньше он на это тратил переустановку на 136 МБ — и она ярлык всё равно не возвращала. Меньше простоя смены и звонков «программа пропала». |
| 2026-07-25 | v2026.725.2033 | Склад / поиск (аварийно) | Утренний релиз по недосмотру отключил складские разделы (номенклатура, приход/расход, спецификации сборки, дефектовка, ремфонд, утиль) и общий поиск — вечером того же дня всё возвращено, плюс починен обмен данными, который останавливался у всех из-за нескольких спецификаций, привязанных только к маркам двигателей. |
| 2026-07-25 | v2026.725.1938 | Справочники (удаление) · Отчёты (утиль, двигатели/контракты) · Планшет · Склейка дублей | **Удаление объекта перестало рвать связи молча, а отчёт по утилю — занижать итог.** (1) При удалении контрагента, марки двигателя или любой записи справочника программа сначала показывает, **где на неё ссылаются** (контракты, наряды, заявки, спецификации сборки), и предлагает выбрать: заменить связи на другой объект, снять их, снять и отложить замену напоминанием в «Заметки» либо оставить как есть. Если ссылок нет — удаление проходит одним нажатием, как раньше. До этого объект удалялся тихо, а в чужих карточках оставались пустые поля — та самая причина жалоб «объект не найден». Сервер держит ту же проверку как страховку: удаление, пришедшее из веб-админки или со старой версии программы, тоже остановится с объяснением. (2) **«Утиль: реестр»** больше не занижает количество: в итог попадают двигатели, признанные утильными целиком, а по деталям считаются **все** дефектовки — при повторном заезде двигателя прежние акты выпадали из отчёта. Подписи итогов стали русскими. (3) **«Двигатели и контракты»** показывает средний срок ремонта, долю утиля и отгрузку не только в разрезе контрактов, но и по маркам и по двигателям. (4) **Планшетный режим** наконец увеличивает всё: кнопки, поля, строки списков и календарь — часть размеров раньше перебивалась личными настройками отображения. (5) **Склейка задвоенных двигателей** переносит складские движения на оставшуюся карточку, а не бросает их на удалённой. (6) **Программа-сторож** сама восстанавливает пропавшие ярлыки, при повторных сбоях делает паузы вместо закачки установщика каждые 15 минут и прикладывает к отчёту о сбое хвост журнала. Для производства: справочники перестают тихо разъезжаться при чистке — работник видит последствия удаления до того, как оно ударит по контрактам и нарядам; отчётность по утилю сходится с цехом при повторных заездах двигателей. |
| 2026-07-24 | v2026.724.1603 | Меню (все разделы) · Карточки (ссылки) · Наряды (сборка) | **Меню снова по ролям, потерянные ссылки видно, сборочные наряды сохраняются.** (1) На обычном компьютере вернулось **полное меню по роли** — из-за ошибки оно у всех сжималось до планшетного набора из 8 кнопок; теперь короткое меню только на цеховом планшете, а у суперадмина видны все разделы. (2) Если выбранный в карточке объект (например, марка в контракте) был позже удалён, поле раньше выглядело просто пустым — теперь помечается красным «выбранное значение удалено, выберите заново». Это и была причина жалоб «объект не найден, хотя он есть»: объект удалён, а ссылка осталась. Теперь такие места видно и их легко поправить. (3) Из-за слишком строгой проверки связей **сборочный наряд с деталью мог не сохраниться** («элемент не найден»), хотя деталь на месте — проверка исправлена, наряды проводятся как обычно. Для производства: мастер перестаёт упираться в непроводящийся наряд, а «висячие» ссылки в справочниках и контрактах перестают тихо ломать отчёты — их теперь видно и можно вычистить. |
| 2026-07-24 | v2026.724.841 | Ремонт/сборка · Норма ремонта · Дефектовка · Комплектование | **Норму ремонта и спецификацию сборки развели, нехватку при выдаче в сборку стало видно, а номерные экземпляры — под резервом.** (1) Раньше «что положено отремонтировать по марке» и «что нужно, чтобы собрать» жили одним списком и мешали друг другу — теперь это два раздельных профиля: **норма ремонта** по маркам (своя страница в «Производстве») и **спецификация сборки**. (2) При комплектовании сборки программа сверяется со складом и показывает **нехватку**; чего нет — мастер отправляет на согласование, согласующий подтверждает с указанием причины (раньше нехватку ловили постфактум). (3) Проведение дефектовки ведёт **историю жизни каждого номерного экземпляра** ремфонда (ушёл в сборку → вернулся → списан повторно), видную в листе дефектовки и в складском документе. (4) Номерная деталь **резервируется за своим сборочным документом** — пока висит бирка сборки, её нельзя выдать дважды; при отмене черновика резерв снимается сам. (5) Плюс защита целостности: запись, на которую ссылаются другие, нельзя удалить и сломать — меньше «висячих» ссылок и непонятных пропаж в отчётах. Для производства: подбор деталей из обезличенного фонда перестаёт двоиться и терять экземпляры, а начало сборки видит реальную картину дефицита. |
| 2026-07-23 | ближайший релиз | Справочник деталей · артикул при заведении позиции | **Программа перестала выдумывать артикулы за работника.** Раньше при быстром создании детали программа сама штамповала внутренний код-заглушку вида `DET-05FE…`. Он выглядел как артикул, но не значил ничего: по нему не найти деталь, и он маскировал одинаковые позиции — на сегодня в базе 145 таких деталей, и 58 из них сидят в 27 группах с полностью совпадающими названиями («Фильтр масляный» — четыре штуки). Теперь при заведении позиции программа спрашивает артикул, а если его правда нет — работник нажимает «Без артикула», и поле остаётся пустым честно. Эффект: перестают плодиться неразличимые двойники деталей, а те, что уже есть, можно будет разобрать — раньше чистку было бессмысленно начинать, потому что заглушки набегали бы снова. |
| 2026-07-23 | ближайший релиз | Планшет в цеху · меню оператора | **В планшетном режиме меню сократилось с 42 разделов до 8.** Остались двигатели (с дефектовкой и комплектованием), наряды, детали, марки двигателей, спецификации сборки, остатки на складе, заявки в снабжение и ремфонд — то есть двигатель и всё, что с ним связано. Бухгалтерия, кадры, контракты и админка в цеховом режиме не показываются. Эффект: на маленьком экране работник не листает чужие разделы и не открывает их по ошибке. Это не ограничение прав — на том же планшете кнопка «Комп» возвращает полное меню, и собственная раскладка меню не теряется. |
| 2026-07-23 | v2026.723.1137 | Все списки · колонка «Дата изменения» | **В списках стало видно, что трогали последним.** Почти во всех списках программы — наряды, двигатели, сотрудники, заявки, складские документы, номенклатура, справочники, контрагенты, контракты — появилась колонка с датой и временем последней правки, и по ней можно отсортировать одним щелчком. Эффект: работник видит свою вчерашнюю работу сверху, а не ищет её по памяти; при разборе «кто последним менял карточку» не нужно открывать карточки по одной. Колонку можно скрыть, если мешает. Там же исправлено: двигатель с меткой «Признан утильным» не подсвечивался в списке и в колонке «Утиль» показывал «Нет», хотя в отчётах числился утилем — список считал утиль по другой метке, чем отчёты. Эффект: утильный двигатель видно с первого взгляда, и цех не начинает по нему работу. |
| 2026-07-23 | v2026.723.1101 | Наряды · список и карточка | **Работа в нарядах перестала тормозить.** Стоило зайти в наряды и начать искать — программа задумывалась на каждой букве, а заодно подвисало всё остальное: карточки, сохранение, обмен с сервером. Причина оказалась не в самих нарядах (их всего 81), а в том, что список ради одной мелочи — подставить марку и номер двигателя — каждый раз перечитывал целиком **все акты дефектовки**, а это почти сотня мегабайт. Теперь читается только нужное и только изменившееся; справочник услуг карточки загружается разом, а не по одной позиции из трёхсот с лишним. Эффект: мастер и нормировщик перестают ждать программу в самом ходовом разделе — а именно ожидание заставляет людей вести параллельный учёт в тетради. |
| 2026-07-23 | v2026.723.1101 | Наряды · номер наряда | **Исправленный номер наряда теперь держится.** Возможность правки номера дали в прошлом релизе, но на практике она не работала: номер вставал и в карточке, и в списке, а через полминуты возвращался старый — сервер, который стережёт номер от случайной подмены, не отличал законную правку от чужой. Теперь отличает. Двум нарядам, потерявшим номера в старом сбое, номера возвращены — 85 и 86. Эффект: нумерация нарядов снова сплошная, а по ней ищут наряд в бумагах и сверяют со складскими документами. |
| 2026-07-22 | v2026.722.1416 | Отчёты · каталог шаблонов | **Отчёты разложены по темам, чтобы нужный находился выбором, а не перебором.** Шаблонов накопилось 32 — это уже куча, в которой человек не ищет, а сдаётся. Теперь сначала восемь крупных плиток (Двигатели, Контракты, Снабжение, Наряды, Зарплата, Склад, Справочники, Проверки) с пояснением и числом отчётов внутри, клик — список темы. Отчёт, который задевает две темы (комплектование — и двигатели, и склад; зарплатные — и зарплата, и наряды), лежит в обеих: не надо угадывать, куда его отнёс программист. Поиск ищет сразу по всем темам. Эффект: экономист и мастер доходят до нужного отчёта, а не бросают на середине списка; редко используемые отчёты перестают быть невидимыми. |
| 2026-07-22 | v2026.722.1416 | Наряды · номер наряда | **У суперадминистратора появилась возможность исправить номер наряда.** Номер по-прежнему закрыт от правки для всех — так и должно быть, он уходит в печатные формы и в номера складских документов. Но из-за старой ошибки у пары нарядов номер обнулился, и вернуть его было нечем. Теперь администратор может поставить правильный номер: программа проверит, что он не занят другим нарядом, и попросит подтверждение. Эффект: журнал нарядов приводится в порядок без обращения к разработчику, а нумерация остаётся сплошной — по ней потом ищут наряд в бумагах. |
| 2026-07-22 | v2026.722.917 | Клиент · планшетный режим цеха | **Программу можно взять в цех: она перестраивается под палец.** Мастер помечает машину планшетом в Настройках, дальше в шапке переключатель «Комп / Планшет»: кнопки и строки списков растут до размера, в который попадаешь пальцем, галочки «на месте» в листе дефектовки становятся заметно крупнее, у количества появляются кнопки «−»/«+» и экранная цифровая клавиатура. Эффект: обход по цеху с отметкой наличия деталей и листом дефектовки перестаёт требовать возврата к столу с клавиатурой — данные заносятся там, где деталь лежит, а не по памяти через час. Оффлайн-работа при этом уже была: планшет пишет в свою базу и досылает всё при появлении Wi-Fi. |
| 2026-07-22 | v2026.722.917 | Двигатели · дефектовка · «Взять в работу» | **Двигатель закрепляется за конкретным работником, чтобы двое не правили один агрегат вслепую.** Кнопка «Взять в работу» в карточке: у остальных карточка и лист дефектовки становятся только для чтения, сверху видно, кто занят и до какого времени, в списке двигателей — значок замка. Закончил — «Вернуть»; забыли — резерв отпустится сам через 12 часов, к следующей смене. Эффект: уходит старая беда цеха, когда работа одного тихо затиралась работой другого, и пропадает необходимость выяснять по цеху, кто уже разбирает этот двигатель. Важно для организации: замок **мягкий** — работу, сделанную в цеху без сети до того, как двигатель заняли, программа принимает как обычно, ничего не теряется. |
| 2026-07-21 | v2026.721.1616 | Отчёты · Двигатели · Контракты | **Один отчёт отвечает на главный управленческий вопрос завода: сколько по контракту заказано, приехало, отгружено и осталось на заводе несделанным.** Раньше эти цифры собирались по разным экранам (список двигателей, стадии, метрики контракта) — теперь один отчёт «Двигатели и контракты» с переключателем разреза: по контрактам (план / приехало / ожидается / на заводе / готово-не-отгружено / отгружено / утиль / выполнение % / просрочка), по маркам (сводка + средний срок ремонта) и подробно по двигателям (даты, дней на заводе, состояние). Внизу — итоги «по-хозяйски»: незакрытый остаток по контрактам, средний TAT, доля утиля, отгрузка в срок. Эффект: директор/ППО видят исполнение каждого контракта на один взгляд, а не пересчётом по цехам; видно, какой контракт недопоставлен и что застряло в цеху. Настройки сворачиваемые, отчёт сохраняется шаблоном. |
| 2026-07-20 | v2026.721.1616 | Марки двигателей · спецификация | **Списочный состав марки виден отдельно от физического количества.** Технолог и ППО сравнивают марки по числу номенклатурных позиций: 12 форсунок или 100 шпилек не раздувают показатель до количества экземпляров. Это помогает быстро заметить, где состав марки недовнесён или перегружен лишними позициями. |
| 2026-07-19 | v2026.719.2040 | Наряды / надёжность данных | **Наряды перестали терять порядковые номера.** Сохранённый наряд мог всплыть «черновиком» без номера и подлежал пересозданию — теперь номер неприкосновенен, испорченные наряды чинятся простым «Сохранить». Плюс программа при закрытии дожидается отправки данных на сервер. Эффект: пересоздание нарядов и «призрачные черновики» на других компьютерах уходят; мастера доверяют номерам в журнале. |
| 2026-07-19 | v2026.719.2040 | ИИ-помощник (чат) | **Кнопки-подсказки и шаблоны запросов.** Формат ответа (Excel/DOCX/PDF/текст), период, кратко/подробно дописываются к вопросу кнопками; удачные вопросы сохраняются шаблонами и переиспользуются. Эффект: меньше переспросов «а пришли Excel'ем», повторяющиеся запросы — в один клик. |
| 2026-07-19 | v2026.719.2040 | Аналитика использования | **Программа считает, чем реально пользуются.** Визиты разделов/карточек/отчётов копятся в журнале действий; ИИ раз в неделю присылает администратору дайджест с наблюдениями и предложениями по развитию программы. Эффект: решения «что улучшать» опираются на реальное поведение операторов, а не на ощущения. |
| 2026-07-19 | v2026.719.1555 | ИИ-помощник (чат) | **Ответы по данным программы без ручного поиска по вкладкам.** Оператор задаёт вопрос своими словами («сколько рубашек на такой-то двигатель на складе и в ремфонде», «сколько двигателей пришло от контрагента и сколько осталось по контракту») — раз в час в рабочее время ИИ читает базу и присылает ответ в чат, большие выборки — файлом. Ответы строго в рамках прав доступа спросившего; спорные вопросы уходят администратору, его решения копятся в правила. Эффект: справочная нагрузка «посмотри мне цифру» снимается с ИТР — цифру достаёт ИИ, человек занимается делом. |
| 2026-07-19 | v2026.719.1157 | «Мои отчёты» · «Мои экраны» (эскизник) | **Отчёт сам считает по группам, а удачный отчёт делается один раз на всех.** (1) **Группировки с подытогами.** В своём отчёте теперь выбирается колонка группировки — строки собираются по её значению (по марке, по мастеру, по локации), и у каждой группы появляется свой подытог; для числовых колонок оператор сам решает, что считать: сумму, количество, среднее, минимум или максимум. Раньше «а сколько по каждой марке отдельно» означало выгрузку в Excel и ручную сводную таблицу — теперь это выбор в один клик, и группы одинаково выглядят на экране, в печати и в CSV. (2) **Общие шаблоны отчётов.** У шаблона появилась галочка «общий»: собранный однажды толковый отчёт видят все, у кого есть доступ к отчётам, а править и удалять его может только автор или администратор. Это снимает главную слабость личных шаблонов — знание одного человека («как посчитать вот это») перестаёт быть его личным и не теряется, когда он в отпуске. (3) **Эскизник — работа группой:** выделение рамкой и Shift-кликом, перемещение/дублирование/удаление сразу нескольких элементов и кнопка «Выровнять». Для производства: типовые сводки перестают быть ручной работой в Excel, а хорошая форма отчёта тиражируется на всю смену, а не переизобретается каждым. |
| 2026-07-19 | v2026.719.1157 | «Мои экраны» (конструктор) · «Мои отчёты» · Прогноз сборки | **Оператор собирает себе рабочий экран, а настройка прогноза перестала быть полем из двух десятков кнопок.** (1) Экраны, которые раньше были только эскизами-набросками, теперь могут **работать**: на холст кладётся блок **«Живой отчёт»** — выбираешь свой сохранённый отчёт из «Моих отчётов», и в режиме просмотра блок показывает настоящие данные (таблица с итогами и кнопкой обновить), а кнопку можно настроить на переход в нужный раздел. Получается личная приборная панель: мастер собирает на одном листе два-три своих отчёта и кнопки к разделам, которыми пользуется каждый день, — и работает с этим экраном, а не собирает его заново по меню. Экраны только **показывают** данные и переводят между разделами: вводить и править данные через самодельный экран нельзя — это осознанно, чтобы никакая самоделка не могла испортить учёт. (2) **Настройки прогноза сборки** свёрнуты в три понятных блока — «Приоритет сборки», «Склады и марки», «Темп и календарь». Блоки закрыты, но в заголовке каждого написано словами, что сейчас выбрано («4 двигателя в сутки · серия 2 · горизонт 7 дней · Пн–Сб»), — вся настройка читается с одного взгляда, разворачивать нужно только то, что меняешь. И главное: настроенный прогноз **сохраняется под именем** и в следующий раз применяется одной кнопкой, а не выставляется заново. Для производства: планировщик перестал тратить время на перенастройку и не боится сбить нужную комбинацию, а частым рабочим местам можно собрать свой экран без ожидания доработки. |
| 2026-07-18 | v2026.719.146 | Склад (отчёты, инвентаризация) · Дефектовка · Ремфонд · Снабжение (нормы) | **Учёт научился отчитываться по-бухгалтерски, а склад — сходиться с тем, что реально лежит в цеху.** (1) **Оборотная ведомость** по складу: за любой период видно «было на начало + пришло − ушло = стало на конец» по каждой детали и локации — классический отчёт, которого бухгалтерии и кладовщикам раньше не хватало (был только плоский журнал движений без сальдо). (2) **Нормы расхода стали числом, а не текстом в примечании**: из них считается **план закупок** — берём спецификацию марки, умножаем на процент нормы и на количество двигателей в плане, вычитаем свободные остатки и получаем «сколько купить». Экономисты получают расчёт кнопкой вместо ручной выкладки; позиции без заданной нормы честно считаются как 100% замены и помечаются, чтобы норму дозаполнили. (3) **Утиль дефектовки уезжает на склад утиля одной кнопкой**: раньше утиль жил только в отчёте, а физическая куча в цеху нигде не числилась — теперь она в складском учёте, её можно инвентаризировать и сверять. Повторное нажатие не двоит: списывается только то, что добавилось с прошлого раза. (4) **Слепая инвентаризация** переключателем: счётчику не показывают учётное количество — он вписывает то, что реально посчитал, и в документ идут только посчитанные строки. Это убирает соблазн «подтвердить не считая» и заодно даёт частичный пересчёт по кусочку склада, без остановки всего склада. (5) **Сверка ремфонда**: отчёт показывает, где клеймёных экземпляров числится больше, чем деталей на остатке фонда — сигнал, что поштучный и количественный учёт разошлись, пока это не превратилось в недостачу. |
| 2026-07-18 | v2026.718.1109 (на проде) | Снабжение · Прогноз сборки · Склад (расход/списание) · Документы | **Прогноз научился видеть будущие поставки, склад — помнить адресата, а ошибки проведения — исправляться штатно.** (1) В заявке снабжения появилась **«ожидаемая дата поставки»**: как только снабжение приняло заявку и указало дату, недопоставленные позиции автоматически считаются прогнозом сборки как будущий приход (двойного счёта нет — уже привезённое и оприходованное вычитается). Планирование сборки перестало занижать приход и плодить лишние закупки по позициям, которые уже едут. (2) **Адресная выдача со склада**: в расходе и списании можно указать **двигатель** и **наряд** (необязательно). Указанный двигатель попадает в его историю — отчёт «Комплектование двигателя» и «выдано в сборку» теперь видят и ручные выдачи кладовщика, а не только автоматические документы сборки; раньше такая выдача пропадала для учёта комплектации. Оттуда же печатаются **«Требование-накладная»** и **«Акт списания»** с подписными строками — бумажный след цеха выходит из программы, а не пишется от руки; черновик документа печатается как пикинг-лист. (3) **Сторно проведённого документа**: ошибочно проведённый приход/расход исправляется кнопкой — программа создаёт зеркальный сторно-документ, остатки возвращаются, оба документа ссылаются друг на друга. История регистра не переписывается, «поправить руками в базе» больше не нужно; сторно уже израсходованного прихода программа не даст сделать. Право — только у администратора. |
| 2026-07-17 | v2026.717.1856 | Отчёты · Комплектование · Склад · Снабжение | **Удар по главному затору завода — началу сборки — и по разрыву «снабжение↔склад».** (1) Новый отчёт **«Комплектование двигателя»**: выбрал двигатель — программа по спецификации его марки показывает всю картину подбора одним листом: что требуется, что уже выдано в сборку, что осталось выдать, сколько доступно на складах и **где именно лежит** (по локациям), что можно закрыть ремонтным фондом, и чистый дефицит. Распечатка = готовый пикинг-лист комплектовщику: взял список — пошёл собирать, не бегая по цехам с вопросом «что есть». Это первый программный ответ на ручной подбор деталей из обезличенного фонда (боль ППО, идея 007). (2) Отчёт **«Готовность двигателей к сборке» починен**: раньше на всех рабочих местах молча показывал ноль компонентов (спецификации не доезжали до клиентов) — теперь считает по-настоящему, по спецификации марки каждого двигателя. (3) **Приход на склад — прямо из заявки снабжения**: на карточке заявки кнопка «Оформить приход на склад» создаёт черновик приходного документа со строками из заявки (осталось проверить и провести), заявка и документ связываются автоматически — снят двойной ручной ввод «снабжение привезло → кладовщик перебивает заново». (4) Новый отчёт **«Заявки снабжения без прихода на склад»** — видно, по каким заявкам привезённое так и не оприходовано (контроль разрыва). |
| 2026-07-17 | v2026.717.1605 | Дефектовка · Двигатели · Отчёты · Склад/BOM | **Прозрачность утиля** (запрос владельца): у каждой утильной позиции теперь фиксируется **причина** — в дефектовке (колонка рядом с «Утиль», подсказки типовых причин) и на двигателе целиком (поле при утильных статусах). Причина видна в акте дефектовки, печати карточки, заявке снабжения и отчётах — снимается боль «через месяц никто не помнит, почему в утиль». Новый отчёт **«Утиль: реестр с причинами»**: все утильные детали и двигатели с фильтрами по маркам/контрактам/контрагентам/датам и по **ветке замещения** (заказчик-давальческая / свой ремонт / закупка) — движение утиля видно с любого угла. Плюс: **нормы расхода УТД-20 и В-84** загружены из документов снабжения (новый BOM УТД-20 на 276 позиций, обновлён BOM В-84; материал и ГОСТ в карточках деталей) — фундамент будущего расчёта плана закупок для экономистов. |
| 2026-07-17 | v2026.717.729 | Интерфейс · «Мои экраны» (эскизник) · «Мои отчёты» | **Чтобы программу для завода делали не только программисты.** (1) «Мои экраны» стали **эскизником**: оператор на свободном холсте накидывает будущий модуль из блоков (кнопки, таблицы, поля, панели, вкладки), растягивает мышкой, связывает нитями (переход/данные/фильтр) и главное — **словами подписывает у каждого блока, что он должен делать**. Такой эскиз — это готовое техзадание: кнопка «Описание для разработчика» собирает его в текст, по которому нужный модуль строится быстрее и точнее. Есть отмена, дублирование, сетка, зум, готовые заготовки. Эскизы ничего не выполняют — это наброски. (2) Новый раздел **«Мои отчёты»**: берёшь любой из 14 готовых отчётов (двигатели, наряды, сотрудники, договоры, услуги…) как основу, накладываешь свои условия по любой колонке, выбираешь и переставляешь колонки, сортируешь — и получаешь свой отчёт с автоитогами; собранный рецепт сохраняется в **личный шаблон** (в следующий раз — один клик), печатается и выгружается в Excel. Для производства: любой мастер/технолог/снабженец может нарисовать, какой экран или отчёт ему нужен, и получить свою выборку данных без ожидания доработки — а понятный эскиз резко ускоряет саму доработку. |
| 2026-07-16 | v2026.716.1619 | Табель | **Распечатка табеля стала читаемой с расстояния и без лишнего.** Буква «Я» (явка) в ячейках больше не печатается — только часы, во всю ячейку, крупно; коды отпуска/больничного — того же размера, что цифры. Линии строк и столбцов почти чёрные — принтер их больше не «съедает», глазу видно, чья строка и какой день. Из двух окон печати осталось одно: настроил шрифты, галочками выбрал блоки (шапка/таблица/легенда/комментарии по сотрудникам — комментарии отдельным листом) — и сразу в печать. Для завода: бумажный табель на стену/в бухгалтерию читается без лупы, а комментарии «где был / что делал» печатаются вторым листом только когда нужны. |
| 2026-07-16 | v2026.716.1408 | Табель | **Табель стал пригоден как бумажный документ, а не только экранный.** Сотрудников можно выстроить по алфавиту одной кнопкой или в свой порядок (стрелки/перетаскивание) — порядок общий для всех, табельщице не приходится каждый раз выискивать людей в порядке добавления. Печать ужата под один лист А4 (альбомный): расстояния минимальные, размер шрифта каждого блока (шапка/ФИО/часы/числа/дни недели/легенда) настраивается отдельно, предпросмотр сразу показывает «влезло на 1 страницу или нет». Для завода: табель на стену/в бухгалтерию печатается одним листом с крупными цифрами — меньше бумаги и меньше ручной возни в предпечати. |
| 2026-07-15 | v2026.715.1549 (на проде) | Карточка двигателя · Наряд на сборку · Дефектовка | **Утильный двигатель стало чем оформить — и на него наконец выписывается наряд на сборку.** Не всякий двигатель уезжает отремонтированным: бывает, разобрали, продефектовали, признали неремонтопригодным, собрали обратно и вернули заказчику как утиль. Отметить это в программе было негде — неиспользуемый статус «Отправлен заказчику на перекомплектацию» (за всё время 0 записей) переоборудован в две честные метки: **«Признан утильным»** (ставится по итогам дефектовки) и **«Утиль — отправлен заказчику»** (по факту отгрузки), у каждой своя дата. Главное — рабочая преграда: с апреля утильная деталь в дефектовке блокирует выдачу наряда на сборку и отзывает уже выданный (защита от «собирать нечем»), а у утильного двигателя такая деталь есть **всегда** — то есть наряд на его сборку нельзя было выдать в принципе, и сборка шла мимо программы. Теперь метка «Признан утильным» выводит двигатель из-под этой защиты: наряд выдаётся, детали берутся с любого склада, **включая «Утиль»**. Для обычного двигателя защита работает как прежде (проверено вживую). Для производства: сборка утильного двигателя перестала быть невидимой для программы — по ней есть наряд, значит есть учёт работ и зарплата бригаде; в отчётах и по контракту видно, что двигатель уехал утилем, а не был отремонтирован. |
| 2026-07-15 | v2026.715.1549 (на проде) | Список двигателей · Отчёты | **Видно, по каким двигателям акт комплектности уже начали заполнять, а по каким ещё нет.** Раньше это можно было узнать, только открыв карточку двигателя и заглянув в список деталей — по 1583 двигателям это не проверишь. Теперь в списке двигателей своя колонка «Акт компл.»: зелёная галочка — акт заполняется, прочерк — к нему ещё не притрагивались. Считается просто: как только работник отметил хотя бы одну деталь «на месте», акт считается начатым. По колонке можно сортировать (сначала все без акта), а рядом появились два фильтра — по контрагенту и по акту. Вместе они дают ровно тот вопрос, который задаёт снабжение и ППО: **«у такого-то заказчика какие двигатели ещё без акта комплектности?»** — два клика вместо обхода карточек. Что осталось в списке после фильтра, можно **распечатать**: галочками отмечаешь нужные графы (например, только номер, марка и контрагент — без служебной привязки к договору) и печатаешь либо весь отфильтрованный список, либо только выделенные строки. И отдельная кнопка «Отчёт по двигателям» — такая же гибкая, как отчёт по нарядам: любые фильтры, свой набор колонок, сохранение шаблонов, печать/PDF/выгрузка. Для производства: план по дефектовке («что ещё не начато») собирается из списка, а не из памяти мастера. |
| 2026-07-15 | v2026.715.1549 (на проде) | Карточка двигателя · Дефектовка · Наряды · Акты · Отчёты | **Внутренний номер двигателя — клеймо, по которому безымянная деталь находит свой двигатель.** В карточке двигателя появился «Внутренний номер» — тот самый номер из журнала дефектовки, который набивают на детали без заводского номера. Номер вводит работник вручную (пока приходит «с земли»; позже программа начнёт выдавать его сама), год подставляется текущий. Программа складывает полный номер вида **41/26** и **не даёт** двум двигателям занять один номер в одном году — а тот же номер в следующем году разрешён, потому что в журнале нумерация каждый год начинается с единицы. Номер виден везде, где опознают двигатель: список (своя колонка и поиск), наряды, форма дефектовки, шапки актов, отчёты, QR-этикетка на тару с деталями. Ищется и коротким «41» (найдёт все 41-е по годам), и точным «41/26». **В «№ на детали» номер двигателя не подставляется** — эта графа только для личного номера детали от изготовителя, и она же ведёт поэкземплярный учёт ремфонда; вместо этого на вкладке «Детали и акты» видна подсказка-клеймо. Для производства: деталь без своего номера больше не теряет хозяина — по клейму находится двигатель, а связь «деталь ↔ двигатель» в отчётах и фильтрах держится программой независимо от того, что набито на металле. |
| 2026-07-15 | v2026.715.45 (на проде) | Интерфейс · «Мои экраны» (конструктор) | **Оператор сам собирает себе рабочий экран из блоков — без программиста (пилот).** В меню «Мой круг» появился раздел «Мои экраны»: конструктор с палитрой блоков (заголовок, текст, кнопка-переход, список), перетаскиванием порядка и живым предпросмотром. Кнопка настраивается на переход к любому доступному разделу/отчёту, список — на последние двигатели или наряды. Экран сохраняется в выбранный раздел доступа и виден всем, у кого есть к нему доступ (общий для завода, не только на своём компьютере); кнопки в закрытый для человека раздел — неактивны. Для производства: частому рабочему месту — свой быстрый экран-навигатор, меньше блужданий по меню. Пробная функция (уровень 1, без нейросети) — присматриваемся, что окажется удобным; уровень 2 (описал экран словами — программа собрала) отложен до доказанной потребности. |
| 2026-07-14 | v2026.714.1646 (на проде) | Карточка двигателя · Склад | **Паспорт ремонта двигателя одним взглядом + печать QR-этикеток на детали.** (1) **«История ремонта»** — новая вкладка в карточке двигателя показывает весь путь единицы лентой сверху вниз (приёмка, дефектовка, акты, передачи между цехами, ремонт, статусы деталей, наряды) со временем и ответственным. Раньше эти события были разбросаны по разным экранам — теперь видно, где двигатель был и что с ним делали, не собирая по крупицам. (2) **Печать QR-этикеток** — в Складе → Номенклатуре кнопка печатает лист этикеток с QR-кодом, наименованием, кодом и группой (обычный принтер, A4). В код зашит артикул — поиск в программе уже находит по нему деталь. Для производства: прослеживаемость истории ремонта по каждому двигателю (отчёт заказчику, дефект-история); маркировка деталей/ячеек под будущее сканирование на складе. |
| 2026-07-13 | v2026.713.2318 (на проде) | Наряд на сборку · Печать · Отчёты · Интерфейс | **Наряд на сборку заполняется только вручную, печать аккуратнее, вкладки не «залипают», у отчёта — свои шаблоны.** (1) **Сборочный наряд больше не подставляет весь список деталей марки** при выборе двигателя — раньше падал полный BOM, который приходилось вычищать; теперь строки добавляют вручную, кнопкой «Заполнить из спецификации» или шаблоном. (2) **Печать наряда**: «Приступить»/«Срок» пишутся месяцем словом («14 июля»), а колонки, пустые во всех строках (№ изделия, артикул), из распечатки исключаются — лист без пустых граф. (3) **Новый интерфейс**: закрытие карточки закрывает и её вкладку — исчезли «залипшие» вкладки, которые переоткрывали старую версию наряда и затирали правки. (4) **Отчёт «Наряды»**: наборы фильтров сохраняются как именованные шаблоны и применяются в один клик. Для производства: сборщик кладёт в наряд ровно то, что нужно; наряды печатаются чище; в новом интерфейсе не теряются данные из-за зависших вкладок; руководитель не настраивает отчёт заново каждый раз. |
| 2026-07-13 | v2026.713.1447 (на проде) | Наряды · Дефектовка | **Утильная деталь автоматически ставит наряд на сборку на паузу.** Реальный случай: во время сборки картер признали утильным — а наряд на сборку этого двигателя оставался «в работе», хотя собирать нечем. Теперь программа сама отзывает такой наряд из работы (с причиной «деталь признана утильной») и не даёт выдать его снова, пока утиль не заменят на годную деталь. При ручном отзыве оператор обязан указать причину. Для производства: наряд на двигатель с утильной деталью больше не «висит» ложно выданным — прогноз и загрузка бригад видят реальную картину, а когда придёт новый картер и наберут на него номер, наряд снова можно выдать в работу. |
| 2026-07-13 | v2026.713.1447 (на проде) | Отчёты · Наряды | **Отчёт «Наряды»: видно, что уже отгружено, и общие итоги.** Добавлена колонка «Отгружен» (дата отправки двигателя заказчику) и сводка в подвале: сколько нарядов выписано, выполнено, с просрочкой, просрочено, отозвано, отгружено и принято заказчиком — с разбивкой по маркам по желанию. Плюс новый статус «Отозван» (раньше отозванный наряд ошибочно числился «Просроченным»). Для производства: по одному отчёту руководитель видит и ход работ, и сколько двигателей ушло заказчику и принято — без ручного пересчёта. |
| 2026-07-13 | v2026.713.1447 (на проде) | Карточки · Справочники | **Изменения в карточках сохраняются только по кнопке.** Раньше карточка марки двигателя (список деталей) и справочники в админке писали правки в базу молча, по мере ввода — случайное касание меняло данные незаметно. Теперь всё сохраняется по кнопке «Сохранить», а при закрытии с несохранёнными правками программа спрашивает. Для производства: меньше случайных «тихих» изменений в справочниках марок и деталей, на которые опираются наряды и спецификации. |
| 2026-07-13 | v2026.713.1017 (на проде) | Отчёты · Наряды | **Отчёт «Наряды» снова показывает номер двигателя и заказчика.** После перехода на «двигатель в шапке наряда» (один раз, а не в каждой строке) у свежевыписанных нарядов в отчёте пустовали колонки «№ двигателя» и «Заказчик» — список и печать это уже учитывали, а отчёт отставал. Теперь отчёт берёт двигатель из шапки и подтягивает номер/марку/заказчика из справочника; старые наряды печатаются как раньше. Для производства: бухгалтерия и мастера снова видят в отчёте, чей двигатель и от какого заказчика по каждому наряду. |
| 2026-07-13 | прод-данные (без релиза) | Наряды · Доступ | **Операторы снова видят все наряды, кроме закрытых (личных).** У одного администраторского логина по ошибке стоял режим «владелец закрытых нарядов» — из-за этого ~270 его нарядов были скрыты от всех обычных операторов (в т.ч. от Фатыховой), хотя это обычные производственные наряды. Режим снят: теперь операторы видят все наряды на сборку/ремонт/изготовление, а закрытыми остаются только личные наряды Рамзии. Для производства: пропавшие из отчётов и списков наряды вернулись в общий доступ. | **Порядок в карточке детали.** (1) **Вложения — один раздел вместо трёх** (были Чертежи / Технология / Вложения): любой файл крепится в одно «Вложения», прежние файлы собираются в общий список сами при первом сохранении — оператору не нужно гадать, в какой из трёх разделов класть чертёж. (2) **Никаких выдуманных артикулов**: раньше деталь без артикула программа помечала техническим кодом «DET-…», который мешал в поиске; теперь графа честно пустая, настоящий артикул вписывается вручную. (3) **Удаление карточки детали убирает и саму деталь** — раньше могла остаться «деталь-невидимка» (в списках нет, а одноимённую завести нельзя — «такая уже есть»). Для производства: работа с деталями и их файлами предсказуемее, поиск чище. |
| 2026-07-10 | v2026.710.244 (на проде) | Интерфейс программы («Резиновый» — по умолчанию) | **Новый интерфейс «Резиновый» включается у всех после обновления; кому неудобно — возвращается на старый одной кнопкой, и программа запоминает выбор навсегда.** Что даёт оператору: колонки (меню · списки · карточки) тянутся и переставляются под свой монитор и привычки; до трёх открытых карточек вкладками и «две рядом» для сверки; **программа помнит, на чём остановился** — утром открывается с теми же карточками, что были вечером. Для производства: меньше кликов и «где я был?» после перерыва, быстрее сверка двух документов (двигатель+наряд, наряд+заявка). |
| 2026-07-10 | v2026.710.244 (на проде) | Черновики (товары/услуги) + прогноз сборки | **Правки карточек товаров и услуг теперь тоже подстраховываются черновиком** (сбой/случайное закрытие — несохранённое ждёт в «Черновиках»), как раньше наряды и заявки. **Прогноз сборки напоминает об открытых нарядах из прежних прогнозов** — защита от случайного дубля наряда на тот же дефицит. Плюс программа стала тише в фоне: свёрнутое окно не гоняет запросы к серверу попусту. |
| 2026-07-09 | v2026.709.1629 (на проде) | Акты приёмки/дефектовки (карточка двигателя) | **Акты можно настроить под свой завод — состав комиссии, кто утверждает, какие пункты проверять, и один раз задать шаблон на марку.** Раньше акт комплектности был жёстким: ровно три члена комиссии (нач. цеха / мастер / нач. ОТК), пять несменяемых пунктов «состояния при поступлении», «Утверждаю» только строкой внизу. Теперь: (1) **«Комиссия в составе» — свой список**: добавляйте/убирайте членов комиссии, у каждого своя должность и роль; кнопка «Заполнить по цеху» по-прежнему разом ставит стандартные роли по цеху двигателя, добавленных вручную не трогает. (2) **«Состояние при поступлении» — свой перечень пунктов**: добавить своё, убрать ненужное, переименовать — под порядок конкретного завода. (3) **Гриф «Утверждаю»** печатается в правом верхнем углу акта (как в наряде): выбор утверждающего (директор / технический директор / директор по качеству) + при желании свой сотрудник и должность; одинаково на комплектности и дефектовке. (4) **Поля ФИО/должностей стали шире**, а если текст не помещается — при наведении всплывает подсказка с полным текстом. (5) **Шаблоны актов по маркам**: настроил комиссию/гриф/пункты один раз, сохранил как шаблон марки — и на любом новом двигателе этой марки применяешь одним нажатием (список деталей не затрагивается). Для производства: акты оформляются так, как принято именно на этом заводе, комиссия и утверждающий проставляются быстро, а на серийных марках акт готовится в один клик. **На проде с v2026.709.1629.** |
| 2026-07-09 | v2026.709.1248 (на проде) | Акты приёмки/дефектовки · Наряд на сборку · Отчёты | **Акты двигателя приведены к рабочему виду, сборка и отчёты — удобнее.** (1) **Акт комплектности и акт дефектовки разведены по отдельным вкладкам** в карточке двигателя: у каждого свои колонки (приёмка — что привезли и что на месте; дефектовка — что в ремонт/утиль/замену), свои подписи и своя печать. Приёмщик и дефектовщик больше не спотыкаются об одну огромную таблицу на двенадцать столбцов. (2) **Печать актов по-человечески**: сверху видная строка «Акт … № <номер двигателя> (в. N) · от <дата> · Цех», строки плотнее и шрифт крупнее (больше на лист), а перед заполнением можно распечатать **пустой бланк** с перечнем деталей и пустыми клетками — комиссия заполняет от руки на месте, потом печатается готовый заполненный акт. (3) В акт приёмки добавлен блок **«Состояние при поступлении»** (упаковка/пломбы/повреждения/следы вскрытия — как в мировой практике приёмки чужого имущества на ремонт), опциональная строка **«Представитель заказчика»** и кнопка **«Заполнить комиссию по цеху»** (нач. цеха/мастер из цеха двигателя, нач. ОТК из базы — одним нажатием). (4) **Наряд на сборку**: двигатель выбирается один раз в шапке и сам проставляется во все строки; при выдаче наряда в работу двигателю **автоматически** ставится «Начат ремонт», при дате выполнения — «Отремонтирован» (не нужно вручную вести статус в карточке). (5) **Отчёт по двигателям**: фильтры по датам начала и окончания ремонта; во всех отчётах у каждого фильтра — «сброс» и «выкл» (временно исключить фильтр из отбора). Для производства: приёмка/дефектовка ведутся и печатаются как положено (в т.ч. заготовка-бланк для комиссии), приёмщик фиксирует состояние присланного двигателя на момент передачи (защита при спорах с заказчиком), сборщик не дублирует номер двигателя по строкам, а статусы ремонта двигателя ведутся сами из нарядов. **На проде с v2026.709.1248.** |
| 2026-07-09 | v2026.709.200 (на проде) | Оболочка интерфейса (весь клиент) | **Появился второй, опциональный вид интерфейса «Резиновый» — по желанию оператора.** Кнопкой в верхней панели (рядом с выбором темы) любой оператор может переключиться на новый трёхколоночный вид — слева свои кнопки-разделы (можно перетаскивать, закреплять нужные, скрывать лишние), посередине списки, справа карточки; колонки тянутся по ширине и сворачиваются, всё запоминается лично для него. В новом виде можно держать **несколько карточек открытыми вкладками** и **показать две карточки рядом** (кнопка «⑃») — сравнить два двигателя или свести наряд с заявкой бок о бок, обе редактируются. **Старый привычный интерфейс остаётся по умолчанию** и работает как раньше; переключение туда-обратно в один клик, ничего необратимого — это **бета**, для обкатки. Для производства: тем, кто работает с двумя-тремя карточками одновременно (сверка, дефектовка с оглядкой на спецификацию), новый вид экономит переключения; консервативным пользователям ничего менять не нужно. **На проде с v2026.709.200.** |
| 2026-07-08 | v2026.708.1553 (на проде) | Марки-детали / Глобальный поиск | **Раздача набора деталей на группу — под задачу, и поиск двигателя по номеру на детали.** (1) Кнопка **«Распространить на группу»** (карточка марки) теперь спрашивает **что** раздать и **как**. *Что:* весь список деталей марки, только детали акта комплектности, только акта дефектовки, или несколько **отмеченных** деталей (галочки прямо в строках списка). *Как:* «только добавить недостающие» (существующее у марок не трогать — безопасно; для актов просто проставить галочку у уже заведённых деталей), «обновить совпадающие и добавить», или «полное замещение» (список деталей марок станет точно таким же, лишнее удалить — под отдельным подтверждением). Технолог раздаёт не «всё или ничего», а ровно нужное: дособрать недостающее по группе, синхронно проставить акт комплектности на всю серию, или привести отставшие марки к эталону. (2) **Глобальный поиск находит двигатель по «№ на детали»** — по номеру, набитому на самой детали (не по сборочному номеру узла). Приёмщик/дефектовщик, держа в руках деталь с номером, одним поиском находит двигатель, в списке деталей которого этот номер записан. Находит и с разделителями, и без. Для производства: меньше ручного перебора при ведении применяемости деталей и мгновенный выход на нужный двигатель по физической метке детали. **На проде с v2026.708.1553.** | **Отчёт по нарядам под задачи цеха, честная просрочка, явное сохранение карточки, набор деталей на всю группу.** (1) **Отчёт по нарядам** переделан: печать аккуратным бланком как форма наряда, оператор сам выбирает колонки, фильтрует по любому полю (даты выдачи/срока/выполнения, статус, тип, ответственный, марка, № наряда, № двигателя, виды работ, **контрагент** — заказчик из контракта двигателя) и сортирует, в т.ч. по статусу; сводка выбранных фильтров сверху. (2) **Просрочка стала честной**: наряд с проставленной датой выполнения в срок больше не горит «просроченным» (розовым) — розовым только реально не сделанное к сроку. (3) **Карточка двигателя не сохраняет втихую**: изменения пишутся только по кнопке «Сохранить»/«Сохранить и выйти», при закрытии с правками программа спрашивает «сохранить или нет» — оператор не теряет работу и не сохраняет случайно «где набедокурил». (4) **Набор деталей акта — на всю группу марок разом**: технолог заводит перечень деталей (комплектность/дефектовка) на одной марке и кнопкой «Распространить на группу» копирует его на все марки группы, не перебирая каждую поимённо. (5) **Прогноз собирает из запасного варианта детали**, если основного нет на складе — меньше ложных дефицитов в заявках снабжения. **На проде с v2026.708.1139.** |
| 2026-07-07 | v2026.707.1412 (на проде) | Марки двигателей / Детали (применяемость) | **Деталь привязывается сразу к группе марок, а не к каждой поимённо.** Марки можно собрать в именованные группы («59-е», «В-серия») с описанием (марка — в нескольких группах). В карточке детали → «Применяемость» два способа привязать деталь ко всей группе: **«разом развернуть»** (сразу подставить все марки группы) и **«живая привязка»** — деталь **следит** за составом группы: добавили марку в группу позже → деталь применилась к ней **сама**, убрали марку → деталь **отвязалась** (симметрично; привязки, сделанные вручную, при этом не трогаются, они помечены «из группы»). В списке «Марки двигателей» добавлен фильтр по группе. Для производства: технолог задаёт применяемость детали одним действием на целую серию моторов и не пропускает марки — завёл новую марку в серию/группу, и все «групповые» детали к ней применились автоматически, без ручного перебора. **На проде с v2026.707.1412.** |
| 2026-07-06 | v2026.706.2335 (на проде) | Спецификация двигателя (BOM) / Прогноз / Сборочный наряд | **Спецификация марки стала гибкой и наглядной, взаимозаменяемые детали учтены правильно.** Раньше все марки загонялись в одну жёсткую схему деталей — а у моторов наборы разные (где два картера, где плита, где блок). Теперь у каждой марки **свой свободный список позиций**; если к позиции подходит **несколько взаимозаменяемых деталей** (например три вида поршня), они заводятся вариантами одной позиции, и один помечается **основным** — именно он идёт в прогноз потребности и в сборку, а запасные видны как замена. Прогноз считает по основному варианту (не задваивает спрос по всем сразу). Сборочный наряд заполняется деталями из спецификации **одной кнопкой** (основной вариант каждой позиции) — сборщику не нужно набивать перечень руками; а если у позиции есть взаимозаменяемые варианты, прямо на строке наряда стоит **переключатель «Вариант»** — сборщик отмечает, какую именно деталь фактически поставил, не разыскивая её по всему справочнику. Редактор спецификации переработан на «позиции и варианты», убрана путаная «глобальная схема связей». Для производства: технолог ведёт спецификацию каждой марки как есть на самом деле, снабжение видит реальную потребность, сборка получает готовый перечень и фиксирует фактически использованный вариант. **На проде с v2026.706.2335.** |
| 2026-07-05 | v2026.705.2235 (на проде) | Заявки в снабжение (карточка) / Черновики | **Незаконченная заявка больше не теряется.** Заявка в снабжение теперь ведёт себя как наряд: пока оператор её заполняет, программа каждые пару секунд втихую сохраняет черновик, и если компьютер выключат, программа обновится или свет пропадёт — при следующем входе всплывёт предложение восстановить незаписанную заявку. Есть и кнопка «Сохранить как черновик» — отложить заявку, не пуская её в документооборот (не занимая номер); все отложенные лежат в разделе «Черновики», откуда открываются и достаются. Для производства: снабженец не теряет наполовину набранную заявку из-за случайного закрытия и может спокойно готовить крупную заявку в несколько заходов. |
| 2026-07-05 | v2026.705.2235 (на проде) | Настройки (резервные копии) | **Раздел резервных копий видят только те, кому положено.** Секция «Ночные резервные копии базы» в Настройках и кнопка ручного запуска бэкапа теперь показываются только сотрудникам с соответствующим правом; у остальных раздел скрыт. Побочно убран «шум» в журнале критических событий — раньше у любого, кто открывал Настройки, фиксировался отказ доступа к бэкапам. Для производства: меньше путаницы у операторов (не видят кнопок, которые им всё равно недоступны) и чище журнал безопасности для владельца. |
| 2026-07-05 | v2026.705.2235 (на проде) | Прогноз сборки / Снабжение / Ремфонд | **Прогноз стал советником снабжения: «это отремонтируй, это закупи».** (1) По каждой нехватающей детали прогноз теперь смотрит и в **ремонтный фонд**: «не хватает 30 — в ремфонде 22, выдай ремнаряд; остальные 8 закупи». Видно, где дефицит закрывается своими силами, а где реально пора покупать. (2) Кнопка **«Создать заявку в снабжение»** прямо под прогнозом — заявка открывается уже заполненной позициями закупки (без того, что закрывается ремонтом), снабженцу остаётся проверить и подписать. (3) На карточке двигателя горит предупреждение **«Не занесено в ремфонд: X шт.»**, если по дефектовке есть годные к ремонту детали, которые оператор забыл занести в фонд — детали больше не «выпадают» из учёта. Попутно проведён полный аудит всей цепочки движения деталей (наряды → склад → ремфонд → дефектовка → прогноз): подтверждено, что документы стыкуются корректно и каждый приход на складе прослеживается до наряда-источника. |
| 2026-07-05 | v2026.705.2235 (на проде) | Марки двигателей (карточка) | **В карточке марки с сотнями деталей теперь можно искать, фильтровать и печатать нужный срез.** Поле фильтра (по названию, артикулу, узлу) со счётчиком «N из M»; режимы отображения: Все / Комплектовка (детали акта комплектности) / Дефектовка / **По узлам** (детали сгруппированы по сборочным единицам, группы разворачиваются); кнопка печати печатает ровно текущий срез, а не все 600 строк. Для производства: технолог/приёмщик находит деталь в марке за секунды и печатает, например, только комплектовку конкретного узла. |
| 2026-07-05 | v2026.705.2235 (на проде) | Поиск (все списки) | **У списков одно поле поиска — верхнее, и оно ищет вглубь карточек.** Нижнее окно поиска (появившееся прошлым релизом) убрано как дублирующее; его умение искать **внутри карточек** перенесено в привычное верхнее поле фильтра и добавлено в списки контрагентов, договоров, марок, услуг и справочников. Для операторов: одно понятное поле в каждом списке — фильтрует и по колонкам, и по содержимому карточек, с исправлением раскладки. |
| 2026-07-04 | v2026.704.951 (на проде) | Поиск (все списки + глобальный) / Безопасность данных | **Найти стало быстро, потерять — трудно.** (1) У списков «Двигатели», «Наряды», «Сотрудники», «Заявки» внизу появилось поле поиска: печатаешь запрос — список сразу сужается до карточек, где это встречается, причём программа ищет и **внутри карточек** (по всем заполненным полям), а не только по видимым колонкам; перепутанная раскладка (набрал «фвы» вместо «abc») исправляется сама. Остальные списки получат такое же поле следующими обновлениями. (2) **Глобальный поиск (Ctrl+K)** теперь находит и сами разделы/кнопки программы («инвентаризация» → «Меню → Склад → Инвентаризация», клик открывает раздел), у каждого результата подписан путь, а совпадения из глубины карточек выводятся отдельной группой. Для производства: оператор ищет деталь/наряд/раздел одним движением, новичок находит нужный экран по слову, не зная меню. (3) **Локальная база на каждом компьютере зашифрована** (ключ привязан к машине): потерянный/украденный ноутбук больше не означает утечку всей производственной базы — файл нечитаем вне своей машины. Включилось само, работа операторов не изменилась. |
| 2026-07-03 | v2026.703.2147 (на проде) | Двигатели (карточка / список / приёмка) | **Карточка двигателя стала вкладками; появились рекламации и повторные заезды.** (1) Карточка больше не «полотенце»: четыре вкладки — Основное, Детали и акты, Фото и документы, Рекламация; сохранение как раньше — одним действием при закрытии. (2) **Рекламационный учёт**: двигатель, вернувшийся по претензии, принимается кнопкой «Принять по рекламации» на той же карточке — фиксируются дата приёмки, причина со слов заказчика, вердикт после разбора (наша вина / вина заказчика / не подтвердилось), статус ремонта и дата отправки обратно; в списке такой двигатель помечен синей точкой, кнопка «Рекламационные» показывает только их — бухгалтерия с одного взгляда видит рекламационный парк. (3) **Повторный заезд**: двигатель с уже известным номером теперь можно завести повторно — программа при вводе номера предлагает три пути (рекламация / тот же двигатель на новый ремонт / другой двигатель с совпавшим номером); у каждого заезда свой заказчик, договор и затраты, старый заезд помечается «архивный», случайные дубли по-прежнему блокируются. Для производства: претензии заказчиков перестали быть устным спором — есть письменный след с вердиктом вины; повторные приезды двигателей оформляются без переоткрытия старых карточек и смешения договоров. **На проде с v2026.703.2147** (тем же релизом уехал фикс подстановки имён деталей в нарядах). |
| 2026-07-03 | v2026.703.1446 + v2026.703.1539 (на проде) | Наряды / печать (сборочный наряд) | **Печатная форма наряда на сборку приведена в порядок по замечаниям с производства.** В шапке печатного наряда теперь стоят даты «Приступить» и «Срок» (без года) и цех, полная дата создания — слева напротив «Утверждаю»; любой реквизит можно убрать с распечатки галочкой. Реквизиты разложены двумя ярусами (наряд / двигатель и заказчик) — длинное название заказчика больше не растягивает бланк за края листа. Исправлена давняя болячка: на печати стояла «застрявшая» неправильная дата — теперь всегда печатается реальная дата наряда. Цех печатается коротко («Цех №1»), из блока подписей убрана лишняя строка «Прогнозируемая дата выполнения». Отдельно: при применении **шаблона наряда** цех из шаблона автоматически проставляется как склад-источник в каждую строку деталей (откуда брать деталь) — с возможностью поменять склад построчно. Для производства: наряд на сборку печатается аккуратным бланком на один лист с правильными датами, цехом и источником деталей; меньше ручной правки оператора. **На проде с v2026.703.1446 и v2026.703.1539.** _(Мелкий доп-фикс — надёжная подстановка названия детали при применении шаблона — смержен, поедет ближайшим релизом.)_ |
| 2026-07-03 | прод-данные (без релиза) | Марки двигателей → Акт комплектности | **Акты комплектности печатаются по утверждённому заводскому составу.** По списку руководства во всех 8 марках (В-59 УМС, В-84, В-84 АМС, В-84МБ-1С, В-84 ДТ, В-46-5С, В-46-2С1, В-46-1) деталям проставлены галочки «В акт комплектности» — 304 позиции с точными Сб.-номерами узлов. Для производства: акт комплектности нового двигателя сразу содержит правильный перечень узлов своей марки, приёмщику не нужно добавлять строки вручную. |
| 2026-07-02 | v2026.703.1049 (на проде, активировано 2026-07-03) | Персонал и доступ (доступы по разделам) | **Гибкое управление «кто что видит и правит» — по разделам программы.** Новая страница «Доступы по разделам» (Персонал и доступ, доступна только владельцу): программа разбита на 10 разделов (Производство, Наряды, Наряды закрытые, Снабжение, Склад, Договоры и контрагенты, Персонал, Отчёты, Справочники, Администрирование), у каждого — два списка людей: **наблюдатели** (видят всё, не меняют) и **редакторы** (полный доступ). Кто не добавлен — раздела не видит вовсе: плитки нет в меню, и программа отказывает в чтении данных даже в обход меню. То же самое видно и правится в карточке сотрудника. При включении все сотрудники автоматически разложатся по разделам из своих текущих ролей — в первый день ни у кого ничего не изменится, дальше владелец правит списки в пару кликов. Для производства: конфиденциальные области (договоры, финансы, чужие наряды) можно точечно закрывать/открывать конкретным людям без программиста. **На проде с v2026.703.1049, списки активированы 2026-07-03** (все 21 сотрудник разложены из текущих ролей — в первый день поведение не изменилось); добавлен и запрет записи: наблюдатель не может ничего изменить ни через кнопки, ни в обход интерфейса. |
| 2026-07-02 | v2026.703.1049 (на проде) | Обновления программы | **Обновление будет скачиваться в ~10 раз быстрее и легче.** Починен механизм «дельта-обновлений»: раньше при выходе новой версии многие компьютеры качали полный установщик (~116 МБ) даже когда реально изменилось ~10 МБ — теперь клиент умеет готовить «топливо» для дельты из того установщика, что у него уже есть, независимо от того, откуда тот был скачан (сервер/Яндекс/GitHub). Для производства: обновления на рабочих местах проходят заметно быстрее и не забивают заводской интернет-канал. Эффект проявится начиная со второго релиза после этого. |
| 2026-07-02 | прод-данные (без релиза) | Склад → Номенклатура + BOM сборки | **Нормативы расхода запчастей на капремонт В-59УМС и В-84АМС теперь в программе.** Заводской список «Нормы расхода запасных частей при КР» (685 позиций) загружен в справочник номенклатуры без дублей: 232 новые детали, у остальных дозаполнены артикулы/группы; крепёж, РТИ, подшипники и электрооборудование разложены по своим группам (удобно фильтровать). В спецификации (BOM) марок В-59 и В-84 — полный состав с количеством каждой детали на двигатель (553 и 547 строк), у каждой строки помечен узел двигателя («Группа 301 Картер» и т.п.). Для производства: прогноз сборки и заявки снабжения могут опираться на реальные заводские нормативы, а не на пустую спецификацию; кладовщик находит деталь по артикулу из бумажного списка. |
| 2026-07-02 | v2026.702.1024 (на проде) | Наряды (карточка) + Прогноз сборки | **Наряд закрывается одним заходом; прогноз честно предупреждает.** (1) В карточке наряда поле «Дата выполнения» (у закрытых — отметка «Завершён») теперь стоит вплотную к кнопке закрытия наряда, а не в другом конце шапки — оператор указывает дату и тут же закрывает, меньше нарядов, закрытых с забытой датой (директива руководства «рядом»). (2) Прогноз сборки перестал молча терять данные: если спецификация (BOM) не привязана ни к одной марке двигателя или в строке указано дробное «кол-во на двигатель», прогноз прямо предупреждает об этом — мастер видит, почему марки/детали нет в расчёте, вместо тихой недостачи. **На проде с v2026.702.1024.** |
| 2026-07-01 | v2026.701.1708 (на проде) | Двигатели (список / отчёт по отгрузке) | **Дата отгрузки в списке двигателей совпадает с карточкой.** У части двигателей (из старой февральской загрузки) в списке двигателей дата отгрузки застревала на старом значении, хотя в карточке её правили — программа в списке брала отдельное «загруженное» поле даты, а карточка пишет дату в статус «Отправлен заказчику». Теперь список (и отчёт по состоянию отгрузки) показывают именно то, что отмечено в карточке. Переходный момент: у двигателей, где отгрузка ещё не отмечена в карточке, список пока показывает старую загруженную дату — она обновится, как только оператор один раз отметит «Дату отгрузки» в карточке двигателя (у двигателей, где отметка уже стояла и расходилась, список исправился сам). Заодно так же приведена в порядок отметка «Утиль». Для производства: список двигателей и отчёты по отгрузке отражают реальные действия оператора, без «застрявших» старых дат. **На проде с v2026.701.1708.** |
| 2026-07-01 | v2026.701.1437 (на проде) | Наряды (доступ / приватность) + вход | **Личные наряды работника видит только он, бухгалтерия и администратор; при входе пароль без подсказок.** По решению руководства наряды, которые ведёт **Рамзия (Валиева)**, теперь: (1) в общем списке нарядов и в отчётах у прочих операторов **не показываются** — их видят только сама Рамзия, **главбух (Купцова)** и суперадминистратор; (2) сама Рамзия видит в списке **только свои** наряды, чужие ей не показываются; (3) **редактировать** её наряд может только она сама и суперадмин (сервер режет чужую правку, даже если наряд каким-то образом виден). Прочие наряды видны всем как раньше. Отдельно: на экране входа поле **пароля больше не показывает подсказки** с ранее введёнными паролями (их мог подсмотреть сосед) — «глазок» показа при наборе оставлен. Примечание по устройству: полная база по-прежнему приходит на каждый компьютер (чтобы любой оператор мог зайти под своим логином на любой машине и увидеть своё), а разграничение — на уровне отображения по авторизованному пользователю; это осознанный выбор руководства (удобство «оператор на любой машине» важнее сокрытия данных на диске). Для производства: приватность нарядов конкретного работника соблюдается на уровне интерфейса и правки; пароли операторов не утекают через подсказки. **На проде с v2026.701.1437.** |
| 2026-06-30 | смержено в main (поедет ближайшим клиентским релизом) | Наряды (карточка / список) | **Сборочный наряд понятно закрывается, список фильтруется по типу, дата выполнения честная.** (1) На наряде **на сборку** кнопка проводки теперь явно подписана **«Наряд выполнен — провести»** — оператор видит, что закрывает наряд (раньше функция была, а понятной кнопки «выполнен» на сборке не было). (2) В списке нарядов — **выбор типа** (Все / Обычный / Ремонт / Сборка / Изготовление) прямо над списком, без отдельных экранов-кнопок (поиск и так искал по содержимому наряда — деталям/операциям/исполнителю). (3) Поле **«Дата выполнения»** теперь пустое, пока оператор не поставит дату (раньше показывало сегодня и «не стиралось»), а **дата создания** наряда зафиксирована (как номер) и не меняется. Для производства: сборщик однозначно завершает наряд, мастер быстро отбирает нужный тип нарядов, дата выполнения отражает реальность. **Смержено в main; на проде — со следующим клиентским релизом.** |
| 2026-06-30 | v2026.630.1141 | Карточки / Наряды / Заявки / Справочники (черновики и защита данных) | **Несохранённые изменения больше не теряются, и нет пустых карточек-призраков.** Несколько связанных улучшений: (1) пока оператор правит наряд, программа сама делает черновик-снимок — при сбое, отключении питания или закрытии без сохранения данные не пропадают: при следующем входе всплывает окно «Восстановить / Отклонить / Позже». (2) При закрытии карточки с изменениями теперь явный выбор «Сохранить / Не сохранять / Оставить черновик» (раньше через 10 секунд изменения сохранялись принудительно, даже если оператор не хотел). (3) Новый раздел «Черновики» (Контроль и аналитика) — все несохранённые карточки в одном месте; и кнопка «Сохранить как черновик» — отложить наряд, не проводя его. (4) Новые **наряд, заявка, контрагент, марка двигателя и договор** больше не создают пустую запись в базе: запись/номер появляются при первом сохранении, а брошенная незаполненная карточка не попадает в список и не разъезжается по другим компьютерам (не плодятся «призраки»). Для производства: меньше потерь работы при сбоях, чище списки нарядов/заявок/справочников, понятный контроль над сохранением. **На проде с v2026.630.1141.** |
| 2026-06-29 | v2026.629.1711 | Склад / Наряды (детали по артикулу) | **Деталь снова находится в наряде по своему артикулу.** Из-за «зеркал» старого переноса номенклатуры деталь (например «Насос водяной» 411-00-35А) была видна в «Ревизии ремфонда», но не подбиралась в наряд на ремонт — поиск шёл по внутреннему авто-коду (DET-…), а не по настоящему артикулу. Теперь ищется по реальному артикулу и там, и там, а одинаково названные позиции («Насос водяной») различаются по коду прямо в списке. Заодно устаревший тип номенклатуры **«Изделие (legacy)» переведён в «Деталь»** для всех таких позиций (256 деталей — гильзы, блоки, валы, вкладыши) — одна истина по типам, без «костыля». Для производства: оператор находит нужную деталь в наряде по артикулу, не путается среди одноимённых, не упирается в «деталь есть в ремфонде, а в наряд не добавить». **На проде с v2026.629.1711.** |
| 2026-06-29 | v2026.629.1711 | Наряды / печать | **В шапке наряда можно выбрать, кто утверждает.** Гриф «Утверждаю» теперь переключается между директором (И.А. Тихомиров) и техническим директором (В.И. Гурьянов) — на каждый наряд отдельно. Для производства: наряд печатается с правильным утверждающим лицом без ручной правки. **На проде с v2026.629.1711.** |
| 2026-06-29 | v2026.629.1711 | Обслуживание базы / Карточки | **Уборка пустых карточек и профилактика их появления.** В «Контроль и аналитика → Пустые карточки» — поиск и безопасное удаление пустых автосозданных карточек/документов (с проверкой ссылок, обратимо). И на будущее: при создании двигателя или сотрудника пустая карточка больше не сохраняется, пока в неё ничего не внесли. Для производства: база чище, не копятся «карточки-призраки», по которым потом ищут и путаются. **На проде с v2026.629.1711.** |
| 2026-06-29 | v2026.628.2326 | Двигатели / Акты (комплектность → дефектовка) | **Дефектовка теперь идёт строго от комплектности.** Раньше деталь можно было разметить в дефектовке (утиль/замена), даже если в акте комплектности не отмечено, что она вообще есть на двигателе — программа по умолчанию считала её присутствующей. Теперь так нельзя: пока деталь не отмечена «на месте» в комплектности, её колонки дефектовки (утиль/замена) заблокированы (серым, с подсказкой); снял отметку наличия — разметка дефекта очищается. Базовые (отслеживаемые) детали ждут ручной отметки оператора, прочая мелочёвка считается на месте по умолчанию. Для производства: исключены «фантомные» дефекты по деталям, которых на двигателе нет; соблюдается правильная последовательность осмотра — сперва проверь наличие, потом размечай дефект. **На проде с v2026.628.2326.** |
| 2026-06-29 | v2026.628.2326 | Склад / Номенклатура / Двигатели | **Переименовал деталь — новое имя и артикул видно везде.** Раньше если в «Склад→Номенклатура» поменять у детали наименование или артикул, в карточке марки двигателя и в карточке двигателя она оставалась под старым именем (программа брала имя из другого места). Теперь имя и артикул во всех карточках берутся из карточки номенклатуры — что поправил, то и видно; уже «разъехавшиеся» детали починились сами после обновления, ничего пересохранять не нужно. Для производства: нет путаницы «одна деталь под двумя именами», подбор деталей и акты используют актуальное наименование/артикул. **На проде с v2026.628.2326.** |
| 2026-06-29 | v2026.628.2326 | Склад / Ремонтный фонд (личные номера) | **Отметка «деталь отремонтирована» по личному номеру.** В реестре личных (набитых) номеров на карточке двигателя у каждой детали, ожидающей ремонта, появилась кнопка «✓ Отремонтирована» (и обратная «↩ В фонд»). Мастер вручную отмечает, что конкретный номерной экземпляр отремонтирован и вышел из фонда — точно по его номеру, без догадок «какой из нескольких». Для производства: видно поштучно, какие именно номерные детали уже отремонтированы, а какие ещё ждут ремонта. **На проде с v2026.628.2326.** |
| 2026-08-04 | v2026.804.1200 | Наряды (сборка) / Установка программы | **Два наряда на один двигатель больше не заведёте.** Раньше на двигатель, который уже стоит в наряде на сборку, можно было молча выписать второй — карточка открывалась как обычно, и о двойнике узнавали, когда сходились итоги: работа, комплектовка и списание оказывались задвоены. Теперь программа проверяет двигатель **до** создания наряда: если на него уже есть действующий наряд на сборку, вместо новой карточки появляется сообщение с номером, датой и статусом существующего и кнопкой «Открыть наряд №N». Проверка стоит на всех трёх путях, которыми наряд заводят: правой кнопкой в списке двигателей, выбором двигателя в шапке карточки и созданием из прогноза сборки. Если прежний наряд уже выполнен или отозван — двигатель считается свободным (повторный приход в ремонт это норма), программа только предупредит. Для производства: задвоенная сборка не попадает в учёт вообще, а не вычищается задним числом; мастер сразу видит, у кого двигатель в работе. **Установка стала компактнее:** все файлы программы теперь лежат в одной папке — антивирусу достаточно один раз разрешить её, чтобы он перестал придираться к обновлениям (раньше файлы были разбросаны по четырём местам, и накрыть их одним правилом было невозможно). |
| 2026-06-28 | v2026.628.1814 | Склад / Ремонтный фонд | **Появился учёт ремонтного фонда — деталей, ожидающих ремонта.** Раньше ремфонд в программе никак не вёлся: после дефектовки деталь «уходила в пустоту», в фонд ничего не записывалось. Теперь ремфонд — это отдельный склад «Ремонтный фонд», где числятся **присутствующие и годные к ремонту** детали (не утиль). Три способа работы: (1) **«Ревизия ремфонда»** в меню «Склад» — занести список деталей с количеством напрямую (для первичной описи того, что уже лежит); (2) **кнопка на карточке двигателя** — занести в фонд годные к ремонту детали прямо из дефектовки (повторное нажатие не задваивает, правка дефектовки корректирует фонд); (3) при **закрытии ремонтного наряда** отремонтированные детали автоматически списываются из фонда. Для производства: видно, сколько и каких деталей ждут ремонта, фонд перестаёт быть «бездонной ямой», движение «дефектовка → ремонт → готово к сборке» отражается в остатках. Дополнительно: детали с **личным (набитым) номером** на корпусе теперь учитываются поштучно — за каждой закреплено, с какого двигателя снята и её судьба (в ремонт / в утиль / под замену). По двигателю можно распечатать **«Требование к заказчику»** — перечень деталей, ушедших в утиль или замену, как обоснование, что мы поставили свои детали и цена ремонта выросла (документ для «дела двигателя», с версионированием каждой печати). **На проде с v2026.628.1814.** |
| 2026-06-28 | v2026.628.1814 | Наряды (карточка) | **Дату выполнения наряда теперь ставит оператор.** Раньше дата завершения проставлялась автоматически в момент нажатия «Закрыть» — если работу сделали вчера, а отметили сегодня, в программе стояло сегодня (расхождение замечали на сборочных нарядах). Теперь в открытом наряде есть поле «Дата выполнения» (по умолчанию сегодня, можно поставить прошлую дату — когда работа реально завершена); после закрытия дата показывается как «Завершён». Эта дата идёт в статус наряда (просрочка) и в период зарплатного отчёта. Для производства: фактическая дата выполнения соответствует реальности, корректнее просрочки и привязка работ к периоду зарплаты. На дату складских движений не влияет. **На проде с v2026.628.1814.** |
| 2026-06-27 | v2026.627.2226 | Наряды (список / карточка) | **Список нарядов нагляднее, карточка удобнее в работе.** В списке колонка «Исполнители» теперь показывает, **кто принял наряд в работу** (начальник цеха из подписей; если приняли двое — первого), а если это ещё не отмечено — как раньше, фамилии бригады. Колонки дат приведены в порядок: «Дата выдачи» переименована в понятную «Дата создания наряда», добавлена «Начало работ» (когда приступить) — теперь в списке видны все ключевые даты наряда (создание / начало / срок / завершение) и по любой можно отсортировать кликом по заголовку; лишняя колонка «Ответственный» убрана. В карточке наряда **кнопки управления (Сохранить / Распечатать / Закрыть / Удалить) и блок с номером, датами и шаблоном закреплены сверху** — при прокрутке длинного наряда не уезжают за край, всегда под рукой. Для производства: мастеру/бригадиру с одного взгляда видно, кто взял наряд в работу и к какому сроку; в длинном наряде не нужно прокручивать вверх, чтобы сохранить или распечатать. Доработка по следам обкатки большого обновления нарядов (v2026.627.1912). |
| 2026-06-27 | v2026.627.1912 | Наряды (карточка / шаблоны / список / отчёт) | **Большое обновление работы с нарядами — удобнее выписывать, вести и контролировать.** **Подписи:** теперь переставляются стрелками вверх/вниз (и этот порядок выходит на печать как нужно), поле выбора сотрудника шире (видно, кого выбрал), должность — отдельной строкой; свои формулировки ролей («Наряд выдал бригадир») запоминаются и предлагаются дальше, а в пустом поле программа подсказывает последних выбранных людей — меньше ручного набора и путаницы у оператора. **Шаблоны нарядов:** в шаблон добавлен цех (где взять деталь) и готовая конфигурация подписей с фамилиями — применил шаблон, и наряд сразу заполнен ролями и людьми. **Сроки и статусы:** у наряда появились даты «приступить к работе» и «срок исполнения», по ним наряд сам показывает статус — Выдан / Выполнен / Просрочен / Выполнен с просрочкой. **Список нарядов** переделан: колонки под себя (дата выдачи, №, срок, виды работ, марка и № двигателя, дата завершения, ответственный), строки подсвечиваются по статусу (жёлтый — выдан, зелёный — выполнен, красный — просрочен), есть колонка «кто принял наряд». **Отчёт по нарядам:** рядом с созданием нарядов — кнопка «Отчёт по нарядам»: слева фильтры (по датам, статусу, ответственному, марке), справа живой предпросмотр, готовый отчёт можно распечатать. Для производства: бригадир/мастер видит сроки и просрочки с одного взгляда, понятно кто за наряд отвечает, а руководитель может быстро собрать сводку по нарядам за период без похода в раздел отчётов. |
| 2026-06-26 | v2026.626.1229 | Двигатели / Акт дефектовки | **Видно, кто разбирал двигатель — прямо в акте дефектовки.** Раньше исполнитель разборки нигде не фиксировался в программе. Теперь в акте дефектовки есть поле «Разборку двигателя произвёл» — оператор выбирает одного или нескольких сотрудников, и их имена с должностями печатаются в акте. Для производства: акт становится полным документом с указанием ответственных за разборку — важно при разборе качества и в случае вопросов. **Поиск и слияние дублей двигателей.** В списке двигателей появилась кнопка «Поиск дублей» (аналогично дублям деталей): программа автоматически находит двигатели с одинаковым или очень похожим номером, показывает группы (точные и похожие), и оператор выбирает «выжившего» — остальные записи сливаются (операции, атрибуты переносятся, дубли помечаются слитыми). Для производства: база двигателей чище, нет путаницы «два разных двигателя с одним номером». **Поле номера двигателя стало шире** — полный длинный номер теперь виден целиком без обрезания. Подсказка «похожий двигатель уже есть» всплывает только при вводе нового двигателя, не мешает при открытии карточки существующего. |
| 2026-06-25 | v2026.625.1722 | Наряды / печать | **Наряд снова уверенно ложится на один лист, таблицы — с видимой сеткой.** Блок подписей раньше прижимался к нижнему краю листа и на полном наряде съезжал на вторую страницу — теперь подписи идут сразу за телом наряда (с небольшим отступом), наряд «тянется» кверху, и всё помещается на один лист А4 (вверху окна печати по-прежнему горит «✓ Помещается на 1 лист»; если наряд очень большой — уменьшить шрифт разделов стрелками). Таблицы **«Виды работ / наименование изделия»** и **«Бригада»** печатаются с такими же чёткими линиями-рамками, как таблица реквизитов наряда (№, дата, марка…) — сетку хорошо видно на бумаге. Поля листа жёстко заданы (А4, 12 мм), поэтому предпросмотр совпадает с печатью один-в-один. Для производства: распечатанный наряд выглядит как аккуратный бланк — один лист, читаемые таблицы, подписи на месте; меньше возни оператора с тем, что «уехало на второй лист». (В этот же релиз вошло служебное упрочнение безопасности на стороне сервера — в работе операторов ничего не меняет.) |
| 2026-06-25 | v2026.625.726 | Наряды / печать / подписи | **Подписи на наряде — по реальной цеховой процедуре, и весь наряд на одном листе.** Раньше под нарядом был один общий блок подписей. Теперь их два — отражают, как наряд реально ходит по заводу: **«Выдача наряда»** (выдал ПДО → согласовал ОТК → принял в работу начальник цеха) и **«Завершение наряда»** (сдал начальник цеха → принял ОТК → принял ПДО). Все шесть ролей уже подставлены на бланк — оператору остаётся выбрать людей; распечатанный наряд сразу выходит со всеми строками под живые подписи, как на бумаге. Печатную форму уплотнили (блоки подписей встали рядом) — полный наряд снова **помещается на один лист А4**. Заодно настройку печати сделали удобнее: размеры шрифта меняются стрелками, «шапку» (гриф + заголовок) можно укрупнить целиком одной кнопкой, а понравившуюся настройку **сохранить шаблоном** и применять одним кликом (или задать умолчание для вида наряда). Для производства: наряд оформляется по понятной цепочке ответственности ПДО↔ОТК↔цех, печать предсказуемая (видно, что ляжет на лист), меньше ручной возни оператора с настройкой каждый раз. (Шаблоны печати пока хранятся на конкретном компьютере.) |
| 2026-06-24 | v2026.624.49 | Наряды / печать (сборка) | **Наряд на сборку — чище и по делу.** Раньше в печатном наряде на сборку марка двигателя, его номер и «Сборка двигателя» повторялись в каждой строке, а колонки «Цена»/«Сумма» загромождали лист. Теперь двигатель и вид работ стоят один раз в шапке, а у каждой детали показан **склад — откуда её взять, где она лежит готовая**; внизу — подписи: «Наряд выдан» (директор / начальник цеха / ОТК) и «Двигатель собран, обкатан и признан годным к отправке заказчику» (те же трое). Для производства: сборщик с одного листа видит, что собирать и с какой полки брать деталь; выдача наряда и приёмка готового двигателя оформляются подписями прямо на бланке. Касается только наряда на сборку. |
| 2026-06-23 | v2026.623.1642 | Надёжность клиента / Двигатели / Доступ | **Программа сама себя поднимает.** Рядом с клиентом теперь работает фоновый «сторож»: если программа повредится или её случайно снесут — он сам поставит её обратно, без вызова мастера и без простоя рабочего места; у администратора появилась кнопка «Переустановить» для удалённого ремонта проблемного компьютера. Для завода: меньше простоев рабочих мест из-за «слетевшей» программы. (Сторож без подписи — первые дни возможны предупреждения антивируса.) **В списке двигателей — столбец «Привязка»:** с одного взгляда видно, к чему привязан двигатель — 🟢 договор, 🟢🟢 ДС, 🔴 не привязан (можно отсортировать и собрать всех «бесхозных», которых надо оформить). **Доступ к договорам/контрагентам стал точечным:** раньше их правил только администратор — теперь это отдельное право, которое выдаётся конкретному сотруднику, не открывая ему прочие справочники (например, договорнику — да, технологу — нет). Также устранён шторм сбоев синхронизации на парке компьютеров (стало стабильнее). |
| 2026-06-22 | v2026.622.1241 | Прогноз сборки / Наряды | **Прогноз сборки двигателей стал честнее — перестал завышать план.** Раньше он мог обещать больше, чем реально соберём: при выборе склада-фильтра один и тот же остаток мог посчитаться дважды, а будущим приходом «с ремонта» считался любой незакрытый ремонтный наряд — даже брошенный черновик. Теперь служебные склады (ремфонд, утиль, «в сборке») не идут в «годное для сборки» ни при каком фильтре, а у ремонтного наряда появилась кнопка **«Выдать в работу»** — прогноз считает приходом только выданные наряды. Для производства: план сборки опирается на реальные детали, а не на фантомы; мастер сам помечает, что реально в ремонте, — это переход на честный учёт ремфонда. **Переходный момент:** сразу после обновления прогноз по ремонту пуст, пока мастера не пройдутся по реально-ремонтируемым нарядам и не нажмут «Выдать в работу». |
| 2026-06-22 | прод-данные (без релиза) | Персонал / Цеха | **Цех №2 укомплектован в программе.** В справочник сотрудников добавлены 27 рабочих Цеха №2 (13 новых + 4 переведены из Цеха №1), без учётных записей — простые работники для табеля, нарядов и подписей. Для производства: табельщик может вести Цех №2 кнопкой «Добавить весь цех», а наряды и подписи по этим людям теперь доступны. Дубли не плодились (программа сверила по ФИО). |
| 2026-06-22 | прод-миграция данных (без релиза клиента) | Персонал / Табель / Цеха | **Устранена «двойная бухгалтерия» по цехам.** Раньше цех существовал в двух местах сразу: при создании Табеля выбирался из справочника цехов, а сотрудники висели на **дублирующих подразделениях** «Цех № 1…4» — из-за чего «Цех №1» ощущался как разные объекты, кнопка «Добавить весь цех» не срабатывала (была пуста), и табельщик добавлял людей по одному. Теперь цех у каждого сотрудника привязан к **единому справочнику цехов**: 126 работников перевешены, 4 дубля-подразделения «Цех № N» убраны, в списке подразделений остались только настоящие оргединицы (бухгалтерия, ОПП, ОТК, снабжение и т.д.). Кнопка **«Добавить весь цех»** в Табеле заработала по-настоящему — цех добавляется одним кликом. Для завода: меньше путаницы «где какой цех», табельщику быстрее набор, единый источник истины по цехам на будущее. (Доедет операторам при обновлении/перелогине клиента.) |
| 2026-06-21 | v2026.621.1815 | Доступ / роли (вся программа) | **Ролевой доступ операторов.** Раньше почти каждый мог менять что угодно в программе. Теперь у каждого — свой участок: двигателист правит двигатели, технолог — детали и номенклатуру, мастер — наряды и услуги, снабжение — заявки; остальное видно, но не правится (чужое чужими руками не испортишь). **Чувствительное защищено:** контракты и контрагенты — только у руководства; личные данные и зарплату сотрудник видит только свои, чужие — нет. Бухгалтеры/кадры доступ к зарплатам сохранили. Изменения учётных записей (добавить/править пользователя) проходят через одобрение. Активировано по 12 операторам; настраивается по людям, дальше дорастёт по факту работы. Для завода — меньше случайных порч чужих данных и защита персональных/договорных сведений. |
| 2026-06-21 | v2026.621.1133 | Персонал / Табель + Обновление программы | **Табель быстрее набирать:** при добавлении сотрудников показываются только работающие — уволенные больше не мешают в списке. Появилась кнопка **«Добавить весь цех»**: открыл табель цеха — одним кликом все его работники в табеле, без перебора по одному. Для этого у сотрудника появилось поле **«Цех»** (в карточке), а на странице «Сотрудники» можно выделить сразу многих и проставить цех всем разом — разовая настройка, дальше экономит время табельщику каждый месяц. **Обновления стали лёгкими по-настоящему:** раньше при каждом обновлении качался весь установщик (~110 МБ) — теперь догружается только изменившееся (несколько МБ); в полную силу со следующего обновления. Меньше трафика и времени на раскатку по рабочим местам завода. |
| 2026-06-20 | v2026.620.1715 | Поиск (вся программа) / Карточки / Отчёт по клиентам | **Глобальный поиск:** по **Ctrl+K** (или кнопке 🔍 вверху) открывается единое окно — ищет сразу везде (детали, двигатели, марки, контрагенты, контракты, наряды, заявки, услуги, инструменты, складские документы); выбрал результат — открылась карточка. Оператору больше не нужно помнить, в каком разделе что лежит — нужная сущность находится за пару секунд из любого места. **«Сохранить и выйти»** добавлена на оставшиеся карточки (складские документы, номенклатура, спецификации сборки) — единообразие во всех карточках. **Учёт рабочего времени переделан:** «Онлайн» в «Сводном отчёте по клиентам» больше не показывает ложные «10 часов» сразу после входа (была неверная цифра) — теперь реальное время с программой открытой; добавлена колонка **«Активно»** — сколько человек реально работал (по вводу мыши/клавиатуры, без простоя). Руководителю — честная картина загрузки рабочих мест, без лишней нагрузки на программу и сеть. |
| 2026-06-19 | v2026.619.1703 | Дубли деталей / Табель / Карточки / «Мой круг» | Пачка правок по живой работе. **Дубли:** модуль больше не упирается в тупиковую ошибку «у главной детали нет складской карточки» — если карточки не было, программа создаёт её сама (переносит с поглощаемой), слить можно любую пару дублей; снимает случаи «деталь не находится / не добавляется». **Табель:** теперь можно вести не только по цеху, но и по подразделению (ОПП, бухгалтерия, производство и др.) — область выбирается в одном списке «Цех / подразделение»; раньше затабелить ОПП было нельзя. **Карточки:** две кнопки сохранения — «Сохранить» (записать и остаться в карточке) и «Сохранить и выйти». **«Мой круг»:** на кнопке загорается колокольчик при новых контрактах/ДС (напоминание привязать двигатели). Меньше тупиков и ручной возни у операторов и табельщика. |
| 2026-06-19 | v2026.619.1041 | Персонал / Табель + интерфейс (везде) + двигатели | Пачка правок по живой работе. **Табель:** панель управления (коды, часы, резинка) закреплена сверху — не уезжает при прокрутке длинного списка; добавлены **права** — табель правит только тот, кто его создал, остальных пускает только автор галочкой «Разрешить редактирование другим» (по умолчанию выключено) — чужой табель случайно не испортят. **Поля ввода с подсказками по всей программе:** текст при наборе больше не выделяется и не стирается после паузы, а сами подсказки не «дёргаются» при наведении мышью — меньше раздражения и потерянного ввода. **Окно «Что нового»** при обновлении подстраивается под экран (на маленьких мониторах не уходит за нижний край), кнопка всегда доступна, закрывается и клавишей Enter. **Список двигателей:** исправлена «залипавшая» красная метка утиля — снял у картера пометку «утиль» в карточке, двигатель перестаёт числиться бракованным. **Дубли деталей:** модуль теперь находит и пары с одинаковым артикулом, но разными названиями — снимает путаницу «деталь не добавляется/не находится». |
| 2026-06-18 | v2026.618.1551 | Персонал / Табель Т-13 | Вторая доводка табеля по живой работе. Коды разложены по смыслу: «с часами» (явка и т.п.) ставятся вместе с часами одной парой; отдельно «без часов» (отпуск/больничный/выходной) — такой код, попав в ячейку, затирает прежнее и оставляет только себя. Появилась «резинка» — стирать коды кликом или протяжкой. Правой кнопкой можно выделить прямоугольник и заполнить его выбранным кодом разом, а простой клик правой по ячейке открывает комментарий. В ячейке часы стали крупнее и заметнее, уголок-метка комментария — больше и тёмно-бордовый. **Починена печать:** раньше при снятии «Месяц целиком» лист уходил пустым — теперь каждая галочка (первая половина / вторая / легенда / расшифровки) работает сама по себе и печатается в любом сочетании; под ФИО на печати добавлена должность. Табельщику быстрее и нагляднее заполнять и печатать табель. |
| 2026-06-18 | v2026.618.1128 | Персонал / Табель Т-13 | Доводка табеля по итогам живой обкатки. Проставление кода стало как кисточкой: выбрал код вверху — он «в руке» (виден у курсора), ставишь кликом по дню или протяжкой мыши сразу по нескольким дням; выбор кода больше не портит уже заполненную ячейку (была частая ошибка). Починена поломка: в комментарий к ячейке не вводился текст — теперь вводится, сохраняется и печатается. Людей в табель добавляют пачкой: выбрал подразделение, отметил галочками — добавил всех разом, так же убираешь несколько. Появились размер шрифта (крупнее/мельче), режимы «1-я половина / 2-я / весь месяц» с авто-подгоном под ширину окна и легенда кодов снизу. Табельщик заполняет и печатает табель заметно быстрее и без раздражающих мелочей. |
| 2026-06-18 | v2026.618.117 | Обновление программы / номенклатура | Лёгкие обновления теперь реально срабатывают при обычном запуске программы (раньше из-за порядка проверок чаще скачивался полный установщик ~110 МБ) — начиная со следующего обновления трафик и время на раскатку по рабочим местам заметно меньше. В карточке номенклатуры убран дублирующий раздел про марки двигателей (осталась одна «Применяемость» — марка + узел сборки + количество), а у части деталей в списке вместо служебного кода (DET-…) теперь виден реальный артикул. Меньше путаницы у оператора и лишней нагрузки на сеть завода. |
| 2026-06-17 | v2026.617.1614 | Склад / номенклатура / меню | В списках склада и в карточке номенклатуры рядом с наименованием теперь виден **артикул (сборочный номер)** — оператор различает одноимённые детали с разными номерами, не открывая карточку, и при подборе в документ сразу видит код. На странице «Мой круг» добавлена кнопка-плитка **«Табель»** для быстрого перехода (каждый может убрать её правым кликом, если не нужна). Слияние дублей номенклатуры **перестало падать с ошибкой** после выбора деталей — оператор доводит объединение до конца. Новые детали сразу получают складскую карточку (раньше часть деталей не показывалась в номенклатуре). Заведены **базовые детали двигателей В-46 и В-84** (картеры, блоки, головки, рубашки, крышки, поршни, гильзы) с привязкой к своим маркам и галочками актов комплектности+дефектовки. Меньше путаницы при подборе деталей и в актах. |
| 2026-06-17 | v2026.617.1614 | Персонал / Табель Т-13 | Появился модуль **табеля учёта рабочего времени по цехам** — взамен ручного Word. Табельщик быстро отмечает явки/неявки официальными кодами Т-13: щёлкнул по коду или часам — вставилось в ячейку, можно «кистью» штамповать серию, печатать с клавиатуры как в Excel, разом «заполнить норму всем» (рабочие дни проставятся, воскресенья пропустятся). Программа на лету считает часы и дни по каждому работнику и сверяет с месячной нормой. Печать на лист A4 для отдела кадров — галочками выбираешь, что вывести: первую половину месяца, вторую, месяц целиком и/или расшифровки. Прямо в табеле к дню работника можно вписать **комментарий «где был / что делал»** (командировка, отгул и т.п.) — на печати выходит отдельной страницей-расшифровкой. Меньше ручного труда табельщика, быстрее сверка с кадрами. (Зарплата считается отдельно — модуль только учёт + печать.) |
| 2026-06-17 | v2026.617.1614 | Двигатели (карточка) / фото | Прикреплённые к двигателю фотографии теперь можно **смотреть альбомом**: сетка превью → крупный просмотр с листанием ←/→, множественный выбор. Над фото — действия: копировать в буфер, удалить, отправить (на флешку/в папку, открыть папку для перетаскивания в Telegram/MAX, черновик письма), напечатать на принтере, собрать выбранные в один PDF. Раньше фото только загружались — посмотреть и работать с ними альбомом было нельзя. Удобнее показывать и передавать снимки дефектов/комплектности. |
| 2026-06-17 | v2026.617.30 | Двигатели / контракты / дефектовка | Наведён порядок в связке двигатель—контрагент—контракт—ДС. В карточке двигателя контрагент выбирается первым и сразу показывает только его контракты (можно начать и с контракта — контрагент подставится сам), а поля в списке деталей выровнены ровно по колонкам — оператор не путается при привязке и быстрее заполняет. В списке двигателей сразу после номера — цветные маркеры привязки (красный — ни к чему не привязан, зелёный — контракт, зелёный по ДС): видно с одного взгляда бесхозные двигатели, не открывая карточку. В карточке контракта/ДС появился раздел «Двигатели» — видно, какие двигатели идут по каждому ДС, можно привязать прямо здесь; пустые разделы свёрнуты в компактные кнопки. Брак двигателя теперь определяется сам: если картер ушёл в утиль при дефектовке — двигатель помечается забракованным автоматически (ручную галку убрали), прочие детали в утиле двигатель не бракуют. Меньше ручной работы и ошибок привязки/учёта брака. |
| 2026-06-16 | v2026.616.2017 | Двигатели / поиск / списки | Поиск двигателей по куску номера теперь выводит самые подходящие совпадения сразу наверх списка (точное → с начала → внутри), а не вперемешку по дате — оператор быстрее находит нужный двигатель; набранный в подсказке текст не стирается, если задумался на пару секунд. Список двигателей сразу показывает свежего контрагента и контракт после правки (без перезахода). В карточке двигателя длинный номер виден целиком (поле не обрезает), шапка списка деталей держится при прокрутке. Журнал «Изменения» и ещё несколько мест показывают человеческие формулировки вместо внутренних кодов и «сырых» данных. Меньше путаницы и лишних кликов в повседневной работе со списками и карточками. |
| 2026-06-15 | v2026.615.1517 | Обновление программы | Лёгкие обновления заработали по-настоящему: программа догружает только изменившиеся части новой версии (обычно единицы МБ вместо ~110 МБ), а всё неизменное берёт из уже установленного. В заводской сети изменившиеся куски берутся у соседнего компьютера с новой версией — интернет-канал завода не нагружается (фолбэк на сервер, если соседа нет). Обновления раскатываются по рабочим местам быстрее и почти без трафика; окно показывает «Лёгкое обновление: ~N МБ вместо ~110 МБ», а скорость/время больше не мельтешат. Меньше простоя при обновлении, меньше нагрузка на сеть завода. |
| 2026-06-15 | v2026.615.108 | Интерфейс (весь клиент) | Рестайл визуального языка: современный вид с мягкими скруглениями; кнопки «выпуклые» и явно отличимы от полей ввода (сразу видно, куда нажимать, а куда вводить) и от подписей; таблицы плотнее (бухгалтерская плотность, как в Excel); ширина колонок подстраивается под данные — узкие поля (даты/числа/галочки) больше не растягиваются от длинных заголовков, а наименования/ФИО получают освободившееся место. Эмблема РМЗ стала иконкой программы (ярлык на рабочем столе, окно, панель задач). **Меню удобнее:** кнопки отделов/разделов и плитки «Быстрый старт» — иконка слева, название справа, краткое описание ниже, по ширине содержимого и собраны по центру (раньше были растянуты на всё окно стопкой). **Списки на всю ширину окна:** убрана пустая полоса справа, длинные наименования и контрагенты видны целиком. Интерфейс выглядит современнее и строже — оператору яснее структура экранов, меньше «пустоты» и путаницы кнопка/поле. |
| 2026-06-14 | v2026.614.1939 | Интерфейс (списки) | Даты и время в списках показываются без секунд (ячейка не обрезается, дата видна целиком), заголовки таблиц переносятся по словам без разрыва посреди слова, двухколоночные списки не рисуют пустые колонки при малых данных, карточка двигателя — поля ровной центрированной читаемой колонкой. Оператор видит данные целиком, экраны опрятнее. |
| 2026-06-14 | v2026.614.1618 | Обновление программы | Новое окно обновления: при скачивании честно видно, сколько мегабайт из скольких загружено, скорость и сколько примерно осталось, а сверху — наглядная цепочка этапов (Проверка → Скачивание → Целостность → Установка → Перезапуск). При сбое — спокойное объяснение и подсказка, что программа сама докачает и повторит установку (вмешательство не нужно). Оператор не гадает, «зависло или идёт», и не нервничает при обновлении. |
| 2026-06-14 | v2026.614.1618 | Интерфейс (везде) | Эргономика проход-1: длинные заголовки и подписи в списках переносятся, а не обрезаются; содержимое списков выровнено по левому краю (читается ровнее); кнопки в верхней панели названы понятнее. В карточке двигателя список деталей больше не уезжает за правый край окна. Меньше «кривых» экранов, оператор быстрее ориентируется. |
| 2026-06-14 | v2026.614.1618 | Версия программы | Версия теперь показывается понятной датой сборки (напр. «14.06.2026 16:18») в окнах обновления и «Что нового», а не техническим номером. Оператору яснее, насколько свежая у него программа. |
| 2026-06-14 | v1.55.0 | Двигатели (карточка) | Список деталей двигателя переработан: по умолчанию таблица, разбитая на два сворачиваемых блока — «Базовые детали (в актах)» и «Остальные детали» (оба свёрнуты при открытии, экран не загромождён). Убрана лишняя левая галочка «в печать» (в акт идут детали с галочками акта комплектности/дефектовки), фото и удаление — в одном ряду. Оператор быстрее работает со списком и не путается в столбцах. |
| 2026-06-14 | v1.55.0 | Двигатели (карточка) | «Контрагент» и «Контракт» в карточке двигателя показываются именем, а не служебным кодом (UUID мелькал при открытии); из подписей вкладок убран технический жаргон «(parts-movement)». Оператор сразу видит понятные названия. |
| 2026-06-13 | v1.54.0 | Двигатели (карточка) | Проактивная подсказка о дублях: при вводе номера двигателя прямо под полем сразу показывается, если двигатель с таким номером уже заведён (красная «уже есть») или есть похожие номера (жёлтая), с маркой и кнопкой «Открыть». Дополняет запрет на сохранение дубля (v1.50.0) — оператор видит дубль ещё до сохранения и переходит к существующей карточке, а не плодит вторую. |
| 2026-06-13 | v1.54.0 | Поиск (везде) | Умный поиск распространён на все списки (марки, сотрудники, договоры, номенклатура и др.) и на автоподсказки в полях ввода — раньше он работал лишь частично. Находит словами в любом порядке («умс в-59» → «В-59 УМС»), по части номера без чёрточек, исправляет забытую раскладку, сортирует по близости к запросу. Оператор быстрее находит нужное в любом справочнике. |
| 2026-06-12 | v1.53.0 | Вход и настройки | Новый экран входа: список работников с ФИО и должностями (видно, кто недавно входил с этого компьютера), поиск по фамилии/должности, клик по человеку подставляет логин; индикатор Caps Lock и показ пароля. Личные настройки интерфейса (вкладки, ярлыки, «Мой Круг») теперь привязаны к работнику и подгружаются на любом компьютере завода. Меню у имени в шапке: Настройки / Смена аккаунта / Выйти — пересменка за общим компьютером без перезапуска программы. |
| 2026-06-12 | v1.53.0 | Акты двигателя | Комиссия акта комплектности (нач. цеха, мастер — из цеха двигателя; нач. ОТК) подставляется автоматически, ручной выбор не перетирается. Оператор не ищет подписантов по базе на каждом акте. |
| 2026-06-12 | v1.53.0 | Обновления и синхронизация | Со следующего релиза клиенты будут скачивать только изменившиеся части обновления вместо полных ~110 МБ (меньше нагрузки на заводскую сеть). При первой установке на новый компьютер рабочие данные (двигатели, склад) появляются раньше — журналы и чат докачиваются фоном. |
| 2026-06-12 | v1.52.1 | Акты двигателя (карточка) | Галочки «в акт комплектности»/«в акт дефектовки» в списке деталей двигателя снова видны (в прошлом релизе были, но не показывались), рядом — «№ на детали» (набитый на детали номер). Сам список деталей растянут на весь экран, длинные названия и номера читаются целиком. В печатных формах артикул = «№ сборочной единицы», лишний столбец «по чертежу» убран. Оператор отмечает детали для актов и вписывает набитые номера прямо в карточке, без путаницы в столбцах. |
| 2026-06-12 | v1.52.0 | Детали (номенклатура) | Артикул стал полноправным различителем: одноимённые детали с разными сборочными номерами больше не путаются при выборе в актах, поиск находит деталь по номеру без чёрточек. Загружен базовый список деталей владельца (59 новых + артикулы 21 существующей), привязан ко всем маркам. Оператор быстрее и точнее выбирает нужную деталь. |
| 2026-06-12 | v1.52.0 | Дубли номенклатуры | Новый раздел: программа сама находит задвоенные детали (точные и похожие написания), оператор выбирает главную — остатки, движения, спецификации и акты перевешиваются без потери данных. Справочник постепенно очищается от исторического мусора, отчёты и остатки перестают расщепляться по дублям. |
| 2026-06-12 | v1.52.0 | Акты двигателя | Галочки «в акт комплектности»/«в акт дефектовки» задаются один раз в марке и наследуются всеми её двигателями (с точечной правкой на конкретном двигателе). Печатается чистый акт только с нужными деталями, пустые клетки вместо нулей — удобно заполнять на бумаге; комплектность подписывает комиссия из трёх, в дефектовке появился «№ на детали». Оператор больше не проставляет галочки на каждом двигателе вручную. |
| 2026-06-11 | v1.51.0 | Синхронизация | Первая загрузка данных на новом компьютере ускорена втрое (17 с → 6 с на копии рабочей базы). Установка программы на новое рабочее место / восстановление после сбоя занимает меньше времени простоя оператора; выигрыш будет расти по мере накопления истории изменений. |
| 2026-06-10 | v1.50.0 | Двигатели | База вычищена от дублей двигателей (25 задвоенных карточек слиты, акты и история сохранены) + программа больше не даёт завести двигатель с уже существующим номером, даже офлайн. Исключён класс ошибок «два оператора завели один двигатель дважды» → акты и зарплата не расщепляются по двум карточкам. |
| 2026-06-10 | v1.50.0 | Поиск (везде) | Умный поиск: номер находится без чёрточек («2401» → «240-1»), при неверной раскладке («ubkmpf» → «гильза»), при опечатке показываются похожие с явной пометкой. Оператор быстрее находит деталь/двигатель, меньше шансов завести дубль из-за «не нашёл». |
| 2026-06-10 | v1.50.0 | Вход в программу | Логин последнего работавшего за компьютером подставляется сам, курсор сразу в пароле. Мелочь, но каждый вход быстрее и без ошибок «вошёл под чужим логином». |
| 2026-06-09 | v1.49.0 | Акты двигателя | Версии актов (каждая печать сохраняется и перепечатывается как была), ветки восполнения дефектных деталей (Заказчик/Свой ремонт/Закупка), акт претензии заказчику, ремонтный наряд из дефектовки одним нажатием. Спор с заказчиком о комплектности теперь подкреплён сохранённой версией акта; дефектная деталь не теряется — у каждой виден путь восполнения и статус «в ремонте → готова к сборке». |
