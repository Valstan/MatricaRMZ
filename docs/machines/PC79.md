# Машина: PC79

> Профиль окружения этого компа. Читается `/start` §0.5 по hostname. Пиши сюда по мере изучения. Без секретов. Конвенция — [`README.md`](README.md).

**Hostname:** PC79
**OS:** Windows 11 Pro (10.0.26200)
**Папка репозитория:** `D:\valstan\REPO\MatricaRMZ` (brain рядом: `D:\valstan\REPO\brain_matrica`)
**Shell:** PowerShell (основной) + Git Bash (Bash tool)

## Тулчейн

- **Node:** v24.19.0 · **pnpm:** 10.26.1 через `corepack pnpm` (стандартно).
- **Свежий клон без установки** (2026-08-21): `node_modules` не было ни в корне, ни в пакетах, `shared/dist` отсутствовал. Первая сессия на этом компе обязана начать с установки — без неё не идут ни тесты, ни `typecheck`.

### ⚠️ Обычный `corepack pnpm install` здесь НЕ проходит (выучено 2026-08-21)

Нет Visual Studio C++ build tools → падает нативная сборка `better-sqlite3-multiple-ciphers` (`gyp ERR! find VS … Could not find any Visual Studio installation`), и установка обрывается **на середине**: бинов нет даже у `tsc`, `shared build` падает с «tsc не является внутренней командой». Рабочая последовательность:

```bash
corepack pnpm install --ignore-scripts
corepack pnpm -F @matricarmz/shared build
corepack pnpm -F @matricarmz/ledger build
```

- **`ledger build` обязателен** — иначе 5 файлов backend-тестов падают на `Cannot find module @matricarmz/ledger/dist/index.js` (`--ignore-scripts` пропускает и workspace-хуки сборки).
- **Нативные модули тянуть готовыми бинарями вручную** — `--ignore-scripts` пропускает их `prebuild-install`, и тесты падают на `Cannot find module '…/build/Release/*.node'`. **VS для этого не нужен: под Node 24 готовые бинари есть у всех трёх.** Ключевой нюанс — `better-sqlite3*` требуют явных `--runtime node --target <версия node>`; форма `-r napi` для них выдаёт `No prebuilt binaries found` и вводит в заблуждение:
  ```bash
  # node-datachannel (иначе 4 файла backend-тестов красные)
  cd node_modules/.pnpm/node-datachannel@0.32.0/node_modules/node-datachannel \
    && node ../../../prebuild-install@7.1.3/node_modules/prebuild-install/bin.js -r napi
  # better-sqlite3 и better-sqlite3-multiple-ciphers (иначе не идут тесты electron-app)
  cd node_modules/.pnpm/better-sqlite3@12.11.1/node_modules/better-sqlite3 \
    && node ../../../prebuild-install@7.1.3/node_modules/prebuild-install/bin.js --runtime node --target 24.19.0
  cd node_modules/.pnpm/better-sqlite3-multiple-ciphers@12.11.1/node_modules/better-sqlite3-multiple-ciphers \
    && node ../../../prebuild-install@7.1.3/node_modules/prebuild-install/bin.js --runtime node --target 24.19.0
  ```
- **`ERR_PNPM_EPERM` на каталоге `node_modules/.pnpm/node-datachannel@0.32.0`** после оборванной установки — снести этот каталог (`rm -rf`) и повторить install.
- **Итог: на этой машине зелены все гейты, кроме живого стенда** (проверено 2026-08-21): backend **72 файла / 555 тестов**, electron-app **63 файла / 434 теста**, `pnpm -r typecheck`, `pnpm -r lint`. Первый прогон тестов electron-app качает Electron-бинарь (~2.5 мин), дальше быстро.
- **Electron-ABI собирается** (с 2026-08-29, после установки VS Build Tools). `corepack pnpm -F @matricarmz/electron-app exec electron-builder install-app-deps` проходит и собирает из исходников: готовых бинарей под ABI Electron 43 (**148**) нет ни в одной версии `better-sqlite3-multiple-ciphers` — максимум `electron-v146`. Поэтому компилятор здесь обязателен, prebuild не спасёт.
- **⚠️ ABI-танец, без него грабли в обе стороны** (подробности — `SKILL.md` скилла `verifier-electron`): один общий pnpm-инстанс `better-sqlite3*` обслуживает и `electron-app` (Electron-ABI), и vitest (Node-ABI). Перед запуском стенда — `install-app-deps`; **перед прогоном тестов вернуть Node-ABI**, иначе 35 красных в `electron-app`. Самый быстрый способ — заранее скопировать оба `build/Release/better_sqlite3.node` и подкладывать обратно.

## Чего на этой машине НЕТ (проверено 2026-08-21)

- ~~Visual Studio C++ build tools~~ — **установлены 2026-08-29** (`winget install Microsoft.VisualStudio.2022.BuildTools` с workload `VCTools`): MSVC 14.44 + Windows SDK 10.0.26100. Нативные модули собираются из исходников; готовые бинари больше не единственный путь.
- **Java / Android SDK (`adb`) не установлены** — сборка и отладка Capacitor-APK отсюда невозможна.
- **Go не установлен** — `watchdog/main.go` собирается только в CI.
- **Ни Docker, ни Podman, ни WSL** (проверено 2026-08-29) — контейнеры здесь не поднять. На проверку SQL это больше не влияет: PostgreSQL стоит нативно (см. ниже).
- **Scope `workflow` у токена `gh` выдан 2026-08-29** (было `gist, read:org, repo` — push в `.github/workflows/**` отклонялся: `refusing to allow an OAuth App to create or update workflow`). Лечится интерактивной `gh auth refresh -h github.com -s workflow`: команда печатает одноразовый код, владелец вводит его на https://github.com/login/device. В агентской сессии запускать в фоне и отдавать код владельцу — сама она не блокируется. SSH-ключа для GitHub на машине нет, других обходных путей не было.

## PostgreSQL (есть с 2026-08-29)

**PostgreSQL 17.11**, служба `postgresql-x64-17` — `Running`, запуск автоматический, порт **5432**. Ставился соседней сессией; прежняя запись «PG не установлен» устарела и снята.

- **`psql` не в PATH:** `C:\Program Files\PostgreSQLin\psql.exe` (там же `pg_ctl`/`initdb`), как на `PC40`.
- **Суперпользователь** — дефолтный от установщика; пароль в репо не пишем (`AGENTS.md` §«Публичный репозиторий»). Для скриптов передавать через `PGPASSWORD` в окружении или класть в gitignored `backend-api/.env.dev`.
- **Локальная база с нуля (проверено 2026-08-29):**
  ```bash
  PGPASSWORD=… "/c/Program Files/PostgreSQL/17/bin/psql.exe" -h 127.0.0.1 -U postgres -d postgres     -c "CREATE DATABASE matricarmz_dev"
  PGHOST=127.0.0.1 PGPORT=5432 PGDATABASE=matricarmz_dev PGUSER=postgres PGPASSWORD=…     corepack pnpm -F @matricarmz/backend-api db:migrate
  ```
  Все 86 миграций накатываются на **пустую** базу — дампы прода больше не нужны (до 2026-08-29 накат падал на `0024`, см. `PC40.md`; починено PR #715).
- **Приёмки B3 гоняются локально** тем же окружением: `users:fixture-check` (нужен `MATRICA_FIXTURE_ALLOW_WRITE=1`), `users:backfill-check`, `users:parity` — все зелёные на этой машине 2026-08-29.
- **Следствие:** ограничение «живой стенд и `verifier-electron` здесь не поднимаются» снято. Для CDP-смоука остаётся доделать Electron-ABI (`electron-builder install-app-deps`) — на этой машине ещё не пробовали.

## Стенд `verifier-electron` (2026-08-29)

- **PowerShell блокирует скрипты скилла:** политика выполнения — `Restricted`. Запускать как `powershell -NoProfile -ExecutionPolicy Bypass -Command ".\.claude\skills\verifier-electron\scripts\<script>.ps1"`.
- **Порт 3001 занят соседним проектом** (Next.js другого репо, живёт постоянно). Стенд переведён на **3011** в `backend-api/.env.dev` и `electron-app/.env.dev` (оба gitignored). `start-backend.ps1` с 2026-08-29 читает `PORT` из env, а не константу.
- **База стенда поднимается с нуля, дампы прода не нужны** (после починки 0024 в PR #715). Последовательность: `setup-env.ps1` → `setup-db.ps1 -Reset` → `migrate-and-seed.ps1`. Логин стенда — `verify` / `verify123`.
- **Стенд поднимается целиком** (проверено 2026-08-29): backend на `127.0.0.1:3011`, Electron-клиент с CDP на 9222, драйвер `cdp-section-access.mjs` прошёл. Логин суперадмина стенда — `valstan` / `valstan-dev`.

## Прод-SSH (работает, проверено 2026-08-29)

- `ssh matricarmz` с этой машины **работает**: пер-машинный ключ заведён и авторизован на проде 2026-08-28, host key принят в `known_hosts` (первый контакт TOFU 2026-08-28). Повторная проверка 2026-08-29 — оба backend-сервиса `active`.
- **Имя ключа и параметры подключения в репо не пишем** — они живут только в `~/.ssh/config` этой машины (`AGENTS.md` §«Публичный репозиторий — тоже recon-поверхность»). Блок `Host matricarmz` обязан нести `IdentitiesOnly yes`, иначе ssh предложит все локальные ключи, каждый — неудачная аутентификация, и fail2ban забанит IP.
- **При отказе — одна попытка, не долбить** (fail2ban агрессивен, G8). Разбираться по порядку: порт → ключ/`IdentitiesOnly` → бан (`AGENTS.md` §SSH tips).

## Порты / сервисы (dev-стенд)

Не заводились (нет PG). При настройке брать значения из `backend-api/.env.dev`; на других машинах backend dev слушает `127.0.0.1:3001`.
