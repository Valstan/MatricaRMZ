# Машина: PC40

> Профиль окружения этого компа. Читается `/start` §0.5 по hostname. Пиши сюда по мере изучения. Без секретов. Конвенция — [`README.md`](README.md).

**Hostname:** PC40
**OS:** Windows 11 Pro
**Родительская папка репозиториев:** `D:\GitHubReps\` (рядом: `brain_matrica`)
**Shell:** PowerShell (основной) + Git Bash (Bash tool)

## Порты / сервисы (dev-стенд)
- **PostgreSQL (локальный dev):** порт **5433** (НЕ 5432!). БД verifier: `matricarmz_dev`, user `postgres`. Пароль — в `backend-api/.env.dev` (`PGPASSWORD`). Кластер = Windows-служба **`postgresql-x64-17`** (data-dir `C:\Program Files\PostgreSQL\17\data`, слушает 5433).
  - ⚠️ **Служба бывает `StartType=Disabled` + Stopped** (так было 2026-07-14) → backend падает `ECONNREFUSED 127.0.0.1:5433`. `net start` из обычной сессии → «Отказано в доступе» (эл-ция), а на Disabled даже elevated `net start` не срабатывает. **Лечение (elevated, один UAC):** `Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile','-Command','Set-Service -Name postgresql-x64-17 -StartupType Manual; Start-Service -Name postgresql-x64-17'`. Проверить: `netstat -ano | grep :5433` слушает + `Get-Service postgresql-x64-17` = Running.
- **Backend dev (verifier-electron):** `http://127.0.0.1:3001`.
- **Vite (electron renderer dev):** `http://127.0.0.1:5173`.
- **CDP (electron remote debugging):** `9222` (`MATRICA_CDP_PORT=9222`).

## Инструменты / пути
- **psql.exe — НЕ в PATH.** Локальный: **`C:\Program Files\PostgreSQL\17\bin\psql.exe`** (там же `pg_ctl`/`initdb`/`postgres`). Для SQL по **проду** — через `ssh matricarmz` (там psql есть): `cd MatricaRMZ/backend-api && set -a && . ./.env && set +a && psql …`.
- **Node/pnpm:** `corepack pnpm` (стандартно).

## Скиллы (как поднимать на этом компе)
### verifier-electron (`/verify` Electron)
- `.env.dev` для `backend-api` и `electron-app` **уже есть** (PG 5433, `matricarmz_dev`).
- **Backend:** `powershell -NoProfile -ExecutionPolicy Bypass -File "D:/GitHubReps/MatricaRMZ/.claude/skills/verifier-electron/scripts/start-backend.ps1"` — **абсолютный путь с форвард-слешами** (относительный `.\…` с бэкслешами бьётся при передаче из Git Bash в PowerShell).
- **Electron:** поднимать `corepack pnpm --filter @matricarmz/electron-app dev` **напрямую долгоживущим фоном** с env `MATRICA_API_URL=http://127.0.0.1:3001 MATRICA_SYNC_V2=1 MATRICA_UPDATE_PEER_HTTP_PORT=3001 MATRICA_UPDATE_LAN_ENABLED=0 MATRICA_CDP_PORT=9222` (не через возвращающийся `.ps1` — иначе vite умирает, окно сиротеет на `chrome-error`).
- **better-sqlite3 ABI (общий инстанс electron+backend):** перед backend-тестами/dev-backend — `corepack pnpm -r rebuild better-sqlite3` (Node-ABI; **обязательно `-r`** — без него из корня rebuild молча no-op, т.к. пакет не прямая зависимость root); перед запуском Electron-клиента — `corepack pnpm -C electron-app exec electron-builder install-app-deps` (Electron-ABI). ABI слетает после каждого `install`/`rebuild` → переставлять перед сменой стека.
- **`ws` в своих CDP-драйверах — НЕ `import WebSocket from 'ws'`** (2026-07-15): `ws` не прямая зависимость корня → `ERR_MODULE_NOT_FOUND`. Грузить из стора, как остальные драйверы: перебрать `node_modules/.pnpm/ws@*/node_modules/ws/wrapper.mjs` и взять `m.default ?? m.WebSocket ?? m` (готовый лоадер — в `_diag.mjs`).
- **Deep-link на карточку двигателя: поле `engineId`, не `entityId`** (2026-07-15) — `navigateDeepLink({ kind:'app_link', tab:'engine', engineId })`. С `entityId` вызов молча резолвится, но окно остаётся на «Выберите двигатель из списка» (тихий промах, не ошибка). Имена полей — по сущностям (`engineBrandId`, `contractId`, …), см. `ChatDeepLinkPayload` в `shared/src/ipc/types.ts`.
- **PG-служба и UAC (2026-07-15):** `Start-Process powershell -Verb RunAs` поднимает диалог UAC на экране; если владельца нет за компом, вызов возвращает exit 0, а служба остаётся `Stopped` (лог elevated-процесса пуст). Не долбить повтором — проверить `(Get-Service postgresql-x64-17).Status` и попросить владельца подтвердить UAC.
- **Наряды в CDP-драйвере: deferred-create** (2026-07-15) — `workOrders.create()` **не пишет в БД**, наряд материализуется первым `workOrders.update({id, payload})` (номер присваивается там же). Поэтому `workOrders.get(id)` сразу после create возвращает `{ok:false, error:'Наряд не найден'}`, а `list()` его не видит — **payload бери из ответа `create`** (`r.payload`), не из `get`. Assembly-наряд для теста: `{...created.payload, workOrderKind:'assembly', assemblyEngineId, repairIssued:true, freeWorks:[{lineNo:1, engineId, workName, qty}]}`.
- **Строки дефектовки в смоуке: без `quantity` утиль молча теряется** (2026-07-15) — `normalizeEngineInventoryRow` считает `scrap = Math.min(quantity, scrap_qty)`, а `quantity` по умолчанию 0 → `scrap_qty:1` схлопывается в 0, `listScrapPartNames` возвращает `[]`, и хук авто-отзыва «не срабатывает». Выглядит как регрессия гейта, хотя это payload драйвера. В тестовой строке **обязательно** `quantity`. Проверять payload дешевле всего вне CDP: `node -e "import('./shared/dist/index.js').then(m => console.log(m.listScrapPartNames(payload)))"`.
- **`checklists.engineSave` — не `payload`, а `answers` + `templateId`** (2026-07-15): `{engineId, stage, templateId, operationId?, answers}`; шаблон брать из `checklists.templatesList({stage})` (не `templates`).
- **CDP-смоук: модалка «Восстановление несохранённых карточек» перекрывает клики** (2026-07-16) — при старте клиента с recovery-драфтами всплывает модалка; пока не нажато «Позже», клики по списку молча не доходят. В драйвере первым делом жать «Позже». Вторая грабля того же прогона: **stale-таб грида** (правая панель восстанавливает «Табель» с удалённым id → «Табель не найден») — byText-поиски находят кнопки stale-грида и смоук ложно «работает» на нём; строки грида отличать по sticky-ячейке ФИО (`td[1].style.position==='sticky'`), а перед навигацией кликать «← К списку». Рабочий драйвер — `.verifier-electron/_ts-reorder-print.mjs`.
- **CDP-навигация по меню: селектор `span.v2-menu-btn-label`** (2026-07-15) — пункты левого меню это `<span class="v2-menu-btn-label">Двигатели</span>` внутри кнопки. Общий перебор `button,a,[role=menuitem],li` по тексту **не находит** пункт (тихий промах, не ошибка: клик не происходит, страница остаётся прежней — в моём случае показывала панель вложений и смоук ложно «не видел» новых колонок). Рабочий приём: `[...document.querySelectorAll('span.v2-menu-btn-label')].find(x=>x.textContent.trim()==='Двигатели').closest('button').click()`.
- **CDP-драйв:** `MATRICA_CDP_PORT=9222 node .verifier-electron/cdp-*.mjs` (примеры лежат в `.verifier-electron/`, gitignored). Логин — через **реальную форму** (`verify`/`verify123`), bridge-`auth.login` UI не переводит. После логина закрыть окно релиз-приветствия (кнопка «За работу!»).
- **Dev-БД — снапшот (~25 мая)**, может ПРЕДШЕСТВОВАТЬ свежим миграциям (напр. `engine_inventory`-акты почти пусты). Для проверки свежих фич создавай данные сам через bridge-IPC, не полагайся на снапшот.
- Список табелей (`TimesheetsPage`) грузится async — после навигации жди строку с данными, не строку пустого состояния.

- **CDP-смоук печати табеля v2 (2026-07-16):** (1) `Input.dispatchMouseEvent` по координатам кнопки «Открыть» в списке табелей промахивается (грид не открывается) — синтетический `mousedown+mouseup+click()` работает; (2) держи каждый `Runtime.evaluate` коротким — poll-циклы byText по всем кнопкам внутри одного eval выбивают 30–60с CDP-таймаут, лучше несколько коротких eval'ов; (3) окно прямой печати (`printSectionsDirect`) — target `about:blank`: армируй поллинг `/json/list` (50мс) ДО клика «Печать…», после смоука закрой его `GET /json/close/<id>`; (4) стейл PID-файл backend'а (`.verifier-electron/backend.pid` указывает на чужой процесс, напр. svchost) — `start-backend.ps1` говорит «already running», а :3001 молчит; удалить pid-файл и перезапустить.
- **CDP-смоук табеля (2026-07-16), три грабли:** (1) модалка «Восстановление несохранённых карточек» перекрывает все клики — в драйвере первым делом жать «Позже»; (2) восстановленный таб грида может держать УДАЛЁННЫЙ табель («Табель не найден») — смоук ложно «не видит» кнопок: перед навигацией закрыть stale-табы/вернуться «← К списку»; (3) строки грида отличать от прочих таблиц в DOM по sticky-ФИО (`td[1].style.position==='sticky'`), а не первым `tbody tr`. Синтетический `btn.click()` для списочных строк работает; Input.dispatchMouseEvent не обязателен. Рабочий драйвер — `.verifier-electron/_ts-reorder-print.mjs` (gitignored).

- **CDP-смоук заявок снабжения (2026-07-17), четыре грабли:** (1) `supplyRequests.create()` — deferred-create как у нарядов: строка в БД материализуется первым `update`; payload брать из ответа `create` (`r.payload`), `get` сразу после create вернёт «Заявка не найдена»; (2) список заявок — stale после bridge-create: пункт меню «Заявки» повторным кликом НЕ перечитывает данные, remount — переключением на другую вкладку и обратно («Наряды» → «Заявки»); (3) карточка заявки сохраняется ТОЛЬКО при закрытии (крестик `.v2-card-tab-close` → диалог → кнопка «Сохранить»), debounce-автосейв пишет только recovery-черновик, НЕ БД — «сохранилось?» проверять после закрытия таба; (4) `Input type=date` в приложении — это react-datepicker (`input.matrica-datepicker-input`, текстовый ввод `дд.мм.гггг`, native-setter+input-event работает), селектор `input[type=date]` НЕ находит ничего, произвольные атрибуты (`title`) в DOM не прокидываются. Рабочий драйвер — `.verifier-electron/_supply-expected-smoke.mjs` (gitignored).
- **CDP-смоук списка «Мои экраны» + SearchSelect (2026-07-19), две грабли:** (1) **выпадашка `SearchSelect` — кликать `div[data-idx]`**, а не искать пункт текстовым сканом по всем узлам: текст пункта живёт в подсвеченных `<span>`-кусках (`buildLookupHighlightParts`), поэтому `x.textContent.trim()===name` находит внешние обёртки, чей `click()` не доходит до `onClick` опции — смоук ложно репортит «шаблон не применился». Рабочее: `[...document.querySelectorAll('div[data-idx]')].find(x=>x.textContent.includes(name)).click()`. (2) **Список экранов stale после bridge-create** (тот же класс, что заявки снабжения): повторный клик по пункту меню «Мои экраны» НЕ перечитывает данные — remount через переключение на другую вкладку и обратно («Наряды» → «Мои экраны»). Название строки — обычный `<button>`, синтетический `click()` открывает просмотр. Драйверы — `.verifier-electron/_dashboard-smoke.mjs`, `_forecast-ux-smoke.mjs` (gitignored).
- **CDP-логин в смоуках: у поля логина НЕТ атрибута `type`** (2026-07-19) — селектор `input[type="text"]` его не находит (React не ставит атрибут по умолчанию; `.type`-свойство при этом «text»). Искать по свойству: `inputs.find(i=>i.type==='password')` / `i.type==='text'`. Симптом: «login form not found» при видимой форме. Рабочий блок — `_close-sync-smoke.mjs`.
- **Dev-реплика cdp-9222: sync.run может ВИСНУТЬ без `done`-строки** (2026-07-19, эскалация push-петли 07-17) — `sync.run.start` в `%APPDATA%/@matricarmz/electron-app-cdp-9222/matricarmz.log` без парного `sync.run.done` минутами; e2e-проверки доставки на dev-backend до `fullPull` бессмысленны. Лечение прежнее — `_heal-replica.mjs` (login + fullPull + контрольный sync.run).
- **CDP-смоук drag&drop на свободном холсте (2026-07-16):** синтетические `new PointerEvent('pointermove')` через `dispatchEvent` НЕ двигают React-drag (pointerdown доходит — выделение появляется, а move «не тянет»); это артефакт untrusted-событий, не баг фичи. Драйвить drag **trusted-вводом**: `Input.dispatchMouseEvent` (`mousePressed` → серия `mouseMoved` с `buttons:1` → `mouseReleased`) — блок реально движется. Пробник — `.verifier-electron/_drag-probe.mjs`; смоук эскизника — `_mockup-smoke.mjs`.

## Приёмка печатных форм БЕЗ стенда (2026-08-03)

Печатные формы собираются чистыми билдерами (`renderer/src/ui/utils/*PrintHtml.ts`, `printPreview.ts`) — их видно глазами, не поднимая Electron/PG:

1. Временный vitest-файл рядом с билдером пишет готовый лист в скретчпад:
   `writeFileSync(process.env.PREVIEW_OUT, buildWorkOrderA4PreviewHtml({ sections: build…Sections(...), extraCss, landscape: true, marginMm: 8 }))`.
2. `PREVIEW_OUT=<scratch>/x.html corepack pnpm vitest run <тот файл>` (из `electron-app`).
3. Снимок: `"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu --hide-scrollbars --window-size=1180,860 --screenshot=<scratch>/x.png "file:///<scratch>/x.html"` (Chrome есть; Edge — `/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`).
4. **Временный тест удалить сразу** (общая рабочая копия, параллельная сессия с `git add -A` — memory `avoid-temp-files-in-shared-repo`).

Гоняй на двух-трёх размерах данных (мало строк / много строк) — вёрстка «на весь лист» ломается именно на краях.

## Машинные грабли
- **Обновление 2026-08-03 к заметке ниже: `gh` писал нормально** — `gh pr create` / `gh pr merge` / `gh run view` отработали подряд без единого TLS-таймаута. То есть флап периодический, а не постоянный: первую попытку записи делать всегда, в цикл не уходить.
- **`gh` пишет через api.github.com — отваливается по TLS-таймауту, читает нормально (2026-07-31).** `gh api … --method POST` (создание PR) и любые GraphQL-вызовы (`gh pr list --json`, `gh pr create`) падают `net/http: TLS handshake timeout` подряд, при этом `gh api user`, `gh pr checks <N>` (REST GET) и `git push` по HTTPS работают в ту же минуту. То есть это НЕ авторизация и НЕ общий сетевой обрыв — режется конкретный путь. Ветку пушим как обычно, PR открываем позже/из браузера; не тратить попытки в цикле.
- **Панель «РАЗДЕЛЫ» сворачивается после первой навигации — клик по пункту меню молча не срабатывает (2026-07-31).** Пункты остаются в DOM, но скрыты (`offsetParent === null`), а `click()` по скрытой кнопке ничего не делает: вкладка не переключается, и прогон продолжает проверять прежний экран. В драйвере перед выбором пункта: если нужный `span.v2-menu-btn-label` не виден — сперва кликнуть кнопку с текстом `РАЗДЕЛЫ`, затем искать пункт **с проверкой `offsetParent !== null`**.
- **Список «Двигатели» НЕ показывает двигатель, созданный через bridge, даже поиском по номеру (2026-07-31).** `window.matrica.engines.list()` его находит (1587 записей, наш есть, марка и пустой контракт на месте), а страница показывает 11 строк и номера среди них нет; ввод номера в «Поиск по всем данным двигателя…» даёт «Ничего не найдено» и с `input`-событием, и с Enter (при этом обычный запрос вроде `Б1` строки возвращает — поиск сам по себе рабочий, просто нашей сущности в его источнике нет).
  - **Лечение (проверено):** создавать двигатель **кнопкой «Добавить двигатель»** на странице списка, а не через `admin.entities.create` — тогда приложение обновляет свои кэши само, карточка открывается сразу, и драйверу список вообще не нужен. Так сделан `_engine-card-slot-smoke.mjs`, прогон детерминирован (два подряд — PASS, мутация — FAIL).
  - Remount переключением вкладки (приём, который лечит stale-списки заявок снабжения и «Моих экранов») здесь **не помогает**: раздел открывался заново, строки всё равно не было. То есть это не stale-кэш списка, а другой источник данных.
- **Пункт меню называется «Контракты», не «Договоры» (2026-07-31).** Полный список ярлыков достаётся одной строкой: `[...document.querySelectorAll('span.v2-menu-btn-label')].map(x => x.textContent.trim())`. Селектор `span.v2-menu-btn-label` по-прежнему верен (см. раздел скилла), но текст пункта угадывать не надо — он там же и читается.
- **`window.matrica.chat.navigateDeepLink` в dev-сборке НЕ проброшен (2026-07-31).** Вызов падает `TypeError: … is not a function`; в типах поле опциональное (`navigateDeepLink?`). В CDP-драйверах на этом стенде навигация — через меню + клик по строке списка, а не deep-link.
- **Мутационная приёмка смоука: фикстура должна отличать фичу от её отсутствия (2026-07-31).** Смоук адресного разнесения аванса сначала бил по ПЕРВОЙ марке контракта — и остался зелёным на сломанной фиче, потому что старое поведение («первые N слотов по порядку массива») на первой марке даёт ровно тот же ответ. Покраснел только после переноса на ВТОРУЮ марку. Вывод общий: прежде чем верить зелёному, спроси, каким входом фикстура отличает правильное поведение от неправильного.
- **`powershell -File .\.claude\…` бьётся:** относительный путь с бэкслешами из Git Bash → PowerShell теряет точки/слеши. Передавай **абсолютный путь с форвард-слешами**.
- **PG слушает 5433, не 5432** — скилл по умолчанию ждёт 5432; `.env.dev` уже настроен на 5433.
- **`ssh matricarmz` может разово оборваться** «Connection closed by 195.161.41.30 port 49217» ещё до banner/auth — тот же класс сетевого флапа, что и push-мерцание ниже (2026-07-02: первая попытка оборвалась, повтор через минуту прошёл штатно). Одна диагностическая `ssh -vv` попытка → если ключ доходит до «Server accepts key», это флап, не fail2ban; не уходить в разбор бана/ключей сразу.
- **`install-app-deps` может молча НЕ пересобрать better-sqlite3 (стейл `.forge-meta`, 2026-07-03):** если `build/Release/.forge-meta` уже говорит `x64--145`, electron-builder пропускает пересборку, даже когда сам `.node` — Node-ABI (например, файл был заблокирован работающим backend-процессом при прошлой пересборке). Симптом: клиент падает «NODE_MODULE_VERSION 137 … requires 145» СРАЗУ после «successful» install-app-deps. Лечение: остановить backend/electron → `rm -rf node_modules/.pnpm/better-sqlite3@*/node_modules/better-sqlite3/build` → `install-app-deps` заново (проверить mtime `.node`).
- **После мажорного апгрейда Electron (41→43, 2026-07-06) `pnpm install` НЕ дотягивает бинарь Electron:** симптом — `electron-vite dev` падает `Error: Electron uninstall` в `getElectronPath`, `node_modules/.pnpm/electron@43.0.0/node_modules/electron/{path.txt,dist/electron.exe}` отсутствуют. `pnpm install` завершается за ~25с (слишком быстро для загрузки ~235МБ), а **`pnpm rebuild electron` — no-op** (пустой вывод). Лечение: запустить postinstall пакета напрямую — `cd node_modules/.pnpm/electron@43.0.0/node_modules/electron && node install.js` (тянет бинарь через `@electron/get`, ~1–2 мин). После этого `path.txt`=`electron.exe`, `dist/electron.exe` ~235МБ, `verifier-electron` поднимается штатно. (`.npmrc` уже разрешает electron в `only-built-dependencies` — гейт не в этом.)
- **verifier-electron под Electron 43 (2026-07-06): работает.** Последовательность ABI на этой машине после сегодняшнего апгрейда: `pnpm install` → `node install.js` (бинарь, см. выше) → `start-backend.ps1` (Node-ABI) → seed → `pnpm -C electron-app exec electron-builder install-app-deps` (Electron-ABI, пересобирает better-sqlite3 **и** better-sqlite3-multiple-ciphers) → `pnpm --filter electron-app dev` напрямую фоном с CDP. Standalone seed-скрипты (`dev:seed-variant-bom-verify`) грузят `.env` через `dotenv/config`, НЕ `.env.dev` → падают EACCES на PG; запускать с `set -a; . ./backend-api/.env.dev; set +a` перед скриптом (dotenv не перезатирает уже заданное).
- **CDP-драйв редактора BOM (2026-07-06):** не делать `Page.reload` на electron-vite dev-стенде (роняет клиент — та же грабля, что в SKILL.md); драйвить раздельными короткими eval'ами (один длинный IIFE >60с ловит CDP-таймаут). Тяжёлые карточки (BOM В-59, 8 вариантов) вешают `Page.captureScreenshot` при `captureBeyondViewport:true` — снимать `captureBeyondViewport:false`. Скрипты наблюдения — `.verifier-electron/cdp-bom-observe.mjs` / `cdp-bom-open.mjs` (gitignored).
- **CDP-драйв: `innerText`/`textContent`-скан по всем `div` вешает рендерер (2026-07-08).** В своём `cdp-*.mjs` НЕ делай `Array.from(document.querySelectorAll('div')).find(d=>txt(d)...)` — `innerText` форсит reflow на каждом узле, на большой карточке это O(n) reflow'ов → `Runtime.evaluate` уходит в CDP-таймаут (120–240с), при том что тривиальный `1+1` eval мгновенный (main-thread освобождается между вызовами). Лечение: узкие селекторы (`table tr`, `[data-testid=...]`, `input[name=...]`), `textContent` вместо `innerText`, скан статуса — только внутри контейнера модалки (`input[name=propagate-group].closest('div[style*="overflow-y"]')`). Логин в драйвере делай идемпотентным (проверь `auth.status()` перед `login()`) — лишние logout/login добавляют секунды к каждому eval. Полный рабочий драйвер фичи «Распространить на группу» — `.verifier-electron/cdp-propagate.mjs` (gitignored, фазы seed/nav/modalInspect/scenario/readback).
- **verifier-electron под Electron 43 отработал фичу распространения деталей на группу (2026-07-08):** последовательность из профиля (`pnpm -r rebuild better-sqlite3` для Node-ABI backend → `start-backend.ps1` → `migrate-and-seed.ps1` → `install-app-deps` для Electron-ABI → `pnpm --filter electron-app dev` фоном с CDP) прошла штатно; данные группы/деталей засевал через bridge (`nomenclatureDirectoryPartCreate` + `nomenclaturePartSpecUpdate` + `admin.entities.setAttr('engine_brand_ids',[...])`).
- **TaskStop/kill pnpm-обёртки НЕ убивает electron+vite (2026-07-17):** после остановки фонового `pnpm --filter electron-app dev` остаются сироты, держащие CDP 9222 и vite 5173 — новый инстанс молча падает («Cannot start http server for devtools», dev-команда завершает exit 0). Перед рестартом: `netstat -ano | grep -E ':(9222|5173)'` → `Stop-Process -Id <pid> -Force` по каждому LISTEN-PID.
- **Новый отчёт по свежесозданным операциям пуст → сначала подозревай застрявший pull, а не отчёт (2026-07-18).** Симптом: bridge-вызов вернул `ok:true` (напр. `repairFundCaptureInstances`), а клиентский отчёт по этим данным показывает 0 строк; `sync.run()` отвечает `pulled:0` при `serverCursor < serverLastSeq`. Это тот же застрявший pull-курсор, что ниже, — данные есть на сервере, но не в реплике. Лечение — `window.matrica.sync.fullPull()` (минуты, 40k+ строк), после него отчёт наполняется. Не лезть отлаживать builder отчёта, пока не проверил `sync.run()` на `pulled`/курсоры.
- **Dev-реплика клиента: sync застревает в push-петле (2026-07-17):** `sync.run()` каждый раз пушит одни и те же ~210 pending-строк (server их скипает по stale-seq), pull-курсор стоит (cursor < serverLastSeq), свежие серверные строки (BOM и т.п.) НЕ приезжают. Лечение — `window.matrica.sync.fullPull()` через CDP (перезаливка реплики, минуты). Реплика зашифрована (db-key/DPAPI) — прямые INSERT вторым процессом невозможны (`SQLITE_NOTADB`); данные для смоука сеять через bridge/REST или fullPull, не через файл. Путь реплики CDP-инстанса: `%APPDATA%/@matricarmz/electron-app-cdp-9222/matricarmz.sqlite`.
- **electron-vite dev НЕ перезапускает main-процесс при правке main-кода на лету гарантированно** — после правок `src/main/**` для чистоты рестартовать dev-процесс целиком (иначе IPC-поверхность старая: смоук видел пресет без свежих правок options).
- **`git push` к `github.com:443` мерцает (TLS RST по SNI):** push периодически падает на TLS-handshake (`schannel`/openssl RST), при этом `gh` и `api.github.com` работают, а локальные read-операции (`git log`, `git status`) не затронуты. Лечится **повтором push'а** (обычно 2-я/3-я попытка проходит) — это сетевой флап, не проблема ключа/репо. `ssh -p 443 git@ssh.github.com` доходит, но dedicated-ключ там не авторизован → SSH-over-443 как обход не годится. Не уходить в диагностику «сломан remote/креды» — просто ретраить push.
- **CDP-смоук платежей (2026-07-29):** драйверы `.verifier-electron/_payments-smoke.mjs` + `_payments-smoke2.mjs` (gitignored). Грабли: (1) **первый запуск свежего dev-стека**: vite посреди прогона делает «optimized dependencies changed. reloading» → renderer-таргет пересоздаётся, ws CDP-драйвера молча закрывается («unsettled top-level await») — прогрей приложение (логин+навигация) или гоняй смоук вторым заходом; (2) **строки списков v3 открываются кликом по `<button>` с номером** (`btn.children.length===0 && btn.textContent.trim()==='TEST-001'`), синтетический click по `<tr>` карточку НЕ открывает; (3) deferred-create контракт (через `setAttr(contract_sections)` без attr `number`) не имеет `displayName` → в списке entities искать по деталям (`admin.entities.get` → attrs), не по displayName/searchText.
- **CDP-смоук v3 «Вкладки» (2026-07-27):** драйвер `.verifier-electron/_v3-shell-smoke.mjs` (gitignored). Грабли прогона: (1) в `/json/list` окно DevTools — тоже `type:'page'` (url `devtools://`) — фильтровать target по `url.startsWith('http')`, не по `!about:`; (2) ws-loader: брать `new URL(...).href` напрямую, `pathToFileURL(url.pathname)` на Windows даёт `D:\D:\…`; (3) dirty-guard закрытия карточки — кнопка «Не сохранять» (не «Закрыть без сохранения»); HMR-ремоунт может пометить карточку dirty — смоук должен уметь жать её и на переоткрытии.
- **CDP-смоук AI-чата (2026-07-19):** драйвер `.verifier-electron/_ai-chat-smoke.mjs` (phase1 → runner post-answer → phase2). Грабля: runner `aiChatRoutineIO.js` на этой машине надо запускать из `backend-api` с `. ./.env.dev` В ТОМ ЖЕ шелле, иначе dotenv подхватывает не тот ledger-каталог и seq записи расходится с backend'ом (клиентский pull её не видит; лечится fullPull, но правильно — общий env). Кнопка «ИИ‑агент» в клиенте больше НЕ зависит от `features.aiEnabled` (гейт снят в Ф6).
- **CDP-смоук панели «РАЗДЕЛЫ» (2026-07-29):** драйверы `.verifier-electron/_sections-smoke.mjs` (14 проверок: список/ширина/drag/📌/разделитель), `_sections-look.mjs` (осмотр + скриншот), `_reset-prefs.mjs` (сброс `shellPrefs` пользователя через bridge). Грабли прогона: (1) **у `Panel` нет `data-panel` в DOM** — ширину колонки мерить по внутреннему `.v3-split-sections`, не по `[data-panel="v3-sections"]` (селектор молча даёт `null`); (2) **прогон не идемпотентен по раскладке** — 📌 копится между запусками, поэтому проверки закрепления писать **дельтой** (`pinned.length === before + 1`), а не абсолютом; для проверки дефолтов сначала `_reset-prefs.mjs` + рестарт клиента; (3) **частичный HMR врёт**: после правки констант/пропсов V3-шелла клиент показывал старую ширину и не сохранял новый pref — правки такого рода проверять только после полного рестарта dev-клиента (`netstat` 9222/5173 → `Stop-Process` → заново).
- **⚠️ ПОПРАВКА 2026-07-30 (вечер) к пункту (1) ниже: «виртуализированные списки рендерят 0 строк» — НЕВЕРНО.** Реальная причина — **v3-оболочка монтирует страницу в панель БЕЗ раскладки, пока её вкладка не в фокусе**: узлы есть в DOM, но `getBoundingClientRect()` = `0×0` в точке `(0,0)`, поэтому виртуализация и рендерит 0 строк при честном «Всего: 7». Лечение — кликнуть вкладку в `.v3-tab-strip` (для списка это кнопка «Список BOM двигателей», для карточки — «Карточка …»), после чего панель получает высоту и список рендерит все строки, а строка открывается обычным доверенным кликом. **Следствие для смоуков:** проверка «элемент найден в DOM» здесь ложно-зелёная — доказательством отрисовки может быть только ненулевой прямоугольник + `document.elementFromPoint(центр) === элемент`. Рабочий пример — `.claude/skills/verifier-electron/scripts/cdp-bom-default-brand-render.mjs`.
- **Класс кнопки закрытия вкладки — `.v3-tab-close`** (не `.v3-card-tab-close`, как записано в заметке от 07-27: такого класса в DOM нет, и цикл очистки вкладок по нему молча не закрывает ничего, отчего прогон стартует с чужими вкладками). Разметка вкладки: `div.v3-tab.v3-tab-card` > `button.v3-tab-label` + `button.v3-tab-split` + `button.v3-tab-close`.
- **В своём CDP-драйвере ОБЯЗАТЕЛЕН таймаут на `send()`** (2026-07-30): без него зависший/занятый renderer даёт бесконечное молчание вместо ошибки, и прогон выглядит как «повис навсегда». 30 с достаточно.
- **Тяжёлые BOM морозят renderer после сохранения** (В-84 — 553 строки, В-59 — 561): следующий `Runtime.evaluate` уходит в таймаут на минуты. Для смоуков брать лёгкую фикстуру (`BOM CONFLICT-2 (smoke)` — 0 строк, 2 марки), боевые BOM трогать только осознанно.
- **CDP: чего на этом стенде НЕ выходит драйвить синтетикой (2026-07-30, три находки за один прогон):** (1) ~~виртуализированные списки (`VirtualTable`) рендерят 0 строк~~ — **см. поправку выше**: контейнер мерится нулевой высотой не из-за `flex:1 1 auto`, а из-за несфокусированной вкладки; (2) **выпадашка `MultiSearchSelect` синтетическими focus/click НЕ открывается** — `ComponentSuggestionsHintButton` перехватывает первый фокус (`hints.suppressed`), нужен trusted `Input.dispatchMouseEvent` (у `SearchSelect` в наряде та же разметка `div[data-idx]` открывается синтетикой — не переносить вывод с одного компонента на другой); (3) **восстановленные вкладки карточек от прежних прогонов ломают список**: при открытой карточке кнопки страницы списка («Создать BOM») в DOM не появляются вовсе — закрывать `.v3-card-tab-close` до навигации, но **сначала дождаться оболочки**, иначе цикл закрытия отработает вхолостую и проверка «вкладок 0» пройдёт ложно-зелёной. Плюс «Создать BOM» отключена, пока в фильтре списка не выбрана РОВНО ОДНА марка (`disabled={!selectedEngineBrandId}`).
- **CDP-смоук флага «основная BOM марки» (2026-07-30):** драйвер `.verifier-electron/_bom-default-brand-smoke.mjs` (7 проверок, через IPC-мост, не через жесты — причины выше). Предусловие: у марки `082470bc…` две активные BOM (вторая — `BOM CONFLICT-2 (smoke)`, `ab10ccc3…`, засеяна REST'ом: `curl -X POST :3001/warehouse/assembly-bom -d '{"name":"BOM CONFLICT-2 (smoke)","status":"active","engineBrandIds":["082470bc-…"],"lines":[]}'`). Смоук идемпотентен: сам снимает флаг, ставит, снимает.
- **CDP-смоук смены двигателя через ПИКЕР ШАПКИ (2026-07-30):** драйвер `.verifier-electron/_bom-conflict-swap-smoke.mjs` (13 проверок, гоняет renderer — bridge-драйверы правку в `WorkOrderDetailsPage` не видят). **Предусловие:** у марки целевого двигателя должно быть ≥2 активные BOM, иначе ветка кандидатов не воспроизводится. Засев второй BOM — REST по dev-backend: `POST /warehouse/assembly-plan`-марка `082470bc-65c8-41e7-a418-1fa674d40f24` (двигатель `ca4c1914…` / Д08АТ5366), `curl -X POST :3001/warehouse/assembly-bom -d '{"name":"BOM CONFLICT-2 (smoke)","status":"active","engineBrandIds":["082470bc-…"],"lines":[]}'`. Грабли прогона: (1) **логин по REST — поле `username`, не `login`** (`{"username":"verify","password":"verify123"}`); (2) **список нарядов после bridge-create пуст** — открывать карточку не кликом по строке, а deep-link'ом `window.matrica.app.navigateDeepLink({kind:'app_link', tab:'work_order', workOrderId})` (это `app`, НЕ `chat` — `chat.navigateDeepLink` не функция); (3) **`EntityReferenceField` держит своё отображаемое значение** — проверка «поле показывает новый номер» остаётся зелёной даже когда payload не изменился, гейтом может быть только чтение `workOrders.get(id)` после сохранения карточки (GOTCHAS M53).
- **CDP-смоук смены двигателя в сборочном наряде (2026-07-29):** драйвер `.verifier-electron/_engine-swap-smoke.mjs` (bridge-IPC: два двигателя → assembly-наряд без BOM → смена двигателя → issueAssembly → статус). Грабли: (1) **`start-backend.ps1` НЕ перезапускает уже идущий backend** («already running» / build-only) — после правок backend-кода убивать PID с порта 3001 (`netstat -ano | grep :3001`) и стартовать заново, иначе смоук гоняет старый код и врёт; (2) **первый `sync.run` после bridge-create наряда может не донести его строку** (server `sync reference rows skipped: invalid_reference` на двигатель из того же батча) — вторая синхронизация доносит; в драйвере перед серверными вызовами (`issueAssembly`) закладывать retry/второй `sync.run`; (3) bridge-API двигателей: `engines.setAttr(id, code, value)` (позиционные) и `engines.get(id)` / `workOrders.get(id)` — НЕ объектные аргументы.
