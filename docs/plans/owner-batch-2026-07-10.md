# Owner-батч 2026-07-10 (диктофон, 11 заданий → 8 тем)

> Надиктовка владельца, причёсана и заземлена в коде мультиагентной разведкой (7 скаутов, все находки с file:line проверены по коду). Выполнять темами, каждая тема = отдельный PR-flow под гейтами. Приоритет: **A → B → C → D/E → F → G → H**.

## ⚡ Немедленные рекомендации владельцу (не код)

1. **Суперадмин-логин на компах операторов — прекратить как только починим роли (тема A).** Пока он активен: наряды/правки операторов пишутся с `performed_by=суперадмин` (атрибуцию задним числом не восстановить), операторы видят закрытые наряды Рамзии и могут править всё, а черновики карточек шарятся между 3 машинами (кандидат на «самопроизвольные переименования», см. A3).
2. Скорее всего «нарядов видно меньше» — это выдача уровня **editor** в разделе «Наряды закрытые»: этот уровень означает «ограниченный владелец — видит ТОЛЬКО свои» (инверсия интуиции «больше доступа»). Плюс первый же выданный membership этого раздела **глобально выключает** легаси-политику (ramzia/glavbux hardcode). Проверим на прод-данных в A1.

---

## Тема A — 🔴 Integrity: роли, атрибуция, синк (диагностика прод + фиксы)

### A1. «Расширяю роль — нарядов видно меньше»
**Механика (по коду):** display-фильтр нарядов — [workOrderService.ts:429-439](../../electron-app/src/main/services/workOrderService.ts) → `canViewWorkOrder` с policy из EAV `section_access`. Уровень **editor раздела `restricted_work_orders` = confined-владелец: видит только свои** ([workOrderAccess.ts:101-114](../../shared/src/domain/workOrderAccess.ts)). Как только ХОТЬ ОДИН сотрудник получает membership этого раздела — легаси-hardcode (ramzia/glavbux) выключается целиком ([workOrderAccess.ts:52-66]). Смена system_role membership НЕ пересеивает (`seedMembershipForRole` — только в backfill-скрипте). Отдельная дыра: пустой/битый `section_access` (`'{}'`, triple-encoded) на клиенте трактуется как «засеянный пустой» → отказ во ВСЕХ разделах ([employeeService.ts:259-260](../../electron-app/src/main/services/employeeService.ts) vs серверная симметрия `restrictedWorkOrders.ts:111`).
**Действия:** (1) прод-диагностика: у кого какой membership `restricted_work_orders`, чей membership включил переключение политики; (2) UX: подписи уровней в AccessSectionsPage/зеркале карточки («editor = видит только свои», предупреждение при первом membership «легаси-режим выключится»); (3) клиент: пустой membership трактовать как unseeded (симметрия с сервером); (4) вернуть операторам их наряды (данные, не код: поправить membership + performed_by созданных под суперадмином). **Размер: M.**

> **Диагностика выполнена 2026-07-10 (прод, read-only):** `fatyhova` получила `restricted_work_orders: editor` 2026-07-10 → confined («нарядов всё меньше» = ровно она); `radik` несёт пустой `{}` (2026-07-10) → на клиенте «все разделы закрыты» при серверном fail-open; membership-режим политики закрытых нарядов активен давно (ramzia editor, glavbux/direktor viewer — корректно). **Код-фиксы (2), (3) — в PR fix/access-integrity-a1-a4 (CDP 8/8).** Данные (4) — по подтверждению владельца: снять `restricted_work_orders` у fatyhova; membership radik пере-засеять или очистить атрибут.

### A2. «Картер пропал из номенклатуры»
**Механика:** пара «Картер верхний/нижний» с общим артикулом `3301-15-30` — санкционированная модель ([warehouseService.ts:1919-1922](../../backend-api/src/services/warehouseService.ts)), НО код зеркала erp_nomenclature = артикул → у второго картера insert зеркала падает об unique-код ([warehouseService.ts:2013-2020], code=article на :816) → деталь живёт в directory_parts, но НЕ ВИДНА в клиентской номенклатуре. Второй капкан: та же пара попадает в code-collision группу экрана дублей ([partsDedup.ts:91-110](../../shared/src/domain/partsDedup.ts)) — один клик merge soft-delete'ит проигравшего.
**Действия:** (1) прод-диагностика: `backfillMissingPartNomenclature` dry-run (orphans) + поиск tombstone `mergedInto`; (2) фикс: `ensurePartNomenclatureMirror` при коллизии кода фоллбечится на синтетический `DET-…` код вместо отказа; (3) backfill найденных сирот; (4) вернуть пропавший картер. **Размер: M.**

### A3. «Марка В-84 переименовалась в код»
**Механика:** записи имени кодом в коде НЕ найдено; три реальных кандидата: (1) **display-fallback на UUID** — если строка имени не доехала синком (класс seq-drift), список марок показывает `displayName || id` ([EngineBrandsPage.tsx:62]); (2) **LWW без сравнения времени** — [applyPushBatch.ts:1035-1043](../../backend-api/src/services/applyPushBatch.ts) перезаписывает attribute_values БЕЗ проверки `updated_at` → отставший клиент со старой pending-очередью тихо затирает свежее имя (**системная дыра всего EAV-синка**); (3) **шаренные черновики** — card_drafts owner-scoped и синкаются; один логин на 3 машинах = общие recovery-черновики + restore поверх committed.
**Действия:** (1) прод-диагностика ДО фикса: история attribute_values/ledger по В-84 (кто/когда писал name; «код» = UUID или реальное значение?); (2) фикс LWW-guard (`updated_at >= existing`) в applyPushBatch — аккуратно, регресс-риск конфликт-резолюции; (3) display-fallback маркировать «(имя не синхронизировано)» вместо голого id. **Размер: L (диагностика может сузить).**

### A4. Суперадмин на N машинах
**Действия:** серверное критсобытие/предупреждение «один логин активен на N client_id» (сервер видит по client_settings); (данные) сверить performed_by нарядов за период практики. Главное лечение — A1, чтобы практика стала не нужна. **Размер: S-M.**

> **Диагностика 2026-07-10:** `valstan` активен на PC34/PC40/PC41; наряды №63–81 (01.07–10.07) созданы под valstan (аудит). **Код:** warn-критсобытие `auth.multi_machine_login` на heartbeat (dedup по составу машин) — в PR fix/access-integrity-a1-a4. **Данные:** переатрибуция performed_by старых нарядов — по решению владельца (кому какой наряд).

### A2/A3 — диагностика выполнена 2026-07-10, код-фиксы в работе (PR-2)

- **A2 (картер):** зеркала erp_nomenclature у всех живых картеров ЕСТЬ (по общему id) — «пропажа» = **merge на экране дублей**: «Картер верхний 3301-15-30» слит В «Картер НИЖНИЙ 3301-15-30» 19.06 (группа по общему артикулу!); «Картер верхний» (без кода) слит 09.07 в «Картер верхний 3301-16-39»; шаблон «Сборка 400» (sapegin) ссылается на слитый `ac7f8a29` → «пропал из шаблона». Merge НЕ пишется в audit_log (находка). Код-фиксы PR-2: аудит merge + предупреждение в UI дублей при РАЗНЫХ именах в code-collision группе. Данные: решение владельца — восстановить «Картер верхний» (un-merge вручную) и перевесить строку шаблона «Сборка 400».
- **A3 (В-84):** имя перезаписано **пустой строкой** 09.07 15:26 (ledger seq 814842, `value_json:""`), владелец вернул 10.07 11:31 (seq 815574); UI показывал fallback `displayName || id` = UUID («серверный код»). Кандидат-механизм записи пустого: шаренный черновик/force-save при одном логине на 3 машинах. Код-фиксы PR-2: (а) запрет сохранения пустого имени марки поверх непустого; (б) fallback-подпись «(имя не синхронизировано)» вместо голого UUID; (в) LWW-guard по `updated_at` в applyPushBatch (системная дыра EAV-синка).

---

## Тема B — Регрессии списка нарядов (быстро)

### B1. Номер/марка двигателя пропали из списка и печати (сборка)
**Механика:** #133 перевёл сборку на SSOT `payload.assemblyEngineId`, а reader'ы остались на построчных штампах: список — `getWorkOrderEngineInfo` ([workOrderService.ts:156-166]), печать — `buildPrintModel` ([WorkOrderDetailsPage.tsx:1444-1445]). Плюс `normalizeWorkOrderLine` ([workOrder.ts:90-99](../../shared/src/domain/workOrder.ts)) стрипает марку у строк без engineId — «Заполнить из спецификации» и наряды из прогноза дают именно такие строки → данные уже сохранённых нарядов «стрижены».
**Действия:** научить оба reader'а падать на `assemblyEngineId` (резолв id→номер/марка; следить за N+1 на 5000 строк) — это чинит и уже-сохранённые наряды; + штамповать строки в write-путях (`fillAssemblyFromBrandSpec`, `createAssemblyFromForecast`). **Размер: M.**

### B2. Колонка «Завершён» — прочерки
`completedAt` отдаётся только для закрытых; «Дата выполнения» (`completedDate`) уже приходит в списке для всех. Фикс: `row.completedAt ?? row.completedDate` в рендере колонки, sort-getter и contextColumns ([WorkOrdersPage.tsx:319-323, 197, 227]). **Размер: S.**

### B3. Поиск по номеру двигателя + сортировка по статусу
Поиск по номеру уже работает через haystack `JSON.stringify(payload)` — но только если номер в строках (лечится B1; надёжнее — явно добавить в haystack резолв assemblyEngineId в [workOrderService.ts:447-458]). Сортировка по статусу: добавить `'status'` в SortKey + sortKey в колонку + ранг `rowStatusCode` в getter ([WorkOrdersPage.tsx:87, 336, 188-207]). **Размер: S.**

---

## Тема C — Тормоза карточки номенклатуры (30-60+ с)

**Механика:** при маунте ~10 remote-first вызовов; форма заполняется после `Promise.all` из 5 ([NomenclatureDetailsPage.tsx:153-159]); nomenclatureList — таймаут 60с ([erpService.ts:638]), остальные 30с, ретраи ×3 с backoff (worst case 92-182с!); abort классифицируется transient и ретраится; `stockList` перед чтением синхронно дренирует outbox (до 15 POST × 30с). Локальные зеркала для номенклатуры/остатков/движений УЖЕ синкаются и локальные читатели готовы ([erpService.ts:104,184,338]).
**Действия:** (1) local-first для list/stock/movements (сначала локальная SQLite + `meta.dataSource`, фоновый remote-refresh); (2) разбить Promise.all — шапку рисовать сразу; (3) интерактивным чтениям срезать таймауты до ~5-8с × 1-2 попытки (адресно, не глобальный default); (4) outbox-дрейн убрать из read-path; (5) кэш lookups/item-types/templates/properties в main с TTL (сейчас — на каждый маунт без кэша). **Размер: M.**

---

## Тема D — «Наряд на сборку» из списка двигателей (ПКМ)

У EnginesPage контекстного меню нет; паттерн готов (useListSelection + ListContextMenu, образец [WorkOrdersPage.tsx:384-388]). Пункт «Наряд на сборку» (single-row, гейт `caps.canCreateWorkOrders`): deferred-create `workOrders.create()` → `openWorkOrder(id, {initialPayload: {workOrderKind: Assembly, assemblyEngineId: engine.id}})` — backend не трогаем. `buildListContextMenuItems` расширить опциональным `extraItems` (общий util 5 страниц). **Размер: M.** Связка с E: сид только engineId, BOM подтянет карточка.

## Тема E — Автоподстановка спецификации марки при выборе двигателя

«Шаблон марки» = BOM-спецификация (assemblyBom), НЕ workOrderTemplates. Врезка — onChange хедерного SearchSelect «Двигатель» ([WorkOrderDetailsPage.tsx:2163-2186]): если выбрана марка и нет строк с partId — автозапуск `fillAssemblyFromBrandSpec` (отрефакторить: параметризовать brandId/payload — сейчас читает из state-замыкания, после patch() данные устаревшие). Заодно снять граблю «кнопка “Заполнить из спецификации” задизейблена у пустого наряда» (fallback `primaryAssemblyEngineBrandId` на марку из assemblyEngineId шапки). Нет активной BOM → молча пропустить. Наряд из прогноза (forecastVariantKey) — не трогать. **Размер: S.**

---

## Тема F — Вложения: одно «Вложения», любые файлы

**Причина ошибки (готовый быстрый фикс):** у типа `engine_brand` НЕ засеяны attribute defs `drawings`/`tech_docs` (и `description`!) — setAttr отвечает «Неизвестный атрибут» ([entityService.ts:200], seed только name/category_id/attachments [seed.ts:182-191]); сам файл при этом успевает залиться и остаётся сиротой. Быстрый фикс: `ensureAttributeDefs` на маунте карточки марки (образец PartDetailsPage:554-561). **S.**
**Унификация:** категория — только EAV-код + путь на Я.Диске; валидации типов файлов нигде нет (уже «любые»); расширения-бейджи и предпросмотры в AttachmentsPanel уже есть ([AttachmentsPanel.tsx:29-67, 100-124]). План: одна панель `attachments` на марке (и далее детали), merged-чтение attachments+drawings+tech_docs с дедупом по FileRef.id, запись всегда в `attachments`, при первом изменении — пересохранить merged и занулить старые attrs. **Порядок раскатки:** сначала объединённый UI всем клиентам, потом чистка attrs (иначе на старых версиях «файлы пропали из Чертежей»). **Размер: M.**

---

## Тема G — Массовые галочки актов + act-scoped «Распространить»

### G1. «Выбрать все / снять все» для Акт компл./Акт деф.
Шапки над колонками нет (подписи внутри строк); проще и надёжнее — два тристейт-чекбокса в тулбар списка (рядом с «Выбрать показанные», [EngineBrandDetailsPage.tsx:1172-1231]), работают по `visibleParts` (уважают фильтр/срез). Батч-API нет — цикл `upsertPartSpecBrandLink` (явный false снимает флаг, undefined сохраняет второй флаг) с прогрессом и блокировкой повторного клика; учесть гонку с useLiveDataRefresh (20с). **Размер: S.**

### G2. Новый режим распространения: «перепривязать акт целиком»
Сейчас replace доступен только для scope=all и УДАЛЯЕТ привязки. Новый режим: replace для scope=акта — у деталей вне набора в марках-целях **снимается галочка акта** (привязка/кол-во/узел/второй флаг сохраняются). Отсутствующий примитив: `clearPartSpecBrandLinkActFlagForBrands` (partsPagination.ts, рядом с :270; один read-modify-write спеки на деталь); расширить тип `PartSpecRow.brandLinks` act-флагами (:23-29 — runtime они уже приходят); UI: разрешить replace для act-scope ([EngineBrandDetailsPage.tsx:195-196, 1087]), scope-зависимые тексты подтверждения («галочки акта будут сняты», не «детали удалены»). Существующие режимы не трогаются. **Размер: M.**

---

## Тема H — Подсказки зависимостей разделов

**Механика симптома «Вера не видит контракты»:** generic-канал `admin:entities:listByEntityType(contract)` гейтится разделом contracts ([sectionGate.ts:155, 71-78]) — у оператора без membership contracts поле «Контракт» карточки двигателя пустое; вдобавок reject роняет весь `Promise.all` в loadLinkLists → пустеет и селектор цеха ([EngineDetailsPage.tsx:990-994]).
**План:** (1) статическая карта `SECTION_DEPENDENCIES` в [sectionAccess.ts](../../shared/src/domain/sectionAccess.ts) (рядом с каталогом): `production→contracts(viewer)`, `work_orders→production(viewer)`, `contracts→production(viewer)`, `supply→warehouse(viewer)` — по фактическим гейтящимся lookup'ам; (2) хелпер `missingDependenciesFor(membership, sectionId)`; (3) confirm-подсказка в ОБЕИХ точках записи membership (AccessSectionsPage.setLevel :58-74 и зеркало EmployeeDetailsPage :2119-2135): «Для полноценной работы с „Производство“ нужен ещё просмотр „Договоры и контрагенты“ — добавить?» — при согласии оба ключа в один setAttr (атомарно); (4) мелочь: per-вызов catch в loadLinkLists двигателя, чтобы отказ одного справочника не ронял остальные. Permission-ось (masterdata.view и overrides) в подсказке упомянуть текстом, но не моделировать. **Размер: M.**

---

## Порядок и статус

| # | Тема | Размер | Статус |
|---|------|--------|--------|
| A1 | Роли: confined-инверсия + переключение политики | M | ✅ #161 + данные (#163–#167, скрипт `fix:owner-batch-20260710`) |
| A2 | Пропавшая номенклатура (зеркало/дедуп) | M | ✅ #162 (аудит merge + предупреждение) + шаблон/данные |
| A3 | Переименование марки (LWW/fallback/черновики) | L | ✅ #162 (pair-LWW guard, запрет пустого имени, fallback-подпись) |
| A4 | Суперадмин на N машинах (критсобытие) | S-M | ✅ #161 + переатрибуция №63–81 → fatyhova (данные) |
| B1 | Номер/марка в списке+печати нарядов | M | ✅ #168 |
| B2 | «Завершён» = Дата выполнения | S | ✅ #168 |
| B3 | Поиск по № двигателя + сортировка по статусу | S | ✅ #168 |
| C | Local-first карточка номенклатуры | M | ✅ (короткие интерактивные таймауты, бюджет outbox-дрейна, TTL-кэш lookups, шапка вне Promise.all) |
| D | ПКМ «Наряд на сборку» в списке двигателей | M | ✅ (контекст-меню + deferred-create с assemblyEngineId) |
| E | Автоподстановка BOM при выборе двигателя | S | ✅ (onChange + авто-fill при открытии из ПКМ; снята грабля disabled-кнопки) |
| F | Вложения: быстрый фикс defs + унификация | S+M | ✅ (марки: ensureAttributeDefs description/attachments + одна панель «Вложения», merge legacy) |
| G1 | Массовые галочки актов | S | ⬜ |
| G2 | Act-scoped replace в «Распространить» | M | ⬜ |
| H | Подсказки зависимостей разделов | M | ⬜ |

## Открытые вопросы владельцу

1. **A1:** какой именно доступ и кому расширяли, когда нарядов стало меньше? (Подозрение: уровень editor в «Наряды закрытые».) Это ускорит прод-диагностику.
2. **A3:** «серверный код» у В-84 — это длинный UUID с дефисами или короткий код вида `MARKA-…`? (Разные механизмы.)
3. **F:** подтверди, что семантика «чертёж vs документ» теряется осознанно (файлы сольются в одно «Вложения» у марок и деталей).
4. **B1:** резолв номера/марки для УЖЕ сохранённых стриженных нарядов сделаем через assemblyEngineId — печатная форма старых нарядов изменится (номер/марка вернутся). Ок?
