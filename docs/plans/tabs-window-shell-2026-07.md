# План: вкладочная оболочка окна («резиновый интерфейс» v2)

**Status:** IN PROGRESS — этапы 1–5 ✅ (2026-07-27), остался этап 6 (приёмка владельцем + перевод v3 в дефолт).
**Заказ владельца:** 2026-07-27, устно в сессии.
**Масштаб:** крупный рефакторинг renderer-оболочки (App.tsx + все страницы списков/карточек). Делать в несколько сессий, поэтапно, за фиче-флагом.

## Context

Отказываемся от деления «резинового» интерфейса на три экрана. Вместо этого — **одно окно с панелью вкладок**.

## Требования владельца (дословно, нормализовано)

1. **Две закреплённые вкладки** (нельзя закрыть, всегда в панели):
   - **«РАЗДЕЛЫ»** — крайняя слева, кнопки навигации по разделам программы.
   - **«Список …»** — всегда вторая (справа от «РАЗДЕЛЫ»); вместо «…» подставляется название списка, открытого из «РАЗДЕЛЫ» (напр. «Список Двигатели»).
2. Эти две вкладки могут **делить окно на две части**: «РАЗДЕЛЫ» — четверть экрана слева, «Список …» — три четверти справа.
3. **Открытие карточки** (из списка или из разделов) → **новая вкладка на весь экран**; «РАЗДЕЛЫ» и «Список …» сворачиваются, но **сохраняют наполнение и позицию фокуса/курсора**.
4. **Лимит вкладок — 10.** При >5 открытых — **красное предупреждение**: «открытых вкладок многовато, закрой отработанные — иначе конфликты или забудешь сохранить».
5. **Активная вкладка подсвечивается.**
6. **Названия вкладок — человекопонятные** (ФИО, номер двигателя, название детали), не технические коды/UUID.
7. **Ширина шапок вкладок:** по ширине названия; растягивать на весь экран не надо; **сжимать только когда не влезают** в ширину окна.
8. **Карточки — резиновые по всей доступной ширине** (поля данных не должны сжиматься, пока на экране есть место).
9. **Карточки компактнее по высоте:** ещё сократить межстрочные расстояния; **длинные списки внутри карточек — сворачиваемые и по умолчанию свёрнутые**.
10. **Прогресс загрузки + блокировка редактирования:** при открытии списка/карточки/документа — крутящийся спиннер (как загрузка Windows); пока объект не загружен целиком — редактирование заблокировано; после загрузки спиннер исчезает. То же — для операций с **прикреплёнными файлами** (начался процесс → спиннер, завершился → убрать).

## Текущее состояние кода (что уже есть)

- `electron-app/src/renderer/src/App.tsx` — центральный роутер экранов; уже есть зачатки «вкладочности» карточек (recovery-табы, `serviceOriginTab`, крестики `.v2-card-tab-close`).
- Планшетный режим (docs/plans/tablet-windows-ui-mode-2026-07.md) — учесть, не сломать: вкладочная оболочка должна работать в обоих режимах Комп/Планшет.
- Меню слева: `span.v2-menu-btn-label` (см. docs/machines/PC40.md — селекторы для смоуков поменяются!).

## Этапы

### Этап 1 — разведка и каркас (без изменения поведения) — ✅ 2026-07-27
**Ключевая находка разведки:** V2Shell («Резиновый») уже несёт бо́льшую часть механики — `v2OpenCards` (вкладки карточек + фокус + dirty-guard + session-restore, персист в `shellPrefs.v2.session`), колонку списка (`v2ActiveListTab`), панель кнопок (`ButtonPanel` + `buttonLayout`). Поэтому v3 сделан **надстройкой над механикой v2**, а не с нуля.

Сделано (PR этапа 1):
- `shared/uiShellV2.ts`: `shellVersion: 'v3'`, `V3Prefs/V3Session`, лимиты `V3_MAX_TOTAL_TABS=10` (2 закреплённые + 8 карточек), `V3_WARN_TOTAL_TABS=5`, хелперы `v3CanOpenCard`/`v3ShowTabsWarning`, sanitize + тесты. Дефолт остаётся v2.
- `shellV3/V3TabShell.tsx` + `shellV3.css`: полоса вкладок ([РАЗДЕЛЫ][Список …][карточки…]), сплит ¼/¾, карточка fullscreen; сплит и карточка держатся смонтированными (display:none) — наполнение/фокус/несохранённое не теряются; красное предупреждение при >5 вкладках; подсветка активной; ширина шапки по названию (ellipsis при нехватке места).
- App.tsx: `isV2` теперь включает v3 (вся v2-механика работает), `isV3` — только рендер-ветка; кап карточек 8 в v3; `v3PinnedFocus` (возврат на закреплённые без закрытия карточки); кнопка «🗂 Вкладки (бета)» в шапке (вход только явным кликом).

Гейты: shared build + typecheck (electron/backend/web-admin) + lint + тесты (shared 499, backend 462) — зелёные. CDP-смоук v3 отложен до этапов 2–3 (дефолтное поведение не менялось, v3 — explicit opt-in бета).

Известные хвосты каркаса (в этапы 2–3): v3 пока пишет сессию в `v2.session` (V3Session не задействован); split-«2 рядом» из v2 в v3 не рендерится (session-restore secondary в v3 надо гасить); лимит 10 не блокирует открытие 11-й (кап пока вытесняет старейшую); заголовки карточек = v2CardTitle (`kind · id6` для не-двигателей — очеловечить).

### Этап 2 — закреплённые вкладки и сплит 1/4 + 3/4 — ✅ 2026-07-27
Каркас этапа 1 закрывал большинство пунктов; добито и проверено живым CDP-смоуком:
- Фикс: несписочные страницы (История/Настройки/Заметки) не рендерились в v3 — теперь любой не-list tab = fullscreen-контент с **эфемерной вкладкой** `📄 <название>` (без крестика); переход на него снимает pinnedFocus (иначе открывался невидимо за сплитом).
- Живой смоук `.verifier-electron/_v3-shell-smoke.mjs` (gitignored) — все шаги зелёные: login → v3 strip → закреплённые [РАЗДЕЛЫ][Список Двигатели] → **сплит ratio 0.25** → карточка TEST-001 вкладкой fullscreen (сплит скрыт) → возврат на закреплённые (карточка остаётся смонтированной, hidden) → рефокус → закрытие с dirty-guard («Не сохранять») → страница История эфемерной вкладкой.
- Гейты: typecheck + lint зелёные.

### Этап 3 — карточки во вкладках — ✅ 2026-07-27
- **Человекопонятные заголовки:** асинхронный `resolveCardTitle` — EAV-сущности по атрибутам (`name`/`full_name`/`contract_number`/…) через `admin.entities.get`; наряды `🛠 Наряд № N`, склад-документы по номеру, заявки `🛒 Заявка № N`, экраны `🖥 имя`, отчёты `📊 <title>` (статический реестр). Резолв один раз на карточку; upsert-эффект не затирает резолвнутое фолбэком (`isFallbackCardTitle`). Живой смоук: карточка TEST-BRAND показала «TEST-BRAND».
- **Лимит 10:** гейт в `setTab` — 11-я карточка не открывается, красное «⛔ Лимит 10 вкладок…» (авто-гаснет 5с); рефокус открытой карточки и session-restore идут с bypass-флагом.
- Предупреждение >5, подсветка активной, dirty-guard закрытия — были с этапов 1–2, живьём проверены.
- Session-restore: работает через существующую механику `v2.session` (**решение:** v3 сознательно делит сессию с v2 — открытые карточки переживают переключение оболочки; отдельный `V3Session` пока не задействован).
- **Известная дыра лимита:** открытие карточки того же kind, что уже в фокусе, идёт мимо setTab-гейта (upsert молча вытесняет старейшую при переполнении). Закрыть на этапе 6 или при жалобе.

### Этап 4 — резиновые компактные карточки — ✅ 2026-07-27
- **Резина через токены, скоуп только v3** (`.v3-card-body` в shellV3.css): `--ui-content-max-width/--ui-content-block-max-width → 100%`, инлайновые потолки 820/900px перебиты `!important` на `.entity-card-shell`/`.entity-card-span-full`/`.card-panel`. Живой замер: карточка двигателя 1906px из 1918 доступных (раньше 820). v1/v2 не тронуты.
- **Компакт по вертикали:** `--card-row-gap: 2px`, `--ui-space-4: 8px`, `--ui-space-5: 12px` в том же скоупе.
- **`CollapsibleSection`** (components/) — общий компонент «свёрнуто по умолчанию» для длинных списков.
- **Первое применение паттерна:** AttachmentsPanel — список файлов >5 по умолчанию свёрнут («▶ Показать файлы (N)»), превью-миниатюры не грузятся, пока свёрнуто (перф-бонус). Покрывает все карточки с вложениями разом.
- Хвост: раскатать CollapsibleSection на прочие длинные списки карточек — по мере жалоб/приёмки (кандидаты: рекламации, история операций).

### Этап 5 — прогресс загрузки и блокировка — ✅ 2026-07-27 (базовый слой)
- Общие примитивы: `Spinner` + `LoadingOverlay` (components/LoadingOverlay.tsx; оверлей перехватывает клики → редактирование заблокировано до загрузки), CSS `.mx-spinner`/`.mx-loading-overlay` в global.css (живьём проверено: keyframes применяются).
- Подключено: Suspense-фолбэк v3 (спиннер при загрузке раздела), карточка двигателя («Загрузка карточки...» со спиннером), AttachmentsPanel (крутилка на время файловых операций — busy-сообщения с «...»; upload и так имел прогресс-бар).
- Хвост: самозагружающиеся карточки (наряд/заявка/контракт/…) держат свои loading-стейты внутри страниц — оборачивать их в `LoadingOverlay` по одной по мере приёмки (готовый компонент есть).

### Этап 5.5 — доработки владельца перед приёмкой — ✅ 2026-07-28
Заказ владельца (устно, сессия 2026-07-28): всё из «Резинового», чего не хватало в v3.
- **Тянущийся разделитель** сплита РАЗДЕЛЫ|Список (react-resizable-panels; ширина в `V3Prefs.splitPct`, кламп 15–85, дебаунс-персист).
- **Сравнение «2 рядом»:** кнопка ⑃ на неактивной вкладке карточки открывает её второй панелью (дефолт пополам, `V3Prefs.comparePct`); ▐-маркер на вкладке; ✕ в заголовке панели и на вкладке закрывает сравнение. Механика — общая с v2 (`v2SecondaryCard`), v3 только рендерит.
- **Крестик на эфемерных вкладках-страницах** (`📄 История` и т.п.): закрытие возвращает к открытому списку (или первому доступному разделу-списку); клик по шапке возвращает фокус на страницу.
- **Пофиксен баг фокуса:** открытие карточки того же kind из списка (двигатель → другой двигатель) не переключало экран со списка — `tab` не менялся и `pinnedFocus` не сбрасывался; ключ фокусной карточки добавлен в зависимости эффекта.
- Живой CDP-смоук `.verifier-electron/_smoke-v3-split-compare.mjs` (gitignored) — 26 проверок PASS, включая персист обоих процентов между прогонами.
- Грабля либы: `onLayoutChanged.meta.isUserInteraction` не взводится на синтетических pointer-событиях → персист через сырой `onLayoutChange` + guard от echo (|Δ|>0.5%).

### Этап 6 — приёмка и смоуки
- Обновить CDP-смоуки (selectors `v2-menu-btn-label` и пр. в docs/machines/PC40.md — задокументировать новые).
- `/verify` через verifier-electron; приёмка владельцем; переключение флага в дефолт; удаление старого шелла отдельным PR.

## Риски
- Keep-alive вкладок × память: 10 тяжёлых карточек одновременно — следить.
- Конфликт с планшетным режимом и recovery-модалкой.
- Все существующие смоук-драйверы завязаны на текущий DOM — сломаются, чинить пакетно на этапе 6.

## Открытые вопросы
- «Список …» при переходе в другой раздел — вкладка одна и переиспользуется (название меняется) или на каждый раздел своя? **Предположение: одна, переиспользуется** (иначе лимит 10 съестся списками).
- Сплит 1/4+3/4 — фиксированный или тянущийся разделитель? Предположение: фиксированный на первом этапе.
