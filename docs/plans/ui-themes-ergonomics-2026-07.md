# План: темы оформления («Строго/Красиво») + эргономика интерфейса

**Status:** DRAFT (не начат). Делать ПОСЛЕ вкладочной оболочки (tabs-window-shell-2026-07.md) — иначе украшать придётся дважды.
**Заказ владельца:** 2026-07-27.

## Требования владельца

1. **Переключатель оформления «Строго / Красиво»** (строгое и украшенное).
2. **Украшенное:** разнообразие цвета, завитушки, даже цветочки — колорит, привлекательно для глаз, проявить фантазию.
3. **По ~5 цветовых гамм у каждого варианта** (на разный вкус).
4. **Строгое тоже улучшить:** данные в строках/блоках не должны сливаться в трудноразличимую массу (зебра, разделители, контраст, воздух).
5. **Эргономика:** перегруженные информацией места разгрузить — разделить на блоки или на разделы-вкладки со взаимосвязанным редактированием.

## Подход

**Сначала концепция, потом реализация** (так просил владелец).

### Фаза A — концепция (1 сессия)
- Инвентаризация текущих стилей: где живут цвета/типографика (CSS-переменные? разрозненные инлайны?) в `electron-app/src/renderer`.
- Спроектировать **дизайн-токены**: `--mx-bg`, `--mx-surface`, `--mx-accent`, `--mx-text`, spacing, radius… Тема = набор токенов.
- Модель темы: `{ mode: 'strict'|'fancy', palette: 1..5 }` → 10 комбинаций. Хранение — локальная настройка клиента (SettingsPage), мгновенное применение без рестарта.
- «Красиво»: декоративный слой поверх тех же токенов — орнаментальные разделители/уголки (SVG-завитушки, цветочные мотивы в шапках), фоновые градиенты малой насыщенности. Декор НЕ должен снижать читаемость данных и скорость рендера (никаких тяжёлых картинок в строках гридов).
- Подобрать 5 гамм × 2 режима (light-базис; контраст по WCAG для текста данных). Скилл `design` (image-нейросеть) — для эскизов украшенного варианта.
- Результат фазы: макеты/эскизы + список токенов → показать владельцу, выбрать направления.

### Фаза B — фундамент токенов (1 сессия)
- Ввести CSS-переменные, перевести на них базовые компоненты (кнопки, таблицы, карточки, меню, вкладки).
- Переключатель «Строго/Красиво» + выбор гаммы в Настройках; дефолт — текущий вид (строгая гамма №1 ≈ сегодняшняя).

### Фаза C — строгий режим: читаемость
- Зебра в длинных таблицах, явные границы блоков, увеличенный межблочный воздух при сохранении компактности строк (согласовано с «компактнее по высоте» из плана вкладок).
- Ревизия контраста второстепенного текста.

### Фаза D — украшенный режим
- Декоративные элементы (SVG-орнаменты, акцентные шапки, завитушки/цветочки) по эскизам фазы A; 5 гамм.

### Фаза E — эргономика перегруженных экранов
- Собрать топ перегруженных мест (кандидаты: карточка двигателя/дефектовка, карточка наряда, карточка BOM, заявки снабжения) — подтвердить у владельца.
- Для каждого: разбить на блоки/вкладки внутри карточки со связями между ними; свернуть второстепенное (переиспользовать `CollapsibleSection` из плана вкладок).
- Приёмка по-экранно.

## Риски
- Декор × производительность больших гридов — декор только на уровне контейнеров, не строк.
- 10 комбинаций тем × все экраны = комбинаторика проверки: смоук прогонять на 2 крайних (строгая №1, красивая №1), остальные — точечно.
- Пересечение с планшетным режимом (крупные кнопки) — токены должны работать в обоих.

## Открытые вопросы
- Нужна ли тёмная гамма среди 5? (Владелец не просил, но спросить.)
- Список перегруженных экранов — подтвердить у владельца в фазе E.
