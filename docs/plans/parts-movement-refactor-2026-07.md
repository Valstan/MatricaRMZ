# Движение запчастей: ревизия модуля и предложения

**Status:** DRAFT — на утверждении владельца
**Created:** 2026-07-17
**Owner ask:** «рефакторинг модуля движения запчастей»: улучшения по мировой практике, удобство операторов, усиление контроля учёта деталей; найти пропущенные связи/отчёты/этапы для контроля ремонта двигателей.

## Ревизия 2026-07-17: что уже сильно (не трогаем)

- Документная модель с проведением: `draft → planned → posted → cancelled`, проведённый документ иммутабелен, оптимистичная блокировка (`warehouseService.ts:3049`, `:2649`).
- Регистры: остатки + журнал движений с hash-chain (SHA-256, env `MATRICA_STOCK_MOVEMENT_HASHCHAIN_ENABLED`) + подписанный ledger (`schema.ts:1353,1373`); журнал событий документа `erp_journal_documents`.
- min/max/«К заказу», резервы, `qty−reserved`, кнопка «Создать заявку на закупку» из остатков; прогноз сборки с BOM-китами, planned incoming, ремфонд-рекомендациями.
- Авто-документы из нарядов: `production_release` (Repair/Manufacturing), `assembly_consumption` + резерв (Assembly), `assembly_return` (rework/scrap), списание ремфонда при закрытии ремнаряда.

По мировой практике (документ-ориентированный inventory с posting, item ledger, reservations, reorder points) фундамент **уже соответствует**. Разрывы — в связках между модулями и в операторском контуре.

## Найденные разрывы (GAP)

| # | Разрыв | Где | Следствие |
|---|---|---|---|
| G1 | **Комплектация двигателя не считается**: нет «что выдано / чего не хватает на этот двигатель»; `engine_readiness_to_assemble` смотрит только остатки, без issued/reserved per engine | `presets/engines.ts:504` | Главный затор завода (начало сборки, идея 007) программой не закрыт |
| G2 | **Поставка по заявке не создаёт приход**: `fulfill_full/partial` меняют только статус+payload.deliveries; приход оператор перебивает вручную | `supplyRequestService.ts:550` | Двойной ввод, расхождения заявка↔склад, нет three-way match |
| G3 | **Выдача со склада free-form**: в UI `stock_issue` нет выбора двигателя/наряда, хотя `movements.engineId` в регистре есть | `StockDocumentDetailsPage.tsx:592` | Теряется «куда ушла деталь» — ослаблен контроль |
| G4 | **Заявки снабжения не видны прогнозу**: `planned_incoming` строится только из складских документов | `warehouseService.ts:1027` | Прогноз занижает приход, дефицит к закупке дублируется |
| G5 | **Сторно не реализовано**: `cancelWarehouseDocument` для posted отсылает к сторно, но функции нет | `warehouseService.ts:2750`, `warehouse.ts:95` | Ошибочный проведённый документ неисправим штатно |
| G6 | **Утиль дефектовки не попадает в scrap-локацию**: учёт только отчётный (реестр из operations); склад утиля наполняется лишь из assembly_return | `presets/engines.ts:339` | Физическая куча утиля ≠ данные; инвентаризация утиля невозможна |
| G7 | **Оборотной ведомости нет**: только плоский `part_movement_journal`, без сальдо на начало/конец периода | `presets/warehouse.ts:307` | Классический учётный отчёт отсутствует |
| G8 | **Нормы расхода нетипизированы**: УТД-20/В-84 в BOM qty + `notes`-текст «% нормы» | `importUtd20V84Norms.ts:42` | План/факт по нормам и «план закупок по нормам» не построить |
| G9 | **Инвентаризация не слепая**: «Факт» предзаполнен учётным | `StockInventoryPage.tsx:144` | Провоцирует подтверждение не считая — мимо мировой практики blind count |
| G10 | **Поэкземплярный ремфонд ↔ агрегатный сток не сверяются**: клеймёные экземпляры в operations, количества в локации repair_fund — независимо | `repairFundInstance.ts`, `repairFundService.ts` | Тихое расползание двух учётов |
| G11 | **Нет печати сопроводительных документов** (требование-накладная/акт) из складского документа | `StockDocumentDetailsPage.tsx` | Бумажный след цеха живёт отдельно от программы |
| G12 | UI-мелочи: дубли номенклатуры в строках не ловятся; минус-остаток виден только при проведении; `SupplyToolMovementsPage` — параллельный реестр вне документной модели | agent-ревизия UI | Ошибки ввода, две модели учёта |

## Предложения (фазы, по убыванию ROI)

### Ф1. «Комплектование двигателя» (kitting) — главный затор ⭐
Экран/отчёт на двигатель: BOM марки × выдано (`assembly_consumption` по engineId) × резерв × остаток × дефицит; пикинг-лист «что взять со склада» с печатью. Закрывает G1, стыкуется с идеей 007 (MVP «наряд→наличие→прогноз»). ~2–3 сессии.

### Ф2. Замкнуть снабжение (G2+G4)
Кнопка «Оформить приход» на отметке поставки заявки → предзаполненный `purchase_receipt` (строки из deliveries, связь заявка↔документ); одобренные заявки с ожидаемой датой → в `planned_incoming` прогноза; отчёт «заявки без прихода» (контроль снабжения). ~2 сессии.

### Ф3. Выдача под двигатель/наряд + печать (G3+G11)
Поля «Двигатель»/«Наряд» в `stock_issue`/`stock_writeoff` (регистр уже готов); печать требования-накладной и акта списания из карточки документа (HTML как в актах). Обязательность привязки — настройкой (мягко, анти-бюрократия). ~1–2 сессии.

### Ф4. Сторно (G5)
`reverseWarehouseDocument`: авто-документ с обратными движениями (`reversalMovementType` уже есть), ссылка на исходный, право `movements.revert`. Исправление ошибок становится штатным и видимым в журнале — усиление контроля вместо соблазна «поправить руками в БД». ~1 сессия.

### Ф5. Отчёты учёта (G7+G8)
Оборотная ведомость по номенклатуре/локации (сальдо нач. + приход − расход = сальдо кон.); типизировать норму (`normPercent` в BOM-строке, миграция notes → поле) → отчёты «расход план/факт по нормам» и «план закупок по нормам» (уже в PENDING §нормы, для экономистов). ~2 сессии.

### Ф6. Контроль физики (G6+G9+G10)
Кнопка «Списать в утиль-локацию» из дефектовки (полуавто, по строкам scrap_qty; анти-бюрократия — не принудительно); переключатель «слепая инвентаризация» (факт пуст, учёт скрыт) + частичная инвентаризация по фильтру (cycle counting); отчёт-сверка «экземпляры ремфонда vs агрегатный сток». ~2 сессии.

### Quick wins (в любую фазу)
Дедуп строк документа; клиентская проверка остатка при вводе расхода (по загруженным балансам, мягкое предупреждение); ед. измерения в данные, а не placeholder.

### Отложено (не сейчас)
- QR-сканер движений — уже в PENDING (блокер: склад REST-only, offline-скан = L–XL сдвиг + железо).
- Партионный учёт (batch как регистр) — на текущем объёме не окупается.
- Слияние `SupplyToolMovementsPage` в документную модель — при явной боли.
- Выделенная роль «кладовщик» (сейчас склад под `operations.edit` у engineer/master) — вопрос владельцу при внедрении МОЛ.

## Открытые вопросы владельцу
1. Порядок фаз подтверждён? (Рекомендация: Ф1 → Ф2 → Ф3, дальше по отзывам.)
2. Привязка выдачи к двигателю/наряду — обязательная или опциональная? (Рекомендация: опциональная + отчёт «выдачи без привязки».)
3. Слепая инвентаризация — включить по умолчанию или переключателем? (Рекомендация: переключатель, дефолт как сейчас.)
4. Проверить на проде включён ли `MATRICA_STOCK_MOVEMENT_HASHCHAIN_ENABLED` (контроль целостности журнала).
