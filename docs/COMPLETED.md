# Completed — done-индекс (Tier-1)

> **Token economy:** одна строка на завершённую **нитку/релиз** — название + итог + релиз/PR. Granular-история (коммиты, row-counts, диагностика) — в `git log` и телах PR, **сюда не дублируется**. Это ledger «не гонять повторно»: грепнул по понятию — увидел, что и когда сделано. `/start` его **не** читает; читается по требованию («это уже сделано?»).
>
> Открытое — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md). Грабли — в [`GOTCHAS.md`](GOTCHAS.md). Архив планов — [`plans/_archive/`](plans/_archive/).
>
> _Наполнен при сливе PENDING (PR-2, 2026-06-16): крупнейшие нитки — строками ниже; **полный список закрытых планов** — в [`plans/_archive/`](plans/_archive/), granular — в `git log`/PR. План реорга: [`plans/_archive/memory-reorg-2026-06.md`](plans/_archive/memory-reorg-2026-06.md)._

## Доступ / RBAC / безопасность

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-01 | Phase 3 — ролевая изоляция нарядов Рамзии (work-order rework) | PR #9 (в `main`, до релиза) | Серверный read-гейт нарядов владельца `ramzia` (видны только ей, `glavbux`-read, superadmin) на 3 sync-поверхностях (`pullChangesSince`+`ledger.ts` query/snapshot) + reports-билдере + AI `get_operations`; pre-sync purge уже-утёкших копий (`/ledger/state/restricted-purge` + клиентский self-healing delete); restricted-config по login (не хардкод UUID). **Аддитивный гейт — НЕ трогает `PRIVACY_TABLES`/pending → нулевая регрессия.** 3 утечки найдены+закрыты в verify: **C1** (AI `get_operations` без гейта), **deleted-payload** (22 soft-deleted наряда хранят `meta_json` → тумбстоун-утечка; restrict независимо от `deletedAt`), **M1** (case-sensitive owner-match). Dual-role live-verify (CDP UI + HTTP, прод-снапшот): оператор 0/37 нарядов Рамзии, владелец/админ 16/16; purge 37/0/0. Write-block read-allowlist (glavbux видит, но не правит) — отложен (PENDING 🟡). План: [`plans/work-order-rework-2026-06.md`](plans/work-order-rework-2026-06.md). |
| 2026-06-30 | Доаудит ротации ledger-ключей (добивка H8) — активные ключи чисты | docs-PR (аудит, без релиза) | Доаудит next-step H8: сверка **живых прод-ключей** с тем, что реально лежало в публичной git-истории (теперь приватный `MatricaRMZ-archive`). **Подписной** `server-key.json`: историч. pub-отпечаток `1563d94f…` ≠ живой `d2b91b5e…` → ротирован H8 (#614), утёкший выведен. **Data-key**: в трекинге была только **legacy** `{keyBase64}` (=`v1-legacy`, удержан для replay `enc:v1`, **не активен**); активный `k-mq3wacgz-6d7aa9` создан 2026-06-07 17:46 MSK — через 11 мин **после** untrack #252 (14:34Z) → в трекинг не попадал. Живой backend (PID 93822, env `/etc/matricarmz/matricarmz.env`) подтверждённо читает `MATRICA_LEDGER_DIR=/home/valstan/matricarmz-ledger` (релокейт H8 цел). Ни один live-ключ не в архиве и не в публичном snapshot (`git ls-files backend-api/ledger` пуст в обоих). **Вердикт: ротация НЕ нужна, H8 полон.** Граница (необратимо): `v1-legacy` остаётся в keyring навсегда (replay), историч.-публичные данные экспонированы навсегда — защищается только будущее (на чистом `k-mq3wacgz`). Побочно: удалён **orphan-ledger** в git-checkout `backend-api/ledger/` (150 МБ, gitignored, живым процессом не использовался; создавался деплоем при cwd=`backend-api` без env → `DEFAULT_LEDGER_DIR=cwd/ledger`). |
| 2026-06-27 | H1 (A+B1a) — pull read-authz + закрытие утечки хэшей паролей | #619,#620,#621 (v2026.627.1151) | H1 «полный рефактор» read-authz, разбит на A+B. **A** (#619): cold-snapshot/`/state/query` применяют per-user privacy чатов/заметок (была утечка чужих чатов/заметок на cold-sync; `/state/changes` уже резал; единый `syncPrivacy.ts`). **B1a** (#620): единый pull read-authz чокпойнт `makePullReadFilter` на 3 поверхностях — найдена и закрыта **утечка `password_hash`** (employee EAV-атрибут синкался на ВСЕ клиенты, PII-фильтр не покрывал; аутентификация только серверная → хэши в локальной БД каждого оператора = оффлайн-крекинг), теперь режется всем; `audit_log` admin-only на pull (живой потребитель — мёртвый `AuditPage.tsx`); HR-поля (`birth_date`/`hire_date`) операторам только своё. **B\*** (#621): клиент при старте идемпотентно чистит уже утёкшие `password_hash` из локального SQLite (без re-sync). Состязательное ревью (7 агентов) поймало 3 перегиба до коммита: `termination_date` оставлен (авторитет ростера табельщика), PII/HR только operator-ролям (legacy `user`/pending не задеты), `system_role`/`access_enabled` отложены (ломали бы колонку «Доступ» — нужен renderer-фикс). Гейты: backend 347/347, electron 185. **B2** (per-role EAV-матрица: договоры/финансы для двигателиста/технолога/мастера + **timekeeper свёрнут сюда** + `system_role`/`access_enabled`+renderer; предусловия: миграция legacy-`user`, wipe stale-кэша; **viewer=бухгалтерия → финансы не скрывать**) — открыто, PENDING + [`plans/h1-table-read-authz-2026-06.md`](plans/h1-table-read-authz-2026-06.md) §12. |
| 2026-06-26 | H8 — утечка ledger-секретов в публичном репо + ремедиация | #614,#615,#616 (v2026.626.2207) | Закоммиченный в **публичный** репо live-ledger вскрыл: подписной `server-key.json` (HEAD, прод подписывал им же — sha совпал), `data-key.json`+`state.json` (история 2026-01-27→06-07), 119 блоков / 552 `enc:v1:` поля **реальных ПДн** (chat/EAV сотрудников) — ключ+шифротекст оба публичны → расшифровываемы. Ремедиация: live-ledger **вынесен из git-checkout** (`MATRICA_LEDGER_DIR`, `mv` 341k блоков — root-cause), подписной ключ **ротирован** (старый retired+бэкап), data-key уже на `k-mq3wacgz` (майская ротация); untrack из HEAD (#614); **репо сделан приватным** (0 форков → эффективно без force-push). Приватизация отключила CodeQL → заменён на **Semgrep SAST** (#616), который сразу нашёл реальный **GCM `authTagLength:16`**-хардеринг (ledger/E2E крипта) — исправлен; gitleaks-CI починен под приватный репо (`pull-requests:read`). GOTCHA **M19** (live-данные трекаются в checkout'е → `git rm`/`reset` удалит прод-данные). История-rewrite деприоритизирована (приватный+ротация). Реакция на утечку ПДн — PENDING §4 (владелец). Релиз **v2026.626.2207**. |
| 2026-06-26 | Клиент hardening + engine features + права Фатыховой | #607–#612 (v2026.626.1229) | Безопасность (клиент): safeStorage для E2E-ключа/токенов (fail-closed, без plaintext), очистка backup-cache при старте, nav/window guards, CDP inert в prod-пакете. Двигатели: поле номера ≥30ch + подсказка дубля только при создании; «Разборку двигателя произвёл» (multi-employee) в акте дефектовки + печать; поиск и слияние дублей (`analyzeEngineDuplicates`/`mergeEngineGroup`, damerauLevenshtein кластер). Права выданы Фатыховой Наталье. Снят masterdata.edit-override у alina_goz/ur491; вычищены 38 призрачных строк user_permissions (ed625dd3 + legacy users). |
| 2026-06-26 | Аудит безопасности — Фаза 3 (bounded + 4 ранее отложенных) | #598,#600,#601,#602 (v2026.626.651) | Закрыт весь bounded-хвост аудита. **#598** (bounded): IPC-гейт `e2e:keys:export/rotate` (`admin.users.manage`), `/ledger/blocks` admin-only (сырые подписанные plaintext-блоки), серверная атрибуция `audit_log` (был подделываем через sync-push), ReDoS-гард `/ledger/state/query`. **4 ранее отложенных, закрытых «по-настоящему» по решению владельца** (design→adversarial-review→live-verify пайплайн): **#600** auth на `/updates/peers` (per-route `requireAuth`+`SyncUse`; токен сессии в апдейтере, до-логина graceful fallback на central-server → P2P/LAN/авто-апдейт сохранены); **#601** IDOR файлов — linkage-authz `canAccessFile` (владелец/админ/чат-участник/заметки-share/engine-операция/EAV-по-типу/`directory_parts`; LIKE+exact-id verify, actor-scoped SQL после ревью — нашло HIGH-регрессию `LIMIT`-без-`ORDER BY`) + allowlist путей `chat:sendFile`/`files:upload` (`pathOriginRegistry`, Yandex+шеринг сохранены); **#602** login typeahead+recent — убран enum-oracle `/auth/login-options`, заменён на `/auth/login-suggest` (префикс ≥2, только login+ФИО, свой лимитер) + machine-local recent 10 дней; live CDP-verify поймал и починил коллизию typeahead с глобальным input-assist. Backend+electron, без миграций; backend 337. Остаток (H1 read-authz, H8 ротация ключа+история, H7-остаток, Фаза 2/инфра) — PENDING + [`plans/security-hardening-2026-06.md`](plans/security-hardening-2026-06.md). |
| 2026-06-25 | Аудит безопасности + упрочнение phase 0/1a (мультиагентный) | #591–#593, #596 (v2026.625.1722) | Исчерпывающий аудит (карта поверхностей + 11 ревьюеров + состязательная проверка каждой находки; 51→46 подтверждённых). Закрыты на проде: **C1** публичная `/auth/register` давала роли `pending` почти все права → выгрузка всей БД через `/ledger` (фикс `pending→{}` в `auth/permissions.ts`); **C2** эскалация до суперадмина записью своего `system_role` через `/ledger/tx/submit` (backstop server-only employee-атрибутов в `ledgerAuthzGuard` для всех ролей); **H2–H5** конструктор отчётов сливал чужие чаты/заметки/EAV-PII + `/ledger/state/query` в обход PII-фильтра (admin-гейт приватных таблиц + `redactReportRows` + `makeOperatorReadFilter`); отзыв refresh-токенов централизован в `setEmployeeAuth` (смена пароля/сброс/отключение). Backend-only, без миграций; shared 251 / backend 322. Прод-периметр проверен (PG/backend на 127.0.0.1, UFW default-DROP, Let's Encrypt-серт, клиент не отключает TLS). Остаток (H1 read-authz рефактор, H8 ротация ключа+чистка истории, mediums/lows) — PENDING + [`plans/security-hardening-2026-06.md`](plans/security-hardening-2026-06.md). В тот же релиз вошли правки печати наряда (#595: подписи на 1 листе без bottom-pin + чёткие рамки таблиц работ/бригады). |
| 2026-06-23 | Watchdog #7 — внешний агент самовосстановления клиента | #548–#553 (v2026.623.1642) | Go-агент (`watchdog/`, чистый stdlib): при сорванном NSIS-обновлении (папка приложения снесена) сам переустанавливает из локального/скачанного инсталлятора; handshake `%APPDATA%\MatricaRMZ\watchdog.json`, bridge `/client/watchdog/report`, 2 per-user Scheduled Task (`installer.nsh`). Кнопка «Переустановить» + серверная `reinstall` в админке, версия-гейт `clientHasWatchdog` (граница 2026.622.1241). Раздаётся **unsigned** (серт владельцу недоступен — РФ) — мониторить AV. План — [`plans/client-watchdog-agent.md`](plans/client-watchdog-agent.md). Хвосты (install-dir дискрепанс, field-тест) — PENDING §G. |
| 2026-06-23 | Отдельное право `contracts.edit` (договоры/контрагенты вне `masterdata.edit`) | #557 (hotfix) → #558 (v2026.623.1642) | Корень: UI гейтил кнопки контрактов по `masterdata.edit`, а ledger-guard требовал `admin` (operator всегда `false`) → «кнопка есть, sync режется» (`forbidden:contract`, шторм у `alina_goz`). #557 — быстрый фикс (contract/customer→`masterdata.edit`, раскатан). #558 — правильный: выделенное право `ContractsEdit` (UI `caps.canEditContracts` + ledger), default-off у всех operator-пресетов (вкл. technolog), выдаётся точечно. Прод: право выдано `alina_goz`+`ur491`. Урок → GOTCHAS M17 (UI-гейт ↔ серверный гейт parity). |
| 2026-06-23 | Колонка «Привязка» в списке двигателей | #556 (v2026.623.1642) | Отдельный сортируемый столбец: 🟢 договор · 🟢🟢 ДС · 🔴 не привязан (вынесен из мелких точек у номера). Доменные хелперы `isContractAddonToken`/`classifyEngineContractBinding` в shared (+тесты). CDP-verify: все три состояния + сортировка. |
| 2026-06-23 | Фикс 429-шторма синхронизации (rate-limiter scope) | #555 (v2026.623.1642) | `trust proxy=1` при цепочке myjino+nginx (2 hops) → лимит ключевался по IP форвардера у ВСЕХ → общий бакет 600/мин+30/15мин на весь парк. Фикс: прод `.env` `MATRICA_TRUST_PROXY_HOPS=2` + лимиты 3000/300; код — строгий `authLimiter` только на `/auth/login`+`/register` (не на частые `/auth/me`/`/refresh`). После раската 0×429. Также снят зависший 22ч `pnpm install` на проде (GOTCHAS M16). |
| 2026-06-21 | RBAC операторов #474 — роли + серверный enforcement | #524–#529 (v2026.621.1815) | Две оси (уровень×участок). **M1** роли-пресеты (`engineer/technolog/master/supply/timekeeper/viewer`) + `normalizeRole`. **M2a** ledger write-гейт — серверная per-operation authz по `entity_type`/`operation_type` в `applyLedgerTxs` (закрыл дыру brain #015: на пути записи `/ledger/tx/submit` авторизации не было, гранулярные коды были UI-only). **M2b** read-ограничение чужих ЛД/зарплат на pull. **M3** deny-лог (`server.authz.denied`). **M4** очередь одобрения изменений пользователей (расширение `change_requests`). Migration-safe (бьёт только операторские роли). Роли активированы 12 операторам (через настоящий ledger, seq→757677). Инцидент с standalone-скриптом (писал в побочный ledger) восстановлен без потерь → GOTCHAS M15/M16. План — [`plans/operator-rbac.md`](plans/operator-rbac.md). Хвосты раската — PENDING. |

## Детали / номенклатура (parts)

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-06-29 | Деталь находится в наряде по реальному артикулу + «Изделие (legacy)»→«Деталь» | #656,#657 (v2026.629.1711) | Срочный баг владельца: деталь (помпы 411-00-35А/411-00-48) есть в «Ревизии ремфонда», но не подбирается в наряд на ремонт. Диагностика: оба пикера читают `erp_nomenclature`, но наряд строил «артикул» опции из `sku` (у мигрированных зеркал = авто `DET-<id>`) → деталь индексировалась под мусорным кодом, поиск по настоящему `code` (411-…) не находил; одноимённые «Насос водяной» неразличимы. Фикс #656 (`WorkOrderDetailsPage`): артикул/подпись из реального `code`, `sku` тоже в поиск, подпись «имя (артикул)» — как в Ревизии. Отдельно: тип «Изделие (legacy)»=`product` устарел (не создаётся, только legacy-ярлык); backfill #657 `product→part` на 256 строках (все реальные детали; через `recordSyncChanges`; pg_dump перед apply). Verified CDP (поиск по коду находит) + psql. Техдолг: глубокая де-дупликация `directory_parts↔erp_nomenclature` (DET-зеркала в данных). |
| 2026-06-29 | Эпик «не сохранять пустые карточки» — Phase 1/2a/2b/3a | #652,#653,#654,#655 (v2026.629.1711) | План [`drafts-no-empty-cards-recovery-2026-06`](plans/drafts-no-empty-cards-recovery-2026-06.md). **Ph1** (#652): «Пустые карточки» (Контроль и аналитика) — `emptyCardsService` (analyze→soft-delete с ре-валидацией+гард ссылок) по двигателям/договорам/сотрудникам/нарядам/заявкам. **Ph2a/2b** (#653/#654): отказ от авто-создания — `createEngine`/employees-create не пишут строку (id-UUID), `get` синтезирует пустую карточку, первая запись атрибута материализует (employees изолированы через `fallbackTypeId`, общий admin-путь не тронут). **Ph3a** (#655): синкаемая таблица `card_drafts` (полная проводка зеркально notes: registry/ledger/applyPushBatch/privacy/migrate 0070 + клиент 0015 + `cardDraftsService`+bridge). Verified: checkSyncContract, CDP round-trip (save→push→fullPull→clear)+psql. **Открыто:** Ph2 договоры/masterdata+наряды, Ph3b/3c/3d — см. SESSION_HANDOFF. |
| 2026-06-29 | Имя/артикул детали в карточке марки/двигателя следуют правке номенклатуры | #648 (v2026.628.2326) | Баг владельца: переименование детали (имя+артикул) в «Склад→Номенклатура» не доезжало в карточку марки и двигателя (показывали старое). Корень — два стора: правка идёт в `erp_nomenclature`, а карточки читают `directory_parts` (`listAllPartSpecs`→`listWarehouseNomenclaturePartSpecs`), где name резолвится «existing wins» и не входит в update-set → не обновляется; code владеет erp. Фикс (read-through, выбор владельца): `listWarehouseNomenclaturePartSpecs` LEFT JOIN `erp_nomenclature` (по id) → name/code из карточки номенклатуры, fallback на directory_parts. Чинит уже разошедшиеся детали сразу, без backfill. Спека (размеры/привязки/metadata) осталась на directory_parts. Verified CDP+dev-PG (развёл erp-имя SQL → read-path вернул erp). Граница: одиночный `getWarehouseNomenclaturePartSpec` не трогали; глубокая де-дупликация — техдолг (UUID-only). |
| 2026-06-20 | Артикул в списке деталей марки + уборка карточки «Деталь» фаза-1 (закрыла B-перепроверку) | #509/#514 (v2026.621.1133) | Второй столбец карточки марки = **артикул** детали (был per-link `assemblyUnitNumber`, пусто→«не задано»); строка `flex`→CSS-grid (колонки выровнены); `addPart` больше не пишет литерал «не задано». Владелец подтвердил «деталь появляется». Уборка карточки «Деталь»: складские списки + usage/файлы — сворачиваемые (`collapsible` в `SectionCard`). |
| 2026-06-20 | Карточка «Деталь» фаза-2 — выпил мёртвого standalone-кода `PartDetailsPage` | #519 (v2026.621.1133) | Компонент всегда embedded (`NomenclatureDetailsPage` всегда передаёт `embedded`) → весь `!embedded` путь недостижим. Снесён standalone-рендер + add-field/brand-link/field-order/dimensions подсистемы + `saveAllAndClose`/`saveCore`/`registerCardCloseActions` + мёртвые импорты. **2826 → 1154 строк (−1691).** Живой embedded-путь (`buildMetadataFromState` побайтово, attribute-save очередь, рендер Основное/Где используется/Файлы) не тронут; tsc/eslint/build зелёные. Живой рендер детали не прогнан (dev-снапшот без номенклатуры) — глянуть на релизе. |
| 2026-06-19 | Дубли: heal зеркала при мердже (создаёт карточку выжившему) | #493 (v2026.619.1703) | `mergeDirectoryParts`: если у выжившего нет складской карточки, а у поглощаемой есть — soft-delete зеркала донора (код свободен под partial-index 0066) + вставка зеркала выжившему → нет тупиковой ошибки «нет складской карточки», слить можно любую пару. Прод-диагностика: 1 «инкогнито» (Гильза 303-07-22) = половина дубля по коду. Heal-тест, 284 backend. |
| 2026-06-19 | Детектор коллизий артикула в «Дубли деталей» | #483 (v2026.619.1041) | Новый ярус «🟠 коллизия артикула» (одинаковый непустой код, разные имена) — раньше слепое пятно (`exact`=имя+код, `fuzzy`=похожие имена). На проде 4 пары (`327-00-62`, `3301-15-30` Картеры, `3309-25-2`, `406-12-44` Блок/Крышка). shared 13/13. Объясняет «не добавляется/не вижу картер». |
| 2026-06-18 | Атомарность мерджа дублей (deep-fix #4 item 1) | #480 (v2026.618.1551) | `mergeDirectoryParts` → один `db.transaction` (нет частично-слитого каталога при сбое на полпути); ledger/sync (свой pipeline, в tx не входят) накапливаются и сбрасываются **после** коммита, при rollback не пишутся. Unit: rollback + happy (278 backend-тестов); live-merge против dev-PG (коммит лёг, повтор tombstoned-loser'а отклонён чисто). Item 2/3 (зеркало survivor + partial unique index) — открыты (PENDING). |
| 2026-06-18 | Phase 3.8 scope A — снос дубль-junction номенклатуры | #464/#465 (v2026.618.117) | Снесён пустой `erp_nomenclature_engine_brand` из sync-протокола/кода/UI (дубль-секция «Применимые марки» убрана, оставлена «Применяемость») + миграция `0064 DROP`; WS-A2 на проде: 18 артикулов `nomenclature.code←directory.code` (ledger-проекция) + 2 имени. Остаток 4 строки → doubles-merge (PENDING). План `plans/_archive/parts-nomenclature-phase3-8.md`. |
| 2026-06-17 | В-46/В-84 импорт + авто-карточка + артикул + фикс мерджа дублей | #457–#459 (v2026.617.1614) | 39 деталей В-46/В-84 + 3 новые марки как базовые (оба акт-флага), 83 зеркала `erp_nomenclature` (вкл. хвост Т3); `createDirectoryPart` авто-создаёт складскую карточку; артикул-колонка в списках склада/номенклатуры; мердж дублей не падает (client timeout 60s+no-retry, backend батч `recordSyncChanges`). |
| 2026-06-16 | parts-articul-acts T1–T6 | #327–#338 | Артикул = различитель, дедуп-модуль, импорт базового списка (итог на проде), наследование галочек актов марка→двигатель, формы актов. T3-импорт сверен — целевой итог уже на проде. |
| 2026-06-08 | Phase 3.5 — депрекация оси «шаблон детали» | v1.46.0 | `part_template` выпилен (рудимент), `/parts/templates/*` снят, EAV в ноль. |
| 2026-06-07 | Phase 3.6/3.7 + parts-chain-audit (brain) | backend-деплои | Снос dead service-fn + dual-write моста; унификация id (`dp.id==nom.id`), dangling ref → 0; единый источник истины для детали. |
| 2026-06-06 | bom-parts-into-engine-brand-lists (brain mandate) | #212/#213 | BOM-детали заведены как «детали марки» + постоянная гарантия в `upsertWarehouseAssemblyBom`. |
| 2026-06-05 | Phase 3 — parts EAV → directory_parts | v1.39.0–v1.42.0 | Остаточные EAV-поля → `directory_parts.metadataJson`; своп всех `/parts/*` вызовов; blanket **410** на `/parts/*`. directory_parts — единственный источник. |
| 2026-06-01 | Phase 2 Variant A — part-spec в directory_parts | v1.35.0–v1.40.0 | `code/templateId/dimensions/brandLinks` колонки; embedded карточка детали в номенклатуре. |
| 2026-05-24 | Phase 1 — зеркало parts↔nomenclature, component types | v1.21.x–v1.22.0 | `component_type_id` нативная колонка, audit/fix mirror-скрипты. |

## Склад / BOM / прогноз

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-06-29 | Ремфонд Ф3.1 — ручная отметка экземпляра `in_fund↔repaired` на карточке двигателя | #646 (v2026.628.2326) | Отложенный хвост ремфонд-эпика. Развилка (наряды количественные, реестр поэкземплярный) разрешена **вариантом C** (выбор владельца): кнопка «✓ Отремонтирована» / «↩ В фонд» у `in_fund`-экземпляра в реестре личных номеров (карточка двигателя) — точная отметка ремонта конкретного физ-экземпляра по номеру, без эвристики «первые N» и без переделки нарядов. Терминальные утиль/замена не трогаются (`canToggleRepairedStatus`). `setStampedInstanceRepaired` через `recordSyncChanges` upsert (доезжает клиентам, класс M6/M8). Verified CDP e2e (клик кнопки + доезд через `fullPull`/DB-seq). |
| 2026-06-28 | Учёт ремонтного фонда (ремфонд) — Фазы 0–4 (эпик завершён) | #638,#639,#640,#642,#643 (v2026.628.1814) | Эпик: ремфонд как регистр не вёлся («бездонная яма»); деталь после дефектовки уходила в пустоту. Опора — существующая системная локация `repair_fund` (количественно, без миграции/схемы). План — [`plans/_archive/repair-fund-2026-06.md`](plans/_archive/repair-fund-2026-06.md). **Ф0** (#638): страница «Ревизия ремфонда» (меню Склад) — прямой массовый ввод деталей в фонд, семантика ревизии (set-абсолют) через `stock_inventory` на `repair_fund`. **Ф1** (#639): кнопка на карточке двигателя — приход **годных к ремонту** (`repairable_qty` по present-строкам, qty из `repairable_qty`) из дефектовки; идемпотентно по двигателю (операция `repair_fund_intake` хранит прошлый занос, проводится только дельта +приход/−расход) → защита от призраков (которые в 2026-05 заморозили старый разборочный поток); partId из мета `__brand_part_id`/`__part_id`. **Ф2** (#640): закрытие ремонтного наряда списывает отремонтированное из фонда (`repairFundService.releaseRepairFundForWorkOrder`, хук в `workOrderClosingService` ветка Repair, динамический import, best-effort + clamp по остатку, `stock_writeoff`). Решения владельца: только количество (личные номера — Ф3); регистр на `repair_fund`. Каждая фаза CDP-verify (фонд 0→4 приход, наряд закрыт → фонд −2). **Ф3** (#642): поэкземплярный учёт деталей по личным (набитым) номерам с провенансом (с какого двигателя снята) + классификация утиль/замена/ремонт; хранилище — `operations` type `repair_fund_instance` (НЕ новая синхро-таблица), идемпотентный upsert по `(engine,nomenclature,stampedNumber)`; кнопка «📌 Зафиксировать личные №» + секция на карточке двигателя. **Ф4** (#643): печатное «Требование к заказчику» по двигателю (детали утиль/замена с личными номерами — обоснование роста цены, в «дело двигателя»); печать с карточки + версионируемый снимок (`repair_fund_requirement`), без сумм (решение владельца). **Заодно фикс Ф3-sync** (#643): server-захват без `recordSyncChanges` не получал `last_server_seq` → не доезжал клиентам инкрементальным pull (реестр жил только в ответе capture) — пойман на Ф4-CDP-смоуке, тот же класс GOTCHAS M6/M8. Релиз эпика — v2026.628.1814 на проде. **Остаток: Ф3.1** (статус `in_fund→repaired` на закрытии ремнаряда — отложено) — PENDING. |
| 2026-06-22 | Аудит точности прогноза сборки — 3 фикса источников данных | #540 (v2026.622.1241) | Перепроверены все источники прогноза (сток/локации, BOM-комплекты M:N, каналы прихода) 3 фокус-агентами. Фиксы: техн.локации (ремфонд/утиль/«в сборке») исключаются из «годного» **всегда**, не только при пустом фильтре → убран двойной счёт под фильтром; `carter`→роль jacket; диагностика «остаток без склада» (NULL `warehouse_location_id`). Подтверждено корректным: merge дублей репойнтит сток↔BOM атомарно, нет двойного reserved. Тесты 50/50. Латентные hxvosты (warning BOM без brand-link / дробный qty / dayOffset) — PENDING. |
| 2026-06-22 | Ремнаряд «Выдан в работу» — только выданные кормят прогноз | #541 (v2026.622.1241) | У ремонта нет статуса «открыт» (draft/closed), `updateWorkOrder` сбрасывает в draft → канал прихода считал черновики приходом, завышая план. Флаг `repairIssued` в `WorkOrderPayload` (едет в metaJson→backend) + кнопка «Выдать в работу/Отозвать» на ремонтном наряде; `buildRepairIncomingFromWorkOrderPayloads` учитывает только выданные. Раскат: прогноз по ремонту пуст, пока мастера не отметят наряды — PENDING (операционный). |
| 2026-06-09 | warehouse-analytics C1–C5 | v1.45.0–v1.46.0 | Выпуск двигателей по маркам/цехам, брак, low-stock setup. |
| 2026-06-09 | shop-statistics Phase 0 | 2026-06-09 | Дашборд: работы + пропускная способность двигателей (honest-banner о покрытии). |
| 2026-06-09 | прогноз сборки (assembly-from-forecast) | v1.29.x | Наряд из прогнозa + резерв; edge cases + warnings. |
| 2026-05-27 | Phase 2.x — склады на FK `warehouse_location_id` | v1.17.x–v1.31.0 | Полная миграция 4 регистров с legacy `warehouse_id text` на uuid FK; DROP COLUMN. |
| 2026-05-22 | BOM-refactor серия | v1.21.0–v1.21.5 | variantGroup, component-type auto-fix, убраны stub-skeleton'ы и technical-fallback'ы. |

## Акты / дефектовка / наряды

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-06-30 | Эпик черновики/восстановление + Phase 2 deferred-create (наряды/заявки/контрагенты/марки/договоры) | #660–#667 (**v2026.630.1141**) | 7 ниток эпика «не сохранять пустые карточки + черновики/восстановление» ([план](plans/drafts-no-empty-cards-recovery-2026-06.md)), все CDP-верифицированы (вкл. kill/restart), CI зелёный. **3b** (#660): debounced-автосейв снимка наряда в `card_drafts`, draft-aware восстановление, редизайн close-guard (диалог «Не сохранять / Оставить черновик / Сохранить», убран форс-сейв по таймеру), панель восстановления после краха. **3c-раздел** (#661): `DraftsPage` (таб «Черновики») — список/открыть/удалить. **Phase 2 наряды** (#662): `createWorkOrder` без вставки + `updateWorkOrder`=upsert (номер при первом сохранении, «№ новый» до того); фикс пробела 3b — `flushSave` чистит recovery-черновик. **3c-«Сохранить как черновик»** (#663). **Phase 2 заявки** (#664): тот же приём (номер `Z-…`, scope подразделения). **Ядро masterdata + слайс 1** (#666): `admin:entities:get/setAttr` получили опц. `fallbackTypeId` (backward-compatible) → **контрагенты + марки двигателей** deferred-create. **Слайс 2** (#667): **договоры** (хелпер `setContractAttr`, все ~16 собственных записей через fallback). Inline-lookup/copy не трогались (не дают призраков). Остаток эпика — товары/услуги (nomenclature-путь, отдельный механизм) + `AdminPage` raw-сетка + 3d → PENDING 🟡. |
| 2026-06-29 | Печать наряда: выбор грифа «Утверждаю» (директор / технический директор) | #651 (v2026.629.1711) | SSOT `WORK_ORDER_APPROVERS` (shared) — два варианта грифа (Директор И.А. Тихомиров / Технический директор В.И. Гурьянов); `printSettings.approver` (хранится только не-дефолтный `technical` → старые наряды печатаются байт-в-байт); сегментированный переключатель в диалоге печати с живым превью, выбор персистится на наряд. Verified CDP e2e (переключение меняет гриф, persist round-trip). |
| 2026-06-29 | Связь комплектность→дефектовка (Т7) — наличие гейтит дефектовку | #647 (v2026.628.2326) | Запрос владельца: деталь попадает в дефектовку только если отмечена «на месте» в комплектности (раньше `repairable_qty` сеялся всегда → отсутствующая деталь «забита намертво что есть»). Доменный инвариант `normalizeEngineInventoryRow` (SSOT для печати/заявок/ремфонда/требований): `present=false` → actual/scrap/replace/repairable=0, branch=null; **дефект⇒наличие** (scrap+replace>0 форсит present=true) — миграция legacy без потери данных. UI: утиль/замена read-only+серым до отметки наличия (с подсказкой) + каскадная очистка при снятии «На месте»; bulk «в утиль/замену» пропускает отсутствующие. Presence-default (базовые→present=false, прочие→true) уже работал. Гейт по галочке «На месте» (бинарно), UI блокирует не скрывает (выбор владельца). Verified CDP e2e (замок/разблокировка/каскад). |
| 2026-06-28 | Наряд: дата выполнения, задаётся оператором («Дата выполнения») | #637 (**в main, до релиза**) | Факт. дата завершения не хранилась — деривилась из `updatedAt` при закрытии → закрыл сегодня наряд вчерашней работы = записывалось сегодня (владелец заметил на сборочных). Поле `completedDate` в payload (нормализация как `startDate`/`dueDate`); карточка: открытый наряд — редактируемое «Дата выполнения» (умолчание сегодня, можно прошлую), закрытый — read-only «Завершён» = `completedDate ?? updatedAt`. Статус/список/зарплатный отчёт читают `completedDate ?? updatedAt`. Все типы (поле в общей шапке). Дату склада не меняет; без изменений бэкенда (едет в metaJson через все пути закрытия). CDP-verify: открытый = редакт.поле сегодня, закрытый = «Завершён» из fallback. |
| 2026-06-27 | Доработка списка нарядов + sticky-шапка карточки (follow-up к капремонту) | #634 → #635 (v2026.627.2226) | Owner follow-up после обкатки v2026.627.1912. **Список** (`WorkOrdersPage`): убрана колонка «Ответственный»; «Дата выдачи» → «Дата создания наряда»; добавлена сортируемая «Начало работ» (`startDate` — «Приступить», раньше отсутствовала в списке); «Исполнители» = кто «Принял в работу» (слот блока «Выдача» по caption «принял», первый заполненный из нескольких нач. цехов — helper `getWorkOrderAcceptedByEmployeeId`), иначе фамилии бригады (fallback «принял, иначе бригада»). `listWorkOrders` отдаёт `startDate`+`acceptedByEmployeeId` (вместо `responsibleEmployeeId`); shared ipc-тип обновлён. **Карточка** (`WorkOrderDetailsPage`): кнопки `CardActionBar` + блок реквизитов (№/даты/шаблон) закреплены сверху одним sticky-заголовком — прямым потомком единственного скролл-контейнера. Был `position:sticky`, но заперт в тесном родителе внутри двойного скролл-контейнера (внутренний `overflow:auto` EntityCardShell инертен, реальный скролл — внешний `A`) → откреплялся сразу. Клиентское изменение (без миграций). Гейты зелёные (typecheck/lint/backend 347/CI); CDP-verify (verifier-electron, 22 наряда): заголовки списка, обе ветки «Исполнители» (fallback + accepted-over-crew через bridge: Коротких vs Шампоров), sticky drift 0px на 277px прокрутки. **Прод: v2026.627.2226 раскатан** (git pull → build серверных → 3 артефакта → ledger-publish → restart; /health+/updates/status=2226, blockmap 200). |
| 2026-06-27 | Капитальная доработка нарядов (5 этапов: подписи, шаблоны, даты/статусы, список, отчёт) | #625–#630 (v2026.627.1912) | Owner-запрос (диктофон). План — [`plans/_archive/work-order-overhaul-2026-06.md`](plans/_archive/work-order-overhaul-2026-06.md). **1a** (#625): подписи — стрелки ↑↓ (порядок → в печать, единый `slots[]`), чистая вёрстка строки (поле ФИО шире, должность отдельной строкой, без `maxWidth:240`-обрезки), «последние использованные» подписанты вверху списка (per-machine localStorage). **1b** (#626): общие кастомные формулировки подписей — новая таблица `work_order_signature_captions` + **миграция 0069** (несинкаемая, прямой HTTP как у templates), сервис/роут/IPC/preload, datalist-merge + persist по blur. **4** (#627): шаблоны — структурный селектор цеха + конфигуратор подписей (роли+ФИО) через `payloadOverrides` (apply = Object.assign, без правки backend); `templateToEditor` извлекает structured-ключи из JSON. **2** (#628): payload v4 — `startDate`/`dueDate` (нормализация, version 3→4 back-compat), `deriveWorkOrderStatusCode` (issued/done/overdue/done_late, pure); «дата завершения» деривится из close-ts (не трогает складской closing); шапка карточки 4 даты + бейдж. +6 shared-тестов. **3** (#629): список — колонки (дата выдачи/№/срок/виды работ/марка/№дв/завершён/ответственный), цвет строк по статусу, ответственный = первый подписант «выдачи» (`listWorkOrders` отдаёт +5 полей, резолв ФИО на странице). **5** (#630): отчёт `work_orders_report` — двухпанельный конструктор (фильтры выдачи/срока/статуса/ответственного/марки) + `buildWorkOrdersReport` + кнопка-deep-link у списка + печать/PDF/CSV из `ReportPresetPage`; due-фильтр не отсекает наряды без срока. Каждый этап CDP-verify со скриншотами; гейты зелёные. **Прод: v2026.627.1912 раскатан** (db:migrate 0069, /health+/updates/status=1912, blockmap 200). |
| 2026-06-25 | Наряд: рестайл формы печати + шаблоны настроек + двухфазная процедура подписей + 1 лист A4 | #586 (v2026.625.726) | Форма печати: ползунки → стрелочные степперы ▲▼; «Шапка» масштабирует гриф+заголовок+подстроку пропорционально (13:22:13, константа `WORK_ORDER_PRINT_HEADER_SUBPART_BASE_PX`); превью — настоящий лист A4 (210×297мм, поля 12мм, `zoom`-масштаб, 1:1 с печатью), крупнее, панель уже. **Шаблоны печати** (localStorage, per-machine): именованные + умолчание для вида наряда, применение одним кликом с живым превью (`woPrintTemplates.ts`). **Подписи** — два блока «Выдача наряда»/«Завершение наряда» для всех типов (ПДО→ОТК→цех / цех→ОТК→ПДО), 6 предзаполненных ролей, обратная совместимость blockId (`default`/`assembly_*`→`issue`/`completion`). Уплотнение (блоки рядом) → наряд на 1 лист A4. Renderer+shared, без миграций; shared 246 + backend 307 тестов, CDP e2e PASS. |
| 2026-06-24 | Наряд: переработка печати + подписанты/контракт/гриф + панель настройки печати | #563/#569/#576/#574/#581/#582 (v2026.624.49→.1651) | Шапка «Наряд на <вид работ>», убраны заголовки разделов, компактные строки, пустые блоки не печатаются. Подписи — слоты (роль+сотрудник, расшифровка «И.О. Фамилия» из БД, по 2 в строку, кнопка «Добавить пустую подпись»), блоки «Наряд выдан»/«Наряд выполнен» + пустые даты от руки, подразделение/цех рядом с должностью. Гриф «Утверждаю» (директор) над шапкой. После №двигателя — `***NNN` (последние 3 цифры номера контракта до `/`) + контрагент (поле «Краткое наименование» у контрагента). Колонка «Артикул» у детали. Панель «Печать»: редакт. заголовок/дата + размер шрифта на раздел + живой A4 с индикатором вместимости (`printSettings` в payload, обратно совместимо). |
| 2026-06-24 | Печать наряда на сборку — двигатель в шапке, склад-источник, подписи приёмки | #563 (v2026.624.49) | Для Assembly-наряда: марка/№ двигателя + вид работ «Сборка двигателя» вынесены в шапку (не повторяются построчно); колонки «Цена»/«Сумма» + денежный Итог заменены колонкой «Склад» (источник детали, резолв `sourceWarehouseId` по id или `workshop_<code>`); новая секция «Подписи» — два блока («Наряд выдан» / «Двигатель собран, обкатан и признан годным к отправке заказчику»), по 3 пустые линии (директор/начцеха/ОТК). Гейт `workOrderKind===Assembly`; прочие типы нарядов без изменений. Renderer-only (`WorkOrderDetailsPage.printWorkOrderCard`), превью сверено с владельцем. В том же релизе — бэкенд `describeError` (логи сохраняют PG-cause, #561). |
| 2026-06-19 | Табель: область = цех XOR подразделение (ОПП и др. можно затабелить) | #497+#498 (v2026.619.1703) | Миграция `0067` (`workshop_id`→nullable + `department_id` FK→entities + CHECK XOR + partial-uniq `(department_id,year,month)`); сервис/роут/IPC/preload/`shared` типы; UI — единый селектор «Цех / подразделение» (optgroups). Табель server-only → без sync/клиентской миграции. 284 backend. |
| 2026-06-18 | Табель Т-13 pass-3 (живая обкатка) | #479 (v2026.618.1551) | ПКМ-протяжка → прямоугольная заливка кистью + резинка; блоки кодов «с часами» (пара код+часы) / «без часов» (затирают ячейку) по `countsAsWorked`; ПКМ-клик без движения → комментарий (без конфликта с ЛКМ-freehand); рендер — часы 2× жирные чёрные / код-без-часов крупно, уголок-метка 12px тёмно-бордовый; **печать починена** — независимые галки секций (баг «пустой лист» от неисполнения inline-`<script>` в Electron-child-window, фикс через CSS `:has` вместо JS), должность под ФИО; убрана «Заполнить норму всем». Renderer-only, CDP-verified. |
| 2026-06-18 | Табель Т-13 pass-2 (живая обкатка) | #469–#471 (v2026.618.1128) | Фикс бага ввода комментария (глобальный grid-keydown глотал клавиши в модалке); единая «кисть» (выбор кода ≠ правка ячейки) + drag-paint + плывущий чип курсора; bulk-add по подразделению + мульти-выбор add/remove; размер шрифта ±/режимы половин месяца/растяжка+авто-fit; легенда на экране + порядок печати. Приветственное окно (B) написано. Renderer-only, без миграций; CDP-verified live. A1: «весь цех»=группировка по подразделению (PENDING). План `plans/_archive/timesheet-t13-pass2.md`. |
| 2026-06-17 | Табель Т-13 (Ф1+Ф2) + Табель в «Мой круг» | #453–#455/#458/#459 (v2026.617.1614) | Модуль табеля учёта рабочего времени по цехам (коды Т-13, кисть/fill-norm/клавиатура, live-итоги, печать A4 half/full/decode, per-cell комментарии). Ф3 (праздники/зарплата) снята владельцем. Removable-плитка «Табель» в «Мой круг». |
| 2026-06-10 | engine-acts Phase 1–5 | 2026-06-09/10 | Акты комплектности/дефектовки двигателя; все 5 фаз /verify PASS. |
| 2026-06-08 | defect-to-supply MVP-1/2 | #283 | «Заявка в снабжение из негодных» + фото на строке (online-кейс). MVP-3 offline — отложен (PENDING). |
| 2026-06-07 | checklist-unify Stage 1–5 (Акт комплектности+дефектовки) | v1.23.0–v1.25.0 + #54 | Объединённый список engine_inventory, 2 печати, фильтр по варианту сборки; Контрольный лист удалён. |

## Обновления / установка / версии

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-06-24 | Watchdog — GUI-subsystem сборка (нет окна терминала у операторов) | #572 (v2026.624.1547) | `go build -ldflags "-s -w -H=windowsgui"` в обоих воркфлоу (релизный + watchdog-build) → Scheduled Task больше не мигает чёрной консолью «watchdog pass start»; файловый лог `%APPDATA%\MatricaRMZ\watchdog.log` сохранён. |
| 2026-06-22 | Watchdog #7 — внешний агент восстановления клиента (Фазы 2/3-4) | #548/#549 (не релизилось) | Восстановление после сорванного NSIS-обновления (папка снесена → встроенный апдейтер бессилен). **Фаза 2:** команда `reinstall` в sync-request + кнопка «Переустановить» в админке (клиент явно пропускает watchdog-тип — anti-race). **Фазы 3-4:** Go-бинарь `watchdog/` (чистый stdlib, 5.1 МБ, build-verified в CI `watchdog-build.yml`) — ладдер presence→installer(pending/dir/download)→validate(MZ+size+sha)→silent`/S`→report+ack, уважает `update.lock`; handshake `%APPDATA%\MatricaRMZ\watchdog.json` (клиент пишет на heartbeat — снимает userData-неоднозначность packaged vs dev, держит Go на stdlib); неаутентиф. `/client/watchdog/report`→критсобытия. **Открыто:** Фаза 5 (installer.nsh Scheduled Task + подпись + раздача) + Фаза 7 (функц-тест) — нужен реальный Windows-стенд (PENDING §G). ⚠️ Кнопку не релизить без Фазы 5. План — [`plans/client-watchdog-agent.md`](plans/client-watchdog-agent.md). |
| 2026-06-21 | delta-обновления — засев топлива при старте + телеметрия исхода | #516 (v2026.621.1133) | Корень: топливо (sidecar+`.blockmap`) не кэшировалось → `haveFuel=false` → дельта молча пропускалась (0 delta-строк за 15 обновлений). Фикс: громкий лог вместо тихих `return`; `ensureDeltaFuelForCurrent` при старте (клиент на latest + stable-installer = серверная сборка → дотягивает blockmap+sidecar) → дельта на следующем релизе; телеметрия `update-applied` через `/logs/client` + CLIENT_PATTERN `client.update.full_download`. **Ждёт полевого подтверждения** (всплеск-затем-тишина после раската). gates+boot-smoke. |
| 2026-06-18 | delta-обновления — добивка foreground | #463 (v2026.618.117) | delta-first + `cacheDeltaFuel` перенесены в foreground `runAutoUpdateFlow` (раньше жили только в background-tick → foreground качал полный installer и quit'ил раньше → в поле не срабатывало). Эффект — со следующего апдейта после прайминга. |
| 2026-06-15 | delta-обновления (Путь B) | v2026.615.1417/1517 | Blockmap-delta вживую (~92 % экономии), delta-first шаг, delta в окне, LAN-peer Range. |
| 2026-06-14 | CalVer + update-module-revamp | v2026.614.1618 | Версия штампится из даты (`bump-version.mjs`); окно обновления (степпер/МБ/ETA/ошибки). |
| 2026-06-05 | Апгрейд Electron 33→41 | v1.43.0 | electron-builder 25→26, better-sqlite3 11→12, source-build нативки. |
| 2026-05-27 | Autoupdater self-heal рефактор F1–F8 | v1.32.x | Детект двойного фейла миграции → emergency installer fetch; Yandex sha; BOM/asar-path фиксы. |

## Поиск / UI

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-06-24 | web-admin «Клиенты» — карточки вместо широкой таблицы | #573 (v2026.624.1547) | 12-колоночная таблица не влезала по ширине (текст/кнопки уходили за край) → адаптивная сетка карточек (`repeat(auto-fill, minmax(310px,1fr))`); выбор для полной синхронизации перенесён галочкой на карточки (убран дублирующий список). |
| 2026-06-24 | Должности сотрудников — синхронизация справочника + бэкфилл | #577/#580 (v2026.624.1547) | `position_ref` (выпадающий список) и free-text `role` сотрудника разошлись → при сохранении карточки должность автоматически заводится в справочник; скрипт `positions:backfill` (createEntity+setEntityAttribute, sync-корректно) — на проде заведено 16 реальных должностей. Dry-run поймал баг «null» до записи (#580). |
| 2026-06-22 | Меню: кнопки разделов целыми словами (без переноса по буквам) | #535+#544 (v2026.622.1241) | `nowrap` + кап ширины 240 + grid `auto auto` → метки одной строкой, кнопка растягивается под текст, без разрыва слов и обрезки последней буквы. Чинит «перенос по буквам» на старых клиентах (#535 был не на проде). CDP-verified live (`labelLines=1`, `clipped=false`; probe 760px держит). |
| 2026-06-22 | Welcome-окно: цитата-эпиграф вверху вместо заголовка | #545 (v2026.622.1241) | Опц. `epigraph` в `ReleaseWelcomeContent` → `<blockquote>` мельче title (clamp 15–21px, курсив), «как у философа»; **новый на каждый релиз** (правило в CLAUDE.md §Release process), можно по теме релиза. Старые записи без него показывают title. |
| 2026-06-20 | **Глобальный поиск Ctrl+K (owner-batch E)** — L1+L2+L3 командная палитра | #501/#502 (v2026.620.1715) | Overlay по Ctrl+K + кнопка 🔍: L1 видимая страница (scope-context), L2 справочники в памяти (двигатели/марки/контрагенты/контракты/сотрудники/наряды/услуги/изделия/инструменты — навигируемые id), L3 сервер `GET /search` (номенклатура+документы, пермишен-фильтр). Переиспользовано: `tieredSearch`/`prepareRecordSearch`, `navigateToRoute` (извлечён из чат-deep-link, +work_order). Группировка по типам, стрелки/Enter/Esc. План → `plans/_archive/global-search.md`. CDP-verified. |
| 2026-06-20 | Учёт рабочего времени: «Онлайн» (фикс «10ч») + «Активно» | #506 (v2026.620.1715) | `recomputeDailySummary`: онлайн = heartbeat-bounded connected time (`sessionizeOnlineMs` чистая+юнит-тест), окно `[00:00, min(now,cutoff)]`, открытая сессия по `lastSeenAt`, убраны хардкод 08–18 и кап 10ч → «только открыл → ~0». Колонка «Активно» = input-based (renderer `activityTracker`, дельта на существующем 60с heartbeat, серверная `statistics_active_time` GREATEST-upsert, **миграция 0068**). План → `plans/_archive/work-time-tracking.md`. «Онлайн» best-effort (lost-stop overcount), «Активно» — точная. |
| 2026-06-20 | «Сохранить и выйти» на 3 карточках с кастомными панелями (C-хвост) | #503 (v2026.620.1715) | StockDocument (`saveDocument`→`Promise<boolean>`), Nomenclature (вынос `saveAll`), BOM-редактор (штатные `onSave`/`onSaveAndClose` на `CardActionBar`). Все 13 карточек теперь с обеими кнопками. CDP-verified (close-on-success). |
| 2026-06-20 | Номенклатура: сброс висящего `specJson.templateId` | #504 (v2026.620.1715) | Деталь со ссылкой на удалённый шаблон не сохранялась (400 «шаблон не найден»); эффект в `NomenclatureDetailsPage` чистит висящую ссылку после загрузки шаблонов → legacy-детали снова сохраняются. Затрагивало и глоб-поиск. CDP-verified. |
| 2026-06-19 | Карточки: «Сохранить» (без закрытия) + «Сохранить и выйти» | #496 (v2026.619.1703) | `onSave` дорощен на 10 карточках через общий `CardActionBar` (Simple уже имел обе); save-функции не закрывают карточку. 3 карточки с кастомными панелями (StockDocument/Nomenclature/BOM-редактор) — хвост (PENDING). |
| 2026-06-19 | Колокольчик на кнопке «Мой круг» при новых контрактах/ДС | #495 (v2026.619.1703) | `historyAlertCount` (App→Tabs, blink-бэйдж 🔔 на кнопке группы history при count>0); загрузчик алертов вынесен в `ui/utils/contractAlerts.ts` и переиспользован HistoryPage → бэйдж и список не расходятся. |
| 2026-06-19 | Карточка марки двигателя: надёжный рефреш списка деталей | #494 (v2026.619.1703) | safety-net в `addPart` (оптимистичный показ подтверждённо-связанной детали) + `useLiveDataRefresh` на счётчик деталей в `EngineBrandsPage`. In-card код-путь согласован, баг статикой не воспроизводился → перепроверить на живом клиенте (PENDING). |
| 2026-06-19 | Поля ввода/подсказки: затирание текста + дёрганье меню (все автокомплиты) | #487 (v2026.619.1041) | Централизованный `SearchSelect`/`useSuggestionDropdown`: `input.select()` на reopen-после-autohide затирал ввод → выделять только при committed-label (SuggestInput — убран совсем); hover→`activeIdx`→scrollIntoView дёргал список → scroll только при клавиатуре (`setActiveByKeyboard`). 4 компонента. CDP-verify (реальные Input). |
| 2026-06-19 | Окно «Что нового» адаптивное | #489 (v2026.619.1041) | Кнопка над текстом (не уезжает за нижний край), карточка ≤`94dvh` + гибкий телесуфлёр (flex), компактнее + media `max-height:760px`, Enter закрывает. |
| 2026-06-19 | Табель Т-13: панель управления закреплена сверху | #484 (v2026.619.1041) | sticky `top:0` обёртка тулбара+кистей в `.ui-content-viewport`; CDP: top=169px при скролле 600px и на дне. |
| 2026-06-19 | Список двигателей: метка утиля картера читается `engine_inventory` | #485 (v2026.619.1041) | После checklist-unify defect-акты soft-deleted → флаг читал пустое и «залипал»; перенаправлен на `engine_inventory` (где пишет карточка). Прод-SQL: 0 в defect / 34 в inventory. CDP mark→pink→unmark→clear. |
| 2026-06-17 | **Модуль «Табель учёта рабочего времени» (Т-13)** — директива brain | #453/#454/#455 (в `main`, ждёт релиза) | DB-backed модуль (цех×месяц): грид сотрудники×дни, 19 кодов Т-13 (засев), палитра чипов + кисть + Excel-клавиатура + дефолт-смена, live-подсчёты; печать A4 модалкой-галочками (1-я/2-я половина/месяц/расшифровки) + комментарии «где был» в ячейке → расшифровка на печати. **Ф1+Ф2; Ф3 (праздники/зарплата) снята владельцем** (частное предприятие; зарплата вне программы). Миграции 0062/0063 (→ db:migrate). CDP-verified live. План: [`plans/_archive/timesheet-t13.md`](plans/_archive/timesheet-t13.md). |
| 2026-06-17 | owner voice-batch **E** фотогалерея #11 + **#7 вкус** | #450/#452 (в `main`, ждёт релиза) | Лайтбокс-галерея фото двигателя: сетка→крупный просмотр ←/→, множественный выбор, тулбар 6 кнопок (копировать/удалить/выбрать/отправить[флешка·папка·почта]/печать/собрать в PDF); renderer+main IPC, без миграций. CDP-verified. #7 — словесные «привязка→применимо» в Услугах-по-маркам. |
| 2026-06-17 | owner voice-batch B-#4 + C(#8/#5/#6) + D-#9 | v2026.617.30 (#443–#448) | Двигатель↔контрагент↔контракт↔ДС: контрагент-первым фильтрует контракты (#444); выравнивание полей таблицы деталей `table-layout:fixed`+colgroup (#443); маркеры привязки контракт/ДС в списке двигателей (#445); раздел «Двигатели» в карточке ДС + скрытие пустых разделов (#446); авто-брак по картеру-в-утиле, ручная галка «Забракован» убрана (#447). **Отгружено на прод v2026.617.30** (client-only, без миграций; smoke OK; каждая фича CDP-verified live). Backlog: остались E/G. |
| 2026-06-16 | owner voice-batch F/A + B-#3/#10 + pass-2 V5 | v2026.616.2017 (#434–#441) | Свежесть списков (diff-guard по денорм-полям), релевантность поиска двигателей + сохранение ввода при автоскрытии, поле номера заполняет ячейку (sticky-шапка деталей), ChangesPage гуманизирован (нет сырого JSON/action-key). **Отгружено на прод v2026.616.2017** (smoke OK; verify: #436/#438C — PASS live, #438D/#434E приняты низкорисковыми без dev-данных). Backlog #4/C/D/E/G открыты. |
| 2026-06-15 | per-page полировка списков (filler/thumbs) | v2026.615.108–1417 | Схлопывание превью-колонок, хвостовой filler от растекания. |
| 2026-06-14 | UI-эргономика проход-1 (brain) | v2026.614.1618 | 6 issues (#365/#371). |
| 2026-06-13 | поиск #035 Ф0–Ф2 (shared tiered-matcher) | 2026-06-13 | exact/prefix/compact/RU↔EN + floor; 11 страниц без пер-страничных правок. |
| 2026-06-07 | глобальный переключатель вида списков | #257 | Паттерн TwoColumnList/VirtualTable на все списки. |
| 2026-05-30 | виртуализация списков | v1.34.0 | 12 списков, sticky-шапка, load-all сортировка склада. |

## Инфра / CI / sync / ledger

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-06-30 | Репо → public (fresh-snapshot) + архив; релиз v2026.630.1141 | v2026.630.1141 | GitHub Actions упал на биллинге («payments failed / spending limit») → всё CI + сборка `.exe` не стартовали. Решение: репо → **публичный** (бесплатные минуты). История содержала ledger-ключи (gitleaks 10 находок, не переписана с #060) → **не флип, а свежий старт**: старый репо → приватный `MatricaRMZ-archive` (история+PR+секреты), новый `Valstan/MatricaRMZ` (тот же URL) из 1 parentless-коммита текущего дерева (gitleaks 0), public. Секреты на новый: `YANDEX_DISK_TOKEN` (с прод-env), var `YANDEX_DISK_FOLDER`; branch protection; `ANTHROPIC_API_KEY` НЕ перенесён (PR-ревью off). CSC не нужен (билд unsigned). Бесплатные Actions собрали+опубликовали `.exe`; прод-деплой (reset на снимок — ledger/state целы; серверная пересборка; артефакты; ledger-publish; 2 рестарта). Память [[repo-public-split-archive]]. Follow-up: ротация ledger-ключей (SESSION_HANDOFF). |
| 2026-06-19 | `erp_nomenclature_code_uq` → partial (cold-rebuild safety) | #492 (v2026.619.1703) | Глобальный unique включал soft-deleted строки → dedupe-merge оставлял два claim'а одного кода, полный `replayLedgerToDb`/cold-rebuild падал (подтверждено probe: пара 3301-15-30 — единственный дубль кода на 281 строку ledger-состояния). Сделан partial `WHERE deleted_at IS NULL` (как соседние identity-uniques). Миграция `0066`. → GOTCHAS M12. |
| 2026-06-17 | LedgerStore: хардненинг Windows file-lock + temp-dir cleanup | #451 (в `main`) | Устранён Windows-флейк `ENOTEMPTY` в `afterEach rmSync` при полном прогоне (race с file-lock); ретраи/уникальные temp-dir. Не влияет на CI (Linux). |
| 2026-06-16 | реорг памяти проекта (open-vs-done split) | #422/#423 | `PENDING` 465→86 open-only, `COMPLETED.md`/`GOTCHAS.md`, 31 план в `_archive/`, скиллы/CLAUDE.md. |
| 2026-06-15 | CI-гейт `typecheck:test` | v2026.615.2312 (#417) | ambient `.d.ts` в vitest-tsconfig + CI-шаг от регресса. |
| 2026-06-13 | LedgerStore: атомарная запись + advisory-lock | (не релизился отдельно) | `writeFileAtomic` + cross-process lock; межпроцессная уникальность `seq` (`signAndAppend`). |
| 2026-06-13 | гейт дублей двигателей + чистка прода | v1.50.0 | 25 групп → 0; write-time check + почасовой auto-merge + in-card подсказка. |
| 2026-06-07 | repo-wide ESLint + CI lint-гейт | #231/#232/#233 | 0 errors/warnings, `--max-warnings 0`, dead-код вычищен. |
| 2026-06-07 | backend systemd-юниты в репо | — | `deploy/systemd/` verbatim-снимок прод-конфигурации. |
| 2026-06-07 | ledger `data-key.json`: untrack + ротация ключа | #252 | untracked+gitignored, новый активный ключ, `state.json` перешифрован (G29). |
| 2026-05-30 | вынос прод-секретов | brain #008 | `/etc/matricarmz/matricarmz.env`, `.env`-симлинк. |
| 2026-05-21 | nginx config в репо + catch-all | — | `deploy/nginx/` + `install.sh`; новые эндпойнты без правки nginx. |

## Прочее

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-06-22 | Массовое добавление простых работников в Цех №2 (прод-данные) | #542/#543 | Скрипт `workshop:add-employees` (dry-run/`--apply`, дедуп фамилия+инициалы с префикс-матчем для неполных ФИО, неоднозначное→ручной разбор). На проде из списка 27: создано **13** новых (без логина/доступа к программе), **4** переведены в Цех №2 (Кудряшов Д.Х/Мурашин Александр/Поткин А.Н/Поткин Г.С.), 10 уже были — дублей нет, однофамильцы не задеты. Через `createEmployeeEntity`+`setEntityAttribute` (ledger/sync, актор-суперадмин). Read-back подтверждён. |
| 2026-06-22 | Конституция проекта (слой принципов) + стойка советника — ратифицирована владельцем | #475 (+#537) | `docs/CONSTITUTION.md`: 12 статей (3 продукт / 5 инженерия / 4 знание-и-работа) из реальной практики + директив brain, ≤1 экран, ценность+«почему». Ст. 12 «Claude — активный советник с предохранителями» (предложить раз с трейд-оффом → отступить; прогноз последствий ДО; идеи в бэклог не в прерывание; решение за владельцем; анти-спам). Ссылка из CLAUDE.md (читается первой). Владелец просмотрел суть всех статей → ратифицировал как есть. Хвост (PENDING 🟡): прополка раздутых доков по трём корзинам. Директива brain `2026-06-18-project-constitution-and-advisor-stance`. |
| 2026-06-22 | SSOT цехов — слияние дубль-департаментов «Цех № N» + бэкафилл `workshop_id` (завершает #513/#517 на данных; brain directive) | #532 + прод-миграция | Прод-аудит выявил «двойную бухгалтерию»: сотрудники привязаны к цеху через department-сущности «Цех № N», дублирующие канонич. `directory_workshops`; живая ось — `department_id`, `workshop_id` (из #513) не наполнен, attr_def на проде отсутствовал. Скрипт `workshop:merge-dup-departments` (expand-contract, через `recordSyncChanges`): создал `employee.workshop_id` def → бэкафилл **116 сотр.** на канонич. цеха (№1=66/№2=11/№3=1/№4=38) → `detachIncomingLinksAndSoftDeleteEntity` обнулил их `department_id` + soft-delete **4 дублей**. Дубль-Табель Цех№4 (Jun-21 черновик) soft-deleted, рабочий Jun-18 (425 ячеек) оставлен. Verify: 0 активных «Цех»-департаментов, 0 orphan-FK, 8 офисных нетронуты, backend active/health-200 (конкурентная запись в ledger безопасна — disk-read индекс + кросс-процессный лок, стоп не нужен). Кнопка «весь цех» ожила. План — [`plans/_archive/ssot-workshops.md`](plans/_archive/ssot-workshops.md). |
| 2026-06-21 | Табель: привязка сотрудник→цех + «Добавить весь цех» + массовое назначение; скрытие уволенных | #513/#517 (v2026.621.1133) | Новый EAV-атрибут `workshop_id` у сотрудника (цех = `directory_workshops`, не EAV-entity → text-атрибут с UUID); поле «Цех» в карточке сотрудника + bulk-назначение на странице «Сотрудники» (выделить многих → проставить разом); кнопка «Добавить весь цех» в пикере табеля (тянет работающих этого цеха). #513 — фильтр уволенных в пикере (`resolveEmploymentStatusCode==='working'`). CDP-smoke: поле «Цех» рендерится. A1 из директивы T-13 pass-2. |
| 2026-06-19 | Табель Т-13: права автора + «разрешить редактирование другим» | #488 (v2026.619.1041) | `created_by` + `allow_others_edit` (миграция **0065**); автор правит всегда, другие — по галке автора (дефолт off, меняет только автор); легаси (null author) открыты. Сервис-гейт на всех мутациях, актор=`req.user.username`. unit 6/6 + CDP author-path. |
| 2026-06-19 | Машинные профили для `/start` | #486 (v2026.619.1041) | `docs/machines/<hostname>.md` — порты dev-стенда/пути/скиллы на конкретный комп; `/start` §0.5 читает свой по hostname, пишется по мере изучения. PC40 заполнен. To-brain idea отправлена. |
| 2026-06-12 | zavod-meta-memory + `/zavod` (brain) | — | `docs/zavod/` (FACTORY_MODEL/INDEX/PROGRAM_EFFECTS) + команда-консультант. |
| 2026-06-09 | профиль рабочего места + новый экран логина + меню аккаунта | #343/#345/#347 | server workspace profile, dual-panel login, account switcher. |
| 2026-06-07 | универсальная система шаблонов нарядов | — | `work_order_templates` + `HIDABLE_FIELD_CATALOG`; Workshop-наряд = repair+шаблон (спец-тип откачен). |
| 2026-06-07 | payroll-подписи: авто-ФИО | #259 | ФИО бригады в блок подписей payroll-отчёта. |
| 2026-06-07 | ColumnSettings на все списки | #260/#261 | показать/скрыть/переупорядочить колонки + reset, persist. |
| 2026-06-08 | мультивыбор марок для номенклатуры | #281 | Поле «Применимые марки» (junction) + сохранена «Марка по умолчанию». |
| 2026-06-05 | правило «логин + ФИО» клиента | v1.42.0 (#203) | `formatClientLabel`/`formatClientShort` везде, где показывается клиент. |
| 2026-05-21 | Ollama → Claude миграция | v1.16.0 | (AI-фичи затем выключены geo-block'ом — см. PENDING). |
