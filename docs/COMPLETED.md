# Completed — done-индекс (Tier-1)

> **Token economy:** одна строка на завершённую **нитку/релиз** — название + итог + релиз/PR. Granular-история (коммиты, row-counts, диагностика) — в `git log` и телах PR, **сюда не дублируется**. Это ledger «не гонять повторно»: грепнул по понятию — увидел, что и когда сделано. `/start` его **не** читает; читается по требованию («это уже сделано?»).
>
> Открытое — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md). Грабли — в [`GOTCHAS.md`](GOTCHAS.md). Архив планов — [`plans/_archive/`](plans/_archive/).
>
> _Наполнен при сливе PENDING (PR-2, 2026-06-16): крупнейшие нитки — строками ниже; **полный список закрытых планов** — в [`plans/_archive/`](plans/_archive/), granular — в `git log`/PR. План реорга: [`plans/_archive/memory-reorg-2026-06.md`](plans/_archive/memory-reorg-2026-06.md)._

## Интерфейс / конструктор / отчёты

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| ✅ | Движение запчастей: заявки в прогнозе + адресная выдача + сторно (Ф2-хвост+Ф3+Ф4 плана) | v2026.718.1109, #256–#259 | Ф2-хвост (G4): поле `expectedDeliveryAt` в заявке снабжения + канал «принятые заявки → planned incoming» прогноза (`loadSupplyRequestIncomingLines`; остаток = заказано − max(deliveries, оформленные приходы по `sourceRef`) — дедуп с Ф2-ядром; мост productId→номенклатура; склад default; просроченные даты вне горизонта). Ф3 (G3+G11): поля «Двигатель/Наряд (адресно)» в шапке `stock_issue`/`stock_writeoff` через всю цепочку (shared→zod→merge/parse→оба DTO→UI+dirty-снапшот, см. GOTCHAS M33); **фикс**: ветки постинга issue/writeoff не штамповали `lineEngineId` в регистр (только `assembly_*`) — адресность была бы декорацией; печать «Требование-накладная»/«Акт списания» из карточки (`openPrintPreview`). Ф4 (G5): `reverseWarehouseDocument` — авто-документ «СТОРНО-<no>» с зеркальными `reversal_*` движениями по строкам **регистра**, запрет минуса, пометки `reversedBy*`/`reversalOf*` на обоих документах, запрет двойного сторно и сторно-сторно, право admin-only `movements.revert` (было объявлено и не использовалось), REST `POST /documents/:id/reverse`, hash-chain+ledger как при проведении, онлайн-only. Каждая фаза — CDP+REST смоук на живом клиенте. План: [`plans/parts-movement-refactor-2026-07.md`](plans/parts-movement-refactor-2026-07.md) (открыты Ф5/Ф6). |
| ✅ | Движение запчастей: комплектование двигателя + приход из заявки + фикс готовности (Ф1+Ф2 плана) | v2026.717.1856, #251–#253 | Ревизия модуля движения запчастей (#250, план [`plans/parts-movement-refactor-2026-07.md`](plans/parts-movement-refactor-2026-07.md), 14 GAP). Ф1: отчёт-пресет `engine_kitting` «Комплектование двигателя» (BOM марки по REST × выдано в сборку по движениям engineId с учётом сторно × доступно без техлокаций + «где лежит» топ-3 × ремфонд × чистый дефицит; вариантные группы схлопнуты; печать = пикинг-лист; селектор двигателей `engines` с плейсхолдером). G13: `engine_readiness_to_assemble` был слеп на клиентах (локальные BOM-таблицы вне sync → `totalComponents=0`) — вынесены общие `loadBomKitForBrand`/`collapseBomKitSlots`, готовность считается по BOM марки, имена деталей в дефицитах. Ф2: кнопка «Оформить приход на склад» на карточке заявки (accepted/fulfilled_*) → черновик `purchase_receipt` со связью `sourceRef`; отчёт `supply_receipt_gap` «Заявки без прихода» (документы по REST — G14: локальный `erp_document_headers` на клиентах не наполняется). Все три фичи CDP-смоук на живом клиенте. Остаток: Ф3–Ф6 + вопрос владельцу про поле «ожидаемая дата поставки» в заявке (PENDING). |
| ✅ | Утиль: причины + отчёт-реестр | v2026.717.1605, #248 | scrap_reason на строке дефектовки и двигателе; причина в актах/печати/заявках; отчёт «Утиль: реестр с причинами» (фильтры: марки/контракты/даты/ветка замещения); план `plans/scrap-transparency-2026-07.md` |
| ✅ | Нормы расхода УТД-20 + В-84 (импорт из docx снабжения) | v2026.717.1605, #247 | Новый BOM УТД-20 (276 строк, марки УТД-20/УТД-20(С1)); BOM В-84 547→558 + qty/notes; материал+ГОСТ в 255 карточек; данные `scripts/data/norms-*.json` |
| ✅ | Отчёт по двигателям: фильтр «В ремонте сейчас» + чистка 25 зависших галочек | v2026.717.1605, #246 | Дата-фильтр «Начало ремонта» — исторический; новый фильтр по текущей галочке; 25 карточек до-#133 разжалованы через admin set-attr |
| ✅ | Watchdog: reinstall не берёт стейл-installer | v2026.717.1605, #245 | findInstaller сверяет локальный кэш с latest-meta; причина «переустановил старую версию» у 4 застрявших машин 2026-07-17 |
| 2026-07-17 | Пилот UI-конструктора → **эскизник модулей** (пивот по уточнению владельца) | #238 (пивот), #239 (Ф4), v2026.717.729 | Назначение переосмыслено: «Мои экраны» не рабочие интерфейсы, а **эскизы-макеты** будущих модулей (визуальное ТЗ оператора для владельца/Claude). shared `uiSpec` v2: свободный холст, 13 эскизных блоков (x/y/w/h + подпись + поле «что должен делать» + items), типизированные связи-нити (navigate/data/filter/other), автоконверсия v1→v2. `MockupCanvas` (drag/resize, SVG-нити со стрелками, нумерованные сноски), редактор (палитра, свойства, режим «Связать», экспорт «Описание для разработчика» в буфер), read-only просмотр с колонкой сносок. Ф4: undo/redo (60 шагов, Ctrl+Z), Ctrl+D дубль, Delete, стрелки-nudge, сетка-прилипание 8px, зум 50–150%, заготовки (форма/список+карточка/отчёт). Удалён рабочий runtime v1 (SpecRenderer/intentRuntime/widgets). CDP-смоук полного сценария + drag через trusted Input.dispatchMouseEvent. План — [`plans/ui-mockup-constructor.md`](plans/ui-mockup-constructor.md). |
| 2026-07-17 | «Мои отчёты» — конструктор отчётов оператора с личными шаблонами | [#240](https://github.com/Valstan/MatricaRMZ/pull/240), v2026.717.729 | Первый рабочий мини-интерфейс данных: источник = один из 14 списочных пресет-отчётов (строится локально из SQLite-реплики, права/редакция полей наследуются), рецепт = произвольные фильтры по колонкам (содержит/равно/больше/пусто; числа «1 234,56» и даты дд.мм.гггг сравниваются семантически), состав+порядок колонок, сортировка (в т.ч. клик по заголовку), лимит, автоитоги. Рецепт → личный шаблон (settingsStore per-user, паттерн шаблонов фильтров). Печать+CSV(BOM) в main (`customReportService.ts`); shared pure-transform (`customReport.ts`, 11 тестов); IPC `reports:custom*` под `reports.view`; таб `custom_reports` в разделе «Отчёты и аналитика». CDP-смоук: 14 источников ok на dev-БД, рецепт+шаблоны+UI. |
| 2026-07-17 | Рефакторинг: распил 5000-строчного `reportPresetService` на слои | [#241](https://github.com/Valstan/MatricaRMZ/pull/241) | Движок пресетов (5003 стр., 27 пресетов, 37 builder-функций) → `electron-app/src/main/services/reports/`: format (форматтеры/итоги/матчеры), context (build-context/EAV-snapshot/кэши), options (опции фильтров + `getReportPresetList`), presets/{contracts,engines,workOrders,catalogs,warehouse,assemblyForecast}, dispatch (switch), render (HTML/CSV/1C-XML/PDF/print), index-фасад. `reportPresetService.ts` — тонкий re-export шим (импортёры не тронуты). **Эквивалентность доказана снапшот-гейтом:** выдача всех 25 пресетов на dev-реплике (1583 двиг., 645 совместимостей…) снята через CDP до/после — **бит-в-бит идентична** (25/25, нормализация только generatedAt); CSV/1С-XML/custom-reports пробированы живьём. Кросс-проектная находка → `to-brain/2026-07-17-refactor-output-equivalence-gate.md`. |
| 2026-07-17 | Релиз v2026.717.729 + уборка прод-диска | [#242](https://github.com/Valstan/MatricaRMZ/pull/242), тег v2026.717.729 | Релиз собрал 4 нитки (#238–#241); welcome-эпиграф про «цифровой мел»; серверный build на проде (без миграций/install — lockfile не менялся); артефакты через локальный download+scp (обход TLS-таймаута gh на проде), blockmap отдельным вызовом; поймана и вылечена грабля M9 (`stale_manifest`+404 blockmap → повторный рестарт); `/health`+`/updates/status`=новая версия, blockmap 200. Диск прода **79%→51%** (~5.3 ГБ): осиротевший `/usr/local/lib/ollama` 4.3 ГБ (после миграции Ollama→Claude сервис снесён, payload остался) + apt-clean + кэши pnpm/electron + `pnpm store prune`. |

## Доступ / RBAC / безопасность

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-16 | Ночной шифрованный бэкап таарил стейл-ledger (паразит `backend-api/ledger`) | [#229](https://github.com/Valstan/MatricaRMZ/pull/229) | С реолокации ledger (H8, ~06-30) `backup-encrypted.sh` бэкапил repo-локальный `./ledger` (стейл 12.07, 58М) вместо канонического `/home/valstan/matricarmz-ledger` (124М): дефолт `LEDGER_DIR` указывал на старый путь и вычислялся ДО сорсинга `.env`. Фикс: дефолт → канонический путь, resolve после сорсинга; раскатан в `/usr/local/sbin/matricarmz-backup-encrypted` (бэкап `.bak-20260716`). Плюс `MATRICA_LEDGER_DIR` добавлен в прод `backend-api/.env` — репо-скрипты больше не отращивают паразита. Проверить лог бэкапа после 03:17 17.07. Заодно: ledger release-token ротирован (exp 2026-08-15). |
| 2026-07-16 | Security H7 шаг (в): fail-closed `normalizeRole` — H7 закрыт полностью | [#228](https://github.com/Valstan/MatricaRMZ/pull/228) | Дефолт `normalizeRole` флипнут: unknown/typo/empty роль → `employee` (без доступа) вместо legacy-`user` (полный доступ) — усилитель «опечатка = max права» закрыт. Явный `user` остаётся распознаваемым legacy-tier'ом (дефолт approve-флоу, единственная роль, предлагаемая админом) — его молчаливый обвал сломал бы онбординг. `role-report` переосмыслен: корзина = явный `user` + fail-closed аномалии (опечатка теперь молча **отбирает** доступ — ловить). +9 тестов. Деплой — backend-only build+restart или со следующим релизом. |
| 2026-07-13 | Фикс видимости нарядов: у `valstan` снят ошибочный `restricted_work_orders: editor` | прод-данные (без релиза) | Симптом: Фатыхова (и все обычные операторы) не видели ~270 нарядов `performed_by=valstan`. Корень: `editor` секции «Наряды закрытые» = **владелец приватных нарядов** (как ramzia) → его наряды скрыты от всех (`canViewWorkOrder`, [workOrderAccess.ts](../shared/src/domain/workOrderAccess.ts)). Вероятно выдан при раздаче доступов темы H (v2026.711). Лечение: убран ключ через канонический `setEntityAttribute` (superadmin-актор, `allowSyncConflicts`, env сорсён — M30); осталось 7 секций. Верификация: PG перечитан (M31), запись в `ledger_tx_index` seq 816053 → разлёт клиентам. superadmin видит всё безусловно — потому владелец не замечал. |
| 2026-07-13 | Security H7 шаг (б): пересадка 4 живых legacy-`user` → viewer + посекционный доступ | #200 (скрипт `security:reassign-legacy-users`), прод-данные | Решение владельца: zamkomdir (Щербик В.Л.) → viewer + contracts/production/reports; novosel (Новоселов С.Н.) / radik / kostroma (Костюнин Р.А.) → viewer + supply/warehouse. Все viewer (читать/печатать, не править). Скрипт (по образцу `backfillSectionAccess`): `system_role`+`section_access` через `setEntityAttribute` (superadmin-актор + `allowSyncConflicts`, M30), заменил editor-доступ (у novosel был editor-во-всём вкл. Персонал/зарплаты), отозвал refresh-токены (перелогин как viewer). Бэкап before-state: `~/backup-h7-reassign-20260713-192331.txt`. Верификация: idempotent re-run (4 уже настроено), `role-report` → **live `user`=0** → шаг (в) fail-closed теперь безопасен (PENDING §Security). |
| 2026-07-12 | Security H7 шаг (а): count-by-role отчёт | #185 (скрипт, релиза не требует) | `security:role-report` — read-only агрегат «живые/отключённые по нормализованной роли» + разбор `user`-корзины по сырому значению (явный legacy / пустой / **неизвестная роль, схлопнутая normalizeRole → user** — усилитель H7, метится ⚠) + пер-логин worklist для миграции. Чистая `buildRoleReport` + 7 тестов. **Прод:** 154 сотрудника; живых `user` — **4** (novosel, radik, kostroma, zamkomdir), отключённых 132, неизвестных ролей 0, операторские пресеты раскатаны. Остаток H7 (б)(в) — PENDING §Security. |
| 2026-07-12 | Watchdog: дискрепанс install-папки (хвост #1) | #178, релиз v2026.712.1818 | Корень: one-click per-user NSIS деривит install-dir из санитизированного package.json `name` (`@matricarmz/electron-app` → `Programs\@matricarmzelectron-app`), не из `productName` — явного `nsis`-override нет. Фикс: `standardInstallExe`-fallback в `watchdog/main.go` перенацелен на реальную папку (был мёртвый — при протухшем `handshake.appExePath` watchdog зря переустанавливал бы), комменты `installer.nsh` + README приведены к реальности. Место установки НЕ менялось (смена = орфан-инсталлы у всех клиентов). Открытый вопрос владельцу — судьба no-op `CleanupMatricaFiles` (PENDING §Watchdog). |
| 2026-07-11 | Owner-батч 2026-07-10 §A+§H: integrity ролей/синка + подсказки зависимостей разделов | #161/#162/#163–#167/#174, релиз v2026.711.2043 | A: пустой membership=unseeded (симметрия с сервером); подписи+confirm уровней «Наряды закрытые» (editor=confined); критсобытие мульти-логина; **LWW-guard по updated_at в applyPushBatch** (системная дыра EAV-синка); запрет пустого имени марки + человеческий fallback; аудит merge дедупа + предупреждение о разных именах. Прод-данные скриптом `fix:owner-batch-20260710` (fatyhova/radik membership, «Картер верхний»/шаблон «Сборка 400», переатрибуция нарядов №63–81). H: `SECTION_DEPENDENCIES` + confirm при выдаче раздела в двух точках membership; per-call catch в loadLinkLists двигателя. План: [`plans/_archive/owner-batch-2026-07-10.md`](plans/_archive/owner-batch-2026-07-10.md). CDP по темам зелёный. |
| 2026-07-05 | Deny-лог watch (RBAC #474 хвост) снят + фикс шума бэкапов | #85 (в `main`, до релиза) | 8 отказов за 2 недели — все `backups.view/run` (ramzia/master, peo_irina/supply): не нехватка прав, а `SettingsPage` безусловно дёргал `/backups/nightly` у всех. Секция бэкапов/список/кнопка загейчены `backups.view`/`backups.run` из `auth.status().permissions`. Роли дорастать не понадобилось. CDP 6/6. |
| 2026-07-05 | Security-остаток аудита: bootstrap-суперадмин + heartbeat-спуфинг | #80 (в `main`, до релиза) | Bootstrap-суперадмин (пароль первым логином) разрешён только на девственной БД — на живой 401 + критсобытие `auth.superadmin.bootstrap_blocked`. Heartbeat `/client/settings`: атрибуция `lastUsername`/active-time принимается только под валидным access-токеном того же логина (клиент прикладывает токен); старые клиенты без токена не затирают, logout чистит. backend 368. |
| 2026-07-04 | Security Фаза 2: шифрование локальной клиентской SQLite | #71 (v2026.704.951, на проде) | `better-sqlite3-multiple-ciphers` 12.11.1 (ChaCha20-Poly1305), ключ 32Б в `db-key.json` под `safeStorage`/DPAPI (паттерн #607); легаси-плейнтекст мигрирует **in-place** `PRAGMA rekey` при первом старте; потеря ключа → существующий self-heal (fresh DB + re-pull, база = кэш). Dev-stand verify: база с 1582 двигателями зашифрована in-place, smoke 7/7, рестарт чистый. Остатки (кэш бэкап-снапшотов, старые `.corrupted-*`) — план `sqlcipher-client-db-2026-07`. |
| 2026-07-04 | Security Фаза 2: nginx CSP/CORS/:18080 — на проде | #69 + прод-деплой | CSP на статический `/admin-ui` (script-src 'self'; inline-скриптов в vite-сборке нет); `MATRICA_CORS_ORIGINS` allow-list в прод `.env` (вместо allow-all; Electron без Origin не затронут); `listen 18080` → 127.0.0.1 (потребовался restart nginx — reload не перевесил сокет). Верифицировано curl на проде: CSP-заголовок отдаётся, preflight с чужого origin отклонён без allow-origin, ss = 127.0.0.1:18080, /health и /updates/status ok, оба сервиса active. Остаток Фазы 2 — SQLCipher, bootstrap-суперадмин, heartbeat-спуфинг (PENDING). |
| 2026-07-03 | **Доступ по разделам (Ф0–Ф3) — на проде и активирован** (замена H1-B2) | #42–#44,#46,#49,#50 (v2026.703.1049) | Программа поделена на 10 разделов, у каждого списки наблюдателей/редакторов (EAV `section_access`); страница «Доступы по разделам» + зеркало в карточке сотрудника + гейтинг меню (Ф1); read-гейт IPC в main-процессе (Ф2, решение владельца: sync не фильтруем — полная база на клиенте); write-гейт Ф3 — editor-требование на мутирующих IPC + серверный ledger-гейт `sectionForLedgerWrite` для всех ролей кроме superadmin (legacy `user` не обходит), «Наряды закрытые» с хардкода на membership (`RestrictedWorkOrderPolicy`, легаси-fallback до засева). Релиз v2026.703.1049 + `access:backfill-sections --apply` (21/21). Двойная JSON-кодировка membership → tolerant-parse #50, грабля M26. План: [`plans/_archive/section-access-2026-07.md`](plans/_archive/section-access-2026-07.md). Щели гейта (осознанные) — план §Ф2. |
| 2026-07-03 | Галочки «В акт комплектности» по маркам — матрица владельца на проде | #47,#48 (данные, без релиза) | Скрипт `warehouse:set-completeness-flags` (dry-run→apply): 8 марок, 304 позиции; матчинг детали по (имя, артикул) с фаззи (составные артикулы `A/B`, «сб»-нормализация, ≥2 общих слова только при точном коде — «Коллектор ⇄ Трубопровод»). Итог: 35 галочек поставлено, 9 линков добавлено, 260 уже стояли, 0 деталей создано; идемпотентность подтверждена (повтор = 0). Бэкап `~/backup-completeness-flags-20260703-1036.sql`. |
| 2026-07-01 | Изоляция нарядов Рамзии — отгружена и **переработана на display-filter модель** | #11–#14 (v2026.701.843→1139) | Эпик изоляции доведён до прода в 4 итерации: **843** — выпуск Phases 0-3 (UI дат/фильтр типа/грифы + первая изоляция). **941** (#12) — закрыта дыра: read-гейт освобождал **весь тир `admin`** (11 чел. на проде видели наряды Рамзии), сужен до `superadmin`+allowlist{ramzia,glavbux}; + **write-block** (правку наряда Рамзии режет `ledgerAuthzGuard`, только владелец+суперадмин); + **редактируемый 2-й гриф** «Утверждаю» (своя должность + сотрудник из базы, `WorkOrderPrintDialog`); + **пароль без подсказок** на входе (глазок раскрывал type=text → `GlobalInputAssist` показывал прежние пароли; `autoComplete=current-password`+`data-input-assist=off`). **1024** (#13) — **confinement**: Рамзия видит ТОЛЬКО свои наряды. **1139** (#14) — **разворот архитектуры по директиве владельца**: убраны sync-гейт и purge, добавлен **фильтр на отображении** (`listWorkOrders` main-процесс по `performed_by`+текущий юзер, `ctx.currentViewer()`); политика вынесена в shared [`workOrderAccess.ts`](../shared/src/domain/workOrderAccess.ts) (`canViewWorkOrder`/`canEditWorkOrder`). **Модель: полная база с прода на каждом клиенте, разграничение — на уровне UI по авторизованному пользователю** (server-enforce read из brain #063 **сознательно развёрнут** — размен на «оператор на любой машине»; конфиденциальность на уровне приложения, не at-rest). Инцидент по пути: purge жёстко удалял наряды с клиентов → пустые списки; вылечено бродкастом `force_full_pull_v2` (чистит+перезаливает). Грабли → GOTCHAS M21. |
| 2026-07-01 | EAV write-баг «списки сущностей не обновляются после правки карточки» | #15,#16 (v2026.701.1325→1437) | Симптом: правишь поле в карточке (дата прихода/отгрузки) → в списке старое. Корень (общий для EAV): `setEntityAttribute`/`setEngineAttribute` искали строку значения по `(entity,attr)` **без `deletedAt IS NULL` и без сортировки**, `limit(1)` → при дублях/soft-deleted правили произвольную, активную строку список читал старой. **Два write-пути:** `setEntityAttribute` (справочники/сотрудники, фикс #15 v1325) и **`setEngineAttribute`** (двигатели — свой путь `engine:setAttr`, пропущен в v1325, фикс #16 v1437). Оба теперь обновляют свежайшую активную строку + гасят дубли; read-запросы (`listEngines`, общий) сортируют по `updatedAt`. На сервере дублей 0. Грабли → GOTCHAS M20. **Открытый хвост:** точечный баг двигателя-дубля `2Ж03АТ0479` (SESSION_HANDOFF). |
| 2026-07-01 | Двигатель `2Ж03АТ0479` «список кажет старую дату отгрузки» — оказался **dual-source**, не дубли | #18,#19,#20 (v2026.701.1708) | Клиент владельца на v1437 + полный ресинк, баг остался → корень НЕ в дублях. Диагностика (код + read-only прод-psql): карточка правит статус-дату `status_customer_sent_date` (флаг «Отправлен заказчику» = «Дата отгрузки», [EngineDetailsPage:39/489]) и **никогда** не пишет прямой EAV-атрибут `shipping_date`; а `listEngines`/`resolveEngineShippingState` **предпочитали** `shipping_date` — замороженный февральский импорт (все 1460 строк 2026-02-10..26, ноль после). Прод-аудит: **1451** двигателей с прямым `shipping_date`, **172** расходятся со статус-датой, **1173** без статус-даты (вкл. `2Ж03АТ0479`). **Фикс #18:** развёрнут приоритет чтения → `status_customer_sent`→`status_customer_accepted`→legacy `shipping_date` (историч. фолбэк), без мутации данных. **Фикс #19:** убран замороженный `is_scrap` из OR утиля списка (тот же класс; 2 прод-записи, обе уже `status_rejected`). **Целостный аудит (запрос владельца):** dual-source — только двигатели (Explore-свип всех модулей); дубли активных строк — 0 по ВСЕМ сущностям. Регресс-тест `reportEngineShippingState` (оба значения → статус-дата). Грабля → GOTCHAS M23. **Полевой хвост закрыт 2026-07-02:** владелец подтвердил на живом клиенте (1024) — дата отгрузки подхватывается после однократного ввода в карточке; нитка dual-source закрыта полностью. |
| 2026-07-01 | Phase 3 — ролевая изоляция нарядов Рамзии (work-order rework) | PR #9 (в `main`, до релиза) | Серверный read-гейт нарядов владельца `ramzia` (видны только ей, `glavbux`-read, superadmin) на 3 sync-поверхностях (`pullChangesSince`+`ledger.ts` query/snapshot) + reports-билдере + AI `get_operations`; pre-sync purge уже-утёкших копий (`/ledger/state/restricted-purge` + клиентский self-healing delete); restricted-config по login (не хардкод UUID). **Аддитивный гейт — НЕ трогает `PRIVACY_TABLES`/pending → нулевая регрессия.** 3 утечки найдены+закрыты в verify: **C1** (AI `get_operations` без гейта), **deleted-payload** (22 soft-deleted наряда хранят `meta_json` → тумбстоун-утечка; restrict независимо от `deletedAt`), **M1** (case-sensitive owner-match). Dual-role live-verify (CDP UI + HTTP, прод-снапшот): оператор 0/37 нарядов Рамзии, владелец/админ 16/16; purge 37/0/0. Write-block read-allowlist (glavbux видит, но не правит) — отложен (PENDING 🟡). План: [`plans/work-order-rework-2026-06.md`](plans/work-order-rework-2026-06.md). |
| 2026-06-30 | Доаудит ротации ledger-ключей (добивка H8) — активные ключи чисты | docs-PR (аудит, без релиза) | Доаудит next-step H8: сверка **живых прод-ключей** с тем, что реально лежало в публичной git-истории (теперь приватный `MatricaRMZ-archive`). **Подписной** `server-key.json`: историч. pub-отпечаток `1563d94f…` ≠ живой `d2b91b5e…` → ротирован H8 (#614), утёкший выведен. **Data-key**: в трекинге была только **legacy** `{keyBase64}` (=`v1-legacy`, удержан для replay `enc:v1`, **не активен**); активный `k-mq3wacgz-6d7aa9` создан 2026-06-07 17:46 MSK — через 11 мин **после** untrack #252 (14:34Z) → в трекинг не попадал. Живой backend (PID 93822, env `/etc/matricarmz/matricarmz.env`) подтверждённо читает `MATRICA_LEDGER_DIR=/home/valstan/matricarmz-ledger` (релокейт H8 цел). Ни один live-ключ не в архиве и не в публичном snapshot (`git ls-files backend-api/ledger` пуст в обоих). **Вердикт: ротация НЕ нужна, H8 полон.** Граница (необратимо): `v1-legacy` остаётся в keyring навсегда (replay), историч.-публичные данные экспонированы навсегда — защищается только будущее (на чистом `k-mq3wacgz`). Побочно: удалён **orphan-ledger** в git-checkout `backend-api/ledger/` (150 МБ, gitignored, живым процессом не использовался; создавался деплоем при cwd=`backend-api` без env → `DEFAULT_LEDGER_DIR=cwd/ledger`). |
| 2026-06-27 | H1 (A+B1a) — pull read-authz + закрытие утечки хэшей паролей | #619,#620,#621 (v2026.627.1151) | H1 «полный рефактор» read-authz, разбит на A+B. **A** (#619): cold-snapshot/`/state/query` применяют per-user privacy чатов/заметок (была утечка чужих чатов/заметок на cold-sync; `/state/changes` уже резал; единый `syncPrivacy.ts`). **B1a** (#620): единый pull read-authz чокпойнт `makePullReadFilter` на 3 поверхностях — найдена и закрыта **утечка `password_hash`** (employee EAV-атрибут синкался на ВСЕ клиенты, PII-фильтр не покрывал; аутентификация только серверная → хэши в локальной БД каждого оператора = оффлайн-крекинг), теперь режется всем; `audit_log` admin-only на pull (живой потребитель — мёртвый `AuditPage.tsx`); HR-поля (`birth_date`/`hire_date`) операторам только своё. **B\*** (#621): клиент при старте идемпотентно чистит уже утёкшие `password_hash` из локального SQLite (без re-sync). Состязательное ревью (7 агентов) поймало 3 перегиба до коммита: `termination_date` оставлен (авторитет ростера табельщика), PII/HR только operator-ролям (legacy `user`/pending не задеты), `system_role`/`access_enabled` отложены (ломали бы колонку «Доступ» — нужен renderer-фикс). Гейты: backend 347/347, electron 185. **B2** (per-role EAV-матрица: договоры/финансы для двигателиста/технолога/мастера + **timekeeper свёрнут сюда** + `system_role`/`access_enabled`+renderer; предусловия: миграция legacy-`user`, wipe stale-кэша; **viewer=бухгалтерия → финансы не скрывать**) — открыто, PENDING + [`plans/h1-table-read-authz-2026-06.md`](plans/h1-table-read-authz-2026-06.md) §12. |
| 2026-06-26 | H8 — утечка ledger-секретов в публичном репо + ремедиация | #614,#615,#616 (v2026.626.2207) | Закоммиченный в **публичный** репо live-ledger вскрыл: подписной `server-key.json` (HEAD, прод подписывал им же — sha совпал), `data-key.json`+`state.json` (история 2026-01-27→06-07), 119 блоков / 552 `enc:v1:` поля **реальных ПДн** (chat/EAV сотрудников) — ключ+шифротекст оба публичны → расшифровываемы. Ремедиация: live-ledger **вынесен из git-checkout** (`MATRICA_LEDGER_DIR`, `mv` 341k блоков — root-cause), подписной ключ **ротирован** (старый retired+бэкап), data-key уже на `k-mq3wacgz` (майская ротация); untrack из HEAD (#614); **репо сделан приватным** (0 форков → эффективно без force-push). Приватизация отключила CodeQL → заменён на **Semgrep SAST** (#616), который сразу нашёл реальный **GCM `authTagLength:16`**-хардеринг (ledger/E2E крипта) — исправлен; gitleaks-CI починен под приватный репо (`pull-requests:read`). GOTCHA **M19** (live-данные трекаются в checkout'е → `git rm`/`reset` удалит прод-данные). История-rewrite деприоритизирована (приватный+ротация). Реакция на утечку ПДн — PENDING §4 (владелец). Релиз **v2026.626.2207**. |
| 2026-06-26 | Клиент hardening + engine features + права Фатыховой | #607–#612 (v2026.626.1229) | Безопасность (клиент): safeStorage для E2E-ключа/токенов (fail-closed, без plaintext), очистка backup-cache при старте, nav/window guards, CDP inert в prod-пакете. Двигатели: поле номера ≥30ch + подсказка дубля только при создании; «Разборку двигателя произвёл» (multi-employee) в акте дефектовки + печать; поиск и слияние дублей (`analyzeEngineDuplicates`/`mergeEngineGroup`, damerauLevenshtein кластер). Права выданы Фатыховой Наталье. Снят masterdata.edit-override у alina_goz/ur491; вычищены 38 призрачных строк user_permissions (ed625dd3 + legacy users). |
| 2026-06-26 | Аудит безопасности — Фаза 3 (bounded + 4 ранее отложенных) | #598,#600,#601,#602 (v2026.626.651) | Закрыт весь bounded-хвост аудита. **#598** (bounded): IPC-гейт `e2e:keys:export/rotate` (`admin.users.manage`), `/ledger/blocks` admin-only (сырые подписанные plaintext-блоки), серверная атрибуция `audit_log` (был подделываем через sync-push), ReDoS-гард `/ledger/state/query`. **4 ранее отложенных, закрытых «по-настоящему» по решению владельца** (design→adversarial-review→live-verify пайплайн): **#600** auth на `/updates/peers` (per-route `requireAuth`+`SyncUse`; токен сессии в апдейтере, до-логина graceful fallback на central-server → P2P/LAN/авто-апдейт сохранены); **#601** IDOR файлов — linkage-authz `canAccessFile` (владелец/админ/чат-участник/заметки-share/engine-операция/EAV-по-типу/`directory_parts`; LIKE+exact-id verify, actor-scoped SQL после ревью — нашло HIGH-регрессию `LIMIT`-без-`ORDER BY`) + allowlist путей `chat:sendFile`/`files:upload` (`pathOriginRegistry`, Yandex+шеринг сохранены); **#602** login typeahead+recent — убран enum-oracle `/auth/login-options`, заменён на `/auth/login-suggest` (префикс ≥2, только login+ФИО, свой лимитер) + machine-local recent 10 дней; live CDP-verify поймал и починил коллизию typeahead с глобальным input-assist. Backend+electron, без миграций; backend 337. Остаток (H1 read-authz, H8 ротация ключа+история, H7-остаток, Фаза 2/инфра) — PENDING + [`plans/security-hardening-2026-06.md`](plans/security-hardening-2026-06.md). |
| 2026-06-25 | Аудит безопасности + упрочнение phase 0/1a (мультиагентный) | #591–#593, #596 (v2026.625.1722) | Исчерпывающий аудит (карта поверхностей + 11 ревьюеров + состязательная проверка каждой находки; 51→46 подтверждённых). Закрыты на проде: **C1** публичная `/auth/register` давала роли `pending` почти все права → выгрузка всей БД через `/ledger` (фикс `pending→{}` в `auth/permissions.ts`); **C2** эскалация до суперадмина записью своего `system_role` через `/ledger/tx/submit` (backstop server-only employee-атрибутов в `ledgerAuthzGuard` для всех ролей); **H2–H5** конструктор отчётов сливал чужие чаты/заметки/EAV-PII + `/ledger/state/query` в обход PII-фильтра (admin-гейт приватных таблиц + `redactReportRows` + `makeOperatorReadFilter`); отзыв refresh-токенов централизован в `setEmployeeAuth` (смена пароля/сброс/отключение). Backend-only, без миграций; shared 251 / backend 322. Прод-периметр проверен (PG/backend на 127.0.0.1, UFW default-DROP, Let's Encrypt-серт, клиент не отключает TLS). Остаток (H1 read-authz рефактор, H8 ротация ключа+чистка истории, mediums/lows) — PENDING + [`plans/security-hardening-2026-06.md`](plans/security-hardening-2026-06.md). В тот же релиз вошли правки печати наряда (#595: подписи на 1 листе без bottom-pin + чёткие рамки таблиц работ/бригады). |
| 2026-06-23 | Watchdog #7 — внешний агент самовосстановления клиента | #548–#553 (v2026.623.1642) | Go-агент (`watchdog/`, чистый stdlib): при сорванном NSIS-обновлении (папка приложения снесена) сам переустанавливает из локального/скачанного инсталлятора; handshake `%APPDATA%\MatricaRMZ\watchdog.json`, bridge `/client/watchdog/report`, 2 per-user Scheduled Task (`installer.nsh`). Кнопка «Переустановить» + серверная `reinstall` в админке, версия-гейт `clientHasWatchdog` (граница 2026.622.1241). Раздаётся **unsigned** (серт владельцу недоступен — РФ) — мониторить AV. План — [`plans/client-watchdog-agent.md`](plans/client-watchdog-agent.md). Хвосты (install-dir дискрепанс, field-тест) — PENDING §G. |
| 2026-06-23 | Отдельное право `contracts.edit` (договоры/контрагенты вне `masterdata.edit`) | #557 (hotfix) → #558 (v2026.623.1642) | Корень: UI гейтил кнопки контрактов по `masterdata.edit`, а ledger-guard требовал `admin` (operator всегда `false`) → «кнопка есть, sync режется» (`forbidden:contract`, шторм у `alina_goz`). #557 — быстрый фикс (contract/customer→`masterdata.edit`, раскатан). #558 — правильный: выделенное право `ContractsEdit` (UI `caps.canEditContracts` + ledger), default-off у всех operator-пресетов (вкл. technolog), выдаётся точечно. Прод: право выдано `alina_goz`+`ur491`. Урок → GOTCHAS M17 (UI-гейт ↔ серверный гейт parity). |
| 2026-06-23 | Колонка «Привязка» в списке двигателей | #556 (v2026.623.1642) | Отдельный сортируемый столбец: 🟢 договор · 🟢🟢 ДС · 🔴 не привязан (вынесен из мелких точек у номера). Доменные хелперы `isContractAddonToken`/`classifyEngineContractBinding` в shared (+тесты). CDP-verify: все три состояния + сортировка. |
| 2026-06-23 | Фикс 429-шторма синхронизации (rate-limiter scope) | #555 (v2026.623.1642) | `trust proxy=1` при цепочке myjino+nginx (2 hops) → лимит ключевался по IP форвардера у ВСЕХ → общий бакет 600/мин+30/15мин на весь парк. Фикс: прод `.env` `MATRICA_TRUST_PROXY_HOPS=2` + лимиты 3000/300; код — строгий `authLimiter` только на `/auth/login`+`/register` (не на частые `/auth/me`/`/refresh`). После раската 0×429. Также снят зависший 22ч `pnpm install` на проде (GOTCHAS M16). |
| 2026-06-21 | RBAC операторов #474 — роли + серверный enforcement | #524–#529 (v2026.621.1815) | Две оси (уровень×участок). **M1** роли-пресеты (`engineer/technolog/master/supply/timekeeper/viewer`) + `normalizeRole`. **M2a** ledger write-гейт — серверная per-operation authz по `entity_type`/`operation_type` в `applyLedgerTxs` (закрыл дыру brain #015: на пути записи `/ledger/tx/submit` авторизации не было, гранулярные коды были UI-only). **M2b** read-ограничение чужих ЛД/зарплат на pull. **M3** deny-лог (`server.authz.denied`). **M4** очередь одобрения изменений пользователей (расширение `change_requests`). Migration-safe (бьёт только операторские роли). Роли активированы 12 операторам (через настоящий ledger, seq→757677). Инцидент с standalone-скриптом (писал в побочный ledger) восстановлен без потерь → GOTCHAS M15/M16. План — [`plans/operator-rbac.md`](plans/operator-rbac.md). Хвосты раската — PENDING. |

## Детали / номенклатура (parts)

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-12 | Ф2: backfill 3 сирот-деталей + усыновление «Гильзы» (коллизия кода) | #187/#188/#189 (скрипты, без релиза) | Прод: `backfill-orphan-part-nomenclature --apply` создал 2 зеркала («Насос водяной В-59 УМС», «Крышка люка»); «Гильза стальная» 303-07-22 — код занят живой легаси-номенклатурой «Гильза» → новый скрипт `warehouse:link-nomenclature-to-part` усыновил её (directory_kind/ref + имя, история движений цела). Сирот 0 (метрика научена ref-bridge конвенции G1, #189). Бэкап `~/backup-orphan-backfill-20260712.sql`. По пути пойманы 2 грабли: **M31** (recordSyncChanges для ERP = PG-no-op → канонический upsert + re-read-assert) и повтор **M30** (unsourced env → паразитный ledger; 2 мусорные строки индекса удалены, все 3 строки переподписаны под боевым env). |
| 2026-07-12 | Де-дуп Ф1: стоп-кран синтетических кодов + симметричное удаление пары | #181, релиз **v2026.712.1818** | Инкремент эпика де-дупа `directory_parts↔erp_nomenclature` (план [`plans/parts-nomenclature-deep-dedup-2026-07.md`](plans/parts-nomenclature-deep-dedup-2026-07.md); Ф2-остаток в PENDING). `ensurePartNomenclatureMirror` + dedupe-survivor: деталь без артикула получает **пустой код** вместо `DET-<id8>`. Partial unique на обеих сторонах — сервер 0075, клиент 0016 + `clientSchemaMigrations` 10→11 (клиентский индекс перестал быть глобальным → снята мина merge-пары, серверный стал partial ещё в 0066). `deleteWarehouseNomenclature` гасит парную деталь (снят «дух»-капкан: пере-создание одноимённой падало `duplicate`). Скрипт Ф2 `blank-synthetic-codes[:apply]`. Ф0 прод-инвентаризация: 123 DET-+22 NM-, ref-only 0, 2 духа. Прод: миграция 0075 применена (индекс→partial `code<>''`), `reconcile-code-name --apply` (13 имён). Verify: backend 385/385, CDP 7/7 (две ""-детали сосуществуют + доезжают клиенту, delete→re-create ОК). |
| 2026-07-12 | Унификация вложений детали (хвост темы F) | #179, релиз **v2026.712.1818** | `PartDetailsPage`: три файловые секции (Чертежи/Технология/Вложения-прочее) → одна панель «Вложения» по образцу марки #172. Гидрация мержит `attachments+drawings+techDocs` из metadataJson (дедуп по `FileRef.id`), запись всегда в `attachments`, `buildMetadataFromState` legacy-ключи не переносит → первый unified-save мигрирует блоб (файлы сохранены). CDP 13/13 (фикстура с f1 в двух списках → рендер один раз; блоб после save: merged 3, legacy-ключи выпали, description цел). |
| 2026-07-11 | Owner-батч 2026-07-10 §C+§F+§G: полировка номенклатуры / вложений / актов | #170/#172/#173, релиз v2026.711.2043 | C: карточка номенклатуры была 30-60+с — переведена на **local-first** (локальная SQLite + фоновый remote-refresh, `meta.dataSource`), шапка рисуется вне `Promise.all`, интерактивным чтениям срезаны таймауты (~5-8с ×1-2 вместо 30-60с ×3), outbox-дрейн убран из read-path, TTL-кэш lookups/типов/шаблонов. F: у типа `engine_brand` не были засеяны attribute defs `description`/`drawings`/`tech_docs` → `setAttr` отвечал «Неизвестный атрибут», а файл успевал залиться сиротой — `ensureAttributeDefs` на маунте карточки марки + унификация в **одну панель «Вложения»** (merged-чтение attachments/drawings/tech_docs с дедупом по `FileRef.id`, запись всегда в `attachments`; раскатка UI раньше чистки старых attrs). G1: тристейт-кнопки «Все показанные ✔/✖» для актов комплектности/дефектовки по `visibleParts` (уважают фильтр/срез). G2: новый режим распространения «перепривязать акт целиком» — act-scoped `replace` снимает галочку акта у деталей вне набора в марках-целях (привязку/кол-во/узел сохраняет), примитив `clearPartSpecBrandLinkActFlagForBrands`. План: [`plans/_archive/owner-batch-2026-07-10.md`](plans/_archive/owner-batch-2026-07-10.md). CDP по темам зелёный. |
| 2026-07-08 | «Распространить на группу» — выбор ЧТО и КАК | #123 (v2026.708.1553) | Кнопка распространения набора деталей марки на группу (v2026.708.1139) получила два выбора: **что** (весь список / детали акта комплектности / акта дефектовки / отмеченные чекбоксами в строках списка) и **как** (add-missing — только добавить недостающие, безопасный дефолт, для актов проставляет галочку у имеющихся; overwrite — обновить совпадающие + добавить = прежнее поведение; replace — полное замещение, список цели = точно набор, лишнее удаляется, под чек-подтверждением). `propagatePartSpecBrandLinkToBrands(+mergeMode/ensureActFlag)` + `removePartSpecBrandLinksForBrands` в [`partsPagination.ts`](../electron-app/src/renderer/src/ui/utils/partsPagination.ts); модалка + чекбоксы выбора в [`EngineBrandDetailsPage.tsx`](../electron-app/src/renderer/src/ui/pages/EngineBrandDetailsPage.tsx). Верифицировано вживую (verifier-electron CDP): 5 сценариев + предохранитель replace-без-confirm. |
| 2026-07-07 | Группы марок двигателей — снимок + живая связь деталь↔группа + фильтр списка марок | #104,#105,#106 (v2026.707.1412, на проде) | EAV-тип `engine_brand_group` (`engine_brand_ids`). В карточке детали → «Применяемость»: «разово развернуть» (снимок, manual-связи, #104) ИЛИ «живая привязка» (#106, авто-поддержка `brandLinks`: марки группы применяются/снимаются автоматически и симметрично, ручные связи не трогаются). Ядро `recomputePartBrandLinks` (`shared/domain/liveGroupLinks.ts`, 11 юнит; маркер `sourceGroupId` → manual/derived/anchor, ≤1 связь на (деталь,марка), пересборка из полного членства); оркестратор `liveGroupSync.ts` (reexpand при save/delete группы + self-heal при открытии карточки). **Резолверы применяемости и BOM-путь (прогноз/разборка/возврат) не тронуты.** Фильтр списка «Марки двигателей» по группе + дедуп `parseIdArray` (5→1, #105). Backend: zod-роут `routes/warehouse.ts` срезал `sourceGroupId` — пофикшено (память `partspec-brandlink-zod-strip`), **DDL нет**. e2e CDP: round-trip, self-heal, re-expansion, симметричное удаление. Прод-деплой: build серверных пакетов (dist с фиксом роута), артефакты по scp (CDN TLS-таймаут), рестарт. |
| 2026-07-04 | Мердж дублей — синк зеркала survivor (последний хвост #4) | #63 (v2026.704.951, на проде) | `mergeDirectoryParts`: существующее зеркало `erp_nomenclature` выжившего теперь синхронизируется (code/name/`specJson.article`) в конце merge-транзакции — после цикла поглощаемых, чтобы унаследованный код был уже освобождён soft-delete'ом зеркала донора (partial unique 0066); ledger-upsert + строка в отчёт оператора, no-op если уже совпадает. 2 новых юнита, 368 backend. Нитка «Мердж дублей #4» закрыта полностью. |
| 2026-06-29 | Деталь находится в наряде по реальному артикулу + «Изделие (legacy)»→«Деталь» | #656,#657 (v2026.629.1711) | Срочный баг владельца: деталь (помпы 411-00-35А/411-00-48) есть в «Ревизии ремфонда», но не подбирается в наряд на ремонт. Диагностика: оба пикера читают `erp_nomenclature`, но наряд строил «артикул» опции из `sku` (у мигрированных зеркал = авто `DET-<id>`) → деталь индексировалась под мусорным кодом, поиск по настоящему `code` (411-…) не находил; одноимённые «Насос водяной» неразличимы. Фикс #656 (`WorkOrderDetailsPage`): артикул/подпись из реального `code`, `sku` тоже в поиск, подпись «имя (артикул)» — как в Ревизии. Отдельно: тип «Изделие (legacy)»=`product` устарел (не создаётся, только legacy-ярлык); backfill #657 `product→part` на 256 строках (все реальные детали; через `recordSyncChanges`; pg_dump перед apply). Verified CDP (поиск по коду находит) + psql. Техдолг: глубокая де-дупликация `directory_parts↔erp_nomenclature` (DET-зеркала в данных). |
| 2026-06-29 | Эпик «не сохранять пустые карточки» — Phase 1/2a/2b/3a | #652,#653,#654,#655 (v2026.629.1711) | План [`drafts-no-empty-cards-recovery-2026-06`](plans/drafts-no-empty-cards-recovery-2026-06.md). **Ph1** (#652): «Пустые карточки» (Контроль и аналитика) — `emptyCardsService` (analyze→soft-delete с ре-валидацией+гард ссылок) по двигателям/договорам/сотрудникам/нарядам/заявкам. **Ph2a/2b** (#653/#654): отказ от авто-создания — `createEngine`/employees-create не пишут строку (id-UUID), `get` синтезирует пустую карточку, первая запись атрибута материализует (employees изолированы через `fallbackTypeId`, общий admin-путь не тронут). **Ph3a** (#655): синкаемая таблица `card_drafts` (полная проводка зеркально notes: registry/ledger/applyPushBatch/privacy/migrate 0070 + клиент 0015 + `cardDraftsService`+bridge). Verified: checkSyncContract, CDP round-trip (save→push→fullPull→clear)+psql. **Открыто:** Ph2 договоры/masterdata+наряды, Ph3b/3c/3d — см. SESSION_HANDOFF. |
| 2026-06-29 | Имя/артикул детали в карточке марки/двигателя следуют правке номенклатуры | #648 (v2026.628.2326) | Баг владельца: переименование детали (имя+артикул) в «Склад→Номенклатура» не доезжало в карточку марки и двигателя (показывали старое). Корень — два стора: правка идёт в `erp_nomenclature`, а карточки читают `directory_parts` (`listAllPartSpecs`→`listWarehouseNomenclaturePartSpecs`), где name резолвится «existing wins» и не входит в update-set → не обновляется; code владеет erp. Фикс (read-through, выбор владельца): `listWarehouseNomenclaturePartSpecs` LEFT JOIN `erp_nomenclature` (по id) → name/code из карточки номенклатуры, fallback на directory_parts. Чинит уже разошедшиеся детали сразу, без backfill. Спека (размеры/привязки/metadata) осталась на directory_parts. Verified CDP+dev-PG (развёл erp-имя SQL → read-path вернул erp). Граница: одиночный `getWarehouseNomenclaturePartSpec` не трогали; глубокая де-дупликация — техдолг (UUID-only). |
| 2026-06-20 | Артикул в списке деталей марки + уборка карточки «Деталь» фаза-1 (закрыла B-перепроверку) | #509/#514 (v2026.621.1133) | Второй столбец карточки марки = **артикул** детали (был per-link `assemblyUnitNumber`, пусто→«не задано»); строка `flex`→CSS-grid (колонки выровнены); `addPart` больше не пишет литерал «не задано». Владелец подтвердил «деталь появляется». Уборка карточки «Деталь»: складские списки + usage/файлы — сворачиваемые (`collapsible` в `SectionCard`). |
| 2026-06-20 | Карточка «Деталь» фаза-2 — выпил мёртвого standalone-кода `PartDetailsPage` | #519 (v2026.621.1133) | Компонент всегда embedded (`NomenclatureDetailsPage` всегда передаёт `embedded`) → весь `!embedded` путь недостижим. Снесён standalone-рендер + add-field/brand-link/field-order/dimensions подсистемы + `saveAllAndClose`/`saveCore`/`registerCardCloseActions` + мёртвые импорты. **2826 → 1154 строк (−1691).** Живой embedded-путь (`buildMetadataFromState` побайтово, attribute-save очередь, рендер Основное/Где используется/Файлы) не тронут; tsc/eslint/build зелёные. Живой рендер детали не прогнан (dev-снапшот без номенклатуры) — глянуть на релизе. |
| 2026-06-19 | Дубли: heal зеркала при мердже (создаёт карточку выжившему) | #493 (v2026.619.1703) | `mergeDirectoryParts`: если у выжившего нет складской карточки, а у поглощаемой есть — soft-delete зеркала донора (код свободен под partial-index 0066) + вставка зеркала выжившему → нет тупиковой ошибки «нет складской карточки», слить можно любую пару. Прод-диагностика: 1 «инкогнито» (Гильза 303-07-22) = половина дубля по коду. Heal-тест, 284 backend. |
| 2026-06-19 | Детектор коллизий артикула в «Дубли деталей» | #483 (v2026.619.1041) | Новый ярус «🟠 коллизия артикула» (одинаковый непустой код, разные имена) — раньше слепое пятно (`exact`=имя+код, `fuzzy`=похожие имена). На проде 4 пары (`327-00-62`, `3301-15-30` Картеры, `3309-25-2`, `406-12-44` Блок/Крышка). shared 13/13. Объясняет «не добавляется/не вижу картер». |
| 2026-06-18 | Атомарность мерджа дублей (deep-fix #4 item 1) | #480 (v2026.618.1551) | `mergeDirectoryParts` → один `db.transaction` (нет частично-слитого каталога при сбое на полпути); ledger/sync (свой pipeline, в tx не входят) накапливаются и сбрасываются **после** коммита, при rollback не пишутся. Unit: rollback + happy (278 backend-тестов); live-merge против dev-PG (коммит лёг, повтор tombstoned-loser'а отклонён чисто). Item 2/3 (зеркало survivor + partial unique index) — открыты (PENDING). |
| 2026-06-18 | Phase 3.8 scope A — снос дубль-junction номенклатуры | #464/#465 (v2026.618.117) | Снесён пустой `erp_nomenclature_engine_brand` из sync-протокола/кода/UI (дубль-секция «Применимые марки» убрана, оставлена «Применяемость») + миграция `0064 DROP`; WS-A2 на проде: 18 артикулов `nomenclature.code←directory.code` (ledger-проекция) + 2 имени. Остаток 4 строки → doubles-merge (PENDING). План `plans/_archive/parts-nomenclature-phase3-8.md`. |
| 2026-06-17 | В-46/В-84 импорт + авто-карточка + артикул + фикс мерджа дублей | #457–#459 (v2026.617.1614) | 39 деталей В-46/В-84 + 3 новые марки как базовые (оба акт-флага), 83 зеркала `erp_nomenclature` (вкл. хвост Т3); `createDirectoryPart` авто-создаёт складскую карточку; артикул-колонка в списках склада/номенклатуры; мердж дублей не падает (client timeout 60s+no-retry, backend батч `recordSyncChanges`). |
| 2026-06-16 | parts-articul-acts T1–T6 | #327–#338 | Артикул = различитель, дедуп-модуль, импорт базового списка (итог на проде), наследование галочек актов марка→двигатель, формы актов. T3-импорт сверен — целевой итог уже на проде. |
| 2026-06-08 | Phase 3.5 — депрекация оси «шаблон детали» | v1.46.0 | `part_template` выпилен (рудимент), `/parts/templates/*` снят, EAV в ноль. |
| 2026-06-07 | Phase 3.6/3.7 + parts-chain-audit (brain) | backend-деплои | Снос dead service-fn + dual-write моста; унификация id (`dp.id==nom.id`), dangling ref → 0; единый источник истины для детали. |
| 2026-06-06 | bom-parts-into-engine-brand-lists (brain mandate) | #212/#213 | BOM-детали заведены как «детали марки» + постоянная гарантия в `upsertWarehouseAssemblyBom`. |
| 2026-06-05 | Phase 3 — parts EAV → directory_parts | v1.39.0–v1.42.0 | Остаточные EAV-поля → `directory_parts.metadataJson`; своп всех `/parts/*` вызовов; blanket **410** на `/parts/*`. directory_parts — единственный источник. |
| 2026-06-01 | Phase 2 Variant A — part-spec в directory_parts | v1.35.0–v1.40.0 | `code/templateId/dimensions/brandLinks` колонки; embedded карточка детали в номенклатуре. |
| 2026-05-24 | Phase 1 — зеркало parts↔nomenclature, component types | v1.21.x–v1.22.0 | `component_type_id` нативная колонка, audit/fix mirror-скрипты. |

## Склад / BOM / прогноз

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-14 | Печать QR-этикеток номенклатуры (бэклог brain #4, только печать) | #206 (в `main`, до релиза) | Кнопка «Печать этикеток» в `NomenclaturePage` → [`LabelPrintDialog`](../electron-app/src/renderer/src/ui/components/LabelPrintDialog.tsx) (выбор позиций, что кодировать, колонки/копии) → офлайн-генерация QR-SVG (dep `qrcode`) → печатный лист A4 через `window.open`+`document.write` ([`qrLabels.ts`](../electron-app/src/renderer/src/ui/utils/qrLabels.ts), QR как готовый inline-SVG — скрипты в окне печати не исполняются, GOTCHAS). В QR — `code` (сканер найдёт по существующему поиску `barcode`/`code` в `nomenclatureSearchCondition`) или стабильный `id`. Диалог тянет каталог при открытии (не зависит от развёрнутой группы). Verify: CDP e2e — диалог 1319 позиций, окно печати с реальными QR-svg. **Сканер-сценарий (движение остатков) НЕ входит** — склад сегодня REST-only (не offline), + железо: L–XL, отдельная нитка (PENDING). |
| 2026-07-09 | Прогноз: показ открытых Assembly-нарядов из прежних прогнозов (dayOffset-хвост) | #150 (v2026.710.244) | `forecastVariantKey` содержит относительный dayOffset → на следующий день открытый наряд не матчит строки нового прогноза, и «Создать наряд» для того же реального дефицита молча разблокируется (дубль-риск). В нотах отчёта — «Открытые наряды на сборку из прежних прогнозов: №N, …». Клиентская set-difference по уже возвращаемому `existingAssemblyOrdersByVariantKey`, API не менялся. CDP: без старых нарядов ноты нет; с фикстурой №99999 — появляется при «Сформировать прогноз». |
| 2026-07-08 | Прогноз сборки — подстановка запасного варианта (Фаза 3, инкремент 1) | #115 (v2026.708.1139) | Сток основного варианта позиции = 0, а запасного > 0 → прогноз берёт запасной с макс. остатком + операторская нота. Backend-only (`collapsePositionsToDefaultOption` stock-aware + реордер загрузки стока перед сборкой китов), симуляция не тронута, 4 теста. Фаза 3b (мало-но-не-0 / пулинг / адаптив смена в горизонте) — PENDING. План: [`plans/engine-spec-forecast-phase3.md`](plans/engine-spec-forecast-phase3.md). |
| 2026-07-06 | **Редизайн спецификации → модель «Позиции + взаимозаменяемые варианты»** (задание владельца: жёсткая глобальная схема) | #93–#97,#99 (**v2026.706.2335**) | 6 PR: **#93** BOM-строки получили `position_key`/`position_label`/`is_default_option` (миграция 0073, аддитивная), снята обязательность глобальной схемы (у марок разные наборы деталей). **#94** прогноз коллапсирует позицию к основному варианту (guard от двойного счёта). **#95** редактор `EngineAssemblyBomDetailsPage` переведён с «блоки-варианты» на «Позиции → варианты (один основной)», убраны дерево/техполя/быстрые-связи/автоскелет/баннер-обязательных-типов (−360). **#96** убран редактор глобальной схемы из карточки, схема оставлена read-only словарём типов (−571). **#97** сборочный наряд: кнопка «Заполнить из спецификации» тянет основной вариант каждой позиции в строки наряда (backend не менялся). **#99 (Фаза 4b)** переключатель варианта позиции прямо на строке сборочного наряда — селект «Вариант» (основной ★), смена меняет деталь строки; карта деривится из спеки при открытии, персистентность не тронута; CDP e2e 15/15. Легаси `variantGroup`-BOM показываются позициями-одиночками + баннер. **На проде v2026.706.2335** (2026-07-06/07, `db:migrate` 0073 применён между build и restart; /health+/updates/status=706, blockmap 200). План (архив): [`plans/_archive/engine-spec-position-variants-2026-07.md`](plans/_archive/engine-spec-position-variants-2026-07.md). Открытые опц. хвосты (PENDING 🟢): backfill легаси-строк, Фаза 3 (подстановка вариантов в прогнозе). |
| 2026-07-05 | **Ремфонд-осведомлённый прогноз + заявка из дефицитов + бейдж заноса** (по итогам аудита цепочки движений) | #78 (в `main`, до релиза) | Всеобщий аудит «наряды→склад→ремфонд→дефектовка→прогноз» (3 Explore-агента): скелет учёта корректен и трассируем (все 4 типа нарядов постят правильные документы, остаток кликается до наряда-источника); 3 разрыва закрыты. Ф1: дефицит = «закрыть ремонтом» (остаток `repair_fund`, `coverableByRepairFund`) + «закупить» (`toPurchase`), подсказка оканчивается действием («выдать ремнаряд» / «заявка в снабжение»). Ф2: кнопка «Создать заявку в снабжение (N позиц.)» на прогнозе → deferred-create заявка с предзаполненными позициями закупки. Ф3: read-only превью дельты заноса (`/repair-fund/intake-preview`, HWM) → бейдж «⚠ Не занесено в ремфонд: X шт.» на карточке двигателя. CDP 7/7, shared 321, backend 368. План: [`plans/_archive/forecast-remfond-aware-2026-07.md`](plans/_archive/forecast-remfond-aware-2026-07.md). |
| 2026-07-05 | Карточка марки: фильтр деталей + режимы (все/комплектовка/дефектовка/по узлам) + печать среза | #77 (в `main`, до релиза) | Раздутые карточки (до ~600 деталей): фильтр по названию/артикулу/узлу со счётчиком «N из M»; срезы по актам; группировка по узлам (артикул = № сб. единицы), группы сворачиваются, при фильтре раскрыты; 🖨 печать текущего среза (`printRowsPreview`). CDP 11/11 на В-59 УМС (125 деталей). |
| 2026-07-05 | Единицы измерения легаси-номенклатуры (прод-данные) | #82 | Скрипт `warehouse:backfill-units` (dry-run/--apply): 185 позиций без `unit_id` (все — детали/узлы двигателя) получили «шт» через `upsertWarehouseNomenclature` (ledger-путь → sync клиентам). Контроль: no_unit 0/1356. |
| 2026-07-02 | Импорт «Нормы расхода запчастей при КР В-59УМС/В-84АМС» (прод-данные) | #33/#35/#37 | Скрипт `warehouse:import-zamena-kr` (dry-run/--apply, идемпотентный): 684 позиции — 232 детали созданы, 452 обновлены (группа/тип/артикул), 683 получили единицу «шт»; 4 новые группы номенклатуры (Крепёжные изделия, РТИ и уплотнения, Подшипники, Электрооборудование и КИП); BOM В-59 34→553 и В-84 19→547 строк с qty на двигатель (узел двигателя — в notes строки), детали привязаны к маркам (`ensureNomenclatureBrandPart`); легаси-киты не тронуты (line-level запись, GOTCHAS M25); бэкап `~/backup-zamena-kr-import-20260702-1503.sql`. Контрольный прогон на проде — 0 изменений. |
| 2026-06-29 | Ремфонд Ф3.1 — ручная отметка экземпляра `in_fund↔repaired` на карточке двигателя | #646 (v2026.628.2326) | Отложенный хвост ремфонд-эпика. Развилка (наряды количественные, реестр поэкземплярный) разрешена **вариантом C** (выбор владельца): кнопка «✓ Отремонтирована» / «↩ В фонд» у `in_fund`-экземпляра в реестре личных номеров (карточка двигателя) — точная отметка ремонта конкретного физ-экземпляра по номеру, без эвристики «первые N» и без переделки нарядов. Терминальные утиль/замена не трогаются (`canToggleRepairedStatus`). `setStampedInstanceRepaired` через `recordSyncChanges` upsert (доезжает клиентам, класс M6/M8). Verified CDP e2e (клик кнопки + доезд через `fullPull`/DB-seq). |
| 2026-06-28 | Учёт ремонтного фонда (ремфонд) — Фазы 0–4 (эпик завершён) | #638,#639,#640,#642,#643 (v2026.628.1814) | Эпик: ремфонд как регистр не вёлся («бездонная яма»); деталь после дефектовки уходила в пустоту. Опора — существующая системная локация `repair_fund` (количественно, без миграции/схемы). План — [`plans/_archive/repair-fund-2026-06.md`](plans/_archive/repair-fund-2026-06.md). **Ф0** (#638): страница «Ревизия ремфонда» (меню Склад) — прямой массовый ввод деталей в фонд, семантика ревизии (set-абсолют) через `stock_inventory` на `repair_fund`. **Ф1** (#639): кнопка на карточке двигателя — приход **годных к ремонту** (`repairable_qty` по present-строкам, qty из `repairable_qty`) из дефектовки; идемпотентно по двигателю (операция `repair_fund_intake` хранит прошлый занос, проводится только дельта +приход/−расход) → защита от призраков (которые в 2026-05 заморозили старый разборочный поток); partId из мета `__brand_part_id`/`__part_id`. **Ф2** (#640): закрытие ремонтного наряда списывает отремонтированное из фонда (`repairFundService.releaseRepairFundForWorkOrder`, хук в `workOrderClosingService` ветка Repair, динамический import, best-effort + clamp по остатку, `stock_writeoff`). Решения владельца: только количество (личные номера — Ф3); регистр на `repair_fund`. Каждая фаза CDP-verify (фонд 0→4 приход, наряд закрыт → фонд −2). **Ф3** (#642): поэкземплярный учёт деталей по личным (набитым) номерам с провенансом (с какого двигателя снята) + классификация утиль/замена/ремонт; хранилище — `operations` type `repair_fund_instance` (НЕ новая синхро-таблица), идемпотентный upsert по `(engine,nomenclature,stampedNumber)`; кнопка «📌 Зафиксировать личные №» + секция на карточке двигателя. **Ф4** (#643): печатное «Требование к заказчику» по двигателю (детали утиль/замена с личными номерами — обоснование роста цены, в «дело двигателя»); печать с карточки + версионируемый снимок (`repair_fund_requirement`), без сумм (решение владельца). **Заодно фикс Ф3-sync** (#643): server-захват без `recordSyncChanges` не получал `last_server_seq` → не доезжал клиентам инкрементальным pull (реестр жил только в ответе capture) — пойман на Ф4-CDP-смоуке, тот же класс GOTCHAS M6/M8. Релиз эпика — v2026.628.1814 на проде. **Остаток: Ф3.1** (статус `in_fund→repaired` на закрытии ремнаряда — отложено) — PENDING. |
| 2026-06-22 | Аудит точности прогноза сборки — 3 фикса источников данных | #540 (v2026.622.1241) | Перепроверены все источники прогноза (сток/локации, BOM-комплекты M:N, каналы прихода) 3 фокус-агентами. Фиксы: техн.локации (ремфонд/утиль/«в сборке») исключаются из «годного» **всегда**, не только при пустом фильтре → убран двойной счёт под фильтром; `carter`→роль jacket; диагностика «остаток без склада» (NULL `warehouse_location_id`). Подтверждено корректным: merge дублей репойнтит сток↔BOM атомарно, нет двойного reserved. Тесты 50/50. Латентные hxvosты (warning BOM без brand-link / дробный qty / dayOffset) — PENDING. |
| 2026-06-22 | Ремнаряд «Выдан в работу» — только выданные кормят прогноз | #541 (v2026.622.1241) | У ремонта нет статуса «открыт» (draft/closed), `updateWorkOrder` сбрасывает в draft → канал прихода считал черновики приходом, завышая план. Флаг `repairIssued` в `WorkOrderPayload` (едет в metaJson→backend) + кнопка «Выдать в работу/Отозвать» на ремонтном наряде; `buildRepairIncomingFromWorkOrderPayloads` учитывает только выданные. Раскат: прогноз по ремонту пуст, пока мастера не отметят наряды — PENDING (операционный). |
| 2026-06-09 | warehouse-analytics C1–C5 | v1.45.0–v1.46.0 | Выпуск двигателей по маркам/цехам, брак, low-stock setup. |
| 2026-06-09 | shop-statistics Phase 0 | 2026-06-09 | Дашборд: работы + пропускная способность двигателей (honest-banner о покрытии). |
| 2026-06-09 | прогноз сборки (assembly-from-forecast) | v1.29.x | Наряд из прогнозa + резерв; edge cases + warnings. |
| 2026-05-27 | Phase 2.x — склады на FK `warehouse_location_id` | v1.17.x–v1.31.0 | Полная миграция 4 регистров с legacy `warehouse_id text` на uuid FK; DROP COLUMN. |
| 2026-05-22 | BOM-refactor серия | v1.21.0–v1.21.5 | variantGroup, component-type auto-fix, убраны stub-skeleton'ы и technical-fallback'ы. |

## Акты / дефектовка / наряды

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-15 | Класс «строки vs шапка» закрыт: наряд на сборку помнит свой двигатель | [#224](https://github.com/Valstan/MatricaRMZ/pull/224) + [#225](https://github.com/Valstan/MatricaRMZ/pull/225), v2026.715.1833 | **Жалоба владельца:** при создании наряда на сборку договор/заказчик не попадают в печать; лечилось ритуалом «удалить номер двигателя → сохранить → вписать → сохранить», и то не всегда. **Корень (глубже симптома):** с #133 двигатель сборки живёт в **шапке** (`assemblyEngineId`), а читатели ходили по построчным штампам `freeWorks[].engineId`; строки бывают без штампа — пикер шапки штампует только **уже существующие** строки, а `buildLinesFromWorkOrderTemplate` (универсальный шаблон своего двигателя не несёт) клал их без `engineId`, после чего `normalizeWorkOrderLine` срезал номер/марку навсегда. Ритуал «работал», т.к. при повторном выборе строки уже есть — сохранение ни при чём. **#224** (печать): `orderEngineIdsKey` + `firstEngineId` → `resolveAssemblyEngineId`. **#225** (корень): строки шаблона наследуют двигатель шапки; `workOrderEngineEntityId` → `resolveAssemblyEngineId` (**не латентно:** наряд с двигателем только в шапке писал `operations.engine_entity_id` = контейнер `…0002` вместо двигателя — провенанс терялся; на проде 2 из 56, оба draft, чинятся своим следующим сохранением); `getActiveAssemblyVariant` (header-only наряд не находился → вариант считался свободным); гейты кнопок «Сохранить как черновик»/«Закрыть и провести» (просили двигатель «хотя бы в одной строке» при выбранном в шапке) — **ослаблены только вместе** с фиксом провенанса. `primaryAssemblyEngineId` помечен legacy: прод-вызовов 0. Ремонтные наряды не затронуты (`assemblyEngineId` пуст → fallback на строки). **Четвёртый рецидив класса** (#168 №/марка, #192 отчёт, #224 печать, #225 корень) → **GOTCHAS M32**. Гейты: shared 420, backend 400, typecheck, lint, CI 5/5 ×3 PR; CDP e2e: A/B строки реквизитов печати (до — `Марка дв.\|№ дв.`, после — `+ ***883 \| ООО "ОВК"`), A/B `engine_entity_id` в PG (старый код → контейнер, новый → двигатель), строки шаблона `engineId:[null,null]` → после сохранения читаются из БД с двигателем шапки. |
| 2026-07-15 | Утиль двигателя: две метки + снятие блока сборки утильного | [#220](https://github.com/Valstan/MatricaRMZ/pull/220) + [#221](https://github.com/Valstan/MatricaRMZ/pull/221), v2026.715.1549 | Неиспользуемый `status_rework_sent` («Отправлен заказчику на перекомплектацию», на проде **0 записей** — сверено по PG) переоборудован в «Утиль — отправлен заказчику» (терминальный, прогресс 100, боковая ветка в advance-ранге). Развилка процесса (решение владельца): **две метки**, т.к. наряд на сборку нужен ДО отправки — добавлен `status_scrap_confirmed` («Признан утильным», по дефектовке, прогресс 0 — двигатель ещё на заводе). **Суть нитки:** связка «утиль ⇄ наряд на сборку» (v2026.713.1447) блокировала выдачу Assembly-наряда при любом утиле в дефектовке, а у утильного двигателя он есть всегда → наряд нельзя было выдать **в принципе**. Общий предикат `isScrapEngine` (признан ∨ отправлен) выводит из-под блока: кнопка активна, авто-отзыв не срабатывает (иначе метка отзывала бы свой же наряд — оба хука, electron main + backend), guard в `advanceEngineStatusForWorkOrder` не даёт наряду поставить «Начат ремонт»/погасить метки. **Ограничения на детали не было и нет** — `warehouseSourceOptions` отдаёт все активные локации, включая `scrap`; поэтому метка на наряде (первоначальная идея владельца) не заводилась — один источник истины вместо двух расходящихся. Гейты: shared build, typecheck ×4, backend 400, lint, CDP-смоук на реальном пути с выданными нарядами (обычный → авто-отзыв «Деталь признана утильной: Картер» = регрессии нет; утильный → остался выдан), CI 5/5. |
| 2026-07-15 | Список двигателей: статус акта комплектности + печать списка + точка входа в отчёт | [#218](https://github.com/Valstan/MatricaRMZ/pull/218), v2026.715.1549 | **Критерий «акт заполнен» — хотя бы одна деталь отмечена «на месте»** (`present`) в стадии `engine_inventory` (решение владельца: важен факт начала заполнения, не полнота). Флаг `EngineListItem.hasCompletenessAct` считается в `listEngines` **тем же проходом** по `operations`, что и авто-брак по картеру (`getEngineInventoryFlagsMap` — второго скана нет). В списке: сортируемая колонка «Акт компл.» (✓/—), фильтр акта (все/заполнен/не заполнен) + фильтр по контрагенту (опции из строк списка) → сценарий «у контрагента X двигатели без акта». Печать списка: диалог с галочками полей (запоминаются в localStorage), печатает текущий фильтр **или** выделенные строки (Shift+клик, `useListSelection` уже был — раньше вёл только ПКМ-меню). Отчёт: кнопка → существующий пресет `engines_list` (нового пресета не заводили), обогащён фильтрами `counterpartyIds`/`completenessActFilter`/`columns` + колонка «Акт комплектности», проекция колонок через `selectEnginesListReportColumns`. Гейты: shared 420, backend 400, CDP-смоук (seed акта через bridge → флаг у TEST-001 + 1 реального из 1583; фильтр → 2 строки; диалог печати; предпросмотр отчёта), CI 5/5. |
| 2026-07-15 | Внутренний номер двигателя — клеймо для безымянных деталей | [#216](https://github.com/Valstan/MatricaRMZ/pull/216), v2026.715.1549 | EAV `engine_internal_number` + `engine_internal_number_year`, домен [`engineInternalNumber.ts`](../shared/src/domain/engineInternalNumber.ts). **Уникальна пара (номер, год)**, не номер: журнальную нумерацию ведёт работник и каждый год начинает с 1 → «41» есть и в 2026, и в 2027. Показ `41/26`, на деталях короткий `41`. Год — текущий на момент ввода (решение владельца; номер приходит «с земли» при дефектовке), поле редактируемо. Гейт дублей — **блок** на клиенте (`engineService.setEngineAttribute`) и на сервере (`adminMasterdataService` + `engineNumberGuard`, дубль может приехать синком); сторожит **обе** записи пары, карточка пишет год до номера. Протащено: карточка/список/поиск/наряды (снимок в строке + фолбэк в карточку у старых)/дефектовка (зеркало, заблокировано)/акты (опц. поле снапшота, строка условная)/5 отчётов + отчёт по нарядам/виджеты/крошки/QR-этикетки (`LabelPrintDialog` переиспользован без правок). **`stamped_number` НЕ автозаполняется** — он ключ поэкземплярного учёта ремфонда, одинаковое значение схлопнуло бы безымянные детали в один экземпляр; связь деталь↔двигатель держит `engineEntityId` записи (CLAUDE.md §Key architecture decisions). Гейты: shared 420, backend 400, CDP-смоук 6/6 + плашка 2/2. |
| 2026-07-14 | Паспорт ремонта двигателя — вкладка «История ремонта» (бэклог brain #1) | #206 (в `main`, до релиза) | Read-only хронологическая лента событий из журнала `operations` в карточке двигателя (вкладка гейтится `operations.view`). Без изменений схемы/sync/прав — таблица уже синкается и наполняется (приёмка/дефектовка/акты/межцеховые/статусы деталей/ремфонд/наряды). Общий нормализатор [`engineTimeline.ts`](../shared/src/domain/engineTimeline.ts) (`buildEngineTimeline`: `operationType`→подпись/иконка/фаза, сортировка по `performedAt`), панель [`EngineTimelinePanel.tsx`](../electron-app/src/renderer/src/ui/components/EngineTimelinePanel.tsx). 5 unit-тестов. Verify: CDP e2e на TEST-001 — лента рисует все типы событий. Fast-follow (не сделан): сшивка заездов по серийнику через `repeatArrival`. |
| 2026-07-13 | Owner-батч 2026-07-13: сборочный наряд без авто-BOM + печать (месяц словом, скрытие пустых колонок) + вкладки v2 + шаблоны фильтров отчёта | #202, релиз **v2026.713.2318** (на проде) | 4 задачи владельца в одном PR. (1) **Сборочный наряд без авто-BOM:** убраны оба silent-триггера `fillAssemblyFromBrandSpecFor` (onChange двигателя в шапке + seed-эффект открытия из ПКМ) в [`WorkOrderDetailsPage.tsx`](../electron-app/src/renderer/src/ui/pages/WorkOrderDetailsPage.tsx) — строки только вручную / кнопкой «Заполнить из спецификации» / шаблоном. (2) **Печать:** `formatMoscowDayMonthName` («14 июля») для «Приступить»/«Срок»; колонки, пустые во всех строках (артикул/№ изделия/двигатель/вид работ), исключаются из `linesTable`. (3) **V2-вкладки:** закрытие карточки закрывает и вкладку — `requestCardClose` через `closeV2Card` + эффект прунинга `v2OpenCards` при занулении selected-id (App.tsx); чинит зависшую вкладку, переоткрывавшую устаревшее состояние и затиравшую данные. (4) **Шаблоны фильтров отчёта:** per-user именованные пресеты (save/apply/delete) через `settingsStore` (`ReportPresetFilterTemplates`) + IPC `reports:filterTemplates*`, UI-блок в [`ReportPresetPage.tsx`](../electron-app/src/renderer/src/ui/pages/ReportPresetPage.tsx). Verify: typecheck+lint+backend 400, CDP e2e 25/25 (`_smoke-owner-batch-4.mjs`). |
| 2026-07-13 | Связка «утильная деталь ⇄ наряд на сборку» + статус «Отозван» | #195, релиз **v2026.713.1447** (на проде) | Утиль в дефектовке (`scrap_qty>0`) авто-отзывает выданный Assembly-наряд двигателя (`applyWorkOrderWithdrawal`, auto-причина `buildAutoWithdrawReason`) — хук в обоих `checklistService` (electron main + backend web-admin с `recordSyncChanges`, идемпотентен, поиск по `resolveAssemblyEngineId`). Кнопка «Выдать в работу» блокируется, пока есть утиль (`listScrapPartNames`). «Отозвать из работы» → модалка с обязательной причиной (в payload `withdrawnAt/Reason/Auto` + auditTrail). Новый вычисляемый статус `withdrawn` (Отозван) — раньше отозванный красился «Просрочен»; в списке/карточке/отчёте (+`WorkOrderStatusCode`, `deriveWorkOrderStatusCode` с `withdrawnAt`, `workOrderWithdrawnAt` guard). Поля в `normalizeWorkOrderPayloadV3Fields` (round-trip). Verify: 774 unit + CDP e2e (авто-отзыв, блокировка, модалка, снятие утиля→выдача). |
| 2026-07-13 | Отчёт «Наряды»: колонка «Отгружен» + сводка статусов + формирование по кнопке | #196, релиз **v2026.713.1447** (на проде) | Колонка `shippedDate` (дата отправки заказчику через `resolveEngineShippingState`: sent_date→accepted_date→legacy) + сортировка. Сводка в подвале (`computeWorkOrdersStatusSummary`): выписано/выполнено/с просрочкой/просрочено/отозвано/отгружено/принято — +опц. разбивка по маркам (чекбокс `summaryByBrand`); считается 1× в main, рендерится в on-screen/предпросмотре/печати через shared-хелперы. **Все отчёты строятся только по кнопке** «Сформировать отчёт» (авто-debounce убран для всех пресетов; бейдж «фильтры изменились»; пустое состояние). Verify: shared unit + CDP e2e (нет авто-билда, колонка/сводка/разбивка по маркам). |
| 2026-07-13 | Отчёт «Наряды»: резолв двигателя/заказчика из шапки | #192, релиз **v2026.713.1017** (на проде) | Хвост #168: список и печать после #133 читают двигатель из шапки (`assemblyEngineId`), а отчёт «Наряды» ([reportPresetService.ts](../electron-app/src/main/services/reportPresetService.ts) `buildWorkOrdersReport`) сканил только `freeWorks`-штампы → у новых нарядов пустые «№ двигателя»/«Заказчик». Теперь: `collectWorkOrderWorkLines` (вкл. `workGroups`) → fallback `resolveAssemblyEngineId` → номер/марка из snapshot; контрагент за резолвнутым engineId. Старые наряды читают построчные штампы первыми. Деплой: build серверных → 3 артефакта (scp) → ledger-publish → **2 рестарта** (первый поймал транзиентный `stale_manifest`, M20) → health/updates/blockmap зелёные. |
| 2026-07-12 | Integration-тест закрывающей ветки Workshop-наряда (техдолг since 2026-05-26) | #186 (тест, без релиза) | `workOrderClosingService.test.ts`: мок db/warehouse/phase-стека при настоящей shared-доменке — 8 кейсов workshop_template close (prune+агрегация+production_release+linkedDocumentId; отказ на пустом шаблоне; идемпотентный re-close; stale-конфликт; неактивный цех; **fail проводки не закрывает наряд**; regular без склада; G1-ремап). Сьют 400/400. |
| 2026-07-11 | Owner-батч 2026-07-10 §B+§D+§E: список нарядов + наряд сборки из двигателей | #168/#171, релиз v2026.711.2043 | B: reader'ы списка и печати резолвят №/марку двигателя сборки из `assemblyEngineId` (после #133 читались только построчные штампы; `fillAssemblyFromBrandSpec` штампует двигатель в строки, переживают `normalizeWorkOrderLine`); «Завершён» = `completedAt ?? completedDate`; поиск по № двигателя в haystack + сортировка по статусу. D: ПКМ по двигателю → «🛠️ Наряд на сборку» (deferred-create c assemblyEngineId; `extraSingleItems` в buildListContextMenuItems). E: авто-fill BOM марки при выборе двигателя/открытии из ПКМ; снята грабля disabled-кнопки «Заполнить из спецификации» на пустом наряде. CDP 7/7 + 5/5. |
| 2026-07-09 | **Акты редактируемые: комиссия/состояние/гриф + шаблоны по маркам** | #141–144 (v2026.709.1629) | Акт комплектности/дефектовки доведён до «редактируемого как наряд». **Комиссия** — из 3 фикс-слотов в динамический список `commission_members` (`kind:'commission'`, add/remove, редакт. должность + роль; «Заполнить по цеху» на стандартные роли). **«Состояние при поступлении»** — из захардкоженного `ENGINE_RECEIPT_CONDITION_FIELDS` в редактируемый список `receipt_condition_list` (add/remove/rename). **Гриф «Утверждаю»** (`approver_grif`, пресеты = SSOT наряда `WORK_ORDER_APPROVERS` + акт-дефолт `quality`) в правом верхнем углу **обоих** актов, нижняя подпись `approved_by` убрана. **Поля ФИО/должности** резиновые + `OverflowTooltipInput` (полупрозрачная плашка полного текста при переполнении). **Шаблоны по маркам** — таблица `engine_act_templates` (миграция **0074**, unique `(brand,name)`), сервис/REST/IPC зеркалят `workOrderTemplates`; `applyEngineActTemplate` замещает комиссию/гриф/состояние (значения пунктов сохраняются по id), строки деталей не трогает. **Ключевое:** ленивая детерминированная миграция `migrateEngineInventoryAnswers` (стабильные derived-id → снапшот-подпись воспроизводима; праймит `lastSavedAnswersRef` → без авто-сейва на загрузке); ридеры печати с **legacy-fallback** (старые снапшоты печатаются как прежде). Verify: 380 backend-тестов, CDP live-драйв редакторов (PASS), печать реальным билдером 16/16 (вкл. fallback+бланк), логика 48 проверок, миграция 0074 на dev+прод. План: [`plans/_archive/editable-engine-acts.md`](plans/_archive/editable-engine-acts.md). |
| 2026-07-09 | **Акты комплектности/дефектовки — разделение, печать, бланки** | #135–138 (v2026.709.1248) | Единый список `engine_inventory` оставлен один (одно сохранение, зависимость дефектовка→комплектность цела), но UI разбит на **две под-вкладки** в `RepairChecklistPanel` (колонки/подписи/кнопки печати по виду). **Печать** (`engineInventoryPrintHtml.ts`): видная шапка «Акт … № <№двигателя>(в.N) · от · Цех» (цех+версия пробрасываются, раньше не печатались), компактный CSS (шрифт 13, padding 1×5, без двойного margin), **режим blank** (пустые клетки + запасные строки) → кнопки «Бланк комплектности/дефектовки». Слот `customer_representative` (`checklistService.ts`), блок «Состояние при поступлении» (`ENGINE_RECEIPT_CONDITION_FIELDS` в shared), кнопка «Заполнить комиссию по цеху» (force-fill по `findEmployeeByPositionGroups`). Приёмка внутренняя (по docs/zavod), заказчик — опционально. Verify: pure-билдеры печати + CDP под-вкладок. |
| 2026-07-09 | Наряд на сборку: двигатель в шапке + авто-статусы двигателя | #133 (v2026.709.1248) | `WorkOrderPayload.assemblyEngineId` + `resolveAssemblyEngineId` (fallback `primaryAssemblyEngineId`): двигатель выбирается раз в шапке, проставляется во все `freeWorks` (per-row колонки скрыты для Assembly). Авто-статус (только вперёд, без отката): «Выдать в работу» → `status_repair_started`, дата выполнения → `status_repaired` через `advanceEngineStatusForWorkOrder` (guard) + IPC `engine:advanceStatus`; взаимоисключение флагов вынесено в shared `applyStatusFlagChange` (карточка переведена на него). CDP: bridge-переходы + guard + header-пропагация. |
| 2026-07-09 | Отчёты: фильтры по датам ремонта + сброс/выкл у каждого фильтра | #134 (v2026.709.1248) | `engines_list` обогащён `date_range` «начало ремонта»/«окончание ремонта» (`status_repair_started_date`/`status_repaired_date`) + колонки. Универсально: у каждого фильтра во **всех** пресетах кнопка «выкл» (исключить из отбора) рядом с «сброс»; `omitDisabledFilterKeys` вырезает ключи, `readPeriod` при опущенном `endMs` даёт `MAX` (реально снимает верх). Спец-панель `assembly_forecast_7d` оставлена со сбросами (отключение параметра прогноза семантически иное). Verify: bridge presetPreview + CDP кнопок. |
| 2026-07-08 | Отчёт по нарядам — редизайн + колонка «Контрагент» | #118 (v2026.708.1139) | Печать под стиль формы наряда (тёмные рамки, цветные статусы, A4-альбом); выбор колонок (`columns` multi_select), фильтр по каждому полю (даты выдачи/срока/выполнения, мульти-статус, мульти-тип, ответственный, марка, №/№дв./виды работ, контрагент), сортировка вкл. по статусу; суперсет 15 колонок + рендер в чистом shared [`workOrdersReport.ts`](../shared/src/domain/workOrdersReport.ts) (12 тестов); «Контрагент» = заказчик из контракта двигателя; preview==print. |
| 2026-07-08 | Наряд ложно «просрочен» при дате выполнения в срок | #117 (v2026.708.1139) | `deriveWorkOrderStatusCode` игнорировала оператор-заданную `completedDate` у незакрытых нарядов (розовый по `now>=dueExpiry`); список отдавал `completedDate` только закрытым. Теперь дата выполнения = «выполнен» независимо от закрытия (в срок→done, позже→done_late); список/отчёт согласованы через единый источник, 4 теста. |
| 2026-07-08 | «Распространить набор деталей на группу марок» | #120 (v2026.708.1139) | Кнопка в карточке марки «Детали для марки» → набор (кол-во/№ узла/флаги комплектность-дефектовка) копируется привязками на все марки выбранной группы; батч `propagatePartSpecBrandLinkToBrands` (1 чтение+запись на деталь), merge по engineBrandId (идемпотентно), флаги явно, чужие детали марок-целей не трогаются. |
| 2026-07-05 | Печать наряда `***NNN`+контрагент — verify вживую | (verify-хвост, без PR-кода) | CDP на живом клиенте: наряд со строкой на двигатель Д08АТ5366 (договор …883, ООО «ОВК») → A4-превью (`iframe.srcdoc`) отрисовало `***883` + «ООО \"ОВК\"». Вся печать наряда теперь проверена вживую. |
| 2026-07-09 | Черновики карточек товаров/услуг (Phase 3d, EAV-редактор) | #151 (v2026.710.244) | `SimpleMasterdataDetailsPage` получил draft-движок пилота: recovery-автосейв 1.5с снимка локальных полей (имя/описание/цех/артикул/ед./цена/марки услуг; файлы пишутся сразу — не в черновике), draft-aware `load()` (снимок побеждает committed, раз на маунт), `keepDraft` в 3-кнопочном close-guard, «Сохранить как черновик»; `saveAll` отменяет отложенный автосейв и чистит черновик; App/DraftsPage знают cardType `product`/`service`. CDP 12/12 (карточка через глобальный поиск — вкладка «Услуги» = номенклатурный справочник, EAV-сущность там не видна). Остаток 3d (контрагенты/договоры/марки/двигатели/сотрудники — свои страницы) — PENDING. |
| 2026-07-09 | Сверка печати наряда вживую (хвост v2026.625.1722 #595) — патч не нужен | verify-only (без PR) | CDP по живому стенду, наряд №25: собственный индикатор приложения «✓ Помещается на 1 лист A4», contentH=листу (1123px, 1 страница), таблицы реквизитов и работ с чёткими solid-рамками, подписи «Выдача/Завершение» подтянуты кверху (не прибиты к низу). Скрин `probe-print-onesheet.png`. Хвост закрыт без правок. |
| 2026-07-05 | Черновики заявок в снабжение (Phase 3d) | #84 (в `main`, до релиза) | `SupplyRequestDetailsPage` получил draft-движок наряда-пилота: cardType `supply_request`, debounced-автосейв 1.5с в `card_drafts` (синк, owner-private), draft-aware `load()` (снимок побеждает committed; для несохранённой новой заявки черновик=seed), `keepDraft` в close-guard (3-кнопочный диалог), «Сохранить как черновик» (explicit, без материализации); маппинги в App/DraftsPage. CDP 18/18 (kill+restart+восстановление). План `plans/drafts-no-empty-cards-recovery-2026-06.md`. |
| 2026-07-03 | **Рекламация MVP + вкладки карточки двигателя + повторный заезд — на проде** (задание владельца /zavod 2026-07-02) | #58,#59,#60,#61 (v2026.703.2147) | Три фазы одним днём. **Ф0** (#58): карточка двигателя «полотенце» → 4 вкладки (Основное / Детали и акты / Фото и документы / Рекламация), панели остаются смонтированными (`hidden`) — save-on-close/черновики/печать не тронуты; dirty-маркер ● на ярлыке; проп `initialTab`. **Ф1** (#59): рекламационный учёт EAV без DDL — 8 атрибутов `reclamation_*`, кнопка «Принять по рекламации» (авто-дата), вердикт вины / статус ремонта / даты / комментарий; shared `reclamation.ts`; в списке синяя точка рядом с привязкой + фильтр «Рекламационные»; статусы/акты/прогноз/зарплата не затронуты. **Ф2** (#60): трёхвариантный выбор при вводе существующего номера (рекламация → существующая карточка на вкладке / повторный заезд → новая карточка с `repeat_arrival_flag`+`previous_arrival_id` / коллизия номера); обход запрета дублей flag+intent на обоих гейтах (клиентский offline + серверный, флаги пишутся ДО номера); исключение флагованных из авто-склейки, «Поиска дублей» и `mergeEngineGroup`; в списке «архивный заезд» на старом (`shared repeatArrival.ts`), панель «прежние заезды». Попутно починена pre-existing гонка: `openEngine` сбрасывает stale `engineDetails` до смены id (свежая карточка монтировалась с чужими атрибутами). CDP-смоуки 12/12+10/10+10/10, backend 366, shared 319. Релиз v2026.703.2147 (увёз и #56), прод верифицирован (health/updates/blockmap 200). План: [`plans/_archive/reclamation-mvp-2026-07.md`](plans/_archive/reclamation-mvp-2026-07.md). |
| 2026-07-03 | Печатная форма наряда: даты/цех + шаблон→склад + вариант Б шапки + фикс имён деталей | #52,#54,#56 (v2026.703.1446, v2026.703.1539; #56 в main, ждёт релиза) | Три итерации по печатной форме сборочного наряда. **#52** (v2026.703.1446): в реквизиты добавлены Дата создания/Приступить/Срок/Цех с галочками «Печатать в шапке» (`hide*`-флаги в `WorkOrderPrintSettings`); фикс «застрявшей» даты печати — `orderDateOverride` вычищается из шаблонов/умолчаний печати (данные наряда, не вёрстка); применение шаблона наряда резолвит цех в склад цеха (`warehouse_locations`) и проставляет `sourceWarehouseId` во все строки (правится построчно). **#54** (v2026.703.1539, вариант Б по макету на утверждение владельца): полная дата создания слева напротив грифа «Утверждаю» (убрана из таблицы), реквизиты двумя ярусами (наряд / двигатель-контракт) + word-break — длинный заказчик не растягивает лист; плановые даты без года (ДД.ММ), цех коротким именем; `orderDateOverride` совсем убран (игнор при рендере + удалён из диалога); у сборки из блока «Выдача наряда» убрана строка «Прогнозируемая дата выполнения». **#56** (в main, клиентский — ждёт релиза): надёжная подстановка имени детали при применении шаблона (дозагрузка по id при гонке со справочником parts) + фолбэк-резолв имени по partId на печати (старые наряды с пустым именем печатают название, не «—»). Все три — CDP e2e-smoke (даты/галочки/цех/шаблон→склад/имена, 1 лист A4). |
| 2026-06-30 | Эпик черновики/восстановление + Phase 2 deferred-create (наряды/заявки/контрагенты/марки/договоры) | #660–#667 (**v2026.630.1141**) | 7 ниток эпика «не сохранять пустые карточки + черновики/восстановление» ([план](plans/drafts-no-empty-cards-recovery-2026-06.md)), все CDP-верифицированы (вкл. kill/restart), CI зелёный. **3b** (#660): debounced-автосейв снимка наряда в `card_drafts`, draft-aware восстановление, редизайн close-guard (диалог «Не сохранять / Оставить черновик / Сохранить», убран форс-сейв по таймеру), панель восстановления после краха. **3c-раздел** (#661): `DraftsPage` (таб «Черновики») — список/открыть/удалить. **Phase 2 наряды** (#662): `createWorkOrder` без вставки + `updateWorkOrder`=upsert (номер при первом сохранении, «№ новый» до того); фикс пробела 3b — `flushSave` чистит recovery-черновик. **3c-«Сохранить как черновик»** (#663). **Phase 2 заявки** (#664): тот же приём (номер `Z-…`, scope подразделения). **Ядро masterdata + слайс 1** (#666): `admin:entities:get/setAttr` получили опц. `fallbackTypeId` (backward-compatible) → **контрагенты + марки двигателей** deferred-create. **Слайс 2** (#667): **договоры** (хелпер `setContractAttr`, все ~16 собственных записей через fallback). Inline-lookup/copy не трогались (не дают призраков). Остаток эпика — товары/услуги (nomenclature-путь, отдельный механизм) + `AdminPage` raw-сетка + 3d → PENDING 🟡. |
| 2026-06-29 | Печать наряда: выбор грифа «Утверждаю» (директор / технический директор) | #651 (v2026.629.1711) | SSOT `WORK_ORDER_APPROVERS` (shared) — два варианта грифа (Директор И.А. Тихомиров / Технический директор В.И. Гурьянов); `printSettings.approver` (хранится только не-дефолтный `technical` → старые наряды печатаются байт-в-байт); сегментированный переключатель в диалоге печати с живым превью, выбор персистится на наряд. Verified CDP e2e (переключение меняет гриф, persist round-trip). |
| 2026-06-29 | Связь комплектность→дефектовка (Т7) — наличие гейтит дефектовку | #647 (v2026.628.2326) | Запрос владельца: деталь попадает в дефектовку только если отмечена «на месте» в комплектности (раньше `repairable_qty` сеялся всегда → отсутствующая деталь «забита намертво что есть»). Доменный инвариант `normalizeEngineInventoryRow` (SSOT для печати/заявок/ремфонда/требований): `present=false` → actual/scrap/replace/repairable=0, branch=null; **дефект⇒наличие** (scrap+replace>0 форсит present=true) — миграция legacy без потери данных. UI: утиль/замена read-only+серым до отметки наличия (с подсказкой) + каскадная очистка при снятии «На месте»; bulk «в утиль/замену» пропускает отсутствующие. Presence-default (базовые→present=false, прочие→true) уже работал. Гейт по галочке «На месте» (бинарно), UI блокирует не скрывает (выбор владельца). Verified CDP e2e (замок/разблокировка/каскад). |
| 2026-06-28 | Наряд: дата выполнения, задаётся оператором («Дата выполнения») | #637 (**в main, до релиза**) | Факт. дата завершения не хранилась — деривилась из `updatedAt` при закрытии → закрыл сегодня наряд вчерашней работы = записывалось сегодня (владелец заметил на сборочных). Поле `completedDate` в payload (нормализация как `startDate`/`dueDate`); карточка: открытый наряд — редактируемое «Дата выполнения» (умолчание сегодня, можно прошлую), закрытый — read-only «Завершён» = `completedDate ?? updatedAt`. Статус/список/зарплатный отчёт читают `completedDate ?? updatedAt`. Все типы (поле в общей шапке). Дату склада не меняет; без изменений бэкенда (едет в metaJson через все пути закрытия). CDP-verify: открытый = редакт.поле сегодня, закрытый = «Завершён» из fallback. |
| 2026-06-27 | Доработка списка нарядов + sticky-шапка карточки (follow-up к капремонту) | #634 → #635 (v2026.627.2226) | Owner follow-up после обкатки v2026.627.1912. **Список** (`WorkOrdersPage`): убрана колонка «Ответственный»; «Дата выдачи» → «Дата создания наряда»; добавлена сортируемая «Начало работ» (`startDate` — «Приступить», раньше отсутствовала в списке); «Исполнители» = кто «Принял в работу» (слот блока «Выдача» по caption «принял», первый заполненный из нескольких нач. цехов — helper `getWorkOrderAcceptedByEmployeeId`), иначе фамилии бригады (fallback «принял, иначе бригада»). `listWorkOrders` отдаёт `startDate`+`acceptedByEmployeeId` (вместо `responsibleEmployeeId`); shared ipc-тип обновлён. **Карточка** (`WorkOrderDetailsPage`): кнопки `CardActionBar` + блок реквизитов (№/даты/шаблон) закреплены сверху одним sticky-заголовком — прямым потомком единственного скролл-контейнера. Был `position:sticky`, но заперт в тесном родителе внутри двойного скролл-контейнера (внутренний `overflow:auto` EntityCardShell инертен, реальный скролл — внешний `A`) → откреплялся сразу. Клиентское изменение (без миграций). Гейты зелёные (typecheck/lint/backend 347/CI); CDP-verify (verifier-electron, 22 наряда): заголовки списка, обе ветки «Исполнители» (fallback + accepted-over-crew через bridge: Коротких vs Шампоров), sticky drift 0px на 277px прокрутки. **Прод: v2026.627.2226 раскатан** (git pull → build серверных → 3 артефакта → ledger-publish → restart; /health+/updates/status=2226, blockmap 200). |
| 2026-06-27 | Капитальная доработка нарядов (5 этапов: подписи, шаблоны, даты/статусы, список, отчёт) | #625–#630 (v2026.627.1912) | Owner-запрос (диктофон). План — [`plans/_archive/work-order-overhaul-2026-06.md`](plans/_archive/work-order-overhaul-2026-06.md). **1a** (#625): подписи — стрелки ↑↓ (порядок → в печать, единый `slots[]`), чистая вёрстка строки (поле ФИО шире, должность отдельной строкой, без `maxWidth:240`-обрезки), «последние использованные» подписанты вверху списка (per-machine localStorage). **1b** (#626): общие кастомные формулировки подписей — новая таблица `work_order_signature_captions` + **миграция 0069** (несинкаемая, прямой HTTP как у templates), сервис/роут/IPC/preload, datalist-merge + persist по blur. **4** (#627): шаблоны — структурный селектор цеха + конфигуратор подписей (роли+ФИО) через `payloadOverrides` (apply = Object.assign, без правки backend); `templateToEditor` извлекает structured-ключи из JSON. **2** (#628): payload v4 — `startDate`/`dueDate` (нормализация, version 3→4 back-compat), `deriveWorkOrderStatusCode` (issued/done/overdue/done_late, pure); «дата завершения» деривится из close-ts (не трогает складской closing); шапка карточки 4 даты + бейдж. +6 shared-тестов. **3** (#629): список — колонки (дата выдачи/№/срок/виды работ/марка/№дв/завершён/ответственный), цвет строк по статусу, ответственный = первый подписант «выдачи» (`listWorkOrders` отдаёт +5 полей, резолв ФИО на странице). **5** (#630): отчёт `work_orders_report` — двухпанельный конструктор (фильтры выдачи/срока/статуса/ответственного/марки) + `buildWorkOrdersReport` + кнопка-deep-link у списка + печать/PDF/CSV из `ReportPresetPage`; due-фильтр не отсекает наряды без срока. Каждый этап CDP-verify со скриншотами; гейты зелёные. **Прод: v2026.627.1912 раскатан** (db:migrate 0069, /health+/updates/status=1912, blockmap 200). |
| 2026-06-25 | Наряд: рестайл формы печати + шаблоны настроек + двухфазная процедура подписей + 1 лист A4 | #586 (v2026.625.726) | Форма печати: ползунки → стрелочные степперы ▲▼; «Шапка» масштабирует гриф+заголовок+подстроку пропорционально (13:22:13, константа `WORK_ORDER_PRINT_HEADER_SUBPART_BASE_PX`); превью — настоящий лист A4 (210×297мм, поля 12мм, `zoom`-масштаб, 1:1 с печатью), крупнее, панель уже. **Шаблоны печати** (localStorage, per-machine): именованные + умолчание для вида наряда, применение одним кликом с живым превью (`woPrintTemplates.ts`). **Подписи** — два блока «Выдача наряда»/«Завершение наряда» для всех типов (ПДО→ОТК→цех / цех→ОТК→ПДО), 6 предзаполненных ролей, обратная совместимость blockId (`default`/`assembly_*`→`issue`/`completion`). Уплотнение (блоки рядом) → наряд на 1 лист A4. Renderer+shared, без миграций; shared 246 + backend 307 тестов, CDP e2e PASS. |
| 2026-06-24 | Наряд: переработка печати + подписанты/контракт/гриф + панель настройки печати | #563/#569/#576/#574/#581/#582 (v2026.624.49→.1651) | Шапка «Наряд на <вид работ>», убраны заголовки разделов, компактные строки, пустые блоки не печатаются. Подписи — слоты (роль+сотрудник, расшифровка «И.О. Фамилия» из БД, по 2 в строку, кнопка «Добавить пустую подпись»), блоки «Наряд выдан»/«Наряд выполнен» + пустые даты от руки, подразделение/цех рядом с должностью. Гриф «Утверждаю» (директор) над шапкой. После №двигателя — `***NNN` (последние 3 цифры номера контракта до `/`) + контрагент (поле «Краткое наименование» у контрагента). Колонка «Артикул» у детали. Панель «Печать»: редакт. заголовок/дата + размер шрифта на раздел + живой A4 с индикатором вместимости (`printSettings` в payload, обратно совместимо). |
| 2026-06-24 | Печать наряда на сборку — двигатель в шапке, склад-источник, подписи приёмки | #563 (v2026.624.49) | Для Assembly-наряда: марка/№ двигателя + вид работ «Сборка двигателя» вынесены в шапку (не повторяются построчно); колонки «Цена»/«Сумма» + денежный Итог заменены колонкой «Склад» (источник детали, резолв `sourceWarehouseId` по id или `workshop_<code>`); новая секция «Подписи» — два блока («Наряд выдан» / «Двигатель собран, обкатан и признан годным к отправке заказчику»), по 3 пустые линии (директор/начцеха/ОТК). Гейт `workOrderKind===Assembly`; прочие типы нарядов без изменений. Renderer-only (`WorkOrderDetailsPage.printWorkOrderCard`), превью сверено с владельцем. В том же релизе — бэкенд `describeError` (логи сохраняют PG-cause, #561). |
| 2026-06-19 | Табель: область = цех XOR подразделение (ОПП и др. можно затабелить) | #497+#498 (v2026.619.1703) | Миграция `0067` (`workshop_id`→nullable + `department_id` FK→entities + CHECK XOR + partial-uniq `(department_id,year,month)`); сервис/роут/IPC/preload/`shared` типы; UI — единый селектор «Цех / подразделение» (optgroups). Табель server-only → без sync/клиентской миграции. 284 backend. |
| 2026-06-18 | Табель Т-13 pass-3 (живая обкатка) | #479 (v2026.618.1551) | ПКМ-протяжка → прямоугольная заливка кистью + резинка; блоки кодов «с часами» (пара код+часы) / «без часов» (затирают ячейку) по `countsAsWorked`; ПКМ-клик без движения → комментарий (без конфликта с ЛКМ-freehand); рендер — часы 2× жирные чёрные / код-без-часов крупно, уголок-метка 12px тёмно-бордовый; **печать починена** — независимые галки секций (баг «пустой лист» от неисполнения inline-`<script>` в Electron-child-window, фикс через CSS `:has` вместо JS), должность под ФИО; убрана «Заполнить норму всем». Renderer-only, CDP-verified. |
| 2026-06-18 | Табель Т-13 pass-2 (живая обкатка) | #469–#471 (v2026.618.1128) | Фикс бага ввода комментария (глобальный grid-keydown глотал клавиши в модалке); единая «кисть» (выбор кода ≠ правка ячейки) + drag-paint + плывущий чип курсора; bulk-add по подразделению + мульти-выбор add/remove; размер шрифта ±/режимы половин месяца/растяжка+авто-fit; легенда на экране + порядок печати. Приветственное окно (B) написано. Renderer-only, без миграций; CDP-verified live. A1: «весь цех»=группировка по подразделению (PENDING). План `plans/_archive/timesheet-t13-pass2.md`. |
| 2026-06-17 | Табель Т-13 (Ф1+Ф2) + Табель в «Мой круг» | #453–#455/#458/#459 (v2026.617.1614) | Модуль табеля учёта рабочего времени по цехам (коды Т-13, кисть/fill-norm/клавиатура, live-итоги, печать A4 half/full/decode, per-cell комментарии). Ф3 (праздники/зарплата) снята владельцем. Removable-плитка «Табель» в «Мой круг». |
| 2026-06-10 | engine-acts Phase 1–5 | 2026-06-09/10 | Акты комплектности/дефектовки двигателя; все 5 фаз /verify PASS. |
| 2026-06-08 | defect-to-supply MVP-1/2 | #283 | «Заявка в снабжение из негодных» + фото на строке (online-кейс). MVP-3 offline — отложен (PENDING). |
| 2026-06-07 | checklist-unify Stage 1–5 (Акт комплектности+дефектовки) | v1.23.0–v1.25.0 + #54 | Объединённый список engine_inventory, 2 печати, фильтр по варианту сборки; Контрольный лист удалён. |

## Обновления / установка / версии

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-05 | Апгрейд Electron 41→43 + релиз v2026.705.2235 (вынес #76–#85 операторам) | #89/#90 (v2026.705.2235 на проде) | `better-sqlite3` plain+форк→12.11.1 собирается под V8 E43 (ABI 148); опасение #87 про нужную «12.11.2» неверно. Реальный блокер — plain `better-sqlite3@12.10.0` (runtime-dep через drizzle-драйвер, до фикса `External::New`). CI installer green + локальный boot-смоук (`SQLite ready` под 148). Прод-install-грабля (флаки-сеть + electron-бинарь) → GOTCHAS M16. План — [`plans/_archive/electron-43-upgrade.md`](plans/_archive/electron-43-upgrade.md). |
| 2026-07-02 | Source-agnostic дельта-засев updater'а (техдолг раската 623: клиенты тянули полные 116 МБ) | #39 (в `main`, поедет со следующим релизом) | Засев дельта-топлива требовал `sha(кэш-exe)===sha(серверной сборки)` → зеркальные exe (Yandex/GitHub, F2) и пропущенное окно «клиент на latest» оставляли без топлива при 90.9% измеренного reuse (харнесс #415). Теперь `cacheServerDeltaArtifacts` при sha-расхождении **генерит blockmap локально из фактических байтов кэшированного installer'а**: Rabin CDC, побайтово совместимый с app-builder (Poly64/window 64/чанки 8-16-32 KiB), blake2b-18 чанк-хэши (`blakejs`); checksums описывают реальный локальный файл → copy-блоки корректны при любом происхождении exe, сборка по-прежнему верифицируется по `meta.sha256`. Валидация: сгенерированный blockmap байт-в-байт совпал с реальным blockmap релиза 2026.702.1024 (116 МБ, opt-in тест `blockmapDelta.generate.test.ts`) + синтетика CDC. Эффект у клиентов — начиная с обновления ПОСЛЕ следующего (фикс сам едет клиентским кодом). Не покрыто: топливо для свежих установок (кэша-exe нет вовсе). |
| 2026-06-24 | Watchdog — GUI-subsystem сборка (нет окна терминала у операторов) | #572 (v2026.624.1547) | `go build -ldflags "-s -w -H=windowsgui"` в обоих воркфлоу (релизный + watchdog-build) → Scheduled Task больше не мигает чёрной консолью «watchdog pass start»; файловый лог `%APPDATA%\MatricaRMZ\watchdog.log` сохранён. |
| 2026-06-22 | Watchdog #7 — внешний агент восстановления клиента (Фазы 2/3-4) | #548/#549 (не релизилось) | Восстановление после сорванного NSIS-обновления (папка снесена → встроенный апдейтер бессилен). **Фаза 2:** команда `reinstall` в sync-request + кнопка «Переустановить» в админке (клиент явно пропускает watchdog-тип — anti-race). **Фазы 3-4:** Go-бинарь `watchdog/` (чистый stdlib, 5.1 МБ, build-verified в CI `watchdog-build.yml`) — ладдер presence→installer(pending/dir/download)→validate(MZ+size+sha)→silent`/S`→report+ack, уважает `update.lock`; handshake `%APPDATA%\MatricaRMZ\watchdog.json` (клиент пишет на heartbeat — снимает userData-неоднозначность packaged vs dev, держит Go на stdlib); неаутентиф. `/client/watchdog/report`→критсобытия. **Открыто:** Фаза 5 (installer.nsh Scheduled Task + подпись + раздача) + Фаза 7 (функц-тест) — нужен реальный Windows-стенд (PENDING §G). ⚠️ Кнопку не релизить без Фазы 5. План — [`plans/client-watchdog-agent.md`](plans/client-watchdog-agent.md). |
| 2026-06-21 | delta-обновления — засев топлива при старте + телеметрия исхода | #516 (v2026.621.1133) | Корень: топливо (sidecar+`.blockmap`) не кэшировалось → `haveFuel=false` → дельта молча пропускалась (0 delta-строк за 15 обновлений). Фикс: громкий лог вместо тихих `return`; `ensureDeltaFuelForCurrent` при старте (клиент на latest + stable-installer = серверная сборка → дотягивает blockmap+sidecar) → дельта на следующем релизе; телеметрия `update-applied` через `/logs/client` + CLIENT_PATTERN `client.update.full_download`. **Ждёт полевого подтверждения** (всплеск-затем-тишина после раската). gates+boot-smoke. |
| 2026-06-18 | delta-обновления — добивка foreground | #463 (v2026.618.117) | delta-first + `cacheDeltaFuel` перенесены в foreground `runAutoUpdateFlow` (раньше жили только в background-tick → foreground качал полный installer и quit'ил раньше → в поле не срабатывало). Эффект — со следующего апдейта после прайминга. |
| 2026-06-15 | delta-обновления (Путь B) | v2026.615.1417/1517 | Blockmap-delta вживую (~92 % экономии), delta-first шаг, delta в окне, LAN-peer Range. |
| 2026-06-14 | CalVer + update-module-revamp | v2026.614.1618 | Версия штампится из даты (`bump-version.mjs`); окно обновления (степпер/МБ/ETA/ошибки). |
| 2026-06-05 | Апгрейд Electron 33→41 | v1.43.0 | electron-builder 25→26, better-sqlite3 11→12, source-build нативки. |
| 2026-05-27 | Autoupdater self-heal рефактор F1–F8 | v1.32.x | Детект двойного фейла миграции → emergency installer fetch; Yandex sha; BOM/asar-path фиксы. |

## Поиск / UI

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-16 | **Печать табеля v2: часы без «Я» во всю ячейку, галочки блоков, прямая печать, тёмные линии** | [#234](https://github.com/Valstan/MatricaRMZ/pull/234), релиз **v2026.716.1619** | По замечаниям владельца после v2026.716.1408. Код «Я» скрыт в ячейках с часами (экран и печать), коды без часов — полным размером ячейки; в `TimesheetPrintDialog` галочки блоков (шапка/таблица/легенда/комментарии, комментарии — отдельным листом, лимит страниц 2); печать напрямую из диалога (`printSectionsDirect`, окно без контролов, автозакрытие по afterprint) — старое промежуточное окно убрано; линии th/td на печати #1f2937 + print-color-adjust exact. CDP-смоук `_ts-print-v2*.mjs` зелёный. |
| 2026-07-16 | **Табель: сортировка сотрудников + компактная печать со шрифтами по блокам** | [#230](https://github.com/Valstan/MatricaRMZ/pull/230), релиз **v2026.716.1408** | По запросу владельца. Реордер строк: кнопка «А→Я», стрелки ↑↓, drag ФИО; новый `PUT /timesheets/:id/rows-order` + IPC `reorderRows` (полная перестановка, sort пересчитывается сервером, параллельные добавления уходят в хвост). Печать: A4 landscape, компактная вёрстка (1–2px, @page 8mm), печатаемая шапка листа; `TimesheetPrintDialog` по образцу нарядов — вариант (месяц/1–15/16–кон.), легенда, 6 пошаговых размеров шрифта (шапка/ФИО/ячейки/числа/дни недели/легенда), живое A4-превью с «✓ Помещается на 1 страницу»; `TimesheetPrintSettings` в shared, localStorage-персист. CDP-смоук `_ts-reorder-print.mjs` зелёный (реордер персистится: sorts = DOM; окно печати несёт landscape-CSS). |
| 2026-07-15 | **Пилот UI-конструктора (уровень 1, без нейронки)** | #210–#212, релиз **v2026.715.45** | Операторские экраны из JSON-спека: `shared/uiSpec.ts` (heading/text/button/list, allowlist-интенты navigate_tab/open_report, tolerant parse двойного EAV-кодирования), `SpecRenderer` + intent runtime (кнопки на section-gated табы — disabled), 2 read-only виджета (recent_engines, my_work_orders). Хранение общезаводское — EAV-тип `ui_screen` через обычный sync; свой IPC-домен `uiScreens:*` (НЕ admin:entities — тот требует masterdata.edit): экран принадлежит AccessSection, просмотр=viewer+, save/delete=editor, superadmin bypass (но несуществующий раздел отклоняется всем — найдено смоуком). Редактор: палитра, dnd-kit сортировка, панель свойств, живой предпросмотр. Меню: «Мои экраны» (группа «Мой круг», v2 list-column) + карточка просмотра/редактирования. CDP-смоук `_smoke-ui-builder.mjs` 15/15 (полная петля собрал→сохранил→переоткрыл→интент). План: [`plans/_archive/ui-builder-pilot-2026-07.md`](plans/_archive/ui-builder-pilot-2026-07.md). Уровень 2 (нейронка) — отложен до доказанной потребности. |
| 2026-07-13 | Явное сохранение: карточка марки (список деталей) + админ-справочники по кнопке | #197, релиз **v2026.713.1447** (на проде) | Конец «тихому» сохранению. **Карточка марки** ([EngineBrandDetailsPage.tsx](../electron-app/src/renderer/src/ui/pages/EngineBrandDetailsPage.tsx)): список деталей (кол-во, галочки актов, add/detach, bulk) больше не пишет в БД сразу — драфт против `committedPartsRef`, запись по «Сохранить» через `applyBrandPartsDiff` (detach/upsert, при частичном фейле dirty держится); close-guard теперь покрывает и список (вопрос да/нет); «Сохранить и выйти» закрывает только при успехе; «Сброс» откатывает список; «Распространить на группу» блокируется при dirty; recovery-draft расширен `parts?`. Создание новой детали справочника + вложения остаются немедленными. **AdminPage**: `FieldEditor` не сохраняет onBlur/onChange — dirty-набор + кнопка «Сохранить» в шапке «Карточки записи» + бейдж (JSON парсится при записи); карточка марки в админке (имя+детали) тоже по кнопке (дифф от committed). Verify: CDP e2e (правка qty не в БД до «Сохранить» — прямой probe; «Сброс» откат; админ-бейдж+сохранение). |
| 2026-07-10 | **Релиз v2026.710.244: интерфейс «Резиновый» по умолчанию** (rename + default-flip) | #153 (v2026.710.244, на проде) | По решению владельца: «Трезубец» → **«Резиновый»** во всех операторских текстах и названиях (слово убрано полностью); **v2 — дефолт для всех клиентов после обновления** (`DEFAULT_UI_SHELL_PREFS`+sanitize: как v1 читается только литеральный сохранённый 'v1' → явный возврат на старый запоминается per-user и переживает обновления; отсутствующая запись prefs → санитизированный дефолт). +6 юнитов sanitize; CDP-смоук 9/9 (uiSet-round-trip дефолта, персист явного v1, «Трезубца» в UI нет). Деплой: артефакты через локальный download+scp (VPS↔GitHub CDN TLS-таймауты, класс M29; sha256 сверен), ledger-publish, оба сервиса active, /health и /updates/status = 2026.710.244, blockmap 200. |
| 2026-07-09 | V2 «Резиновый» Фаза 4: drag-перестановка колонок + session-restore карточек | #149 (v2026.710.244) | Колонки переставляются drag'ом заголовков («Меню»/список — хэндлы, подсветка стороны вставки, все 6 порядков достижимы; `columnOrder` персистится). Сессия рабочей области (вкладки+фокус+split-secondary) — в новый `shellPrefs.v2.session` (дебаунс 800мс, сигнатура против echo), восстанавливается раз на пользователя при старте. Попутно закрыта гонка: отложенный flush раскладки V2Shell мержит pending-размеры поверх свежих prefs (не перезаписывает блоб). CDP: drag-смоук 13/13; restore после **настоящего рестарта** клиента 7/7. Остаток Ф4 (перф-полировка) — PENDING. |
| 2026-07-09 | Второй интерфейс «Резиновый» (v2, opt-in) — полная нитка (Фазы 1–3 + сплит + видимый тумблер) | #127,#128,#129,#130 (v2026.709.200) | Альтернативная оболочка клиента с per-user переключателем (дефолт остаётся v1, старый путь нетронут). **#127** каркас: 3 резиновые/сворачиваемые/переставляемые колонки (панель кнопок с dnd/pin/hide/overlay · списки · рабочая область), персист per-user через `ui:prefs` (`shared/domain/uiShellV2.ts`, `settingsStore` `UiShellPrefs`); shell рендерится оборачиванием существующей цепочки страниц (`renderTabContent(t)`), primary-путь v1 не тронут. **#128** вкладки открытых карточек (до 3, дедуп по kind+entityId, кап с вытеснением, dirty-guard на закрытии/замене). **#129** настоящий сплит «2 рядом»: параметризованный `renderSecondaryCard()` (engine self-load отдельным IPC, остальные 16 видов по id) + pane-aware close-backstop (`cardClosePanesRef`, «Сохранить/Отклонить все» при двух грязных). **#130** видимый тумблер v1/v2 в шапке рядом с темами. Библиотеки: `react-resizable-panels@4` (Group/Panel/Separator, Layout=flexGrow), `@dnd-kit`. Верифицировано CDP: split 18/18, cards 22/22, shell 26/26, тумблер 10/10, регрессия v1 12/12. План: [`plans/ui-shell-v2.md`](plans/ui-shell-v2.md) + [`plans/ui-shell-v2-split.md`](plans/ui-shell-v2-split.md). |
| 2026-07-08 | Глобальный поиск двигателя по «№ на детали» (набитому номеру) | #124 (v2026.708.1553) | Глобальный поиск (кнопка сверху) не находил двигатель по индивидуальному номеру детали — `stamped_number` («№ на детали») живёт в `operations.metaJson` (engine_inventory), не в EAV/номенклатуре → ни один уровень (L2 справочники / L2.5 card-content по attribute_values / L3 сервер по номенклатуре) не матчил. Новый клиентский уровень `searchEnginesByStampedPartNumber` ([`cardContentSearchService.ts`](../electron-app/src/main/services/cardContentSearchService.ts)) сканирует локальный SQLite `operations`, матчит **только** `stamped_number` (не сборочный `assembly_unit_number`, не чертёжный `part_number` — по просьбе владельца), возвращает двигатели (`engineEntityId`=id карточки), регистронезависимо + compact («2401»→«240-1»). IPC `search.enginesByStampedNumber` + группа «Двигатели · по № на детали» в [`GlobalSearchOverlay.tsx`](../electron-app/src/renderer/src/ui/components/GlobalSearchOverlay.tsx). Верифицировано вживую (bridge + оверлей: набитый номер → двигатель, compact → двигатель, сборочный номер → пусто). |
| 2026-07-08 | Карточка двигателя не сохраняет втихую | #119 (v2026.708.1139) | Модалка закрытия для legacy-карточек (без черновика) больше не авто-дожимает в `save` по 10-сек таймауту — ждёт явного «Сохранить»/«Не сохранять»; ничего не пишется без осознанного клика. Deferred-save + кнопки «Сохранить»/«Сохранить и выйти» уже были; убран тихий путь (App.tsx close-flow). Общее для всех non-draft карточек. |
| 2026-07-08 | Список «Марки двигателей» — вид «Секции по группам» | #116 (v2026.708.1139) | Аддитивный переключатель (default off → нулевой регресс); марка из N групп в каждой своей секции, «Без группы» в конце. Чистый хелпер [`brandGroupSections.ts`](../electron-app/src/renderer/src/ui/utils/brandGroupSections.ts) (6 тестов). |
| 2026-07-05 | **Поиск объединён в верхнее поле списков** (директива владельца: одно поле, нижняя панель убрана) — закрыло Ф3 плана поиска | #76 (в `main`, до релиза) | Нижний `ListSearchBar` удалён (дублировал верхний фильтр); его tier-2 (поиск внутри карточек, EAV `search:cardContent`) влит в верхние фильтры. `useListDeepFilter` → controlled query + низкоуровневый `useCardContentIds`. Двигатели/Сотрудники — верхний фильтр tier-1+tier-2; Наряды/Заявки — верхний q уже искал по payload в main (панель просто убрана); tier-2 добавлен: Контрагенты, Контракты, Марки, Услуги-по-маркам, справочники masterdata. Счётчик «N из M» у Двигателей. CDP 16/16. План: [`plans/_archive/list-bottom-filter-and-global-search-2026-07.md`](plans/_archive/list-bottom-filter-and-global-search-2026-07.md) (открытый хвост — пополнение `uiSearchRegistry` по жалобам, см. handoff). |
| 2026-06-24 | web-admin «Клиенты» — карточки вместо широкой таблицы | #573 (v2026.624.1547) | 12-колоночная таблица не влезала по ширине (текст/кнопки уходили за край) → адаптивная сетка карточек (`repeat(auto-fill, minmax(310px,1fr))`); выбор для полной синхронизации перенесён галочкой на карточки (убран дублирующий список). |
| 2026-06-24 | Должности сотрудников — синхронизация справочника + бэкфилл | #577/#580 (v2026.624.1547) | `position_ref` (выпадающий список) и free-text `role` сотрудника разошлись → при сохранении карточки должность автоматически заводится в справочник; скрипт `positions:backfill` (createEntity+setEntityAttribute, sync-корректно) — на проде заведено 16 реальных должностей. Dry-run поймал баг «null» до записи (#580). |
| 2026-06-22 | Меню: кнопки разделов целыми словами (без переноса по буквам) | #535+#544 (v2026.622.1241) | `nowrap` + кап ширины 240 + grid `auto auto` → метки одной строкой, кнопка растягивается под текст, без разрыва слов и обрезки последней буквы. Чинит «перенос по буквам» на старых клиентах (#535 был не на проде). CDP-verified live (`labelLines=1`, `clipped=false`; probe 760px держит). |
| 2026-06-22 | Welcome-окно: цитата-эпиграф вверху вместо заголовка | #545 (v2026.622.1241) | Опц. `epigraph` в `ReleaseWelcomeContent` → `<blockquote>` мельче title (clamp 15–21px, курсив), «как у философа»; **новый на каждый релиз** (правило в CLAUDE.md §Release process), можно по теме релиза. Старые записи без него показывают title. |
| 2026-06-20 | **Глобальный поиск Ctrl+K (owner-batch E)** — L1+L2+L3 командная палитра | #501/#502 (v2026.620.1715) | Overlay по Ctrl+K + кнопка 🔍: L1 видимая страница (scope-context), L2 справочники в памяти (двигатели/марки/контрагенты/контракты/сотрудники/наряды/услуги/изделия/инструменты — навигируемые id), L3 сервер `GET /search` (номенклатура+документы, пермишен-фильтр). Переиспользовано: `tieredSearch`/`prepareRecordSearch`, `navigateToRoute` (извлечён из чат-deep-link, +work_order). Группировка по типам, стрелки/Enter/Esc. План → `plans/_archive/global-search.md`. CDP-verified. |
| 2026-06-20 | Учёт рабочего времени: «Онлайн» (фикс «10ч») + «Активно» | #506 (v2026.620.1715) | `recomputeDailySummary`: онлайн = heartbeat-bounded connected time (`sessionizeOnlineMs` чистая+юнит-тест), окно `[00:00, min(now,cutoff)]`, открытая сессия по `lastSeenAt`, убраны хардкод 08–18 и кап 10ч → «только открыл → ~0». Колонка «Активно» = input-based (renderer `activityTracker`, дельта на существующем 60с heartbeat, серверная `statistics_active_time` GREATEST-upsert, **миграция 0068**). План → `plans/_archive/work-time-tracking.md`. «Онлайн» best-effort (lost-stop overcount), «Активно» — точная. |
| 2026-06-20 | «Сохранить и выйти» на 3 карточках с кастомными панелями (C-хвост) | #503 (v2026.620.1715) | StockDocument (`saveDocument`→`Promise<boolean>`), Nomenclature (вынос `saveAll`), BOM-редактор (штатные `onSave`/`onSaveAndClose` на `CardActionBar`). Все 13 карточек теперь с обеими кнопками. CDP-verified (close-on-success). |
| 2026-06-20 | Номенклатура: сброс висящего `specJson.templateId` | #504 (v2026.620.1715) | Деталь со ссылкой на удалённый шаблон не сохранялась (400 «шаблон не найден»); эффект в `NomenclatureDetailsPage` чистит висящую ссылку после загрузки шаблонов → legacy-детали снова сохраняются. Затрагивало и глоб-поиск. CDP-verified. |
| 2026-06-19 | Карточки: «Сохранить» (без закрытия) + «Сохранить и выйти» | #496 (v2026.619.1703) | `onSave` дорощен на 10 карточках через общий `CardActionBar` (Simple уже имел обе); save-функции не закрывают карточку. 3 карточки с кастомными панелями (StockDocument/Nomenclature/BOM-редактор) — хвост (PENDING). |
| 2026-06-19 | Колокольчик на кнопке «Мой круг» при новых контрактах/ДС | #495 (v2026.619.1703) | `historyAlertCount` (App→Tabs, blink-бэйдж 🔔 на кнопке группы history при count>0); загрузчик алертов вынесен в `ui/utils/contractAlerts.ts` и переиспользован HistoryPage → бэйдж и список не расходятся. |
| 2026-06-19 | Карточка марки двигателя: надёжный рефреш списка деталей | #494 (v2026.619.1703) | safety-net в `addPart` (оптимистичный показ подтверждённо-связанной детали) + `useLiveDataRefresh` на счётчик деталей в `EngineBrandsPage`. In-card код-путь согласован, баг статикой не воспроизводился → перепроверить на живом клиенте (PENDING). |
| 2026-06-19 | Поля ввода/подсказки: затирание текста + дёрганье меню (все автокомплиты) | #487 (v2026.619.1041) | Централизованный `SearchSelect`/`useSuggestionDropdown`: `input.select()` на reopen-после-autohide затирал ввод → выделять только при committed-label (SuggestInput — убран совсем); hover→`activeIdx`→scrollIntoView дёргал список → scroll только при клавиатуре (`setActiveByKeyboard`). 4 компонента. CDP-verify (реальные Input). |
| 2026-06-19 | Окно «Что нового» адаптивное | #489 (v2026.619.1041) | Кнопка над текстом (не уезжает за нижний край), карточка ≤`94dvh` + гибкий телесуфлёр (flex), компактнее + media `max-height:760px`, Enter закрывает. |
| 2026-06-19 | Табель Т-13: панель управления закреплена сверху | #484 (v2026.619.1041) | sticky `top:0` обёртка тулбара+кистей в `.ui-content-viewport`; CDP: top=169px при скролле 600px и на дне. |
| 2026-06-19 | Список двигателей: метка утиля картера читается `engine_inventory` | #485 (v2026.619.1041) | После checklist-unify defect-акты soft-deleted → флаг читал пустое и «залипал»; перенаправлен на `engine_inventory` (где пишет карточка). Прод-SQL: 0 в defect / 34 в inventory. CDP mark→pink→unmark→clear. |
| 2026-06-17 | **Модуль «Табель учёта рабочего времени» (Т-13)** — директива brain | #453/#454/#455 (в `main`, ждёт релиза) | DB-backed модуль (цех×месяц): грид сотрудники×дни, 19 кодов Т-13 (засев), палитра чипов + кисть + Excel-клавиатура + дефолт-смена, live-подсчёты; печать A4 модалкой-галочками (1-я/2-я половина/месяц/расшифровки) + комментарии «где был» в ячейке → расшифровка на печати. **Ф1+Ф2; Ф3 (праздники/зарплата) снята владельцем** (частное предприятие; зарплата вне программы). Миграции 0062/0063 (→ db:migrate). CDP-verified live. План: [`plans/_archive/timesheet-t13.md`](plans/_archive/timesheet-t13.md). |
| 2026-06-17 | owner voice-batch **E** фотогалерея #11 + **#7 вкус** | #450/#452 (в `main`, ждёт релиза) | Лайтбокс-галерея фото двигателя: сетка→крупный просмотр ←/→, множественный выбор, тулбар 6 кнопок (копировать/удалить/выбрать/отправить[флешка·папка·почта]/печать/собрать в PDF); renderer+main IPC, без миграций. CDP-verified. #7 — словесные «привязка→применимо» в Услугах-по-маркам. |
| 2026-06-17 | owner voice-batch B-#4 + C(#8/#5/#6) + D-#9 | v2026.617.30 (#443–#448) | Двигатель↔контрагент↔контракт↔ДС: контрагент-первым фильтрует контракты (#444); выравнивание полей таблицы деталей `table-layout:fixed`+colgroup (#443); маркеры привязки контракт/ДС в списке двигателей (#445); раздел «Двигатели» в карточке ДС + скрытие пустых разделов (#446); авто-брак по картеру-в-утиле, ручная галка «Забракован» убрана (#447). **Отгружено на прод v2026.617.30** (client-only, без миграций; smoke OK; каждая фича CDP-verified live). Backlog: остались E/G. |
| 2026-06-16 | owner voice-batch F/A + B-#3/#10 + pass-2 V5 | v2026.616.2017 (#434–#441) | Свежесть списков (diff-guard по денорм-полям), релевантность поиска двигателей + сохранение ввода при автоскрытии, поле номера заполняет ячейку (sticky-шапка деталей), ChangesPage гуманизирован (нет сырого JSON/action-key). **Отгружено на прод v2026.616.2017** (smoke OK; verify: #436/#438C — PASS live, #438D/#434E приняты низкорисковыми без dev-данных). Backlog #4/C/D/E/G открыты. |
| 2026-06-15 | per-page полировка списков (filler/thumbs) | v2026.615.108–1417 | Схлопывание превью-колонок, хвостовой filler от растекания. |
| 2026-06-14 | UI-эргономика проход-1 (brain) | v2026.614.1618 | 6 issues (#365/#371). |
| 2026-06-13 | поиск #035 Ф0–Ф2 (shared tiered-matcher) | 2026-06-13 | exact/prefix/compact/RU↔EN + floor; 11 страниц без пер-страничных правок. |
| 2026-06-07 | глобальный переключатель вида списков | #257 | Паттерн TwoColumnList/VirtualTable на все списки. |
| 2026-05-30 | виртуализация списков | v1.34.0 | 12 списков, sticky-шапка, load-all сортировка склада. |

## Инфра / CI / sync / ledger

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-09 | Тише клиент↔сервер — остаток закрыт (pulse-консолидация + все визуальные поллы visibility-gated) | #147 (v2026.710.244) | `pollWhenVisible` пересажен на общий 15с-pulse `liveDataService` (один таймер на всех вместо таймера на вызов; пол 5с против alt-tab-шторма; первый тик через полный intervalMs). На него мигрированы: 4 IPC-статус-полла App (`sectionsSelf`/`backups.status`/`update.status`/`sync.status`), `/health`-проба AuthPage (свёрнутый на логин-экране клиент больше не поллит всю ночь), ChatPanel (users/unread/messages 30с + 60с `sync.run`), плитки HistoryPage (заметки/чат 20с, contract-alerts 60с). Бонус: скрытый чат не помечает входящие прочитанными. Осознанно не гейтятся: chat-unread (звук), presence (M28), activityTracker. CDP-смоук 8/8 (census по ownership: ровно один 15000мс-таймер; probe: visible тикает / hidden 0 / возврат — немедленный тик). |
| 2026-07-08 | Тише клиент↔сервер (перф, follow-up presence-инцидента #110) | #113,#114 (v2026.708.1139) | `/presence/me` 20→60с, `/auth/me` 30→60с; утилита `pollWhenVisible` — 4 прод-полла спят при скрытом окне (`auth.sync`/`server.health`/`notes.burningCount`/contract-alerts) + refresh на возврате фокуса; chat-unread (звук нотификации) и presence-heartbeat сознательно не тронуты. Свёрнутый клиент тише к прод-серверу. Остаток (консолидация таймеров, пауза локальных IPC-поллов) — PENDING 🟢. |
| 2026-07-07 | Прод-инцидент CPU: presence убран из durable-ledger | #110 (`10022b3c`, backend-only hot-deploy, без миграций) | Оба backend ~50% CPU, клиентов зависало/выкидывало в релогин. Корень: `user_presence`-хартбиты (`/presence/me` раз в 60с × клиент + `touchPresence` в чате) писались в зашифрованный fanned-out ledger через `recordSyncChanges` (~⅔ всех записей) → голова ledger двигалась постоянно → все клиенты re-pull/re-decrypt перекрывающихся окон `/ledger/state/changes` на главном JS-потоке (O(N²)). presence и так живёт в таблице `userPresence` (онлайн-статус в chat/notes + self-ping), клиент синканный presence для UI не читает → ledger-фанаут был дублем. Фикс: presence только в таблицу; защитный фильтр в `writeSyncChanges` (инвариант) + тест. Прод-verify: presence-запись 9/мин→**0**, load 1.5→0.6. Грабля → GOTCHAS **M28**. Возможный follow-up: увеличить интервалы поллинга/хартбита (ещё тише). |
| 2026-07-05 | Ledger release-token ротирован + backfill единиц номенклатуры | #81, #82/#83 | Токен `MATRICA_LEDGER_RELEASE_TOKEN` ротирован на проде до 2026-08-04 (минт старым, бэкап `.env.bak-20260705`, проверен `/auth/me`). Скрипт `warehouse:backfill-units` (dry-run/--apply): 185 легаси-позиций без `unit_id` → «шт» через ledger-путь (синк клиентам), контроль no_unit 0/1356. |
| 2026-06-30 | Репо → public (fresh-snapshot) + архив; релиз v2026.630.1141 | v2026.630.1141 | GitHub Actions упал на биллинге («payments failed / spending limit») → всё CI + сборка `.exe` не стартовали. Решение: репо → **публичный** (бесплатные минуты). История содержала ledger-ключи (gitleaks 10 находок, не переписана с #060) → **не флип, а свежий старт**: старый репо → приватный `MatricaRMZ-archive` (история+PR+секреты), новый `Valstan/MatricaRMZ` (тот же URL) из 1 parentless-коммита текущего дерева (gitleaks 0), public. Секреты на новый: `YANDEX_DISK_TOKEN` (с прод-env), var `YANDEX_DISK_FOLDER`; branch protection; `ANTHROPIC_API_KEY` НЕ перенесён (PR-ревью off). CSC не нужен (билд unsigned). Бесплатные Actions собрали+опубликовали `.exe`; прод-деплой (reset на снимок — ledger/state целы; серверная пересборка; артефакты; ledger-publish; 2 рестарта). Память [[repo-public-split-archive]]. Follow-up: ротация ledger-ключей (SESSION_HANDOFF). |
| 2026-06-19 | `erp_nomenclature_code_uq` → partial (cold-rebuild safety) | #492 (v2026.619.1703) | Глобальный unique включал soft-deleted строки → dedupe-merge оставлял два claim'а одного кода, полный `replayLedgerToDb`/cold-rebuild падал (подтверждено probe: пара 3301-15-30 — единственный дубль кода на 281 строку ledger-состояния). Сделан partial `WHERE deleted_at IS NULL` (как соседние identity-uniques). Миграция `0066`. → GOTCHAS M12. |
| 2026-06-17 | LedgerStore: хардненинг Windows file-lock + temp-dir cleanup | #451 (в `main`) | Устранён Windows-флейк `ENOTEMPTY` в `afterEach rmSync` при полном прогоне (race с file-lock); ретраи/уникальные temp-dir. Не влияет на CI (Linux). |
| 2026-06-16 | реорг памяти проекта (open-vs-done split) | #422/#423 | `PENDING` 465→86 open-only, `COMPLETED.md`/`GOTCHAS.md`, 31 план в `_archive/`, скиллы/CLAUDE.md. |
| 2026-06-15 | CI-гейт `typecheck:test` | v2026.615.2312 (#417) | ambient `.d.ts` в vitest-tsconfig + CI-шаг от регресса. |
| 2026-06-13 | LedgerStore: атомарная запись + advisory-lock | (не релизился отдельно) | `writeFileAtomic` + cross-process lock; межпроцессная уникальность `seq` (`signAndAppend`). |
| 2026-06-13 | гейт дублей двигателей + чистка прода | v1.50.0 | 25 групп → 0; write-time check + почасовой auto-merge + in-card подсказка. |
| 2026-06-07 | repo-wide ESLint + CI lint-гейт | #231/#232/#233 | 0 errors/warnings, `--max-warnings 0`, dead-код вычищен. |
| 2026-06-07 | backend systemd-юниты в репо | — | `deploy/systemd/` verbatim-снимок прод-конфигурации. |
| 2026-06-07 | ledger `data-key.json`: untrack + ротация ключа | #252 | untracked+gitignored, новый активный ключ, `state.json` перешифрован (G29). |
| 2026-05-30 | вынос прод-секретов | brain #008 | `/etc/matricarmz/matricarmz.env`, `.env`-симлинк. |
| 2026-05-21 | nginx config в репо + catch-all | — | `deploy/nginx/` + `install.sh`; новые эндпойнты без правки nginx. |

## Прочее

| ✅ | Нитка | Релиз / PR | Итог |
|---|---|---|---|
| 2026-07-02 | Backlog-sweep сессия + релиз **v2026.702.1024** (на проде) | #22–#29 (v2026.702.1024) | 10 пунктов бэклога одной сессией: ledger release-token ротирован на проде (exp ~2026-08-01, рецепт самопродления в PENDING ⏳); «Дата выполнения» вплотную к кнопке закрытия наряда (#22, CDP-verified); FK `user_permissions→entities` (миграция 0071, #23); дроп legacy-`users` + report-пресет с `password_hash` убран + FK `ai_chat_history→entities` (миграция 0072, #24; бэкап 5 строк `~/users-table-backup-20260702.sql`); 2 warning'а прогноза — BOM без brand-link и дробный qty (#25, +2 юнит-теста); semgrep → блокирующий diff-aware гейт на PR (#26); прополка PROJECT_STATE 105→50 строк (#27); справочник «Должности» очищен (тест+дубль soft-deleted штатным API, #28). Релиз #29: миграции применены (ownership-грабля `ai_chat_history` → GOTCHAS M24), 3 артефакта + ledger-publish + рестарт, `/health`+`/updates/status` = 2026.702.1024, blockmap 200. |
| 2026-06-22 | Массовое добавление простых работников в Цех №2 (прод-данные) | #542/#543 | Скрипт `workshop:add-employees` (dry-run/`--apply`, дедуп фамилия+инициалы с префикс-матчем для неполных ФИО, неоднозначное→ручной разбор). На проде из списка 27: создано **13** новых (без логина/доступа к программе), **4** переведены в Цех №2 (Кудряшов Д.Х/Мурашин Александр/Поткин А.Н/Поткин Г.С.), 10 уже были — дублей нет, однофамильцы не задеты. Через `createEmployeeEntity`+`setEntityAttribute` (ledger/sync, актор-суперадмин). Read-back подтверждён. |
| 2026-06-22 | Конституция проекта (слой принципов) + стойка советника — ратифицирована владельцем | #475 (+#537) | `docs/CONSTITUTION.md`: 12 статей (3 продукт / 5 инженерия / 4 знание-и-работа) из реальной практики + директив brain, ≤1 экран, ценность+«почему». Ст. 12 «Claude — активный советник с предохранителями» (предложить раз с трейд-оффом → отступить; прогноз последствий ДО; идеи в бэклог не в прерывание; решение за владельцем; анти-спам). Ссылка из CLAUDE.md (читается первой). Владелец просмотрел суть всех статей → ратифицировал как есть. Хвост (PENDING 🟡): прополка раздутых доков по трём корзинам. Директива brain `2026-06-18-project-constitution-and-advisor-stance`. |
| 2026-06-22 | SSOT цехов — слияние дубль-департаментов «Цех № N» + бэкафилл `workshop_id` (завершает #513/#517 на данных; brain directive) | #532 + прод-миграция | Прод-аудит выявил «двойную бухгалтерию»: сотрудники привязаны к цеху через department-сущности «Цех № N», дублирующие канонич. `directory_workshops`; живая ось — `department_id`, `workshop_id` (из #513) не наполнен, attr_def на проде отсутствовал. Скрипт `workshop:merge-dup-departments` (expand-contract, через `recordSyncChanges`): создал `employee.workshop_id` def → бэкафилл **116 сотр.** на канонич. цеха (№1=66/№2=11/№3=1/№4=38) → `detachIncomingLinksAndSoftDeleteEntity` обнулил их `department_id` + soft-delete **4 дублей**. Дубль-Табель Цех№4 (Jun-21 черновик) soft-deleted, рабочий Jun-18 (425 ячеек) оставлен. Verify: 0 активных «Цех»-департаментов, 0 orphan-FK, 8 офисных нетронуты, backend active/health-200 (конкурентная запись в ledger безопасна — disk-read индекс + кросс-процессный лок, стоп не нужен). Кнопка «весь цех» ожила. План — [`plans/_archive/ssot-workshops.md`](plans/_archive/ssot-workshops.md). |
| 2026-06-21 | Табель: привязка сотрудник→цех + «Добавить весь цех» + массовое назначение; скрытие уволенных | #513/#517 (v2026.621.1133) | Новый EAV-атрибут `workshop_id` у сотрудника (цех = `directory_workshops`, не EAV-entity → text-атрибут с UUID); поле «Цех» в карточке сотрудника + bulk-назначение на странице «Сотрудники» (выделить многих → проставить разом); кнопка «Добавить весь цех» в пикере табеля (тянет работающих этого цеха). #513 — фильтр уволенных в пикере (`resolveEmploymentStatusCode==='working'`). CDP-smoke: поле «Цех» рендерится. A1 из директивы T-13 pass-2. |
| 2026-06-19 | Табель Т-13: права автора + «разрешить редактирование другим» | #488 (v2026.619.1041) | `created_by` + `allow_others_edit` (миграция **0065**); автор правит всегда, другие — по галке автора (дефолт off, меняет только автор); легаси (null author) открыты. Сервис-гейт на всех мутациях, актор=`req.user.username`. unit 6/6 + CDP author-path. |
| 2026-06-19 | Машинные профили для `/start` | #486 (v2026.619.1041) | `docs/machines/<hostname>.md` — порты dev-стенда/пути/скиллы на конкретный комп; `/start` §0.5 читает свой по hostname, пишется по мере изучения. PC40 заполнен. To-brain idea отправлена. |
| 2026-06-12 | zavod-meta-memory + `/zavod` (brain) | — | `docs/zavod/` (FACTORY_MODEL/INDEX/PROGRAM_EFFECTS) + команда-консультант. |
| 2026-06-09 | профиль рабочего места + новый экран логина + меню аккаунта | #343/#345/#347 | server workspace profile, dual-panel login, account switcher. |
| 2026-06-07 | универсальная система шаблонов нарядов | — | `work_order_templates` + `HIDABLE_FIELD_CATALOG`; Workshop-наряд = repair+шаблон (спец-тип откачен). |
| 2026-06-07 | payroll-подписи: авто-ФИО | #259 | ФИО бригады в блок подписей payroll-отчёта. |
| 2026-06-07 | ColumnSettings на все списки | #260/#261 | показать/скрыть/переупорядочить колонки + reset, persist. |
| 2026-06-08 | мультивыбор марок для номенклатуры | #281 | Поле «Применимые марки» (junction) + сохранена «Марка по умолчанию». |
| 2026-06-05 | правило «логин + ФИО» клиента | v1.42.0 (#203) | `formatClientLabel`/`formatClientShort` везде, где показывается клиент. |
| 2026-05-21 | Ollama → Claude миграция | v1.16.0 | (AI-фичи затем выключены geo-block'ом — см. PENDING). |
