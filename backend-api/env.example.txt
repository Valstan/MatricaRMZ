PORT=3001
TZ=Europe/Moscow
PGHOST=localhost
PGPORT=5432
PGDATABASE=matricarmz
PGUSER=valstan
PGPASSWORD=CHANGE_ME

# JWT секрет (минимум 32+ символа, случайная строка).
MATRICA_JWT_SECRET=CHANGE_ME_SUPER_SECRET

# Refresh token TTL (в днях)
MATRICA_REFRESH_TTL_DAYS=30

# CORS allow-list (CSV, без пробелов). Пусто/'*' — разрешать любой Origin (legacy).
# Electron-клиент не шлёт Origin, поэтому ему всё равно. Ужесточи перед публичным выкатом
# web-admin: 'https://web-admin.example.com,https://admin.example.com'
MATRICA_CORS_ORIGINS=

# Количество reverse-proxy hop'ов перед backend (nginx считается за 1). НЕ выставляй >1,
# если перед backend только nginx — иначе атакующий сможет подделать X-Forwarded-For.
MATRICA_TRUST_PROXY_HOPS=1

# Rate-limit (express-rate-limit):
# глобальный — N запросов в минуту на IP, авторизация — N попыток в 15 минут на IP.
MATRICA_RATE_LIMIT_GLOBAL=600
MATRICA_RATE_LIMIT_AUTH=30

# Yandex.Disk (files + backups)
YANDEX_DISK_TOKEN=CHANGE_ME
YANDEX_DISK_BASE_PATH=/MatricaRMZ/releases

# Torrent updates
MATRICA_UPDATES_DIR=/opt/matricarmz/updates
MATRICA_PUBLIC_BASE_URL=https://matricarmz.example.com
MATRICA_TORRENT_TRACKER_PORT=6969
MATRICA_TORRENT_TRACKER_URLS=https://matricarmz.example.com/announce
# Фиксированный порт для peer/DHT WebTorrent (TCP+UDP). Без него WebTorrent выбирает случайный
# порт при каждом старте, что мешает писать стабильные правила firewall.
MATRICA_TORRENT_PEER_PORT=51413

# Глобальный рубильник AI. Когда false — ни нейрочат, ни аналитика, ни cron-агент
# логов НЕ обращаются к Anthropic API, /health возвращает features.aiEnabled=false,
# UI скрывает кнопку нейрочата. Использовать, если запросы к Claude блокируются
# провайдером (например, регион не поддерживается).
AI_ENABLED=true

# Claude API (Anthropic). Получить ключ: https://console.anthropic.com/settings/keys
# Префикс MATRICA_* — основной; ANTHROPIC_API_KEY поддержан как fallback для совместимости со стандартом SDK.
MATRICA_AI_CLAUDE_API_KEY=CHANGE_ME
# Профиль AI (fast|balanced|quality). Влияет на выбор моделей и таймауты.
# fast: Haiku везде. balanced: Haiku для чата + Sonnet для аналитики (default).
# quality: Sonnet для чата + Opus для аналитики.
AI_PROFILE=balanced
# Опциональные оверрайды конкретных моделей (точные ID Anthropic).
CLAUDE_MODEL_CHAT=
CLAUDE_MODEL_ANALYTICS=
# Таймауты по режимам.
CLAUDE_TIMEOUT_MS=60000
CLAUDE_TIMEOUT_CHAT_MS=30000
CLAUDE_TIMEOUT_ANALYTICS_MS=60000

# Read-only PG user для сбора корпуса чатов (используется в aiAgentChatLearningService)
AI_DB_RO_USER=ai_ro
AI_DB_RO_PASSWORD=CHANGE_ME

# AI Agent режимы и отчеты
# AI_AGENT_MODE=analytics|chat
AI_AGENT_MODE=analytics
AI_REPORT_ENABLED=true
AI_REPORT_TIMES=12:00,17:00
AI_REPORT_TZ=Europe/Moscow

# AI Agent chat learning
AI_CHAT_LEARNING_ENABLED=true
AI_CHAT_LEARNING_WINDOW_HOURS=24
AI_CHAT_LEARNING_LIMIT=1000
AI_CHAT_LEARNING_INTERVAL_MS=600000
AI_RAG_ENABLED=true
AI_RAG_TOP_K=3
AI_RAG_LOOKBACK_HOURS=336
AI_RAG_SEARCH_LIMIT=250
AI_CHAT_FAST_PATH_ENABLED=true
AI_CHAT_MAX_TOKENS=768
AI_ANALYTICS_MAX_TOKENS=1536
# Read-only tool use (Claude function calling) — БД-tools для нейрочата и аналитики.
AI_CHAT_TOOLS_ENABLED=true
AI_ANALYTICS_TOOLS_ENABLED=true
# Сколько подряд ответов "не знаю" в чате до автоэскалации Haiku → Sonnet
AI_CHAT_ESCALATE_AFTER=3

# История нейрочата: автоудаление сообщений старше N дней.
AI_CHAT_HISTORY_RETENTION_DAYS=90
AI_CHAT_HISTORY_CLEANUP_ENABLED=true
# Интервал между прогонами cleanup-крона (default 24h в миллисекундах).
AI_CHAT_HISTORY_CLEANUP_INTERVAL_MS=86400000

# Cron-агент анализа логов (Sonnet 4.6). По умолчанию выключен.
# Когда включён — собирает criticalEvents + ai_agent_metrics + последние ERROR/WARN
# из server-*.log и формирует структурированный отчёт. При severity warn/critical
# пишет запись в раздел «Критические события приложения» с eventCode
# server.ai_log_analysis.report.
AI_LOG_ANALYSIS_ENABLED=false
AI_LOG_ANALYSIS_TIMES=06:00,18:00
AI_LOG_ANALYSIS_TZ=Europe/Moscow
AI_LOG_ANALYSIS_LOOKBACK_HOURS=12

# Telegram (api.telegram.org: getUpdates, sendMessage). Заблокирован в РФ — false, иначе лишние ошибки опроса.
MATRICA_TELEGRAM_ENABLED=false

