-- «У детали бывает свой номер» — флаг строки списка деталей (владелец 22.09.2026).
--
-- Бланк дефектовки печатается чистым и заполняется от руки. У части деталей есть СВОЙ номер;
-- такой детали в бланке нужно ещё одно поле — под номер. У остальных печатается просто строка,
-- и за счёт этого весь список дефектовки умещается на один лист (можно в два ряда).
--
-- Пара колонок повторяет устройство флагов актов (0090): `has_own_number` — эффективное
-- значение строки (наследуется из комплекта марки, PartSpecBrandLink.hasOwnNumber, и
-- пересчитывается при brand-resync), `has_own_number_override` — операторская правка
-- конкретного листа, которая побеждает значение марки.
--
-- Обе nullable, бэкфилл не нужен: NULL = строка заведена до флага (поле не рисуется).
-- Колонка в строгой таблице обязательна, а не meta_json: `inventoryRowFromLine` собирает
-- raw-строку листа по белому списку полей, поэтому ключ без колонки терялся бы при чтении
-- из реплики.

ALTER TABLE erp_engine_inventory_lines ADD COLUMN IF NOT EXISTS has_own_number boolean;
--> statement-breakpoint
ALTER TABLE erp_engine_inventory_lines ADD COLUMN IF NOT EXISTS has_own_number_override boolean;
