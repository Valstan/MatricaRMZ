-- D-042 (решение владельца 2026-08-25): контакт техподдержки, который оператор видит
-- в окне приветствия релиза, переезжает из литерала в коде в настройки экземпляра.
-- Показывать сотруднику и публиковать в открытом git — два разных вопроса, и первый
-- не требует второго; репозиторий публичен с 2026-08-17.
--
-- Новой сущности и новых EAV-атрибутов не заводим (EAV-freeze, план v4): значение
-- инстанс-широкое и живёт там же, где остальные такие — в строке
-- `__global_ui_defaults__` таблицы `client_settings`. Она уже доезжает до клиента
-- неаутентифицированным `GET /client/settings`, то есть до логина и мимо
-- sync-контракта, а именно так окно приветствия и открывается.
--
-- Колонки, а не ключи в `ui_global_settings_json`: тот JSON пересобирается
-- `sanitizeUiControlSettings` из дефолтов, и незнакомый ключ там молча срезался бы.
ALTER TABLE "client_settings"
  ADD COLUMN IF NOT EXISTS "support_phone" text,
  ADD COLUMN IF NOT EXISTS "support_person" text;
