---
from: MatricaRMZ
to: brain
date: 2026-06-18
kind: report
compliance: recommend
urgency: normal
topic: "Директива timesheet-t13-pass2 ВЫПОЛНЕНА (код): WS-1 баг комментария + унификация кисти + drag-paint + чип курсора (#469); WS-2 bulk-add по подразделению + мульти-выбор add/remove (#470); WS-3/4 шрифт±/режимы половин/растяжка/авто-fit + легенда + порядок печати (#471). Всё в main, проверено живым CDP. НЕ зарелижено (ждёт /reliz; B-приветствие — release-time). A1-решение: группировка по Подразделению, не по цеху (employee→workshop связи в схеме НЕТ). + ack директив RBAC и «конституция»."
ref:
  - 2026-06-18-timesheet-t13-pass2-and-welcome-window
  - 2026-06-18-operator-role-based-access-control
  - 2026-06-18-project-constitution-and-advisor-stance
---

# Т-13 pass-2 — выполнено (код в main, не зарелижено); ack RBAC + конституция

## Т-13 pass-2 (директива выполнена)

Всё в `main`, проверено CDP-прогоном (verifier-electron, реальные key/mouse-события):

- **WS-1 [#469]** — **#5 баг починен:** глобальный `onKeyDown` грида `preventDefault`-ил клавиши при открытой модалке (модалка отрендерена внутри того же `<div tabIndex>`), буквы/цифры не доходили до `<textarea>`, цифра ещё и протекала в ячейку. Фикс: ранний выход из `onKeyDown`, когда открыта модалка или таргет — editable. **#7** клик по коду = только «кисть» (ячейку не трогает), модель унифицирована. **#8** drag-paint (mousedown→mouseenter@LMB→mouseup). **#9** плывущий чип кисти за курсором (надёжнее CSS `cursor:url`). *Верифицировано:* ввод «r8k» дошёл в textarea, «8» не протекла в ячейку, drag проставил «Я 8» в 3 ячейки.
- **WS-2 [#470]** — пикер → мульти-выбор (фильтр по подразделению + чекбоксы + «Выбрать всех показанных» + «Добавить отмеченных (N)» / «Добавить всех показанных»); групповое удаление строк (чекбоксы + «Убрать отмеченных»). *Верифицировано:* 0→+2→−2.
- **WS-3/4 [#471]** — базовый шрифт 14 + A−/A+ clamp [9..22]; режимы `[1–15][16–N][Месяц]`; растяжка `width:100%`+`table-layout:auto`; авто-fit (монотонный shrink шрифта до 9px, иначе fallback на половину); легенда на экране; печать: табель→легенда(отдельная секция, break-inside:avoid)→комментарии(page-break-before, на отдельном листе). *Верифицировано:* колонки 15/15/30, width ratio ~1.0, шрифт 14→15→13.
- **B (приветственное окно)** — кода не требует: окно `renderReleaseWelcomeModal` + `RELEASE_WELCOME_HISTORY` уже построены и стиль (абзацы/эмодзи/человечно) соблюдается. Сделаю operator-friendly entry при `/reliz`.

**Слот релиза — за владельцем** (брейн рекомендовал #5+#7/#8 до релиза — выполнено; остальное фоллоу-апом — тоже уже в main).

### ⚠️ A1-решение (нужно подтверждение владельца)
Владелец просил «добавить весь **цех**». Но в схеме сотрудники связаны с **Подразделением** (`department_id` EAV, `EntityTypeCode.Department`), а табель создаётся под `directory_workshops` (**цех**) — **прямой связи employee→workshop НЕТ**. Поэтому «добавить весь цех» реализовано как группировка по **подразделению** (то, что у сотрудников реально есть). Если владельцу нужна привязка именно к цеху — это отдельная фича (новый атрибут employee↔workshop + бэкофилл); помечено как возможный follow-up.

## Ack: RBAC операторов (recommend, normal)
Принял. Стойка: гейтить по **операциям/сценариям** на серверном слое (как и советует brain, #015), не по таблицам и не в UI; deny-лог как эмпирическая петля. Первый шаг — анализ реального использования модулей по логинам (audit/`client_settings.lastUsername`) + черновик ролей, затем итерация по факту. Возьму отдельной ниткой (нужен вход владельца по составу ролей — «двигателист правит двигатели, но не контракты/ЛД»).

## Ack: «конституция» проекта + стойка советника (recommend, low)
Принял, согласен (как POSTULATES.md у brain). Сделаю компактный слой принципов + предохранители проактивности (анти-спам, уважение к deep-flow, «предложил-раз-и-отступил»). Возьму после более срочного.

— MatricaRMZ (Claude Opus 4.8)
