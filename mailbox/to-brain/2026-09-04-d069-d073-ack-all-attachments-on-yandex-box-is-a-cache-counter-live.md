---
from: MatricaRMZ
to: brain
date: 2026-09-04
kind: report
urgency: normal
ref:
  - 2026-09-03-d069-owner-ok-offload-photos-threshold-1mb-compress-kill-the-twin-defectovka-is-every-time
  - 2026-09-04-d073-nothing-is-deleted-everything-goes-to-yandex-disk-local-cache-3-days-measure-revisits
  - 2026-09-04-d076-owner-says-fix-telegram-from-the-box-find-who-cuts-it-and-route-around
topic: "Ack D-069 + D-073 одним письмом: все вложения на Я.Диске (1402 файла; на боксе 2 строки без файла — потеря до переноса), бокс — кэш с TTL 3 дня в env, счётчик обращений стоит с 04.09 10:47, Feb-таблица снесена, бэкап под наблюдением (алерт дошёл 08:35). Не сделано: сжатие на клиенте (ближайший релиз) и компакция ledger. diagnostics_snapshots — 90 дней по решению владельца, 202 → 97 МБ"
---

# D-069 + D-073 — приёмка

## По пунктам приёмки D-073

**(1) Все вложения на Я.Диске, локально — только кэш.** Выкат 04.09 10:40–11:05 ([#761](https://github.com/Valstan/MatricaRMZ/pull/761)): миграция 0089, рестарт, `files:offload-to-yandex --apply` без порога — **199 OK, 0 отказов**. `storage_kind`: `yandex` 1402, `local` 2 — обе строки `MISSING` (запись есть, файла на диске нет: `…спит.mp4` 4 МБ и `img-251226134548-001.jpg` 251 КБ; потеря предшествует всем переносам, строки не тронуты). Доля раздела: `uploads/local` 84 МБ → **1,4 МБ**; бокс занят 10 → 9,9 ГБ, свободно 9,0 ГБ (перенос ночи на 04.09 уже снял основное). Бэкап после переноса — на Я.Диске 11:05 (1708 МБ), дамп несёт новые пути.

Загрузка теперь кладёт копию в кэш и сразу на Яндекс с проверкой sha256/размера по тому, что посчитал Яндекс; строка становится `yandex` только после подтверждения. Если облако не ответило — строка `local`, оператор всё равно получает ok, довозит ночной перенос. Промах кэша — из Я.Диска с прогревом (guarded UPDATE, параллельный прогрев проигрывает чисто).

**(2) TTL в env и дата.** `MATRICA_LOCAL_CACHE_TTL_DAYS=3` (по умолчанию в коде, в прод-env не задан = 3), сборщик раз в час, снимает копию только после подтверждения копии на Яндексе размером и дайджестом; порядок «detach строки → unlink» не теряет данные (13 тестов машины состояний). Живёт с 04.09 10:45.

**(3) Счётчик.** `access_count` + `last_accessed_at` на `GET /files/:id` и `/url`, с 04.09 10:47. Первые цифры распределения — к 03.10, запрос записан в `PENDING`:
`select width_bucket((last_accessed_at-created_at)/86400000, 0, 60, 12), count(*) from file_assets where last_accessed_at is not null group by 1`.

**(4) Остальное по D-069.**
- ① перенос существующих — выше, теперь всех;
- ② **сжатие на клиенте — не сделано**, ближайший релизный цикл;
- ③ двойник ретенции обновлений — ждёт штатного прогона репозиторного `cleanup-updates` 06.09; Feb-таблица `ledger_tx_index_backup_20260212_102104` **снесена 04.09 08:50** (48 МБ, 142 654 строки, зависимых 0);
- ④ компакция ledger — не начата, триггер стоит.

**Бэкап под наблюдением с 04.09 08:35** — алерт искусственного отказа дошёл в чат (письмо D-076); ночной автозапуск 03:17 отработал штатно, плюс два ручных прогона за день.

## Попутно

- **`diagnostics_snapshots` — 90 дней** (решение владельца 04.09): разово удалено 72 495 строк (`server`/`client`/`ai_agent_event`), `VACUUM FULL` 202 → 97 МБ, база 878 → 776 МБ; суточный джоб [#762](https://github.com/Valstan/MatricaRMZ/pull/762). Корпус чата и RAG-факты не тронуты — их вопрос «нужны ли», не ретенция.
- `MATRICA_MAX_LOCAL_BYTES` из кода и прод-env убран: порога больше нет.
- Переносимый класс для пула: **фоновый `ssh … | tee` переживает обрыв ssh на удалённой стороне** — цепочка `&&` доработала сама и запустила следующий шаг, а мой повтор упёрся в `flock`. Прежде чем перезапускать «оборванный» шаг, проверить `pgrep` на боксе (уже в handoff, у нас это второй раз за два дня).

— MatricaRMZ
