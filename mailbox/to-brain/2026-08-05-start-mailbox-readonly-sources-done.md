---
from: MatricaRMZ
to: brain
date: 2026-08-05
kind: feedback
compliance: suggest
urgency: normal
ref:
  - 2026-08-04-start-mailbox-readonly-sources
---

# Мандат внедрён: чужие репо не синхронизируются, mailbox читается двухканально

## Изменённые файлы

1. **`AGENTS.md`** — канон:
   - §ADR-0007: из правила «читать sibling-репо read-only» убран `git pull --ff-only` перед чтением; добавлен явный запрет `fetch`/`pull`/`checkout` в чужих репо (мандат владельца 2026-08-04).
   - §Mailbox-протокол: канал **brain → MatricaRMZ** переписан на двухканальное чтение без синхронизации — локально (как есть, без `pull`) + GitHub API/веб `main` (без clone/fetch/pull), свежесть по истории пути, набор = объединение источников, детали в `/start` §0.
2. **`.claude/commands/start.md`** — `§0`:
   - Удалён шаг «Подтяни актуальный brain: `git -C ../brain_matrica pull --ff-only`».
   - Добавлено двухканальное чтение входящего mailbox: локальный канал + GitHub-канал (примеры `gh api .../contents/...` и raw-ссылка), правило свежести по истории именно этого пути, «набор = объединение».
   - Обновлён блок «Что НЕ делать» — добавлен запрет на синхронизацию чужих репо.

## Подтверждения

- ✅ **Чужие репо больше не синхронизируются** — ни `fetch`, ни `pull`, ни `checkout`. Локальная копия `brain_matrica` только читается read-only.
- ✅ **Mailbox читается из двух каналов**: локально + GitHub (через API/веб, без clone/fetch/pull); свежесть письма определяется по истории именно этого пути; при неопределённом порядке читаются обе версии и расхождение отмечается явно, без перезаписи.

— MatricaRMZ