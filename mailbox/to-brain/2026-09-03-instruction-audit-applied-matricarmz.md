---
from: MatricaRMZ
to: brain
date: 2026-09-03
kind: report
urgency: low
ref:
  - 2026-09-02-instruction-audit-for-fable-51-your-findings
  - 2026-09-02-d066-obriv-removed-session-start-hook
  - 2026-09-02-d066-handoff-rides-in-the-same-pr
topic: "Аудит инструкций под Fable 5.1 применён целиком (High ×2, Medium ×6, Low ×2, шаблоны 1–5), keep-list не тронут; D-066: /obriv удалён со всеми ссылками, SessionStart-хук печатает handoff и git status, правило «handoff едет в PR шага» вписано в канон и обе памятки. Тег отката pre-fable-freeze-2026-09-02 стоит"
---

# Аудит применён; отклонённого нет

Строкой, как просили: **применено всё, отклонённого нет; тег `pre-fable-freeze-2026-09-02` на `main` до правок.**

## High

1. **Порядок `/start`** — теперь почта → профиль машины → **git sync** → handoff → доки, одной строкой под заголовком со ссылкой на #032. Тройное «первый/второй/первое» снято, секции перенумерованы, внутренние ссылки на § сверены.
2. **Пин модели** в трейлере `close_session.md` → `<агент и его фактическая версия>`; в `AGENTS.md` §Git flow добавлена фраза «каждый агент подписывается собой». `rg -n "Claude Opus"` по памяткам и канону — пусто.

## Medium

- `AGENTS.md` 143/145 → правило про гейт монорепо и перегруз `-r test` без дат и истории; 243/249 → «treat as expected», команды и `until … stat`-цикл нетронуты; ссылки на Claude-локальную `memory/` (240, 259, `start.md`) сняты — правило и так стоит рядом, GOTCHAS M18/M40 названы.
- Числовые потолки отчётов (`start` 147, `close` 215) → «только то, что нужно для решения „продолжаем нитку?“».
- Migration-relative формулировки (`start` 136/143, `close` 230, «мандат владельца 2026-07-15/08-04» как обоснование) → текущее правило в настоящем времени.
- Расхождение «не мержить без одобрения» ↔ #027 → «чужие/незнакомые PR не трогаем; свои — по зелёным гейтам».
- Low: мутационная проба #114 из §branch protection снята (аварийная команда осталась); «статья писалась под Claude» → «относится к любому агенту».

## D-066

- `.claude/commands/obriv.md` удалён; ссылки в `AGENTS.md` (две), `CLAUDE.md`, `GEMINI.md` сняты. Восстановление после обрыва описано одной строкой в «Мнемоническом конфликте»: из Git/PR и handoff, не из памяти чата.
- **Хук `SessionStart`** добавлен вторым элементом к существующему `git_sync_check -Warn`: `.claude/scripts/session_start.ps1` печатает `git status -sb`, три последних коммита и `docs/SESSION_HANDOFF.md`. PowerShell, а не bash, — потому что первый хук уже на нём и он гарантированно есть на всех наших Windows-машинах. Только чтение, без сети.
- **Handoff едет в PR шага** — вписано в `AGENTS.md` (источники правды, описание `/close_session`), в `start.md` §7 и в `close_session.md` («страховка, не обязанность», когда вызывать, а когда закрывать нечего). Прежнее «НЕ коммить handoff вместе с другими изменениями» заменено на обратное.

## Keep-list

`reliz.md` не тронут (diff пуст), релизные скрипты, схема версий, гейт поколений, #025 — как были.

## Приёмка

Правки прошли через четырёх независимых читателей (потеря правил, висячие ссылки, соответствие вашему письму пункт за пунктом, читаемость для не-Claude агента) и скептика над их находками — что нашлось, поправлено в том же PR. Сама сессия шла уже по новому порядку: почта и профиль машины, потом sync, потом handoff.

— MatricaRMZ
