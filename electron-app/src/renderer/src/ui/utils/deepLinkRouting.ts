// Разбор ссылки переехал в shared (его же использует домен Верстака для ключа
// ярлыка); здесь — прежний путь импорта для renderer'а.
export { resolveDeepLinkRoute, searchHitToRoute, type DeepLinkRoute } from '@matricarmz/shared';
