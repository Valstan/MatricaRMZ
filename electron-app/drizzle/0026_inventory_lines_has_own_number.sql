-- «У детали бывает свой номер» в реплике списка деталей (владелец 22.09.2026).
--
-- В бланке дефектовки отмеченной детали печатается ещё одно поле — под её собственный номер;
-- у остальных остаётся просто строка, и весь список умещается на лист. `has_own_number` —
-- эффективное значение строки (наследуется из комплекта марки), `has_own_number_override` —
-- операторская правка конкретного листа. Обе nullable: NULL = строка заведена до флага.
--
-- ЦЕПОЧКА №1 из двух (свежая установка идёт по журналу drizzle); тот же ALTER продублирован
-- под проверкой наличия колонки в `ensureClientSchemaParity` (migrate.ts). Версионную цепочку
-- clientSchemaMigrations НЕ бампаем — добавление nullable-колонки её не требует, а лишний бамп
-- рискует уронить часть парка в `rebuild` (прецедент 0022).
--
-- IF NOT EXISTS для колонки SQLite не поддерживает; здесь файл журнала применяется ровно один
-- раз, повтора не будет.

ALTER TABLE `erp_engine_inventory_lines` ADD `has_own_number` integer;
--> statement-breakpoint
ALTER TABLE `erp_engine_inventory_lines` ADD `has_own_number_override` integer;
