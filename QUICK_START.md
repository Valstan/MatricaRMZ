# Быстрый старт MatricaRMZ

## ✅ Что уже готово

1. ✅ База данных PostgreSQL создана и миграции применены
2. ✅ Backend API готов к запуску
3. ✅ Electron приложение готово к сборке

---

## 🚀 Запуск Backend на VPS

```bash
cd /home/valstan/MatricaRMZ/backend-api
pnpm run dev
```

Backend слушает локально (по умолчанию): `http://127.0.0.1:3001`
А **снаружи** доступен через nginx/панель провайдера (80/443): `http://a6fd55b8e0ae.vps.myjino.ru`

**Проверка:**
```bash
curl http://localhost:3001/health
# Должен вернуть: {"ok":true}
```

---

## 💻 Сборка и запуск Electron клиента

### Вариант 1: Запуск в режиме разработки (на VPS, через X11 forwarding)

```bash
cd /home/valstan/MatricaRMZ/electron-app
pnpm run dev
```

**Требования:** SSH с X11 forwarding (`ssh -X user@server`)

### Вариант 2: Сборка для Windows (на вашем компьютере)

1. **Установите на Windows:**
   - Node.js v22+ (https://nodejs.org/)
   - pnpm: `npm install -g pnpm`
   - Git (https://git-scm.com/)

2. **Скачайте проект:**
   ```bash
   # Через Git (если есть репозиторий)
   git clone <ваш_репозиторий>
   
   # Или через SCP/SFTP скачайте папку /home/valstan/MatricaRMZ
   ```

3. **Установите зависимости:**
   ```bash
   cd MatricaRMZ
   pnpm install
   ```

4. **Настройте URL backend:**
   
   Откройте файл `electron-app/src/main/index.ts` и измените строку ~50:
   ```typescript
   const apiBaseUrl = process.env.MATRICA_API_URL ?? 'http://a6fd55b8e0ae.vps.myjino.ru';
   ```

5. **Соберите приложение:**
   ```bash
   cd electron-app
   pnpm run build
   ```

6. **Создайте установщик:**
   ```bash
   pnpm run dist
   ```
   
   Установщик будет в папке `electron-app/release/`

7. **Запустите приложение:**
   ```bash
   # В режиме разработки
   pnpm run dev
   
   # Или запустите собранный файл из dist/
   ```

---

## 🧪 Тестирование синхронизации

### 1. Запустите backend на VPS:
```bash
cd /home/valstan/MatricaRMZ/backend-api
pnpm run dev
```

### 2. Запустите Electron приложение:
```bash
cd /home/valstan/MatricaRMZ/electron-app
pnpm run dev
```

### 3. Проверьте работу:

1. В Electron приложении:
   - Откройте вкладку **"Двигатели"**
   - Нажмите **"Добавить двигатель"**
   - Введите номер двигателя (например: "ENG-001")
   - Введите марку (например: "ЯМЗ-238")
   - Нажмите **"Сохранить"**

2. Проверьте синхронизацию:
   - Перейдите на вкладку **"Синхронизация"**
   - Нажмите **"Синхронизировать сейчас"**
   - Должно появиться сообщение об успешной синхронизации

3. Проверьте в базе данных:
   ```bash
   sudo -u postgres psql -d matricarmz -c "SELECT * FROM entities LIMIT 5;"
   ```

---

## 📝 Важные замечания

1. **Backend должен быть запущен** перед использованием Electron приложения
2. **URL backend** должен быть доступен с вашего компьютера
   - рекомендуемый вариант: через nginx/панель (80/443), без порта `:3001`
   - `:3001` — только для локальной диагностики на сервере

---

## 🔧 Настройка firewall (если нужно)

Если backend не доступен с вашего компьютера (через nginx):

```bash
# Разрешить доступ к порту 80/443 (если у вас включен firewall)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

---

## 🔒 HTTPS: важное про ошибку Windows (schannel / отзыв сертификата)

Если на Windows команда вида:

`curl https://<domain>/...`

падает с ошибкой `0x80092013` (не удалось проверить функцию отзыва), это значит, что **Windows не может проверить CRL/OCSP** сертификата на стороне провайдера/панели.

Что делать:
- **Быстро (для синхронизации)**: используйте `http://<domain>` как `apiBaseUrl` (порт 80).
- **Правильно (рекомендуется)**: включить на VPS свой HTTPS (nginx + Let's Encrypt) и в панели поставить проброс **443 → 443** (а не 443 → 80).
- **Для диагностики в curl на Windows** можно временно отключить проверку отзыва: `curl.exe --ssl-no-revoke https://<domain>/health` (не решение для прод).

---

## ❓ Что делать дальше?

После успешного тестирования:
1. Настроить автозапуск backend через PM2 или systemd
2. Собрать финальный установщик Electron для Windows
3. Настроить автообновления через GitHub Releases
4. Добавить аутентификацию пользователей

---

## 🧯 Troubleshooting (коротко)

### 1) `push HTTP 502` / `502 Bad Gateway`
Обычно значит, что nginx не смог получить ответ от backend.

В этом проекте уже была реальная причина: **PostgreSQL `integer` переполняется временем в миллисекундах** (`Date.now()`), из-за чего `/sync/push` валил backend, а nginx отдавал 502.

Правило: все поля времени, которые хранят ms Unix-time (`*_at`), в PostgreSQL должны быть **`bigint`**, не `integer`.

### 2) `https://...` падает на Windows с `0x80092013`
Это schannel/CRL/OCSP. Для синхронизации используйте `http://<domain>` или поднимайте свой TLS на VPS (443→443 + Let's Encrypt).

Подробнее: `docs/TROUBLESHOOTING.md`

