# Быстрый старт MatricaRMZ

## ✅ Что уже готово

1. ✅ База данных PostgreSQL создана и миграции применены
2. ✅ Backend API готов к запуску
3. ✅ Electron приложение готово к сборке

---

## 🚀 Запуск Backend на VPS

```bash
cd /home/valstan/MatricaRMZ/backend-api
pnpm run dev
```

Backend будет доступен на: `http://a6fd55b8e0ae.vps.myjino.ru:3001`

**Проверка:**
```bash
curl http://localhost:3001/health
# Должен вернуть: {"ok":true}
```

---

## 💻 Сборка и запуск Electron клиента

### Вариант 1: Запуск в режиме разработки (на VPS, через X11 forwarding)

```bash
cd /home/valstan/MatricaRMZ/electron-app
pnpm run dev
```

**Требования:** SSH с X11 forwarding (`ssh -X user@server`)

### Вариант 2: Сборка для Windows (на вашем компьютере)

1. **Установите на Windows:**
   - Node.js v22+ (https://nodejs.org/)
   - pnpm: `npm install -g pnpm`
   - Git (https://git-scm.com/)

2. **Скачайте проект:**
   ```bash
   # Через Git (если есть репозиторий)
   git clone <ваш_репозиторий>
   
   # Или через SCP/SFTP скачайте папку /home/valstan/MatricaRMZ
   ```

3. **Установите зависимости:**
   ```bash
   cd MatricaRMZ
   pnpm install
   ```

4. **Настройте URL backend:**
   
   Откройте файл `electron-app/src/main/index.ts` и измените строку ~50:
   ```typescript
   const apiBaseUrl = process.env.MATRICA_API_URL ?? 'http://a6fd55b8e0ae.vps.myjino.ru:3001';
   ```

5. **Соберите приложение:**
   ```bash
   cd electron-app
   pnpm run build
   ```

6. **Создайте установщик:**
   ```bash
   pnpm run dist
   ```
   
   Установщик будет в папке `electron-app/release/`

7. **Запустите приложение:**
   ```bash
   # В режиме разработки
   pnpm run dev
   
   # Или запустите собранный файл из dist/
   ```

---

## 🧪 Тестирование синхронизации

### 1. Запустите backend на VPS:
```bash
cd /home/valstan/MatricaRMZ/backend-api
pnpm run dev
```

### 2. Запустите Electron приложение:
```bash
cd /home/valstan/MatricaRMZ/electron-app
pnpm run dev
```

### 3. Проверьте работу:

1. В Electron приложении:
   - Откройте вкладку **"Двигатели"**
   - Нажмите **"Добавить двигатель"**
   - Введите номер двигателя (например: "ENG-001")
   - Введите марку (например: "ЯМЗ-238")
   - Нажмите **"Сохранить"**

2. Проверьте синхронизацию:
   - Перейдите на вкладку **"Синхронизация"**
   - Нажмите **"Синхронизировать сейчас"**
   - Должно появиться сообщение об успешной синхронизации

3. Проверьте в базе данных:
   ```bash
   sudo -u postgres psql -d matricarmz -c "SELECT * FROM entities LIMIT 5;"
   ```

---

## 📝 Важные замечания

1. **Backend должен быть запущен** перед использованием Electron приложения
2. **URL backend** должен быть доступен с вашего компьютера (проверьте firewall на VPS)
3. **Порт 3001** должен быть открыт для доступа (если подключаетесь с другого компьютера)

---

## 🔧 Настройка firewall (если нужно)

Если backend не доступен с вашего компьютера:

```bash
# Разрешить доступ к порту 3001
sudo ufw allow 3001/tcp
# Или для конкретного IP
sudo ufw allow from ваш_IP to any port 3001
```

---

## ❓ Что делать дальше?

После успешного тестирования:
1. Настроить автозапуск backend через PM2 или systemd
2. Собрать финальный установщик Electron для Windows
3. Настроить автообновления через GitHub Releases
4. Добавить аутентификацию пользователей

